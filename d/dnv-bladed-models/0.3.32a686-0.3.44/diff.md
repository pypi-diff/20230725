# Comparing `tmp/dnv_bladed_models-0.3.32a686.tar.gz` & `tmp/dnv_bladed_models-0.3.44.tar.gz`

## filetype from file(1)

```diff
@@ -1 +1 @@
-gzip compressed data, was "/local/artefacts/.tmp-dd08_tk2/dnv_bladed_models-0.3.32a686.tar", last modified: Mon Feb  6 09:39:38 2023, max compression
+gzip compressed data, was "/local/artefacts/.tmp-h93p84se/dnv_bladed_models-0.3.44.tar", last modified: Wed Jul 19 10:00:31 2023, max compression
```

## Comparing `dnv_bladed_models-0.3.32a686.tar` & `dnv_bladed_models-0.3.44.tar`

### file list

```diff
@@ -1,490 +1,491 @@
-drwxr-xr-x   0 root         (0) root         (0)        0 2023-02-06 09:39:38.000000 dnv_bladed_models-0.3.32a686/
--rw-r--r--   0 root         (0) root         (0)     1079 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/LICENSE
--rw-r--r--   0 root         (0) root         (0)     1609 2023-02-06 09:39:38.000000 dnv_bladed_models-0.3.32a686/PKG-INFO
--rw-r--r--   0 root         (0) root         (0)     1222 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/README.md
--rw-r--r--   0 root         (0) root         (0)      852 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/pyproject.toml
--rw-r--r--   0 root         (0) root         (0)      614 2023-02-06 09:39:38.000000 dnv_bladed_models-0.3.32a686/setup.cfg
-drwxr-xr-x   0 root         (0) root         (0)        0 2023-02-06 09:39:34.000000 dnv_bladed_models-0.3.32a686/src/
-drwxr-xr-x   0 root         (0) root         (0)        0 2023-02-06 09:39:38.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/
--rw-r--r--   0 root         (0) root         (0)    40083 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/__init__.py
--rw-r--r--   0 root         (0) root         (0)     6528 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/active_damper.py
--rw-r--r--   0 root         (0) root         (0)     6714 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/active_yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     7562 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/actuated_yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     7615 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/added_inertia.py
--rw-r--r--   0 root         (0) root         (0)     7001 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/additional_constrained_wave.py
--rw-r--r--   0 root         (0) root         (0)     7362 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerodynamic_information_calculation.py
--rw-r--r--   0 root         (0) root         (0)     7352 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerodynamic_model.py
--rw-r--r--   0 root         (0) root         (0)     6137 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerodynamic_settings.py
--rw-r--r--   0 root         (0) root         (0)     6304 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerofoil.py
--rw-r--r--   0 root         (0) root         (0)     6423 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/air_characteristics.py
--rw-r--r--   0 root         (0) root         (0)     6425 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/alpha_line.py
--rw-r--r--   0 root         (0) root         (0)     6149 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/angle_range.py
--rw-r--r--   0 root         (0) root         (0)     6358 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/angle_vs_coefficient_of_drag_multiplied_by_area.py
--rw-r--r--   0 root         (0) root         (0)     6171 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/angle_vs_stiffness_torque.py
--rw-r--r--   0 root         (0) root         (0)     8203 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_load.py
--rw-r--r--   0 root         (0) root         (0)     6272 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_moment_vs_friction_torque.py
--rw-r--r--   0 root         (0) root         (0)     6257 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_xy_force_vs_friction_torque.py
--rw-r--r--   0 root         (0) root         (0)     6249 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_z_force_vs_friction_torque.py
--rw-r--r--   0 root         (0) root         (0)     7450 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/assembly_node.py
--rw-r--r--   0 root         (0) root         (0)     6303 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/automatic_mac_camy_fuchs.py
--rw-r--r--   0 root         (0) root         (0)     6402 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/automatic_simple_cutoff_frequency.py
--rw-r--r--   0 root         (0) root         (0)     6527 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/axisymmetric_nacelle_cover.py
--rw-r--r--   0 root         (0) root         (0)     8512 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/beddoes_leishman_model.py
--rw-r--r--   0 root         (0) root         (0)     8789 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade.py
--rw-r--r--   0 root         (0) root         (0)     6056 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_accelerometer.py
--rw-r--r--   0 root         (0) root         (0)     6343 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_additional_inertia.py
--rw-r--r--   0 root         (0) root         (0)     5728 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_aerodynamic_outputs.py
--rw-r--r--   0 root         (0) root         (0)     6790 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_element_momentum.py
--rw-r--r--   0 root         (0) root         (0)     6676 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_element_theory_corrections.py
--rw-r--r--   0 root         (0) root         (0)     5909 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_hydrodynamic_outputs.py
--rw-r--r--   0 root         (0) root         (0)     8184 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_load_outputs.py
--rw-r--r--   0 root         (0) root         (0)     7643 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_modelling.py
--rw-r--r--   0 root         (0) root         (0)     6277 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_motion_outputs.py
--rw-r--r--   0 root         (0) root         (0)     6044 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_mounting.py
--rw-r--r--   0 root         (0) root         (0)     6824 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_mounting_on_rotor.py
--rw-r--r--   0 root         (0) root         (0)     7125 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_output_group.py
--rw-r--r--   0 root         (0) root         (0)     7887 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_outputs_for_location.py
--rw-r--r--   0 root         (0) root         (0)     6343 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_outputs_for_position.py
--rw-r--r--   0 root         (0) root         (0)     6351 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_outputs_for_section.py
--rw-r--r--   0 root         (0) root         (0)     6281 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_point_loading.py
--rw-r--r--   0 root         (0) root         (0)     6077 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_point_mass.py
--rw-r--r--   0 root         (0) root         (0)     6538 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_aerodynamic_properties.py
--rw-r--r--   0 root         (0) root         (0)     7888 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_axes_and_coordinate_systems.py
--rw-r--r--   0 root         (0) root         (0)     7210 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_definition.py
--rw-r--r--   0 root         (0) root         (0)     6071 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_mass_coordinate_system.py
--rw-r--r--   0 root         (0) root         (0)     6523 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_mass_properties.py
--rw-r--r--   0 root         (0) root         (0)     6235 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_neutral_axis_coordinate_system.py
--rw-r--r--   0 root         (0) root         (0)     6207 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_offset_and_rotated_axes_system.py
--rw-r--r--   0 root         (0) root         (0)     6232 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_offset_axes_system.py
--rw-r--r--   0 root         (0) root         (0)     6899 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_output_coordinate_system.py
--rw-r--r--   0 root         (0) root         (0)     6388 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_quarter_chord_coordinate_system.py
--rw-r--r--   0 root         (0) root         (0)     6297 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_reference_coordinate_system.py
--rw-r--r--   0 root         (0) root         (0)     6084 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_shear_axis.py
--rw-r--r--   0 root         (0) root         (0)     7377 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_structural_properties.py
--rw-r--r--   0 root         (0) root         (0)     7150 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_sensor.py
--rw-r--r--   0 root         (0) root         (0)     6798 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis.py
--rw-r--r--   0 root         (0) root         (0)     6769 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_calculation.py
--rw-r--r--   0 root         (0) root         (0)     6509 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_idling.py
--rw-r--r--   0 root         (0) root         (0)     7850 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_mode_of_operation.py
--rw-r--r--   0 root         (0) root         (0)     6457 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_parked.py
--rw-r--r--   0 root         (0) root         (0)     5798 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_perturbations.py
--rw-r--r--   0 root         (0) root         (0)     6032 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_strain_gauge.py
--rw-r--r--   0 root         (0) root         (0)     6883 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/bladed_analysis.py
--rw-r--r--   0 root         (0) root         (0)     7081 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/bladed_control.py
--rw-r--r--   0 root         (0) root         (0)     7323 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/brake.py
--rw-r--r--   0 root         (0) root         (0)     6515 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_diagram.py
--rw-r--r--   0 root         (0) root         (0)     7639 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_diagram_mode_of_operation.py
--rw-r--r--   0 root         (0) root         (0)     6109 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_idling.py
--rw-r--r--   0 root         (0) root         (0)     6109 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_parked.py
--rw-r--r--   0 root         (0) root         (0)     6208 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_power_production.py
--rw-r--r--   0 root         (0) root         (0)     6249 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/circular_lidar_scan.py
--rw-r--r--   0 root         (0) root         (0)     8659 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/component.py
--rw-r--r--   0 root         (0) root         (0)     6281 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/compressible_beddoes_leishman_model.py
--rw-r--r--   0 root         (0) root         (0)     5737 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/conditional_event.py
--rw-r--r--   0 root         (0) root         (0)     6720 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/connectable_node.py
--rw-r--r--   0 root         (0) root         (0)     6433 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/constants.py
--rw-r--r--   0 root         (0) root         (0)     7973 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/control_strategy.py
--rw-r--r--   0 root         (0) root         (0)     6355 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controlled_operation.py
--rw-r--r--   0 root         (0) root         (0)     5977 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controlled_stop.py
--rw-r--r--   0 root         (0) root         (0)     6578 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controller_fault.py
--rw-r--r--   0 root         (0) root         (0)     6197 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controller_lidar_settings.py
--rw-r--r--   0 root         (0) root         (0)     6491 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/counterweight_hub.py
--rw-r--r--   0 root         (0) root         (0)     6446 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/coupling_terms.py
--rw-r--r--   0 root         (0) root         (0)     6754 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/cuboid_nacelle_cover.py
--rw-r--r--   0 root         (0) root         (0)     6910 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current.py
--rw-r--r--   0 root         (0) root         (0)     7027 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_shear_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7480 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_shear_variation.py
--rw-r--r--   0 root         (0) root         (0)     6764 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7215 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_variation.py
--rw-r--r--   0 root         (0) root         (0)     6904 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_horizontal_shear_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7372 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_horizontal_shear_variation.py
--rw-r--r--   0 root         (0) root         (0)     6760 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_mean_speed_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7216 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_mean_speed_variation.py
--rw-r--r--   0 root         (0) root         (0)     6858 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_vertical_shear_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7320 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_vertical_shear_variation.py
--rw-r--r--   0 root         (0) root         (0)     6657 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/currents.py
--rw-r--r--   0 root         (0) root         (0)     6186 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/custom_linear_model_dll.py
--rw-r--r--   0 root         (0) root         (0)     5651 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/damper.py
--rw-r--r--   0 root         (0) root         (0)     6210 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/damper_force_or_torque_vs_rate.py
--rw-r--r--   0 root         (0) root         (0)     6253 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dimensionless_range.py
--rw-r--r--   0 root         (0) root         (0)     6478 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dnv.py
--rw-r--r--   0 root         (0) root         (0)     8497 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/drivetrain.py
--rw-r--r--   0 root         (0) root         (0)     5921 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/drivetrain_output_group.py
--rw-r--r--   0 root         (0) root         (0)     8922 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dynamic_stall.py
--rw-r--r--   0 root         (0) root         (0)     7682 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dynamic_upstream_wake.py
--rw-r--r--   0 root         (0) root         (0)     7694 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dynamic_wake.py
--rw-r--r--   0 root         (0) root         (0)     6247 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/earthquake.py
--rw-r--r--   0 root         (0) root         (0)     7617 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/electrical_grid.py
--rw-r--r--   0 root         (0) root         (0)     7100 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/electrical_losses.py
--rw-r--r--   0 root         (0) root         (0)     5974 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/emergency_stop_button.py
--rw-r--r--   0 root         (0) root         (0)     6216 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/emergency_stop_operation.py
--rw-r--r--   0 root         (0) root         (0)     6172 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/environment.py
--rw-r--r--   0 root         (0) root         (0)     6103 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/equilibrium_wake_model.py
--rw-r--r--   0 root         (0) root         (0)     8222 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/event.py
--rw-r--r--   0 root         (0) root         (0)     6897 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/evolving_current_turbulence.py
--rw-r--r--   0 root         (0) root         (0)     7572 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/evolving_wind_turbulence.py
--rw-r--r--   0 root         (0) root         (0)     6901 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/explicit_newmark_beta_fixed_step.py
--rw-r--r--   0 root         (0) root         (0)     6606 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/explicit_tower_can.py
--rw-r--r--   0 root         (0) root         (0)     6509 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/exponential_shear_model.py
--rw-r--r--   0 root         (0) root         (0)     6234 2023-02-06 09:39:17.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/extension_pieces.py
--rw-r--r--   0 root         (0) root         (0)     9209 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_controller.py
--rw-r--r--   0 root         (0) root         (0)     6572 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_flow_source_for_wind.py
--rw-r--r--   0 root         (0) root         (0)     6297 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_module.py
--rw-r--r--   0 root         (0) root         (0)     7020 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_module_component.py
--rw-r--r--   0 root         (0) root         (0)     6916 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/externally_stepped_simulation.py
--rw-r--r--   0 root         (0) root         (0)     6235 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fault.py
--rw-r--r--   0 root         (0) root         (0)     6272 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/finite_element_blade_modelling.py
--rw-r--r--   0 root         (0) root         (0)     6310 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/first_order_response.py
--rw-r--r--   0 root         (0) root         (0)     6134 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_speed_active_damper.py
--rw-r--r--   0 root         (0) root         (0)     6520 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_speed_generator.py
--rw-r--r--   0 root         (0) root         (0)     6635 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_speed_induction_generator.py
--rw-r--r--   0 root         (0) root         (0)     6101 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_step_integrator.py
--rw-r--r--   0 root         (0) root         (0)     5889 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/flex_body.py
--rw-r--r--   0 root         (0) root         (0)     5682 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/flexible_joint.py
--rw-r--r--   0 root         (0) root         (0)     6227 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/flow_obstruction.py
--rw-r--r--   0 root         (0) root         (0)     6939 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/foundation.py
--rw-r--r--   0 root         (0) root         (0)     6019 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/free_flow_model.py
--rw-r--r--   0 root         (0) root         (0)     6230 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/frequency_vs_spectral_density.py
--rw-r--r--   0 root         (0) root         (0)     6140 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/friction.py
--rw-r--r--   0 root         (0) root         (0)     6284 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/friction_torque_vs_stiction_torque.py
--rw-r--r--   0 root         (0) root         (0)     6734 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gaussian.py
--rw-r--r--   0 root         (0) root         (0)     6632 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gearbox.py
--rw-r--r--   0 root         (0) root         (0)     6434 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gearbox_mounting_rolling.py
--rw-r--r--   0 root         (0) root         (0)     6779 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generalised_alpha_fixed_step.py
--rw-r--r--   0 root         (0) root         (0)     6447 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator.py
--rw-r--r--   0 root         (0) root         (0)     6030 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_output_group.py
--rw-r--r--   0 root         (0) root         (0)     6293 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_overpower.py
--rw-r--r--   0 root         (0) root         (0)     6317 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_overspeed.py
--rw-r--r--   0 root         (0) root         (0)     6130 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_short_circuit.py
--rw-r--r--   0 root         (0) root         (0)     6320 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gl2010_icing.py
--rw-r--r--   0 root         (0) root         (0)     6209 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/glauert_skew_wake_model.py
--rw-r--r--   0 root         (0) root         (0)     5959 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/grid_loss.py
--rw-r--r--   0 root         (0) root         (0)     6228 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/grid_loss_energy_sink.py
--rw-r--r--   0 root         (0) root         (0)     6268 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hardware_tracking.py
--rw-r--r--   0 root         (0) root         (0)     6275 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/height_up_tower_vs_cd_cm.py
--rw-r--r--   0 root         (0) root         (0)     6110 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/height_vs_shear_factor.py
--rw-r--r--   0 root         (0) root         (0)     6127 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/high_speed_shaft.py
--rw-r--r--   0 root         (0) root         (0)     6280 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/high_speed_shaft_flexibility.py
--rw-r--r--   0 root         (0) root         (0)     7965 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hub.py
--rw-r--r--   0 root         (0) root         (0)     6636 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hub_mass_properties.py
--rw-r--r--   0 root         (0) root         (0)     6067 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hub_output_group.py
--rw-r--r--   0 root         (0) root         (0)     7327 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hydraulic_accumulator_yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     6678 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hydrodynamic_calculation.py
--rw-r--r--   0 root         (0) root         (0)     6497 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/iced_condition.py
--rw-r--r--   0 root         (0) root         (0)     6223 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/idealised_pitch_actuator.py
--rw-r--r--   0 root         (0) root         (0)     6000 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/idealised_yaw_manoever.py
--rw-r--r--   0 root         (0) root         (0)     6099 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/iec4_icing.py
--rw-r--r--   0 root         (0) root         (0)     7115 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/implicit_newmark_beta_fixed_step.py
--rw-r--r--   0 root         (0) root         (0)     6305 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/incompressible_beddoes_leishman_model.py
--rw-r--r--   0 root         (0) root         (0)     6120 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/independent_pitch_hub.py
--rw-r--r--   0 root         (0) root         (0)     6445 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_azimuth_position.py
--rw-r--r--   0 root         (0) root         (0)     7597 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_condition.py
--rw-r--r--   0 root         (0) root         (0)     6269 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_control_state.py
--rw-r--r--   0 root         (0) root         (0)     6219 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_inactive_control_state.py
--rw-r--r--   0 root         (0) root         (0)     6327 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_machine_position.py
--rw-r--r--   0 root         (0) root         (0)     6258 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_position.py
--rw-r--r--   0 root         (0) root         (0)     6074 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/input_power_vs_loss.py
--rw-r--r--   0 root         (0) root         (0)     6141 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/instantaneous.py
--rw-r--r--   0 root         (0) root         (0)     7377 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/integrator.py
--rw-r--r--   0 root         (0) root         (0)     5984 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/interface_load_file.py
--rw-r--r--   0 root         (0) root         (0)     6800 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/irregular_waves.py
--rw-r--r--   0 root         (0) root         (0)     6733 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/jonswap_pierson_moskowitz.py
--rw-r--r--   0 root         (0) root         (0)     7450 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/laminar_flow_current.py
--rw-r--r--   0 root         (0) root         (0)     7360 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/laminar_flow_wind.py
--rw-r--r--   0 root         (0) root         (0)     6607 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_blade_sensor_mapping.py
--rw-r--r--   0 root         (0) root         (0)     6493 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_nacelle_sensor_mapping.py
--rw-r--r--   0 root         (0) root         (0)     8022 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_sensor_mapping.py
--rw-r--r--   0 root         (0) root         (0)     6569 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_sensor_mappings.py
--rw-r--r--   0 root         (0) root         (0)     6319 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_tower_sensor_mapping.py
--rw-r--r--   0 root         (0) root         (0)     6064 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/length_vs_radius.py
--rw-r--r--   0 root         (0) root         (0)     7549 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar.py
--rw-r--r--   0 root         (0) root         (0)     6551 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_beam.py
--rw-r--r--   0 root         (0) root         (0)     6222 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_controller_scan.py
--rw-r--r--   0 root         (0) root         (0)     7797 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_focal_distance_control.py
--rw-r--r--   0 root         (0) root         (0)     6243 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_mounting_position.py
--rw-r--r--   0 root         (0) root         (0)     7159 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_scanning_pattern.py
--rw-r--r--   0 root         (0) root         (0)     5968 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_airy.py
--rw-r--r--   0 root         (0) root         (0)     6601 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_electrical_losses.py
--rw-r--r--   0 root         (0) root         (0)     6587 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_foundation.py
--rw-r--r--   0 root         (0) root         (0)     6994 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_passive_damper.py
--rw-r--r--   0 root         (0) root         (0)     7409 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_pitch_actuator.py
--rw-r--r--   0 root         (0) root         (0)     6175 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_spring_damper.py
--rw-r--r--   0 root         (0) root         (0)     6368 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_spring_yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     6785 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linearisation_calculation.py
--rw-r--r--   0 root         (0) root         (0)     6524 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/loading_dependent_friction.py
--rw-r--r--   0 root         (0) root         (0)     6485 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/logarithmic_shear_model.py
--rw-r--r--   0 root         (0) root         (0)     6619 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/look_up_shear_model.py
--rw-r--r--   0 root         (0) root         (0)     6097 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/look_up_table_element.py
--rw-r--r--   0 root         (0) root         (0)     7009 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/low_speed_shaft.py
--rw-r--r--   0 root         (0) root         (0)     6392 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/mac_camy_fuchs.py
--rw-r--r--   0 root         (0) root         (0)     6410 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/main_bearing.py
--rw-r--r--   0 root         (0) root         (0)     6190 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/meandering_wake.py
--rw-r--r--   0 root         (0) root         (0)    17422 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/measured_signal_properties.py
--rw-r--r--   0 root         (0) root         (0)     7096 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/mechanical_losses.py
--rw-r--r--   0 root         (0) root         (0)     6172 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/midpoint_method_fixed_step.py
--rw-r--r--   0 root         (0) root         (0)     6801 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/modal_blade_modelling.py
--rw-r--r--   0 root         (0) root         (0)     6910 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/model_linearisation.py
--rw-r--r--   0 root         (0) root         (0)     7005 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/model_linearisation_perturbations.py
--rw-r--r--   0 root         (0) root         (0)     6193 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/moment_vs_coulomb_friction.py
--rw-r--r--   0 root         (0) root         (0)     7855 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/momentum_theory_corrections.py
--rw-r--r--   0 root         (0) root         (0)     6341 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/mooring_initial_position.py
--rw-r--r--   0 root         (0) root         (0)     6430 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/multiple_lidar_focal_distances.py
--rw-r--r--   0 root         (0) root         (0)     7283 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle.py
--rw-r--r--   0 root         (0) root         (0)     6144 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_accelerometer.py
--rw-r--r--   0 root         (0) root         (0)     6357 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_centre_of_gravity.py
--rw-r--r--   0 root         (0) root         (0)     7124 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_cover.py
--rw-r--r--   0 root         (0) root         (0)     6132 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_inclinometer.py
--rw-r--r--   0 root         (0) root         (0)     6459 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_mass_properties.py
--rw-r--r--   0 root         (0) root         (0)     6420 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_moment_of_inertia.py
--rw-r--r--   0 root         (0) root         (0)     7221 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_sensor.py
--rw-r--r--   0 root         (0) root         (0)     6316 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_vibration.py
--rw-r--r--   0 root         (0) root         (0)     6068 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/near_shore_current.py
--rw-r--r--   0 root         (0) root         (0)     6351 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/near_surface_current.py
--rw-r--r--   0 root         (0) root         (0)     6692 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/network_frequency_disturbance.py
--rw-r--r--   0 root         (0) root         (0)     6650 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/network_voltage_disturbance.py
--rw-r--r--   0 root         (0) root         (0)     5997 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/non_actuated_yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     6620 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/non_linear_electrical_losses.py
--rw-r--r--   0 root         (0) root         (0)     6403 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/non_linear_shaft_brake.py
--rw-r--r--   0 root         (0) root         (0)     6180 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/normal_stop_operation.py
--rw-r--r--   0 root         (0) root         (0)     6495 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/outputs.py
--rw-r--r--   0 root         (0) root         (0)     6509 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/oye_dynamic_wake.py
--rw-r--r--   0 root         (0) root         (0)     5965 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/oye_model.py
--rw-r--r--   0 root         (0) root         (0)     6437 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pallet_mounting_flexibility.py
--rw-r--r--   0 root         (0) root         (0)     6155 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pallet_nodding.py
--rw-r--r--   0 root         (0) root         (0)     6146 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pallet_rolling.py
--rw-r--r--   0 root         (0) root         (0)     6477 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/passive.py
--rw-r--r--   0 root         (0) root         (0)     6213 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/passive_yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     8407 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pendulum_damper.py
--rw-r--r--   0 root         (0) root         (0)     6318 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pendulum_damper_mounting_position.py
--rw-r--r--   0 root         (0) root         (0)     7347 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/performance_coefficients_calculation.py
--rw-r--r--   0 root         (0) root         (0)     6368 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/permanently_stuck_pitch_system.py
--rw-r--r--   0 root         (0) root         (0)     6527 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/perturbation_settings.py
--rw-r--r--   0 root         (0) root         (0)     6194 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_acceleration_limits.py
--rw-r--r--   0 root         (0) root         (0)     7253 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_actuation_limited.py
--rw-r--r--   0 root         (0) root         (0)     7086 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_actuator.py
--rw-r--r--   0 root         (0) root         (0)     6514 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_rate_relationship.py
--rw-r--r--   0 root         (0) root         (0)     6628 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_xy_force_relationship.py
--rw-r--r--   0 root         (0) root         (0)     6647 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_xy_moment_relationship.py
--rw-r--r--   0 root         (0) root         (0)     6616 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_z_force_relationship.py
--rw-r--r--   0 root         (0) root         (0)     7317 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_kinetic_friction.py
--rw-r--r--   0 root         (0) root         (0)     6683 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_stiction.py
--rw-r--r--   0 root         (0) root         (0)     6504 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_constant_rate_safety_system.py
--rw-r--r--   0 root         (0) root         (0)     6242 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_controller.py
--rw-r--r--   0 root         (0) root         (0)     5876 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_controller_rate_safety_system.py
--rw-r--r--   0 root         (0) root         (0)     5936 2023-02-06 09:39:18.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_controller_torque_safety_system.py
--rw-r--r--   0 root         (0) root         (0)     6337 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_end_stops.py
--rw-r--r--   0 root         (0) root         (0)     5965 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault.py
--rw-r--r--   0 root         (0) root         (0)     6418 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_constant_rate.py
--rw-r--r--   0 root         (0) root         (0)     6387 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_constant_torque.py
--rw-r--r--   0 root         (0) root         (0)     6297 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_limp.py
--rw-r--r--   0 root         (0) root         (0)     6095 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_seizure.py
--rw-r--r--   0 root         (0) root         (0)     7029 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_seizure_at_angle.py
--rw-r--r--   0 root         (0) root         (0)     6366 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_force_limits.py
--rw-r--r--   0 root         (0) root         (0)     6083 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_force_minimum_maximum.py
--rw-r--r--   0 root         (0) root         (0)     6213 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_limit_switches.py
--rw-r--r--   0 root         (0) root         (0)     6169 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_position_demand.py
--rw-r--r--   0 root         (0) root         (0)     6447 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_position_vs_minimum_and_maximum_moment.py
--rw-r--r--   0 root         (0) root         (0)     6120 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_demand.py
--rw-r--r--   0 root         (0) root         (0)     6406 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_demand_set_by_external_controller.py
--rw-r--r--   0 root         (0) root         (0)     6084 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_limits.py
--rw-r--r--   0 root         (0) root         (0)     6635 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_varies_with_position.py
--rw-r--r--   0 root         (0) root         (0)     6575 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_varies_with_time.py
--rw-r--r--   0 root         (0) root         (0)     6444 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_vs_minimum_and_maximum_moment.py
--rw-r--r--   0 root         (0) root         (0)     6136 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_safety_limit_switches.py
--rw-r--r--   0 root         (0) root         (0)     7839 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_safety_system.py
--rw-r--r--   0 root         (0) root         (0)     8503 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system.py
--rw-r--r--   0 root         (0) root         (0)     7270 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system_demand.py
--rw-r--r--   0 root         (0) root         (0)     6136 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system_output_group.py
--rw-r--r--   0 root         (0) root         (0)     7894 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system_rotary_drive.py
--rw-r--r--   0 root         (0) root         (0)     6715 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_torque_limits.py
--rw-r--r--   0 root         (0) root         (0)     6163 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_torque_minimum_maximum.py
--rw-r--r--   0 root         (0) root         (0)     6370 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_torque_set_by_external_controller.py
--rw-r--r--   0 root         (0) root         (0)     6080 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitt_and_peters_model.py
--rw-r--r--   0 root         (0) root         (0)     6846 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/point_inertia.py
--rw-r--r--   0 root         (0) root         (0)     6418 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/point_on_span.py
--rw-r--r--   0 root         (0) root         (0)     6266 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/position_of_upwind_turbine.py
--rw-r--r--   0 root         (0) root         (0)     6593 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/power_scaling_losses.py
--rw-r--r--   0 root         (0) root         (0)     7922 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_direction_shear_transient.py
--rw-r--r--   0 root         (0) root         (0)     7612 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_direction_transient.py
--rw-r--r--   0 root         (0) root         (0)     7796 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_horizontal_shear_transient.py
--rw-r--r--   0 root         (0) root         (0)     7663 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_mean_speed_transient.py
--rw-r--r--   0 root         (0) root         (0)     7753 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_vertical_shear_transient.py
--rw-r--r--   0 root         (0) root         (0)     6557 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_lidar_scan.py
--rw-r--r--   0 root         (0) root         (0)     7892 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_direction_shear_transient.py
--rw-r--r--   0 root         (0) root         (0)     7561 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_direction_transient.py
--rw-r--r--   0 root         (0) root         (0)     7718 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_horizontal_shear_transient.py
--rw-r--r--   0 root         (0) root         (0)     7594 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_mean_speed_transient.py
--rw-r--r--   0 root         (0) root         (0)     7674 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_vertical_shear_transient.py
--rw-r--r--   0 root         (0) root         (0)     7934 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/proportional_integral_deriviative.py
--rw-r--r--   0 root         (0) root         (0)     6652 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/quadratic_yaw_damper.py
--rw-r--r--   0 root         (0) root         (0)     6079 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/radius_vs_deficit.py
--rw-r--r--   0 root         (0) root         (0)     6176 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rate_vs_friction_torque.py
--rw-r--r--   0 root         (0) root         (0)     6087 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rate_vs_position.py
--rw-r--r--   0 root         (0) root         (0)     6035 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rate_vs_time.py
--rw-r--r--   0 root         (0) root         (0)     6259 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/regular_waves.py
--rw-r--r--   0 root         (0) root         (0)     7404 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/response_to_demand.py
--rw-r--r--   0 root         (0) root         (0)     6138 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rigid_blade_modelling.py
--rw-r--r--   0 root         (0) root         (0)     6708 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rosette_lidar_scan.py
--rw-r--r--   0 root         (0) root         (0)     6280 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_idling_control_state.py
--rw-r--r--   0 root         (0) root         (0)     7010 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_imbalances.py
--rw-r--r--   0 root         (0) root         (0)     6328 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_in_power_production.py
--rw-r--r--   0 root         (0) root         (0)     6283 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_overspeed.py
--rw-r--r--   0 root         (0) root         (0)     6286 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_parked_control_state.py
--rw-r--r--   0 root         (0) root         (0)     6222 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/runge_kutta4th_order_fixed_step.py
--rw-r--r--   0 root         (0) root         (0)     6867 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/runge_kutta_variable_step.py
--rw-r--r--   0 root         (0) root         (0)     7688 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/safety_system.py
--rw-r--r--   0 root         (0) root         (0)     7017 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/safety_system_circuit.py
--rw-r--r--   0 root         (0) root         (0)     6226 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sea_state.py
--rw-r--r--   0 root         (0) root         (0)     6533 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/second_order_response.py
--rw-r--r--   0 root         (0) root         (0)     6547 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/selected_component_output_group.py
--rw-r--r--   0 root         (0) root         (0)     6565 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sensor_mapping.py
--rw-r--r--   0 root         (0) root         (0)     6444 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sensor_reference.py
--rw-r--r--   0 root         (0) root         (0)     6989 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sensors_used.py
--rw-r--r--   0 root         (0) root         (0)     6261 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sequential_timestep_adaptation.py
--rw-r--r--   0 root         (0) root         (0)     6685 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/setpoint_trajectory_planning.py
--rw-r--r--   0 root         (0) root         (0)     6681 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/settings.py
--rw-r--r--   0 root         (0) root         (0)     6167 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/shaft_speed_vs_power_loss.py
--rw-r--r--   0 root         (0) root         (0)     6260 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/shaft_speed_vs_resisting_torque.py
--rw-r--r--   0 root         (0) root         (0)     6276 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/shear_stiffnesses.py
--rw-r--r--   0 root         (0) root         (0)     6142 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/short_circuit.py
--rw-r--r--   0 root         (0) root         (0)     7113 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties.py
--rw-r--r--   0 root         (0) root         (0)     6520 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_acceleration.py
--rw-r--r--   0 root         (0) root         (0)     6362 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_angle.py
--rw-r--r--   0 root         (0) root         (0)     6627 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_angular_acceleration.py
--rw-r--r--   0 root         (0) root         (0)     6541 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_angular_velocity.py
--rw-r--r--   0 root         (0) root         (0)     6358 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_force.py
--rw-r--r--   0 root         (0) root         (0)     6372 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_length.py
--rw-r--r--   0 root         (0) root         (0)     6408 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_moment.py
--rw-r--r--   0 root         (0) root         (0)     6448 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_power.py
--rw-r--r--   0 root         (0) root         (0)     6426 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_velocity.py
--rw-r--r--   0 root         (0) root         (0)     6513 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simple_cutoff_frequency.py
--rw-r--r--   0 root         (0) root         (0)     6447 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simple_shaft_brake.py
--rw-r--r--   0 root         (0) root         (0)     6813 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simple_tower_can.py
--rw-r--r--   0 root         (0) root         (0)     7059 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simplified_linear_foundation.py
--rw-r--r--   0 root         (0) root         (0)     6258 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/single_actuator_collective_hub.py
--rw-r--r--   0 root         (0) root         (0)     6408 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/single_lidar_focal_distance.py
--rw-r--r--   0 root         (0) root         (0)     6283 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/six_by_six_inertia.py
--rw-r--r--   0 root         (0) root         (0)     5677 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/skew_wake_model.py
--rw-r--r--   0 root         (0) root         (0)     6039 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/slipping_clutch.py
--rw-r--r--   0 root         (0) root         (0)     6351 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/solver_settings.py
--rw-r--r--   0 root         (0) root         (0)     6493 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/spinner.py
--rw-r--r--   0 root         (0) root         (0)     6258 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/spring_force_or_torque_vs_position.py
--rw-r--r--   0 root         (0) root         (0)     6037 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/standard_hub.py
--rw-r--r--   0 root         (0) root         (0)     6153 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/standard_pitch_limit_switches.py
--rw-r--r--   0 root         (0) root         (0)     6356 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/start_up_operation.py
--rw-r--r--   0 root         (0) root         (0)     7667 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_calculation.py
--rw-r--r--   0 root         (0) root         (0)     5734 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_calculation_outputs.py
--rw-r--r--   0 root         (0) root         (0)     6193 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_calculation_with_component_outputs.py
--rw-r--r--   0 root         (0) root         (0)     7554 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_operational_loads_calculation.py
--rw-r--r--   0 root         (0) root         (0)     8426 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_parked_loads_calculation.py
--rw-r--r--   0 root         (0) root         (0)     7484 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_power_curve_calculation.py
--rw-r--r--   0 root         (0) root         (0)     7621 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_wake_deficit.py
--rw-r--r--   0 root         (0) root         (0)     6850 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/stream_function.py
--rw-r--r--   0 root         (0) root         (0)     6036 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structural_mode.py
--rw-r--r--   0 root         (0) root         (0)     6234 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structural_modelling.py
--rw-r--r--   0 root         (0) root         (0)     7424 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structural_modelling_settings.py
--rw-r--r--   0 root         (0) root         (0)     5682 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structure.py
--rw-r--r--   0 root         (0) root         (0)     6377 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sub_surface_current.py
--rw-r--r--   0 root         (0) root         (0)     7579 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/superelement.py
--rw-r--r--   0 root         (0) root         (0)     5779 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/support_structure_point_loading.py
--rw-r--r--   0 root         (0) root         (0)     6174 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/thrust_vs_coulomb_friction.py
--rw-r--r--   0 root         (0) root         (0)     5799 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tide.py
--rw-r--r--   0 root         (0) root         (0)     6561 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_current.py
--rw-r--r--   0 root         (0) root         (0)     9637 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_outputs.py
--rw-r--r--   0 root         (0) root         (0)     8168 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_simulation.py
--rw-r--r--   0 root         (0) root         (0)     7716 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_wind.py
--rw-r--r--   0 root         (0) root         (0)     6122 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_direction.py
--rw-r--r--   0 root         (0) root         (0)     6218 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_direction_shear.py
--rw-r--r--   0 root         (0) root         (0)     6204 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_horizontal_shear.py
--rw-r--r--   0 root         (0) root         (0)     6136 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_mean_speed.py
--rw-r--r--   0 root         (0) root         (0)     6180 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_vertical_shear.py
--rw-r--r--   0 root         (0) root         (0)     6167 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_frequency_change.py
--rw-r--r--   0 root         (0) root         (0)     6084 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_torque.py
--rw-r--r--   0 root         (0) root         (0)     6127 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_voltage_change.py
--rw-r--r--   0 root         (0) root         (0)     6098 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_direction.py
--rw-r--r--   0 root         (0) root         (0)     6194 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_direction_shear.py
--rw-r--r--   0 root         (0) root         (0)     6180 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_horizontal_shear.py
--rw-r--r--   0 root         (0) root         (0)     6112 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_mean_speed.py
--rw-r--r--   0 root         (0) root         (0)     6156 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_vertical_shear.py
--rw-r--r--   0 root         (0) root         (0)     5848 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/timed_event.py
--rw-r--r--   0 root         (0) root         (0)     6250 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/torque_controlled_yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     6631 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/torque_scaling_losses.py
--rw-r--r--   0 root         (0) root         (0)     8303 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower.py
--rw-r--r--   0 root         (0) root         (0)     6065 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_accelerometer.py
--rw-r--r--   0 root         (0) root         (0)     6204 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_aerodynamic_properties.py
--rw-r--r--   0 root         (0) root         (0)     7246 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can.py
--rw-r--r--   0 root         (0) root         (0)     6320 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_modelling.py
--rw-r--r--   0 root         (0) root         (0)     6239 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_by_material.py
--rw-r--r--   0 root         (0) root         (0)     6395 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_by_material_where_different.py
--rw-r--r--   0 root         (0) root         (0)     7357 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_explicit.py
--rw-r--r--   0 root         (0) root         (0)     7513 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_explicit_where_different.py
--rw-r--r--   0 root         (0) root         (0)     6152 2023-02-06 09:39:19.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_section_properties.py
--rw-r--r--   0 root         (0) root         (0)     6264 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_section_properties_where_different.py
--rw-r--r--   0 root         (0) root         (0)     5970 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_connectable_node.py
--rw-r--r--   0 root         (0) root         (0)     6579 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_hydrodynamic_properties.py
--rw-r--r--   0 root         (0) root         (0)     6053 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_inclinometer.py
--rw-r--r--   0 root         (0) root         (0)     6299 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_material.py
--rw-r--r--   0 root         (0) root         (0)     6614 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_node_at_height_outputs.py
--rw-r--r--   0 root         (0) root         (0)     6877 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_node_loads.py
--rw-r--r--   0 root         (0) root         (0)     6295 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_node_outputs.py
--rw-r--r--   0 root         (0) root         (0)     6840 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_output_group.py
--rw-r--r--   0 root         (0) root         (0)     6336 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_point_loading.py
--rw-r--r--   0 root         (0) root         (0)     7436 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_sensor.py
--rw-r--r--   0 root         (0) root         (0)     6028 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_shadow_potential_flow.py
--rw-r--r--   0 root         (0) root         (0)     7343 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_shadow_powles.py
--rw-r--r--   0 root         (0) root         (0)     6054 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_strain_gauge.py
--rw-r--r--   0 root         (0) root         (0)     5682 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_behaviour.py
--rw-r--r--   0 root         (0) root         (0)     8375 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults.py
--rw-r--r--   0 root         (0) root         (0)     7126 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_acceleration.py
--rw-r--r--   0 root         (0) root         (0)     6942 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_angle.py
--rw-r--r--   0 root         (0) root         (0)     7308 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_angular_acceleration.py
--rw-r--r--   0 root         (0) root         (0)     7176 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_angular_velocity.py
--rw-r--r--   0 root         (0) root         (0)     6934 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_force.py
--rw-r--r--   0 root         (0) root         (0)     6916 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_length.py
--rw-r--r--   0 root         (0) root         (0)     7104 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_moment.py
--rw-r--r--   0 root         (0) root         (0)     6926 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_power.py
--rw-r--r--   0 root         (0) root         (0)     7038 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_velocity.py
--rw-r--r--   0 root         (0) root         (0)     6879 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/turbine.py
--rw-r--r--   0 root         (0) root         (0)     8719 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/turbulent_current.py
--rw-r--r--   0 root         (0) root         (0)     9095 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/turbulent_wind.py
--rw-r--r--   0 root         (0) root         (0)     6811 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/user_defined_wake_deficit.py
--rw-r--r--   0 root         (0) root         (0)     6374 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/user_defined_waves.py
--rw-r--r--   0 root         (0) root         (0)     7253 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_slip_generator.py
--rw-r--r--   0 root         (0) root         (0)     6113 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_speed_active_damper.py
--rw-r--r--   0 root         (0) root         (0)     6993 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_speed_generator.py
--rw-r--r--   0 root         (0) root         (0)     7106 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_speed_induction_generator.py
--rw-r--r--   0 root         (0) root         (0)     6056 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/vector3_d.py
--rw-r--r--   0 root         (0) root         (0)     6220 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/velocity_range.py
--rw-r--r--   0 root         (0) root         (0)     6212 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/velocity_vs_damping_torque.py
--rw-r--r--   0 root         (0) root         (0)     7219 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/vertical_shear.py
--rw-r--r--   0 root         (0) root         (0)     9412 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/vortex_line.py
--rw-r--r--   0 root         (0) root         (0)     6570 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wake_properties_of_upstream_turbine.py
--rw-r--r--   0 root         (0) root         (0)     6161 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/water_characteristics.py
--rw-r--r--   0 root         (0) root         (0)     7599 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wave_diffraction_approximation.py
--rw-r--r--   0 root         (0) root         (0)     6814 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wave_spectrum.py
--rw-r--r--   0 root         (0) root         (0)     7008 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/waves.py
--rw-r--r--   0 root         (0) root         (0)     6879 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind.py
--rw-r--r--   0 root         (0) root         (0)     6982 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_shear_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7435 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_shear_variation.py
--rw-r--r--   0 root         (0) root         (0)     6710 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7161 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_variation.py
--rw-r--r--   0 root         (0) root         (0)     6899 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_horizontal_shear_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7318 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_horizontal_shear_variation.py
--rw-r--r--   0 root         (0) root         (0)     6712 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_mean_speed_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7162 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_mean_speed_variation.py
--rw-r--r--   0 root         (0) root         (0)     6800 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_vertical_shear_time_history.py
--rw-r--r--   0 root         (0) root         (0)     7266 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_vertical_shear_variation.py
--rw-r--r--   0 root         (0) root         (0)     6256 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/xml_blade_outputs.py
--rw-r--r--   0 root         (0) root         (0)     5656 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault.py
--rw-r--r--   0 root         (0) root         (0)     6520 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault_constant_rate.py
--rw-r--r--   0 root         (0) root         (0)     6305 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault_constant_torque.py
--rw-r--r--   0 root         (0) root         (0)     6758 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault_limp.py
--rw-r--r--   0 root         (0) root         (0)     6433 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_manoever.py
--rw-r--r--   0 root         (0) root         (0)     6318 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_system.py
--rw-r--r--   0 root         (0) root         (0)     5932 2023-02-06 09:39:20.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_system_output_group.py
-drwxr-xr-x   0 root         (0) root         (0)        0 2023-02-06 09:39:38.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models.egg-info/
--rw-r--r--   0 root         (0) root         (0)     1609 2023-02-06 09:39:33.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models.egg-info/PKG-INFO
--rw-r--r--   0 root         (0) root         (0)    23291 2023-02-06 09:39:34.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models.egg-info/SOURCES.txt
--rw-r--r--   0 root         (0) root         (0)        1 2023-02-06 09:39:33.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models.egg-info/dependency_links.txt
--rw-r--r--   0 root         (0) root         (0)        9 2023-02-06 09:39:33.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models.egg-info/requires.txt
--rw-r--r--   0 root         (0) root         (0)       18 2023-02-06 09:39:33.000000 dnv_bladed_models-0.3.32a686/src/dnv_bladed_models.egg-info/top_level.txt
+drwxr-xr-x   0 root         (0) root         (0)        0 2023-07-19 10:00:31.000000 dnv_bladed_models-0.3.44/
+-rwxrwxrwx   0 root         (0) root         (0)     1079 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/LICENSE
+-rw-r--r--   0 root         (0) root         (0)     2944 2023-07-19 10:00:31.000000 dnv_bladed_models-0.3.44/PKG-INFO
+-rwxrwxrwx   0 root         (0) root         (0)     1299 2023-07-19 10:00:11.000000 dnv_bladed_models-0.3.44/README.md
+-rwxrwxrwx   0 root         (0) root         (0)     1163 2023-07-19 10:00:11.000000 dnv_bladed_models-0.3.44/pyproject.toml
+-rwxrwxrwx   0 root         (0) root         (0)      454 2023-07-19 10:00:31.000000 dnv_bladed_models-0.3.44/setup.cfg
+drwxr-xr-x   0 root         (0) root         (0)        0 2023-07-19 10:00:23.000000 dnv_bladed_models-0.3.44/src/
+drwxr-xr-x   0 root         (0) root         (0)        0 2023-07-19 10:00:31.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/
+-rwxrwxrwx   0 root         (0) root         (0)    40215 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/__init__.py
+-rwxrwxrwx   0 root         (0) root         (0)     6561 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/active_damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     6733 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/active_yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     7604 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/actuated_yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     7651 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/added_inertia.py
+-rwxrwxrwx   0 root         (0) root         (0)     7050 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/additional_constrained_wave.py
+-rwxrwxrwx   0 root         (0) root         (0)     7413 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerodynamic_information_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     7364 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerodynamic_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6479 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerodynamic_settings.py
+-rwxrwxrwx   0 root         (0) root         (0)     6351 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerofoil.py
+-rwxrwxrwx   0 root         (0) root         (0)     6451 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/air_characteristics.py
+-rwxrwxrwx   0 root         (0) root         (0)     6458 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/alpha_line.py
+-rwxrwxrwx   0 root         (0) root         (0)     6181 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/angle_range.py
+-rwxrwxrwx   0 root         (0) root         (0)     6389 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/angle_vs_coefficient_of_drag_multiplied_by_area.py
+-rwxrwxrwx   0 root         (0) root         (0)     6202 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/angle_vs_stiffness_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     8214 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_load.py
+-rwxrwxrwx   0 root         (0) root         (0)     6303 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_moment_vs_friction_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6288 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_xy_force_vs_friction_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6280 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_z_force_vs_friction_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     7468 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/assembly_node.py
+-rwxrwxrwx   0 root         (0) root         (0)     6356 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/automatic_mac_camy_fuchs.py
+-rwxrwxrwx   0 root         (0) root         (0)     6464 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/automatic_simple_cutoff_frequency.py
+-rwxrwxrwx   0 root         (0) root         (0)     6588 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/axisymmetric_nacelle_cover.py
+-rwxrwxrwx   0 root         (0) root         (0)     8576 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/beddoes_leishman_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     9092 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade.py
+-rwxrwxrwx   0 root         (0) root         (0)     6101 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_accelerometer.py
+-rwxrwxrwx   0 root         (0) root         (0)     6390 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_additional_inertia.py
+-rwxrwxrwx   0 root         (0) root         (0)     5718 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_aerodynamic_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     6830 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_element_momentum.py
+-rwxrwxrwx   0 root         (0) root         (0)     6751 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_element_theory_corrections.py
+-rwxrwxrwx   0 root         (0) root         (0)     5954 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_hydrodynamic_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     8285 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_load_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     7665 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_modelling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6354 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_motion_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     6075 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_mounting.py
+-rwxrwxrwx   0 root         (0) root         (0)     6858 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_mounting_on_rotor.py
+-rwxrwxrwx   0 root         (0) root         (0)     7141 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_output_group.py
+-rwxrwxrwx   0 root         (0) root         (0)     7954 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_outputs_for_location.py
+-rwxrwxrwx   0 root         (0) root         (0)     6394 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_outputs_for_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     6401 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_outputs_for_section.py
+-rwxrwxrwx   0 root         (0) root         (0)     6331 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_point_loading.py
+-rwxrwxrwx   0 root         (0) root         (0)     6101 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_point_mass.py
+-rwxrwxrwx   0 root         (0) root         (0)     6570 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_aerodynamic_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     7881 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_axes_and_coordinate_systems.py
+-rwxrwxrwx   0 root         (0) root         (0)     7216 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_definition.py
+-rwxrwxrwx   0 root         (0) root         (0)     6061 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_mass_coordinate_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6555 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_mass_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     6225 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_neutral_axis_coordinate_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6237 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_offset_and_rotated_axes_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6263 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_offset_axes_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6930 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_output_coordinate_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6378 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_quarter_chord_coordinate_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6308 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_reference_coordinate_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6074 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_shear_axis.py
+-rwxrwxrwx   0 root         (0) root         (0)     7398 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_structural_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     7169 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_sensor.py
+-rwxrwxrwx   0 root         (0) root         (0)     7024 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis.py
+-rwxrwxrwx   0 root         (0) root         (0)     6788 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6548 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_idling.py
+-rwxrwxrwx   0 root         (0) root         (0)     7863 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_mode_of_operation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6496 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_parked.py
+-rwxrwxrwx   0 root         (0) root         (0)     5788 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_perturbations.py
+-rwxrwxrwx   0 root         (0) root         (0)     6075 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_strain_gauge.py
+-rwxrwxrwx   0 root         (0) root         (0)     7305 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/bladed_analysis.py
+-rwxrwxrwx   0 root         (0) root         (0)     7255 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/bladed_control.py
+-rwxrwxrwx   0 root         (0) root         (0)     7342 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/brake.py
+-rwxrwxrwx   0 root         (0) root         (0)     6741 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_diagram.py
+-rwxrwxrwx   0 root         (0) root         (0)     7649 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_diagram_mode_of_operation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6147 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_idling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6147 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_parked.py
+-rwxrwxrwx   0 root         (0) root         (0)     6255 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_power_production.py
+-rwxrwxrwx   0 root         (0) root         (0)     6294 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/circular_lidar_scan.py
+-rwxrwxrwx   0 root         (0) root         (0)     8631 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/component.py
+-rwxrwxrwx   0 root         (0) root         (0)     6345 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/compressible_beddoes_leishman_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     5727 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/conditional_event.py
+-rwxrwxrwx   0 root         (0) root         (0)     6753 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/connectable_node.py
+-rwxrwxrwx   0 root         (0) root         (0)     6451 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/constants.py
+-rwxrwxrwx   0 root         (0) root         (0)     8055 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/control_strategy.py
+-rwxrwxrwx   0 root         (0) root         (0)     6385 2023-07-17 23:23:02.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/controlled_operation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6007 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/controlled_stop.py
+-rwxrwxrwx   0 root         (0) root         (0)     6627 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/controller_fault.py
+-rwxrwxrwx   0 root         (0) root         (0)     6239 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/controller_lidar_settings.py
+-rwxrwxrwx   0 root         (0) root         (0)     6541 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/counterweight_hub.py
+-rwxrwxrwx   0 root         (0) root         (0)     6478 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/coupling_terms.py
+-rwxrwxrwx   0 root         (0) root         (0)     6796 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/cuboid_nacelle_cover.py
+-rwxrwxrwx   0 root         (0) root         (0)     6926 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current.py
+-rwxrwxrwx   0 root         (0) root         (0)     7079 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_shear_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7498 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_shear_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6816 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7233 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6952 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_horizontal_shear_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7390 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_horizontal_shear_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6808 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_mean_speed_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7234 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_mean_speed_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6906 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_vertical_shear_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7338 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_vertical_shear_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6679 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/currents.py
+-rwxrwxrwx   0 root         (0) root         (0)     6243 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/custom_linear_model_dll.py
+-rwxrwxrwx   0 root         (0) root         (0)     5641 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     6241 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/damper_force_or_torque_vs_rate.py
+-rwxrwxrwx   0 root         (0) root         (0)     6285 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/dimensionless_range.py
+-rwxrwxrwx   0 root         (0) root         (0)     6511 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/dnv.py
+-rwxrwxrwx   0 root         (0) root         (0)     8711 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/drivetrain.py
+-rwxrwxrwx   0 root         (0) root         (0)     5966 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/drivetrain_output_group.py
+-rwxrwxrwx   0 root         (0) root         (0)     8953 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/dynamic_stall.py
+-rwxrwxrwx   0 root         (0) root         (0)     7711 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/dynamic_upstream_wake.py
+-rwxrwxrwx   0 root         (0) root         (0)     7781 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/dynamic_wake.py
+-rwxrwxrwx   0 root         (0) root         (0)     6279 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/earthquake.py
+-rwxrwxrwx   0 root         (0) root         (0)     7669 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/electrical_grid.py
+-rwxrwxrwx   0 root         (0) root         (0)     7118 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/electrical_losses.py
+-rwxrwxrwx   0 root         (0) root         (0)     6004 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/emergency_stop_button.py
+-rwxrwxrwx   0 root         (0) root         (0)     6270 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/emergency_stop_operation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6359 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/environment.py
+-rwxrwxrwx   0 root         (0) root         (0)     6519 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/equilibrium_wake_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     8208 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/event.py
+-rwxrwxrwx   0 root         (0) root         (0)     6928 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/evolving_current_turbulence.py
+-rwxrwxrwx   0 root         (0) root         (0)     7605 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/evolving_wind_turbulence.py
+-rwxrwxrwx   0 root         (0) root         (0)     6971 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/explicit_newmark_beta_fixed_step.py
+-rwxrwxrwx   0 root         (0) root         (0)     6642 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/explicit_tower_can.py
+-rwxrwxrwx   0 root         (0) root         (0)     6563 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/exponential_shear_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6265 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/extension_pieces.py
+-rwxrwxrwx   0 root         (0) root         (0)     9316 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_controller.py
+-rwxrwxrwx   0 root         (0) root         (0)     6631 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_flow_source_for_wind.py
+-rwxrwxrwx   0 root         (0) root         (0)     6328 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_module.py
+-rwxrwxrwx   0 root         (0) root         (0)     7094 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_module_component.py
+-rwxrwxrwx   0 root         (0) root         (0)     6942 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/externally_stepped_simulation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6265 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/fault.py
+-rwxrwxrwx   0 root         (0) root         (0)     6331 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/finite_element_blade_modelling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6361 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/first_order_response.py
+-rwxrwxrwx   0 root         (0) root         (0)     6188 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_speed_active_damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     6559 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_speed_generator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6668 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_speed_induction_generator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6132 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_step_integrator.py
+-rwxrwxrwx   0 root         (0) root         (0)     5912 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/flex_body.py
+-rwxrwxrwx   0 root         (0) root         (0)     5672 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/flexible_joint.py
+-rwxrwxrwx   0 root         (0) root         (0)     6244 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/flow_obstruction.py
+-rwxrwxrwx   0 root         (0) root         (0)     7001 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/foundation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6336 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/free_flow_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6261 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/frequency_vs_spectral_density.py
+-rwxrwxrwx   0 root         (0) root         (0)     6157 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/friction.py
+-rwxrwxrwx   0 root         (0) root         (0)     6315 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/friction_torque_vs_stiction_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6404 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/frozen_wake_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6776 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/gaussian.py
+-rwxrwxrwx   0 root         (0) root         (0)     6666 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/gearbox.py
+-rwxrwxrwx   0 root         (0) root         (0)     6466 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/gearbox_mounting_rolling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6845 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/generalised_alpha_fixed_step.py
+-rwxrwxrwx   0 root         (0) root         (0)     6640 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6075 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_output_group.py
+-rwxrwxrwx   0 root         (0) root         (0)     6325 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_overpower.py
+-rwxrwxrwx   0 root         (0) root         (0)     6349 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_overspeed.py
+-rwxrwxrwx   0 root         (0) root         (0)     6161 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_short_circuit.py
+-rwxrwxrwx   0 root         (0) root         (0)     6364 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/gl2010_icing.py
+-rwxrwxrwx   0 root         (0) root         (0)     6240 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/glauert_skew_wake_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     5999 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/grid_loss.py
+-rwxrwxrwx   0 root         (0) root         (0)     6259 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/grid_loss_energy_sink.py
+-rwxrwxrwx   0 root         (0) root         (0)     6314 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/hardware_tracking.py
+-rwxrwxrwx   0 root         (0) root         (0)     6307 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/height_up_tower_vs_cd_cm.py
+-rwxrwxrwx   0 root         (0) root         (0)     6141 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/height_vs_shear_factor.py
+-rwxrwxrwx   0 root         (0) root         (0)     6158 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/high_speed_shaft.py
+-rwxrwxrwx   0 root         (0) root         (0)     6311 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/high_speed_shaft_flexibility.py
+-rwxrwxrwx   0 root         (0) root         (0)     8000 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/hub.py
+-rwxrwxrwx   0 root         (0) root         (0)     6669 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/hub_mass_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     6128 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/hub_output_group.py
+-rwxrwxrwx   0 root         (0) root         (0)     7393 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/hydraulic_accumulator_yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6729 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/hydrodynamic_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6524 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/iced_condition.py
+-rwxrwxrwx   0 root         (0) root         (0)     6272 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/idealised_pitch_actuator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6030 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/idealised_yaw_manoever.py
+-rwxrwxrwx   0 root         (0) root         (0)     6140 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/iec4_icing.py
+-rwxrwxrwx   0 root         (0) root         (0)     7186 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/implicit_newmark_beta_fixed_step.py
+-rwxrwxrwx   0 root         (0) root         (0)     6371 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/incompressible_beddoes_leishman_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6171 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/independent_pitch_hub.py
+-rwxrwxrwx   0 root         (0) root         (0)     6500 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_azimuth_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     7603 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_condition.py
+-rwxrwxrwx   0 root         (0) root         (0)     6299 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_control_state.py
+-rwxrwxrwx   0 root         (0) root         (0)     6250 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_inactive_control_state.py
+-rwxrwxrwx   0 root         (0) root         (0)     6382 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_machine_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     6288 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     6105 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/input_power_vs_loss.py
+-rwxrwxrwx   0 root         (0) root         (0)     6186 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/instantaneous.py
+-rwxrwxrwx   0 root         (0) root         (0)     7387 2023-07-17 23:23:03.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/integrator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6014 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/interface_load_file.py
+-rwxrwxrwx   0 root         (0) root         (0)     7054 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/irregular_waves.py
+-rwxrwxrwx   0 root         (0) root         (0)     6791 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/jonswap_pierson_moskowitz.py
+-rwxrwxrwx   0 root         (0) root         (0)     8148 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/laminar_flow_current.py
+-rwxrwxrwx   0 root         (0) root         (0)     8034 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/laminar_flow_wind.py
+-rwxrwxrwx   0 root         (0) root         (0)     6665 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_blade_sensor_mapping.py
+-rwxrwxrwx   0 root         (0) root         (0)     6552 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_nacelle_sensor_mapping.py
+-rwxrwxrwx   0 root         (0) root         (0)     8040 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_sensor_mapping.py
+-rwxrwxrwx   0 root         (0) root         (0)     6638 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_sensor_mappings.py
+-rwxrwxrwx   0 root         (0) root         (0)     6375 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_tower_sensor_mapping.py
+-rwxrwxrwx   0 root         (0) root         (0)     6095 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/length_vs_radius.py
+-rwxrwxrwx   0 root         (0) root         (0)     7953 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar.py
+-rwxrwxrwx   0 root         (0) root         (0)     6592 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_beam.py
+-rwxrwxrwx   0 root         (0) root         (0)     6268 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_controller_scan.py
+-rwxrwxrwx   0 root         (0) root         (0)     7814 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_focal_distance_control.py
+-rwxrwxrwx   0 root         (0) root         (0)     6275 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_mounting_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     7175 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_scanning_pattern.py
+-rwxrwxrwx   0 root         (0) root         (0)     6010 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_airy.py
+-rwxrwxrwx   0 root         (0) root         (0)     6657 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_electrical_losses.py
+-rwxrwxrwx   0 root         (0) root         (0)     6669 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_foundation.py
+-rwxrwxrwx   0 root         (0) root         (0)     7043 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_passive_damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     7723 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_pitch_actuator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6206 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_spring_damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     6422 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_spring_yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6825 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/linearisation_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6587 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/loading_dependent_friction.py
+-rwxrwxrwx   0 root         (0) root         (0)     6539 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/logarithmic_shear_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6684 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/look_up_shear_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6128 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/look_up_table_element.py
+-rwxrwxrwx   0 root         (0) root         (0)     7044 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/low_speed_shaft.py
+-rwxrwxrwx   0 root         (0) root         (0)     6437 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/mac_camy_fuchs.py
+-rwxrwxrwx   0 root         (0) root         (0)     6443 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/main_bearing.py
+-rwxrwxrwx   0 root         (0) root         (0)     6221 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/meandering_wake.py
+-rwxrwxrwx   0 root         (0) root         (0)    17149 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/measured_signal_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     7114 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/mechanical_losses.py
+-rwxrwxrwx   0 root         (0) root         (0)     6227 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/midpoint_method_fixed_step.py
+-rwxrwxrwx   0 root         (0) root         (0)     6881 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/modal_blade_modelling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6942 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/model_linearisation.py
+-rwxrwxrwx   0 root         (0) root         (0)     7054 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/model_linearisation_perturbations.py
+-rwxrwxrwx   0 root         (0) root         (0)     6224 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/moment_vs_coulomb_friction.py
+-rwxrwxrwx   0 root         (0) root         (0)     8143 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/momentum_theory_corrections.py
+-rwxrwxrwx   0 root         (0) root         (0)     6389 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/mooring_initial_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     6501 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/multiple_lidar_focal_distances.py
+-rwxrwxrwx   0 root         (0) root         (0)     7499 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle.py
+-rwxrwxrwx   0 root         (0) root         (0)     6189 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_accelerometer.py
+-rwxrwxrwx   0 root         (0) root         (0)     6389 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_centre_of_gravity.py
+-rwxrwxrwx   0 root         (0) root         (0)     7143 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_cover.py
+-rwxrwxrwx   0 root         (0) root         (0)     6176 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_inclinometer.py
+-rwxrwxrwx   0 root         (0) root         (0)     6477 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_mass_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     6452 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_moment_of_inertia.py
+-rwxrwxrwx   0 root         (0) root         (0)     7233 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_sensor.py
+-rwxrwxrwx   0 root         (0) root         (0)     6348 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_vibration.py
+-rwxrwxrwx   0 root         (0) root         (0)     6099 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/near_shore_current.py
+-rwxrwxrwx   0 root         (0) root         (0)     6383 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/near_surface_current.py
+-rwxrwxrwx   0 root         (0) root         (0)     6769 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/network_frequency_disturbance.py
+-rwxrwxrwx   0 root         (0) root         (0)     6725 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/network_voltage_disturbance.py
+-rwxrwxrwx   0 root         (0) root         (0)     6027 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/non_actuated_yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6694 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/non_linear_electrical_losses.py
+-rwxrwxrwx   0 root         (0) root         (0)     6467 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/non_linear_shaft_brake.py
+-rwxrwxrwx   0 root         (0) root         (0)     6231 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/normal_stop_operation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6565 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     6798 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/oye_dynamic_wake.py
+-rwxrwxrwx   0 root         (0) root         (0)     6005 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/oye_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6448 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pallet_mounting_flexibility.py
+-rwxrwxrwx   0 root         (0) root         (0)     6186 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pallet_nodding.py
+-rwxrwxrwx   0 root         (0) root         (0)     6177 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pallet_rolling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6550 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/passive.py
+-rwxrwxrwx   0 root         (0) root         (0)     6261 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/passive_yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     8480 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pendulum_damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     6350 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pendulum_damper_mounting_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     7385 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/performance_coefficients_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6428 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/permanently_stuck_pitch_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6570 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/perturbation_settings.py
+-rwxrwxrwx   0 root         (0) root         (0)     6225 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_acceleration_limits.py
+-rwxrwxrwx   0 root         (0) root         (0)     7336 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_actuation_limited.py
+-rwxrwxrwx   0 root         (0) root         (0)     7102 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_actuator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6561 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_rate_relationship.py
+-rwxrwxrwx   0 root         (0) root         (0)     6675 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_xy_force_relationship.py
+-rwxrwxrwx   0 root         (0) root         (0)     6694 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_xy_moment_relationship.py
+-rwxrwxrwx   0 root         (0) root         (0)     6663 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_z_force_relationship.py
+-rwxrwxrwx   0 root         (0) root         (0)     7323 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_kinetic_friction.py
+-rwxrwxrwx   0 root         (0) root         (0)     6731 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_stiction.py
+-rwxrwxrwx   0 root         (0) root         (0)     6549 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_constant_rate_safety_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6712 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_controller.py
+-rwxrwxrwx   0 root         (0) root         (0)     5866 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_controller_rate_safety_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     5926 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_controller_torque_safety_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6369 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_end_stops.py
+-rwxrwxrwx   0 root         (0) root         (0)     5995 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault.py
+-rwxrwxrwx   0 root         (0) root         (0)     6473 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_constant_rate.py
+-rwxrwxrwx   0 root         (0) root         (0)     6444 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_constant_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6344 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_limp.py
+-rwxrwxrwx   0 root         (0) root         (0)     6144 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_seizure.py
+-rwxrwxrwx   0 root         (0) root         (0)     7087 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_seizure_at_angle.py
+-rwxrwxrwx   0 root         (0) root         (0)     6406 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_force_limits.py
+-rwxrwxrwx   0 root         (0) root         (0)     6114 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_force_minimum_maximum.py
+-rwxrwxrwx   0 root         (0) root         (0)     6230 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_limit_switches.py
+-rwxrwxrwx   0 root         (0) root         (0)     6209 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_position_demand.py
+-rwxrwxrwx   0 root         (0) root         (0)     6479 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_position_vs_minimum_and_maximum_moment.py
+-rwxrwxrwx   0 root         (0) root         (0)     6156 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_demand.py
+-rwxrwxrwx   0 root         (0) root         (0)     6471 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_demand_set_by_external_controller.py
+-rwxrwxrwx   0 root         (0) root         (0)     6115 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_limits.py
+-rwxrwxrwx   0 root         (0) root         (0)     6706 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_varies_with_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     6642 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_varies_with_time.py
+-rwxrwxrwx   0 root         (0) root         (0)     6476 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_vs_minimum_and_maximum_moment.py
+-rwxrwxrwx   0 root         (0) root         (0)     6167 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_safety_limit_switches.py
+-rwxrwxrwx   0 root         (0) root         (0)     7865 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_safety_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     8736 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     7567 2023-07-17 23:23:04.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system_demand.py
+-rwxrwxrwx   0 root         (0) root         (0)     6197 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system_output_group.py
+-rwxrwxrwx   0 root         (0) root         (0)     8194 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system_rotary_drive.py
+-rwxrwxrwx   0 root         (0) root         (0)     6772 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_torque_limits.py
+-rwxrwxrwx   0 root         (0) root         (0)     6194 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_torque_minimum_maximum.py
+-rwxrwxrwx   0 root         (0) root         (0)     6431 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_torque_set_by_external_controller.py
+-rwxrwxrwx   0 root         (0) root         (0)     6130 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitt_and_peters_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6903 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/point_inertia.py
+-rwxrwxrwx   0 root         (0) root         (0)     6451 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/point_on_span.py
+-rwxrwxrwx   0 root         (0) root         (0)     6298 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/position_of_upwind_turbine.py
+-rwxrwxrwx   0 root         (0) root         (0)     6660 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/power_scaling_losses.py
+-rwxrwxrwx   0 root         (0) root         (0)     7974 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_direction_shear_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7664 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_direction_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7836 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_horizontal_shear_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7707 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_mean_speed_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7793 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_vertical_shear_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     6600 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_lidar_scan.py
+-rwxrwxrwx   0 root         (0) root         (0)     7944 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_direction_shear_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7613 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_direction_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7770 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_horizontal_shear_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7646 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_mean_speed_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     7726 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_vertical_shear_transient.py
+-rwxrwxrwx   0 root         (0) root         (0)     8023 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/proportional_integral_deriviative.py
+-rwxrwxrwx   0 root         (0) root         (0)     6685 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/quadratic_yaw_damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     6110 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/radius_vs_deficit.py
+-rwxrwxrwx   0 root         (0) root         (0)     6207 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rate_vs_friction_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6118 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rate_vs_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     6066 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rate_vs_time.py
+-rwxrwxrwx   0 root         (0) root         (0)     6291 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/regular_waves.py
+-rwxrwxrwx   0 root         (0) root         (0)     7414 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/response_to_demand.py
+-rwxrwxrwx   0 root         (0) root         (0)     6189 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rigid_blade_modelling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6765 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rosette_lidar_scan.py
+-rwxrwxrwx   0 root         (0) root         (0)     6323 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_idling_control_state.py
+-rwxrwxrwx   0 root         (0) root         (0)     7093 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_imbalances.py
+-rwxrwxrwx   0 root         (0) root         (0)     6382 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_in_power_production.py
+-rwxrwxrwx   0 root         (0) root         (0)     6315 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_overspeed.py
+-rwxrwxrwx   0 root         (0) root         (0)     6329 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_parked_control_state.py
+-rwxrwxrwx   0 root         (0) root         (0)     6281 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/runge_kutta4th_order_fixed_step.py
+-rwxrwxrwx   0 root         (0) root         (0)     6925 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/runge_kutta_variable_step.py
+-rwxrwxrwx   0 root         (0) root         (0)     7699 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/safety_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     7053 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/safety_system_circuit.py
+-rwxrwxrwx   0 root         (0) root         (0)     6596 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/sea_state.py
+-rwxrwxrwx   0 root         (0) root         (0)     6586 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/second_order_response.py
+-rwxrwxrwx   0 root         (0) root         (0)     6578 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/selected_component_output_group.py
+-rwxrwxrwx   0 root         (0) root         (0)     6627 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/sensor_mapping.py
+-rwxrwxrwx   0 root         (0) root         (0)     6465 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/sensor_reference.py
+-rwxrwxrwx   0 root         (0) root         (0)     7007 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/sensors_used.py
+-rwxrwxrwx   0 root         (0) root         (0)     6292 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/sequential_timestep_adaptation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6731 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/setpoint_trajectory_planning.py
+-rwxrwxrwx   0 root         (0) root         (0)     6686 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/settings.py
+-rwxrwxrwx   0 root         (0) root         (0)     6198 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/shaft_speed_vs_power_loss.py
+-rwxrwxrwx   0 root         (0) root         (0)     6291 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/shaft_speed_vs_resisting_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6307 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/shear_stiffnesses.py
+-rwxrwxrwx   0 root         (0) root         (0)     6187 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/short_circuit.py
+-rwxrwxrwx   0 root         (0) root         (0)     7198 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     6545 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_acceleration.py
+-rwxrwxrwx   0 root         (0) root         (0)     6387 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_angle.py
+-rwxrwxrwx   0 root         (0) root         (0)     6652 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_angular_acceleration.py
+-rwxrwxrwx   0 root         (0) root         (0)     6566 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_angular_velocity.py
+-rwxrwxrwx   0 root         (0) root         (0)     6383 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_force.py
+-rwxrwxrwx   0 root         (0) root         (0)     6397 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_length.py
+-rwxrwxrwx   0 root         (0) root         (0)     6433 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_moment.py
+-rwxrwxrwx   0 root         (0) root         (0)     6473 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_power.py
+-rwxrwxrwx   0 root         (0) root         (0)     6451 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_velocity.py
+-rwxrwxrwx   0 root         (0) root         (0)     6567 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/simple_cutoff_frequency.py
+-rwxrwxrwx   0 root         (0) root         (0)     6497 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/simple_shaft_brake.py
+-rwxrwxrwx   0 root         (0) root         (0)     6848 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/simple_tower_can.py
+-rwxrwxrwx   0 root         (0) root         (0)     7121 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/simplified_linear_foundation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6317 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/single_actuator_collective_hub.py
+-rwxrwxrwx   0 root         (0) root         (0)     6460 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/single_lidar_focal_distance.py
+-rwxrwxrwx   0 root         (0) root         (0)     6347 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/six_by_six_inertia.py
+-rwxrwxrwx   0 root         (0) root         (0)     5667 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/skew_wake_model.py
+-rwxrwxrwx   0 root         (0) root         (0)     6070 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/slipping_clutch.py
+-rwxrwxrwx   0 root         (0) root         (0)     6694 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/solver_settings.py
+-rwxrwxrwx   0 root         (0) root         (0)     6541 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/spinner.py
+-rwxrwxrwx   0 root         (0) root         (0)     6289 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/spring_force_or_torque_vs_position.py
+-rwxrwxrwx   0 root         (0) root         (0)     6061 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/standard_hub.py
+-rwxrwxrwx   0 root         (0) root         (0)     6184 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/standard_pitch_limit_switches.py
+-rwxrwxrwx   0 root         (0) root         (0)     6405 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/start_up_operation.py
+-rwxrwxrwx   0 root         (0) root         (0)     7673 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     5724 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_calculation_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     6239 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_calculation_with_component_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     7599 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_operational_loads_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     8477 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_parked_loads_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     7523 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_power_curve_calculation.py
+-rwxrwxrwx   0 root         (0) root         (0)     7641 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_wake_deficit.py
+-rwxrwxrwx   0 root         (0) root         (0)     6897 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/stream_function.py
+-rwxrwxrwx   0 root         (0) root         (0)     6066 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/structural_mode.py
+-rwxrwxrwx   0 root         (0) root         (0)     6281 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/structural_modelling.py
+-rwxrwxrwx   0 root         (0) root         (0)     7487 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/structural_modelling_settings.py
+-rwxrwxrwx   0 root         (0) root         (0)     5672 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/structure.py
+-rwxrwxrwx   0 root         (0) root         (0)     6409 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/sub_surface_current.py
+-rwxrwxrwx   0 root         (0) root         (0)     7668 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/superelement.py
+-rwxrwxrwx   0 root         (0) root         (0)     5769 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/support_structure_point_loading.py
+-rwxrwxrwx   0 root         (0) root         (0)     6205 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/thrust_vs_coulomb_friction.py
+-rwxrwxrwx   0 root         (0) root         (0)     5829 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tide.py
+-rwxrwxrwx   0 root         (0) root         (0)     6770 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_current.py
+-rwxrwxrwx   0 root         (0) root         (0)     9833 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     8249 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_simulation.py
+-rwxrwxrwx   0 root         (0) root         (0)     8171 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_wind.py
+-rwxrwxrwx   0 root         (0) root         (0)     6153 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_direction.py
+-rwxrwxrwx   0 root         (0) root         (0)     6249 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_direction_shear.py
+-rwxrwxrwx   0 root         (0) root         (0)     6235 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_horizontal_shear.py
+-rwxrwxrwx   0 root         (0) root         (0)     6167 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_mean_speed.py
+-rwxrwxrwx   0 root         (0) root         (0)     6211 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_vertical_shear.py
+-rwxrwxrwx   0 root         (0) root         (0)     6193 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_frequency_change.py
+-rwxrwxrwx   0 root         (0) root         (0)     6115 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6158 2023-07-17 23:23:05.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_voltage_change.py
+-rwxrwxrwx   0 root         (0) root         (0)     6129 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_direction.py
+-rwxrwxrwx   0 root         (0) root         (0)     6225 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_direction_shear.py
+-rwxrwxrwx   0 root         (0) root         (0)     6211 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_horizontal_shear.py
+-rwxrwxrwx   0 root         (0) root         (0)     6143 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_mean_speed.py
+-rwxrwxrwx   0 root         (0) root         (0)     6187 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_vertical_shear.py
+-rwxrwxrwx   0 root         (0) root         (0)     5878 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/timed_event.py
+-rwxrwxrwx   0 root         (0) root         (0)     6307 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/torque_controlled_yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     6699 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/torque_scaling_losses.py
+-rwxrwxrwx   0 root         (0) root         (0)     8422 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower.py
+-rwxrwxrwx   0 root         (0) root         (0)     6110 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_accelerometer.py
+-rwxrwxrwx   0 root         (0) root         (0)     6228 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_aerodynamic_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     7259 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can.py
+-rwxrwxrwx   0 root         (0) root         (0)     6362 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_modelling.py
+-rwxrwxrwx   0 root         (0) root         (0)     6269 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_by_material.py
+-rwxrwxrwx   0 root         (0) root         (0)     6425 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_by_material_where_different.py
+-rwxrwxrwx   0 root         (0) root         (0)     7392 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_explicit.py
+-rwxrwxrwx   0 root         (0) root         (0)     7548 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_explicit_where_different.py
+-rwxrwxrwx   0 root         (0) root         (0)     6182 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_section_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     6294 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_section_properties_where_different.py
+-rwxrwxrwx   0 root         (0) root         (0)     6000 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_connectable_node.py
+-rwxrwxrwx   0 root         (0) root         (0)     6627 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_hydrodynamic_properties.py
+-rwxrwxrwx   0 root         (0) root         (0)     6097 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_inclinometer.py
+-rwxrwxrwx   0 root         (0) root         (0)     6327 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_material.py
+-rwxrwxrwx   0 root         (0) root         (0)     6684 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_node_at_height_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     7004 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_node_loads.py
+-rwxrwxrwx   0 root         (0) root         (0)     6364 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_node_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     6887 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_output_group.py
+-rwxrwxrwx   0 root         (0) root         (0)     6386 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_point_loading.py
+-rwxrwxrwx   0 root         (0) root         (0)     7454 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_sensor.py
+-rwxrwxrwx   0 root         (0) root         (0)     6058 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_shadow_potential_flow.py
+-rwxrwxrwx   0 root         (0) root         (0)     7390 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_shadow_powles.py
+-rwxrwxrwx   0 root         (0) root         (0)     6097 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_strain_gauge.py
+-rwxrwxrwx   0 root         (0) root         (0)     5672 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_behaviour.py
+-rwxrwxrwx   0 root         (0) root         (0)     8527 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults.py
+-rwxrwxrwx   0 root         (0) root         (0)     7161 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_acceleration.py
+-rwxrwxrwx   0 root         (0) root         (0)     6977 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_angle.py
+-rwxrwxrwx   0 root         (0) root         (0)     7343 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_angular_acceleration.py
+-rwxrwxrwx   0 root         (0) root         (0)     7211 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_angular_velocity.py
+-rwxrwxrwx   0 root         (0) root         (0)     6969 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_force.py
+-rwxrwxrwx   0 root         (0) root         (0)     6951 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_length.py
+-rwxrwxrwx   0 root         (0) root         (0)     7139 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_moment.py
+-rwxrwxrwx   0 root         (0) root         (0)     6961 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_power.py
+-rwxrwxrwx   0 root         (0) root         (0)     7073 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_velocity.py
+-rwxrwxrwx   0 root         (0) root         (0)     6926 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/turbine.py
+-rwxrwxrwx   0 root         (0) root         (0)     8759 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/turbulent_current.py
+-rwxrwxrwx   0 root         (0) root         (0)     9133 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/turbulent_wind.py
+-rwxrwxrwx   0 root         (0) root         (0)     6871 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/user_defined_wake_deficit.py
+-rwxrwxrwx   0 root         (0) root         (0)     6434 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/user_defined_waves.py
+-rwxrwxrwx   0 root         (0) root         (0)     7296 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_slip_generator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6170 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_speed_active_damper.py
+-rwxrwxrwx   0 root         (0) root         (0)     7051 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_speed_generator.py
+-rwxrwxrwx   0 root         (0) root         (0)     7141 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_speed_induction_generator.py
+-rwxrwxrwx   0 root         (0) root         (0)     6088 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/vector3_d.py
+-rwxrwxrwx   0 root         (0) root         (0)     6252 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/velocity_range.py
+-rwxrwxrwx   0 root         (0) root         (0)     6243 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/velocity_vs_damping_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     7235 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/vertical_shear.py
+-rwxrwxrwx   0 root         (0) root         (0)     9538 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/vortex_line.py
+-rwxrwxrwx   0 root         (0) root         (0)     6593 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wake_properties_of_upstream_turbine.py
+-rwxrwxrwx   0 root         (0) root         (0)     6188 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/water_characteristics.py
+-rwxrwxrwx   0 root         (0) root         (0)     7613 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wave_diffraction_approximation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6871 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wave_spectrum.py
+-rwxrwxrwx   0 root         (0) root         (0)     7020 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/waves.py
+-rwxrwxrwx   0 root         (0) root         (0)     6895 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind.py
+-rwxrwxrwx   0 root         (0) root         (0)     7034 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_shear_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7453 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_shear_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6762 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7179 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6951 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_horizontal_shear_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7336 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_horizontal_shear_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6764 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_mean_speed_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7180 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_mean_speed_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6852 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_vertical_shear_time_history.py
+-rwxrwxrwx   0 root         (0) root         (0)     7284 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_vertical_shear_variation.py
+-rwxrwxrwx   0 root         (0) root         (0)     6333 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/xml_blade_outputs.py
+-rwxrwxrwx   0 root         (0) root         (0)     5646 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault.py
+-rwxrwxrwx   0 root         (0) root         (0)     6574 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault_constant_rate.py
+-rwxrwxrwx   0 root         (0) root         (0)     6360 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault_constant_torque.py
+-rwxrwxrwx   0 root         (0) root         (0)     6805 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault_limp.py
+-rwxrwxrwx   0 root         (0) root         (0)     6471 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_manoever.py
+-rwxrwxrwx   0 root         (0) root         (0)     6373 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_system.py
+-rwxrwxrwx   0 root         (0) root         (0)     5977 2023-07-17 23:23:06.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_system_output_group.py
+drwxr-xr-x   0 root         (0) root         (0)        0 2023-07-19 10:00:31.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models.egg-info/
+-rw-r--r--   0 root         (0) root         (0)     2944 2023-07-19 10:00:22.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models.egg-info/PKG-INFO
+-rw-r--r--   0 root         (0) root         (0)    23334 2023-07-19 10:00:23.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models.egg-info/SOURCES.txt
+-rw-r--r--   0 root         (0) root         (0)        1 2023-07-19 10:00:22.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models.egg-info/dependency_links.txt
+-rw-r--r--   0 root         (0) root         (0)       14 2023-07-19 10:00:22.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models.egg-info/requires.txt
+-rw-r--r--   0 root         (0) root         (0)       18 2023-07-19 10:00:22.000000 dnv_bladed_models-0.3.44/src/dnv_bladed_models.egg-info/top_level.txt
```

### Comparing `dnv_bladed_models-0.3.32a686/LICENSE` & `dnv_bladed_models-0.3.44/LICENSE`

 * *Files identical despite different names*

### Comparing `dnv_bladed_models-0.3.32a686/README.md` & `dnv_bladed_models-0.3.44/README.md`

 * *Files 20% similar despite different names*

```diff
@@ -1,31 +1,35 @@
 # Bladed Next Gen Python Models API
 
-`dnv_bladed_models=0.3.32a686`
+`dnv_bladed_models=0.3.44`
 
 A Python package to easily work with JSON input models for Bladed Next Generation.
 
 Visit <https://bladednextgen.dnv.com/> for more information.
 
+## Prerequisites
+
+ - Requires Python 3.7 or above
+ 
 ## Usage
 
 Load a JSON model from file:
 
 ```python
 import dnv_bladed_models as models
 
 analysis = models.BladedAnalysis.from_file('/path/to/analysis.json')
 ```
 
 Modify a model object in code:
 
 ```python
-analysis.SteadyCalculation.PitchRange.Minimum = 0.
-analysis.SteadyCalculation.PitchRange.Maximum = 0.
-analysis.SteadyCalculation.PitchRange.Interval = 0.1
+analysis.SteadyCalculation.TipSpeedRatioRange.Minimum = 4.
+analysis.SteadyCalculation.TipSpeedRatioRange.Maximum = 10.
+analysis.SteadyCalculation.TipSpeedRatioRange.Interval = 0.1
 ```
 
 Work with a turbine assembly component in the Component Library, in code:
 
 ```python
 blade: models.Blade = analysis.ComponentDefinitions['Blade']
 ```
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/__init__.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/__init__.py`

 * *Files 0% similar despite different names*

```diff
@@ -1,10 +1,12 @@
 # coding: utf-8
 # flake8: noqa
-
+__doc__ = """
+An API for working with Bladed Next Gen models.
+"""
 from dnv_bladed_models.active_damper import ActiveDamper
 from dnv_bladed_models.active_yaw_system import ActiveYawSystem
 from dnv_bladed_models.actuated_yaw_system import ActuatedYawSystem
 from dnv_bladed_models.added_inertia import AddedInertia, AddedInertia_AddedInertiaTypeEnum
 from dnv_bladed_models.additional_constrained_wave import AdditionalConstrainedWave, AdditionalConstrainedWave_MethodEnum
 from dnv_bladed_models.aerodynamic_information_calculation import AerodynamicInformationCalculation
 from dnv_bladed_models.aerodynamic_model import AerodynamicModel, AerodynamicModel_AerodynamicModelTypeEnum
@@ -137,14 +139,15 @@
 from dnv_bladed_models.flexible_joint import FlexibleJoint
 from dnv_bladed_models.flow_obstruction import FlowObstruction
 from dnv_bladed_models.foundation import Foundation
 from dnv_bladed_models.free_flow_model import FreeFlowModel
 from dnv_bladed_models.frequency_vs_spectral_density import FrequencyVsSpectralDensity
 from dnv_bladed_models.friction import Friction
 from dnv_bladed_models.friction_torque_vs_stiction_torque import FrictionTorqueVsStictionTorque
+from dnv_bladed_models.frozen_wake_model import FrozenWakeModel
 from dnv_bladed_models.gl2010_icing import GL2010Icing
 from dnv_bladed_models.gaussian import Gaussian
 from dnv_bladed_models.gearbox import Gearbox
 from dnv_bladed_models.gearbox_mounting_rolling import GearboxMountingRolling
 from dnv_bladed_models.generalised_alpha_fixed_step import GeneralisedAlphaFixedStep
 from dnv_bladed_models.generator import Generator
 from dnv_bladed_models.generator_output_group import GeneratorOutputGroup
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/active_damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/active_damper.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.damper import Damper
 
 
 class ActiveDamper(Damper):
     """ActiveDamper - An active damper, applying a force which is specified by the controller.
-
+    
     Attributes:
-        - ForceLag: float => A number representing a time.  The SI units for time are seconds.
-        - AccelerationLag: float => A number representing a time.  The SI units for time are seconds.
-        - ForceTransferFunction: float => A number representing a force.  The SI units for force are Newtons.
-        - PerturbationForLinearisationCalculation: float => The perturbation step to use during linearisation calculation. 
+    ----------
+    ForceLag : float
+        A number representing a time.  The SI units for time are seconds.
+    AccelerationLag : float
+        A number representing a time.  The SI units for time are seconds.
+    ForceTransferFunction : float
+        A number representing a force.  The SI units for force are Newtons.
+    PerturbationForLinearisationCalculation : float
+        The perturbation step to use during linearisation calculation. 
+    
     """
 
     ForceLag: Optional[float] = Field(alias="ForceLag", default=None)
     AccelerationLag: Optional[float] = Field(alias="AccelerationLag", default=None)
     ForceTransferFunction: Optional[float] = Field(alias="ForceTransferFunction", default=None)
     PerturbationForLinearisationCalculation: Optional[float] = Field(alias="PerturbationForLinearisationCalculation", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/active_yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/active_yaw_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,44 +9,49 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.loading_dependent_friction import LoadingDependentFriction
 
 from dnv_bladed_models.non_actuated_yaw_system import NonActuatedYawSystem
 
 from dnv_bladed_models.quadratic_yaw_damper import QuadraticYawDamper
 
 
 class ActiveYawSystem(NonActuatedYawSystem):
     """ActiveYawSystem - An active yaw system, which can change the turbine&#39;s direction on instruction from the controller.
-
+    
     Attributes:
-        - QuadraticDamper: QuadraticYawDamper 
-        - LoadingDependentFriction: LoadingDependentFriction 
-        - CoulombStiction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    ----------
+    QuadraticDamper : QuadraticYawDamper
+    LoadingDependentFriction : LoadingDependentFriction
+    CoulombStiction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    
     """
 
     QuadraticDamper: Optional[QuadraticYawDamper] = Field(alias="QuadraticDamper", default=None)
     LoadingDependentFriction: Optional[LoadingDependentFriction] = Field(alias="LoadingDependentFriction", default=None)
     CoulombStiction: Optional[float] = Field(alias="CoulombStiction", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/actuated_yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/actuated_yaw_system.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,33 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.gearbox import Gearbox
 
 from dnv_bladed_models.high_speed_shaft import HighSpeedShaft
 
 from dnv_bladed_models.yaw_system import YawSystem
 
 
 class ActuatedYawSystem(YawSystem, ComponentType='ActuatedYawSystem'):
     """ActuatedYawSystem - An active yaw system with a modelled actuator system.
-
+    
     Attributes:
-        - NumberOfMotors: int => The number of separate motors in the yaw system.
-        - TotalMomentOfInertiaOfMotors: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - BearingStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - BearingDamping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
-        - Gearbox: Gearbox 
-        - HighSpeedShaft: HighSpeedShaft 
-        - ActuatorTorquePerturbation: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ActuatedYawSystem&#39;
+    ----------
+    NumberOfMotors : int
+        The number of separate motors in the yaw system.
+    TotalMomentOfInertiaOfMotors : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    BearingStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    BearingDamping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    Gearbox : Gearbox
+    HighSpeedShaft : HighSpeedShaft
+    ActuatorTorquePerturbation : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ComponentType : str, readonly, default='ActuatedYawSystem'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ActuatedYawSystem'
+    
     """
 
     NumberOfMotors: Optional[int] = Field(alias="NumberOfMotors", default=None)
     TotalMomentOfInertiaOfMotors: Optional[float] = Field(alias="TotalMomentOfInertiaOfMotors", default=None)
     BearingStiffness: Optional[float] = Field(alias="BearingStiffness", default=None)
     BearingDamping: Optional[float] = Field(alias="BearingDamping", default=None)
     Gearbox: Optional[Gearbox] = Field(alias="Gearbox", default=None)
@@ -46,15 +55,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/added_inertia.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/added_inertia.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,33 +9,40 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AddedInertia_AddedInertiaTypeEnum(str, Enum):
     SIX_BY_SIX_INERTIA = "SixBySixInertia"
     POINT_INERTIA = "PointInertia"
 
 class AddedInertia(Dnv):
     """AddedInertia - The common properties of inertias added to the tower component.
-
+    
     Attributes:
-        - IgnoreGravityLoads: bool => If true, the loads due to gravity on this inertia will be ignored.
-        - HeightUpTower: float => A number representing a length.  The SI units for length are metres.
-        - SnapToNearestNode: bool => If true, the inertias will be added to the nearest structural node, which may be at a significantly different height to that specified.
-        - AddedInertiaType: AddedInertia_AddedInertiaTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    IgnoreGravityLoads : bool, default=False
+        If true, the loads due to gravity on this inertia will be ignored.
+    HeightUpTower : float
+        A number representing a length.  The SI units for length are metres.
+    SnapToNearestNode : bool
+        If true, the inertias will be added to the nearest structural node, which may be at a significantly different height to that specified.
+    AddedInertiaType : AddedInertia_AddedInertiaTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PointInertia
-        - SixBySixInertia
+        PointInertia
+        SixBySixInertia
+    
     """
 
     IgnoreGravityLoads: Optional[bool] = Field(alias="IgnoreGravityLoads", default=False)
     HeightUpTower: Optional[float] = Field(alias="HeightUpTower", default=None)
     SnapToNearestNode: Optional[bool] = Field(alias="SnapToNearestNode", default=None)
     AddedInertiaType: Optional[AddedInertia_AddedInertiaTypeEnum] = Field(alias="AddedInertiaType", default=None)
 
@@ -68,15 +75,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'AddedInertia'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/additional_constrained_wave.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/additional_constrained_wave.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AdditionalConstrainedWave_MethodEnum(str, Enum):
     OFF = "OFF"
     ON = "ON"
     NON_LINEAR = "NON-LINEAR"
 
 class AdditionalConstrainedWave(Dnv):
     """AdditionalConstrainedWave - The definition of additional constrained wave options.
-
+    
     Attributes:
-        - Method: AdditionalConstrainedWave_MethodEnum => The method used to define a constrained wave.
-        - WaveHeight: float => A number representing a length.  The SI units for length are metres.
-        - Period: float => A number representing a time.  The SI units for time are seconds.
-        - BlendNonLinearConstrained: bool => If true, the non-linear surface elevation will be bleded in.
-        - TimeOfWaveCrest: float => A number representing a time.  The SI units for time are seconds.
-        - NumberOfConstraints: int => The number of constraints.
+    ----------
+    Method : AdditionalConstrainedWave_MethodEnum
+        The method used to define a constrained wave.
+    WaveHeight : float
+        A number representing a length.  The SI units for length are metres.
+    Period : float
+        A number representing a time.  The SI units for time are seconds.
+    BlendNonLinearConstrained : bool, default=True
+        If true, the non-linear surface elevation will be bleded in.
+    TimeOfWaveCrest : float
+        A number representing a time.  The SI units for time are seconds.
+    NumberOfConstraints : int
+        The number of constraints.
+    
     """
 
     Method: Optional[AdditionalConstrainedWave_MethodEnum] = Field(alias="Method", default=None)
     WaveHeight: Optional[float] = Field(alias="WaveHeight", default=None)
     Period: Optional[float] = Field(alias="Period", default=None)
     BlendNonLinearConstrained: Optional[bool] = Field(alias="BlendNonLinearConstrained", default=True)
     TimeOfWaveCrest: Optional[float] = Field(alias="TimeOfWaveCrest", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerodynamic_information_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerodynamic_information_calculation.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,28 +9,35 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.steady_calculation import SteadyCalculation
 
 from dnv_bladed_models.steady_calculation_outputs import SteadyCalculationOutputs
 
 
 class AerodynamicInformationCalculation(SteadyCalculation, SteadyCalculationType='AerodynamicInformation'):
     """AerodynamicInformationCalculation - Defines a calculation which produces the aerodynamic parameters of the rotor in a steady flow, including local aerodynamic loading at each blade station.  The rotor is modelled as being completely rigid in this calculation, without prebend and sweep.  The rotor is analysed in isolation without any influence from the rest of the turbine structure.
-
+    
     Attributes:
-        - HubWindSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - PitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - RotorSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Outputs: SteadyCalculationOutputs 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;AerodynamicInformation&#39;
+    ----------
+    HubWindSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    PitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    RotorSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Outputs : SteadyCalculationOutputs
+    SteadyCalculationType : str, readonly, default='AerodynamicInformation'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'AerodynamicInformation'
+    
     """
 
     HubWindSpeed: Optional[float] = Field(alias="HubWindSpeed", default=None)
     PitchAngle: Optional[float] = Field(alias="PitchAngle", default=None)
     RotorSpeed: Optional[float] = Field(alias="RotorSpeed", default=None)
     Outputs: Optional[SteadyCalculationOutputs] = Field(alias="Outputs", default=None)
     SteadyCalculationType: Optional[str] = Field(alias="SteadyCalculationType", default='AerodynamicInformation', allow_mutation=False)
@@ -38,15 +45,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerodynamic_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerodynamic_model.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,33 +9,37 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.sequential_timestep_adaptation import SequentialTimestepAdaptation
 
 
 class AerodynamicModel_AerodynamicModelTypeEnum(str, Enum):
     VORTEX_LINE = "VortexLine"
     BLADE_ELEMENT_MOMENTUM = "BladeElementMomentum"
 
 class AerodynamicModel(Dnv):
     """AerodynamicModel - Common properties for all aerodynamics models.
-
+    
     Attributes:
-        - SequentialTimestepAdaptation: SequentialTimestepAdaptation 
-        - AerodynamicModelType: AerodynamicModel_AerodynamicModelTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    SequentialTimestepAdaptation : SequentialTimestepAdaptation
+    AerodynamicModelType : AerodynamicModel_AerodynamicModelTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - BladeElementMomentum
-        - VortexLine
+        BladeElementMomentum
+        VortexLine
+    
     """
 
     SequentialTimestepAdaptation: Optional[SequentialTimestepAdaptation] = Field(alias="SequentialTimestepAdaptation", default=None)
     AerodynamicModelType: Optional[AerodynamicModel_AerodynamicModelTypeEnum] = Field(alias="AerodynamicModelType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -66,15 +70,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'AerodynamicModel'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerodynamic_settings.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerodynamic_settings.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,40 +9,53 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.aerodynamic_model import AerodynamicModel
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.dynamic_stall import DynamicStall
 
 
 class AerodynamicSettings(Dnv):
     """AerodynamicSettings - Settings controlling the aerodynamic modelling in Bladed.
-
+    
     Attributes:
-        - AerodynamicModel: AerodynamicModel 
-        - DynamicStall: DynamicStall 
+    ----------
+    AerodynamicModel : AerodynamicModel, abstract
+    DynamicStall : DynamicStall, abstract
+    
+    Notes:
+    ---------
+    AerodynamicModel has the following concrete types:
+        - BladeElementMomentum
+        - VortexLine
+    DynamicStall has the following concrete types:
+        - CompressibleBeddoesLeishmanModel
+        - IncompressibleBeddoesLeishmanModel
+        - OyeModel
+    
     """
 
     AerodynamicModel: Optional[AerodynamicModel] = Field(alias="AerodynamicModel", default=None)
     DynamicStall: Optional[DynamicStall] = Field(alias="DynamicStall", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/aerofoil.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/aerofoil.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.alpha_line import AlphaLine
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class Aerofoil(Dnv):
     """Aerofoil - The definition of a single 2D aerodynamic aerofoil section.
-
+    
     Attributes:
-        - ChordwiseOriginForForcesAndMoments: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AerodynamicPerformance: List[AlphaLine] => The relationship between angle of attack, and the lift and drag coefficients (https://www.wikipedia.org/wiki/Stall_(fluid_dynamics)#Variation_of_lift_with_angle_of_attack).
+    ----------
+    ChordwiseOriginForForcesAndMoments : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AerodynamicPerformance : List[AlphaLine], default=list()
+        The relationship between angle of attack, and the lift and drag coefficients (https://www.wikipedia.org/wiki/Stall_(fluid_dynamics)#Variation_of_lift_with_angle_of_attack).
+    
     """
 
     ChordwiseOriginForForcesAndMoments: Optional[float] = Field(alias="ChordwiseOriginForForcesAndMoments", default=None)
     AerodynamicPerformance: Optional[List[AlphaLine]] = Field(alias="AerodynamicPerformance", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/air_characteristics.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/air_characteristics.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AirCharacteristics(Dnv):
     """AirCharacteristics - The properties of air for use in both onshore and offshore calculations.
-
+    
     Attributes:
-        - Density: float => A number representing a material&#39;s density.  The SI units for volume are kilograms per cubic metre.
-        - AtmosphericPressure: float => A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
-        - Viscosity: float => A number representing a viscosity [https://www.wikipedia.org/wiki/Viscosity].  The SI units for viscosity are Newton seconds per metre squared.
+    ----------
+    Density : float
+        A number representing a material's density.  The SI units for volume are kilograms per cubic metre.
+    AtmosphericPressure : float
+        A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    Viscosity : float
+        A number representing a viscosity [https://www.wikipedia.org/wiki/Viscosity].  The SI units for viscosity are Newton seconds per metre squared.
+    
     """
 
     Density: Optional[float] = Field(alias="Density", default=None)
     AtmosphericPressure: Optional[float] = Field(alias="AtmosphericPressure", default=None)
     Viscosity: Optional[float] = Field(alias="Viscosity", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/alpha_line.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/alpha_line.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AlphaLine(Dnv):
     """AlphaLine - The relationship between angle of attack, and the lift and drag coefficients (https://www.wikipedia.org/wiki/Stall_(fluid_dynamics)#Variation_of_lift_with_angle_of_attack).
-
+    
     Attributes:
-        - Angle: float => A number representing an angle.  The SI units for angles are radians.
-        - Cl: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Cd: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Cm: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Angle : float
+        A number representing an angle.  The SI units for angles are radians.
+    Cl : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Cd : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Cm : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Angle: Optional[float] = Field(alias="Angle", default=None)
     Cl: Optional[float] = Field(alias="Cl", default=None)
     Cd: Optional[float] = Field(alias="Cd", default=None)
     Cm: Optional[float] = Field(alias="Cm", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/angle_range.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/angle_range.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AngleRange(Dnv):
     """AngleRange - The range of angles to consider.
-
+    
     Attributes:
-        - Minimum: float => A number representing an angle.  The SI units for angles are radians.
-        - Maximum: float => A number representing an angle.  The SI units for angles are radians.
-        - Interval: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    Minimum : float
+        A number representing an angle.  The SI units for angles are radians.
+    Maximum : float
+        A number representing an angle.  The SI units for angles are radians.
+    Interval : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     Minimum: Optional[float] = Field(alias="Minimum", default=None)
     Maximum: Optional[float] = Field(alias="Maximum", default=None)
     Interval: Optional[float] = Field(alias="Interval", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/angle_vs_coefficient_of_drag_multiplied_by_area.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/angle_vs_coefficient_of_drag_multiplied_by_area.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AngleVsCoefficientOfDragMultipliedByArea(Dnv):
     """AngleVsCoefficientOfDragMultipliedByArea - The relationship between the subtending angle and the coefficient of drag multiplied by the area.
-
+    
     Attributes:
-        - Angle: float => A number representing an angle.  The SI units for angles are radians.
-        - CoefficientOfDragMultipliedByArea: float => A number representing an area.  The SI units for area are square metres.
+    ----------
+    Angle : float
+        A number representing an angle.  The SI units for angles are radians.
+    CoefficientOfDragMultipliedByArea : float
+        A number representing an area.  The SI units for area are square metres.
+    
     """
 
     Angle: Optional[float] = Field(alias="Angle", default=None)
     CoefficientOfDragMultipliedByArea: Optional[float] = Field(alias="CoefficientOfDragMultipliedByArea", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/angle_vs_stiffness_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/angle_vs_stiffness_torque.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AngleVsStiffnessTorque(Dnv):
     """AngleVsStiffnessTorque - A line or row in a look-up table, specifying a value of StiffnessTorque for a specified Angle.
-
+    
     Attributes:
-        - Angle: float => A number representing an angle.  The SI units for angles are radians.
-        - StiffnessTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Angle : float
+        A number representing an angle.  The SI units for angles are radians.
+    StiffnessTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Angle: Optional[float] = Field(alias="Angle", default=None)
     StiffnessTorque: Optional[float] = Field(alias="StiffnessTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_load.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_load.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.vector3_d import Vector3D
 
 
 class AppliedLoad_AppliedLoadTypeEnum(str, Enum):
     TOWER_POINT_LOADING = "TowerPointLoading"
     BLADE_POINT_LOADING = "BladePointLoading"
 
 class AppliedLoad(Dnv):
     """AppliedLoad - The common properties of a point loading time history.
-
+    
     Attributes:
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - LoadingFilepath: str => A filepath or URI containing one or six degree of loading data.  In the case of the six degrees of freedom, these will be applied in the component&#39;s coordinate system.  Where a single degree of freedom is provided, SingleDirectionLoading must also be specified.
-        - DirectionOfLoading: Vector3D 
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
-        - AppliedLoadType: AppliedLoad_AppliedLoadTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    LoadingFilepath : str
+        A filepath or URI containing one or six degree of loading data.  In the case of the six degrees of freedom, these will be applied in the component's coordinate system.  Where a single degree of freedom is provided, SingleDirectionLoading must also be specified.
+    DirectionOfLoading : Vector3D
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    AppliedLoadType : AppliedLoad_AppliedLoadTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - BladePointLoading
-        - TowerPointLoading
+        BladePointLoading
+        TowerPointLoading
+    
     """
 
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     LoadingFilepath: Optional[str] = Field(alias="LoadingFilepath", default=None)
     DirectionOfLoading: Optional[Vector3D] = Field(alias="DirectionOfLoading", default=None)
     OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
     AppliedLoadType: Optional[AppliedLoad_AppliedLoadTypeEnum] = Field(alias="AppliedLoadType", default=None)
@@ -72,15 +79,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'AppliedLoad'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_moment_vs_friction_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_moment_vs_friction_torque.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AppliedMomentVsFrictionTorque(Dnv):
     """AppliedMomentVsFrictionTorque - A line or row in a look-up table, specifying a value of FrictionTorque for a specified AppliedMoment.
-
+    
     Attributes:
-        - AppliedMoment: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - FrictionTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    AppliedMoment : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    FrictionTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     AppliedMoment: Optional[float] = Field(alias="AppliedMoment", default=None)
     FrictionTorque: Optional[float] = Field(alias="FrictionTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_xy_force_vs_friction_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_xy_force_vs_friction_torque.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AppliedXYForceVsFrictionTorque(Dnv):
     """AppliedXYForceVsFrictionTorque - A line or row in a look-up table, specifying a value of FrictionTorque for a specified AppliedForce.
-
+    
     Attributes:
-        - AppliedForce: float => A number representing a force.  The SI units for force are Newtons.
-        - FrictionTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    AppliedForce : float
+        A number representing a force.  The SI units for force are Newtons.
+    FrictionTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     AppliedForce: Optional[float] = Field(alias="AppliedForce", default=None)
     FrictionTorque: Optional[float] = Field(alias="FrictionTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/applied_z_force_vs_friction_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/applied_z_force_vs_friction_torque.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class AppliedZForceVsFrictionTorque(Dnv):
     """AppliedZForceVsFrictionTorque - A line or row in a look-up table, specifying a value of FrictionTorque for a specified AppliedForce.
-
+    
     Attributes:
-        - AppliedForce: float => A number representing a force.  The SI units for force are Newtons.
-        - FrictionTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    AppliedForce : float
+        A number representing a force.  The SI units for force are Newtons.
+    FrictionTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     AppliedForce: Optional[float] = Field(alias="AppliedForce", default=None)
     FrictionTorque: Optional[float] = Field(alias="FrictionTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/assembly_node.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/assembly_node.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,26 +9,30 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from pydantic import ValidationError
 from pydantic.error_wrappers import ErrorWrapper
 from pydantic.utils import ROOT_KEY
 
 
 class AssemblyNode(BaseModel):
     """AssemblyNode - A tree definition of the turbine&#39;s physical structure
-
+    
     Attributes:
-        - ComponentReference: str => This is a JSON $ref style reference to a component in the assembly tree.
-        - RelativeOrientation: float 
-        - ConnectedToNode: int 
+    ----------
+    ComponentReference : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    RelativeOrientation : float
+    ConnectedToNode : int
+    
     """
 
     ComponentReference: Optional[str] = Field(alias="ComponentReference", default=None)
     RelativeOrientation: Optional[float] = Field(alias="RelativeOrientation", default=None)
     ConnectedToNode: Optional[int] = Field(alias="ConnectedToNode", default=None)
 
     class Config:
@@ -60,15 +64,15 @@
         for key, val in additional_data.items():
             prop_obj = AssemblyNode(**val)
             instance.__setattr__(key, prop_obj)
 
         return instance
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/automatic_mac_camy_fuchs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/automatic_mac_camy_fuchs.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wave_diffraction_approximation import WaveDiffractionApproximation
 
 
 class AutomaticMacCamyFuchs(WaveDiffractionApproximation, WaveDiffractionApproximationType='AutomaticMacCamyFuchs'):
     """AutomaticMacCamyFuchs - The MacCamy-Fuchs methodology will be used, with the member diameter automatically determined from the model.
-
+    
     Attributes:
-        - WaveDiffractionApproximationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;AutomaticMacCamyFuchs&#39;
+    ----------
+    WaveDiffractionApproximationType : str, readonly, default='AutomaticMacCamyFuchs'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'AutomaticMacCamyFuchs'
+    
     """
 
     WaveDiffractionApproximationType: Optional[str] = Field(alias="WaveDiffractionApproximationType", default='AutomaticMacCamyFuchs', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/automatic_simple_cutoff_frequency.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/automatic_simple_cutoff_frequency.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wave_diffraction_approximation import WaveDiffractionApproximation
 
 
 class AutomaticSimpleCutoffFrequency(WaveDiffractionApproximation, WaveDiffractionApproximationType='AutomaticSimpleCutoffFrequency'):
     """AutomaticSimpleCutoffFrequency - The MacCamy-Fuchs methodology will be used, with the member diameter automatically determined from the model.
-
+    
     Attributes:
-        - WaveDiffractionApproximationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;AutomaticSimpleCutoffFrequency&#39;
+    ----------
+    WaveDiffractionApproximationType : str, readonly, default='AutomaticSimpleCutoffFrequency'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'AutomaticSimpleCutoffFrequency'
+    
     """
 
     WaveDiffractionApproximationType: Optional[str] = Field(alias="WaveDiffractionApproximationType", default='AutomaticSimpleCutoffFrequency', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/axisymmetric_nacelle_cover.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/axisymmetric_nacelle_cover.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.length_vs_radius import LengthVsRadius
 
 from dnv_bladed_models.nacelle_cover import NacelleCover
 
 
 class AxisymmetricNacelleCover(NacelleCover, NacelleCoverType='Axisymmetric'):
     """AxisymmetricNacelleCover - A nacelle cover that is most easily represented by a tube that varies in radius down its length.
-
+    
     Attributes:
-        - LengthVsRadius: List[LengthVsRadius] => A list of radii at given lengths.  The lowest length should be 0.0, and the highest length will specify the length of the nacelle cover.
-        - NacelleCoverType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Axisymmetric&#39;
+    ----------
+    LengthVsRadius : List[LengthVsRadius], default=list()
+        A list of radii at given lengths.  The lowest length should be 0.0, and the highest length will specify the length of the nacelle cover.
+    NacelleCoverType : str, readonly, default='Axisymmetric'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Axisymmetric'
+    
     """
 
     LengthVsRadius: Optional[List[LengthVsRadius]] = Field(alias="LengthVsRadius", default=list())
     NacelleCoverType: Optional[str] = Field(alias="NacelleCoverType", default='Axisymmetric', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/beddoes_leishman_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/beddoes_leishman_model.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,30 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dynamic_stall import DynamicStall
 
 
 class BeddoesLeishmanModel(DynamicStall):
     """BeddoesLeishmanModel - The common properties for the Beddoes Leishman dynamic stall models.
-
+    
     Attributes:
-        - UseKirchoffFlow: bool => If true,the normal force coefficient is computed using the dynamic separation position in Kirchoff&#39;s equation directly.  If false, the dynamic separation position is used to linearly interpolate between fully separated and fully attached flow.   In normal operating conditions this setting will not lead to significant differences, but it has been found that in parked/idling cases (where the blade experiences high angles of attack) this option will improve the aerodynamic damping of the blade.  The explanation for the damping differences is given in the aerodynamic validation document on the User Portal.
-        - UseImpulsiveContributions: bool => Both Beddoes-Leishman models contain terms that model the non-circulatory contributions in lift and moment coefficient.  Note that some moment contribution terms are not controlled by this option, but are controlled by the ye Dynamic pitching moment coefficient option.
-        - PressureLagTimeConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - VortexLiftTimeConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - VortexTravelTimeConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AttachedFlowConstantA1: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AttachedFlowConstantA2: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AttachedFlowConstantB1: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AttachedFlowConstantB2: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    UseKirchoffFlow : bool, default=False
+        If true,the normal force coefficient is computed using the dynamic separation position in Kirchoff's equation directly.  If false, the dynamic separation position is used to linearly interpolate between fully separated and fully attached flow.   In normal operating conditions this setting will not lead to significant differences, but it has been found that in parked/idling cases (where the blade experiences high angles of attack) this option will improve the aerodynamic damping of the blade.  The explanation for the damping differences is given in the aerodynamic validation document on the User Portal.
+    UseImpulsiveContributions : bool, default=False
+        Both Beddoes-Leishman models contain terms that model the non-circulatory contributions in lift and moment coefficient.  Note that some moment contribution terms are not controlled by this option, but are controlled by the ye Dynamic pitching moment coefficient option.
+    PressureLagTimeConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    VortexLiftTimeConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    VortexTravelTimeConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AttachedFlowConstantA1 : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AttachedFlowConstantA2 : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AttachedFlowConstantB1 : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AttachedFlowConstantB2 : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     UseKirchoffFlow: Optional[bool] = Field(alias="UseKirchoffFlow", default=False)
     UseImpulsiveContributions: Optional[bool] = Field(alias="UseImpulsiveContributions", default=False)
     PressureLagTimeConstant: Optional[float] = Field(alias="PressureLagTimeConstant", default=None)
     VortexLiftTimeConstant: Optional[float] = Field(alias="VortexLiftTimeConstant", default=None)
     VortexTravelTimeConstant: Optional[float] = Field(alias="VortexTravelTimeConstant", default=None)
@@ -44,15 +56,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.aerofoil import Aerofoil
 
 from dnv_bladed_models.blade_additional_inertia import BladeAdditionalInertia
 
 from dnv_bladed_models.blade_modelling import BladeModelling
 
 from dnv_bladed_models.blade_mounting import BladeMounting
@@ -28,27 +29,44 @@
 from dnv_bladed_models.blade_sensor import BladeSensor
 
 from dnv_bladed_models.component import Component
 
 
 class Blade(Component, ComponentType='Blade'):
     """Blade - A blade component.
-
+    
     Attributes:
-        - Modelling: BladeModelling 
-        - AerofoilLibrary: Dict[str, Aerofoil] => A library of Aerofoil objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
-        - InterpolatedAerofoilLibrary: Dict[str, Dict] => A library of interpolated AerofoilLibrary, each of which has a 2D interpolation over thickness and Reynold&#39;s number.
-        - AileronAerofoilLibrary: Dict[str, Dict] => A library of aileron or aileron interpolated aerofoil objects, interpolated on their deployment angle.  Each is specified with a unique keyword which is used for referencing the item elsewhere in the model.
-        - Mounting: BladeMounting 
-        - ToleranceForRepeatedSections: float => A number representing a length.  The SI units for length are metres.
-        - SectionDefinitions: List[BladeSectionDefinition] => A list of section definitions which describle the aerodynamic and structural properties of the blade.
-        - AdditionalInertia: BladeAdditionalInertia 
-        - OutputGroups: Dict[str, BladeOutputGroup] => A library which contains any number of named output groups.  These can be referenced from &#39;SelectedOutputGroup&#39;
-        - Sensors: Dict[str, BladeSensor] => A library of sensor objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Blade&#39;
+    ----------
+    Modelling : BladeModelling, abstract
+    AerofoilLibrary : Dict[str, Aerofoil], default=dict()
+        A library of Aerofoil objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
+    InterpolatedAerofoilLibrary : Dict[str, Dict], default=dict()
+        A library of interpolated AerofoilLibrary, each of which has a 2D interpolation over thickness and Reynold's number.
+    AileronAerofoilLibrary : Dict[str, Dict], default=dict()
+        A library of aileron or aileron interpolated aerofoil objects, interpolated on their deployment angle.  Each is specified with a unique keyword which is used for referencing the item elsewhere in the model.
+    Mounting : BladeMounting
+    ToleranceForRepeatedSections : float
+        A number representing a length.  The SI units for length are metres.
+    SectionDefinitions : List[BladeSectionDefinition], default=list()
+        A list of section definitions which describle the aerodynamic and structural properties of the blade.
+    AdditionalInertia : BladeAdditionalInertia
+    OutputGroups : Dict[str, BladeOutputGroup], default=dict()
+        A library which contains any number of named output groups.  These can be referenced from 'SelectedOutputGroup'
+    Sensors : Dict[str, BladeSensor], default=dict()
+        A library of sensor objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
+    ComponentType : str, readonly, default='Blade'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Blade'
+    
+    Notes:
+    ---------
+    Modelling has the following concrete types:
+        - FiniteElementBladeModelling
+        - ModalBladeModelling
+        - RigidBladeModelling
+    
     """
 
     Modelling: Optional[BladeModelling] = Field(alias="Modelling", default=None)
     AerofoilLibrary: Optional[Dict[str, Aerofoil]] = Field(alias="AerofoilLibrary", default=dict())
     InterpolatedAerofoilLibrary: Optional[Dict[str, Dict]] = Field(alias="InterpolatedAerofoilLibrary", default=dict())
     AileronAerofoilLibrary: Optional[Dict[str, Dict]] = Field(alias="AileronAerofoilLibrary", default=dict())
     Mounting: Optional[BladeMounting] = Field(alias="Mounting", default=None)
@@ -62,15 +80,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_accelerometer.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_accelerometer.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_sensor import BladeSensor
 
 
 class BladeAccelerometer(BladeSensor, BladeSensorType='Accelerometer'):
     """BladeAccelerometer - An accelerometer on the blade.
-
+    
     Attributes:
-        - BladeSensorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Accelerometer&#39;
+    ----------
+    BladeSensorType : str, readonly, default='Accelerometer'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Accelerometer'
+    
     """
 
     BladeSensorType: Optional[str] = Field(alias="BladeSensorType", default='Accelerometer', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_additional_inertia.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_additional_inertia.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_point_mass import BladePointMass
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeAdditionalInertia(Dnv):
     """BladeAdditionalInertia - The definition of additional inertias applied in the whole blade context.
-
+    
     Attributes:
-        - PointInertias: List[BladePointMass] => A list of point inertias to be applied to the blade structure.
-        - AdditionalWholeBladePitchingInertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    ----------
+    PointInertias : List[BladePointMass], default=list()
+        A list of point inertias to be applied to the blade structure.
+    AdditionalWholeBladePitchingInertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    
     """
 
     PointInertias: Optional[List[BladePointMass]] = Field(alias="PointInertias", default=list())
     AdditionalWholeBladePitchingInertia: Optional[float] = Field(alias="AdditionalWholeBladePitchingInertia", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_aerodynamic_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_aerodynamic_outputs.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeAerodynamicOutputs(Dnv):
     """BladeAerodynamicOutputs - Loads selected
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_element_momentum.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_element_momentum.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,42 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.aerodynamic_model import AerodynamicModel
 
 from dnv_bladed_models.blade_element_theory_corrections import BladeElementTheoryCorrections
 
 from dnv_bladed_models.momentum_theory_corrections import MomentumTheoryCorrections
 
 
 class BladeElementMomentum(AerodynamicModel, AerodynamicModelType='BladeElementMomentum'):
     """BladeElementMomentum - The Blade Element Momentum (BEM) theory model [https://www.wikipedia.org/wiki/Blade_element_momentum_theory].
-
+    
     Attributes:
-        - MomentumTheoryCorrections: MomentumTheoryCorrections 
-        - BladeElementTheoryCorrections: BladeElementTheoryCorrections 
-        - AerodynamicModelType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;BladeElementMomentum&#39;
+    ----------
+    MomentumTheoryCorrections : MomentumTheoryCorrections
+    BladeElementTheoryCorrections : BladeElementTheoryCorrections
+    AerodynamicModelType : str, readonly, default='BladeElementMomentum'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'BladeElementMomentum'
+    
     """
 
     MomentumTheoryCorrections: Optional[MomentumTheoryCorrections] = Field(alias="MomentumTheoryCorrections", default=None)
     BladeElementTheoryCorrections: Optional[BladeElementTheoryCorrections] = Field(alias="BladeElementTheoryCorrections", default=None)
     AerodynamicModelType: Optional[str] = Field(alias="AerodynamicModelType", default='BladeElementMomentum', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_element_theory_corrections.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_element_theory_corrections.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeElementTheoryCorrections(Dnv):
     """BladeElementTheoryCorrections - Properties related to the blade element aspects of the Blade Element Momentum model [https://www.wikipedia.org/wiki/Blade_element_theory].
-
+    
     Attributes:
-        - UsePradtlCorrectionForTipLoss: bool => If true, a Prandtl correction for tip loss effects will be applied.
-        - UsePradtlCorrectionForRootLoss: bool => If true, a Prandtl correction for hub loss effects will be applied.
-        - UseDragInInduction: bool => If true, the drag coefficient will be included in the induction calculations. This mainly influences the BEM solution near the root of the blade, where the drag force is significant.
+    ----------
+    UsePradtlCorrectionForTipLoss : bool, default=True
+        If true, a Prandtl correction for tip loss effects will be applied.
+    UsePradtlCorrectionForRootLoss : bool, default=False
+        If true, a Prandtl correction for hub loss effects will be applied.
+    UseDragInInduction : bool, default=True
+        If true, the drag coefficient will be included in the induction calculations. This mainly influences the BEM solution near the root of the blade, where the drag force is significant.
+    
     """
 
     UsePradtlCorrectionForTipLoss: Optional[bool] = Field(alias="UsePradtlCorrectionForTipLoss", default=True)
     UsePradtlCorrectionForRootLoss: Optional[bool] = Field(alias="UsePradtlCorrectionForRootLoss", default=False)
     UseDragInInduction: Optional[bool] = Field(alias="UseDragInInduction", default=True)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_hydrodynamic_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_hydrodynamic_outputs.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeHydrodynamicOutputs(Dnv):
     """BladeHydrodynamicOutputs - Loads selected
-
+    
     Attributes:
-        - WaterKinematics: bool => If true, output water blade kinematics (tidal only)
+    ----------
+    WaterKinematics : bool, default=False
+        If true, output water blade kinematics (tidal only)
+    
     """
 
     WaterKinematics: Optional[bool] = Field(alias="WaterKinematics", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_load_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_load_outputs.py`

 * *Files 16% similar despite different names*

```diff
@@ -9,33 +9,48 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeLoadOutputs(Dnv):
     """BladeLoadOutputs - Loads selected
-
+    
     Attributes:
-        - FlapwiseBendingLoads: bool =>  Output blade bending moments for the flapwise direction (0&#x3D;no, 1&#x3D;yes).
-        - EdgewiseBendingLoads: bool =>  Output blade bending moments for the edgewise direction (0&#x3D;no, 1&#x3D;yes).
-        - FlapwiseShearLoads: bool =>  Output blade shear forces for the flapwise direction (0&#x3D;no, 1&#x3D;yes).
-        - EdgewiseShearLoads: bool =>  Output blade shear forces for the edgewise direction (0&#x3D;no, 1&#x3D;yes).
-        - OutOfPlaneBendingLoads: bool =>  Output blade bending moments for out of plane direction (0&#x3D;no, 1&#x3D;yes).
-        - InPlaneBendingLoads: bool =>  Output blade bending moments for in plane direction (0&#x3D;no, 1&#x3D;yes).
-        - OutOfPlaneShearLoads: bool =>  Output blade shear forces for out of plane direction (0&#x3D;no, 1&#x3D;yes).
-        - InPlaneShearLoads: bool =>  Output blade shear forces for in plane direction (0&#x3D;no, 1&#x3D;yes).
-        - RadialForces: bool =>  Output blade radial forces (0&#x3D;no, 1&#x3D;yes).
-        - LoadsInRootAxisSystem: bool =>  Output blade loads about the root axes system (0&#x3D;no, 1&#x3D;yes).
-        - LoadsInAeroAxisSystem: bool =>  Output blade loads about the aero axes system (0&#x3D;no, 1&#x3D;yes).
-        - LoadsInUserAxisSystem: bool =>  Output blade loads about the user defined axes system (0&#x3D;no, 1&#x3D;yes).
+    ----------
+    FlapwiseBendingLoads : bool, default=False
+         Output blade bending moments for the flapwise direction (0=no, 1=yes).
+    EdgewiseBendingLoads : bool, default=False
+         Output blade bending moments for the edgewise direction (0=no, 1=yes).
+    FlapwiseShearLoads : bool, default=False
+         Output blade shear forces for the flapwise direction (0=no, 1=yes).
+    EdgewiseShearLoads : bool, default=False
+         Output blade shear forces for the edgewise direction (0=no, 1=yes).
+    OutOfPlaneBendingLoads : bool, default=False
+         Output blade bending moments for out of plane direction (0=no, 1=yes).
+    InPlaneBendingLoads : bool, default=False
+         Output blade bending moments for in plane direction (0=no, 1=yes).
+    OutOfPlaneShearLoads : bool, default=False
+         Output blade shear forces for out of plane direction (0=no, 1=yes).
+    InPlaneShearLoads : bool, default=False
+         Output blade shear forces for in plane direction (0=no, 1=yes).
+    RadialForces : bool, default=False
+         Output blade radial forces (0=no, 1=yes).
+    LoadsInRootAxisSystem : bool, default=False
+         Output blade loads about the root axes system (0=no, 1=yes).
+    LoadsInAeroAxisSystem : bool, default=False
+         Output blade loads about the aero axes system (0=no, 1=yes).
+    LoadsInUserAxisSystem : bool, default=False
+         Output blade loads about the user defined axes system (0=no, 1=yes).
+    
     """
 
     FlapwiseBendingLoads: Optional[bool] = Field(alias="FlapwiseBendingLoads", default=False)
     EdgewiseBendingLoads: Optional[bool] = Field(alias="EdgewiseBendingLoads", default=False)
     FlapwiseShearLoads: Optional[bool] = Field(alias="FlapwiseShearLoads", default=False)
     EdgewiseShearLoads: Optional[bool] = Field(alias="EdgewiseShearLoads", default=False)
     OutOfPlaneBendingLoads: Optional[bool] = Field(alias="OutOfPlaneBendingLoads", default=False)
@@ -50,15 +65,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_modelling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_modelling.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,33 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeModelling_BladeModellingTypeEnum(str, Enum):
     MODAL_BLADE_MODELLING = "ModalBladeModelling"
     FINITE_ELEMENT_BLADE_MODELLING = "FiniteElementBladeModelling"
     RIGID_BLADE_MODELLING = "RigidBladeModelling"
 
 class BladeModelling(Dnv):
     """BladeModelling - Common properties for all blade modelling methods.
-
+    
     Attributes:
-        - IgnoreAxesOrientationDifferencesForShear: bool => With this option selected, the effect of orientation difference between the neutral axis and shear axis on the blade elements are not taken into account. Please refer to the Bladed documentation \&quot;Bend-twist coupling in Bladed beam elements\&quot; doc No. 110052-UKBR-T-30 for further information.
-        - BladeModellingType: BladeModelling_BladeModellingTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    IgnoreAxesOrientationDifferencesForShear : bool, default=False
+        With this option selected, the effect of orientation difference between the neutral axis and shear axis on the blade elements are not taken into account. Please refer to the Bladed documentation \"Bend-twist coupling in Bladed beam elements\" doc No. 110052-UKBR-T-30 for further information.
+    BladeModellingType : BladeModelling_BladeModellingTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - FiniteElementBladeModelling
-        - ModalBladeModelling
-        - RigidBladeModelling
+        FiniteElementBladeModelling
+        ModalBladeModelling
+        RigidBladeModelling
+    
     """
 
     IgnoreAxesOrientationDifferencesForShear: Optional[bool] = Field(alias="IgnoreAxesOrientationDifferencesForShear", default=False)
     BladeModellingType: Optional[BladeModelling_BladeModellingTypeEnum] = Field(alias="BladeModellingType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -66,15 +71,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'BladeModelling'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_motion_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/controlled_operation.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,51 +9,57 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.timed_event import TimedEvent
 
-class BladeMotionOutputs(Dnv):
-    """BladeMotionOutputs - Loads selected
 
+class ControlledOperation(TimedEvent):
+    """ControlledOperation - An operation occuring at a specified time.  This will be implemented by the controller, and Bladed will send it a signal at the time it should occur.
+    
     Attributes:
-        - OutOfPlaneDeflection: bool =>  Which blade deflections to output in the out of plane direction (x, y, z).
-        - InPlaneDeflection: bool =>  Which blade deflections to output in the in plane direction (x, y, z).
-        - BladeAccelerations: bool =>  Which blade accelerations to output (x, y, z).
+    ----------
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    
     """
 
-    OutOfPlaneDeflection: Optional[bool] = Field(alias="OutOfPlaneDeflection", default=False)
-    InPlaneDeflection: Optional[bool] = Field(alias="InPlaneDeflection", default=False)
-    BladeAccelerations: Optional[bool] = Field(alias="BladeAccelerations", default=False)
+    OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
                             allowed_child_vals[child_key] = child_val
                     allowed_vals[key] = allowed_child_vals
                 else:
                     allowed_vals[key] = val
         return allowed_vals
 
+    @validator("OnComponentInAssembly")
+    def OnComponentInAssembly_pattern(cls, value):
+        if value is not None and not re.match(r"^#(\/.+)+$", value):
+            raise ValueError(f"OnComponentInAssembly did not match the expected format (found {value})")
+        return value
+
     def to_json(
         self,
         *, 
         include: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         exclude: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         by_alias: bool = True, 
         skip_defaults: Optional[bool] = None, 
@@ -106,25 +112,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        BladeMotionOutputs
+        ControlledOperation
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = BladeMotionOutputs.from_file('/path/to/file')
+        >>> model = ControlledOperation.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -136,25 +142,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        BladeMotionOutputs
+        ControlledOperation
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = BladeMotionOutputs.from_json('{ ... }')
+        >>> model = ControlledOperation.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -168,15 +174,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        BladeMotionOutputs
+        ControlledOperation
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -200,8 +206,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-BladeMotionOutputs.update_forward_refs()
+ControlledOperation.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_mounting.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_mounting.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeMounting(Dnv):
     """BladeMounting - The blade mounting angles, outboard of pitch bearing.
-
+    
     Attributes:
-        - ConingAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - SweepAngle: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    ConingAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    SweepAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     ConingAngle: Optional[float] = Field(alias="ConingAngle", default=None)
     SweepAngle: Optional[float] = Field(alias="SweepAngle", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_mounting_on_rotor.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_mounting_on_rotor.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeMountingOnRotor(Dnv):
     """BladeMountingOnRotor - The properties defining where the blades or pitch systems attach to the hub.  All of the angles described are for INSIDE of the pitch bearing - the blade has its own values for sweep and cone outside of the pitch bearing.
-
+    
     Attributes:
-        - Overhang: float => A number representing a length.  The SI units for length are metres.
-        - RadiusOfBladeConnection: float => A number representing a length.  The SI units for length are metres.
-        - ConingAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - SetAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - SweepAngle: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    Overhang : float
+        A number representing a length.  The SI units for length are metres.
+    RadiusOfBladeConnection : float
+        A number representing a length.  The SI units for length are metres.
+    ConingAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    SetAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    SweepAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     Overhang: Optional[float] = Field(alias="Overhang", default=None)
     RadiusOfBladeConnection: Optional[float] = Field(alias="RadiusOfBladeConnection", default=None)
     ConingAngle: Optional[float] = Field(alias="ConingAngle", default=None)
     SetAngle: Optional[float] = Field(alias="SetAngle", default=None)
     SweepAngle: Optional[float] = Field(alias="SweepAngle", default=None)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_output_group.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_aerodynamic_outputs import BladeAerodynamicOutputs
 
 from dnv_bladed_models.blade_hydrodynamic_outputs import BladeHydrodynamicOutputs
 
 from dnv_bladed_models.blade_load_outputs import BladeLoadOutputs
 
 from dnv_bladed_models.blade_motion_outputs import BladeMotionOutputs
@@ -26,22 +27,25 @@
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.xml_blade_outputs import XMLBladeOutputs
 
 
 class BladeOutputGroup(Dnv):
     """BladeOutputGroup - An output group definition for the blade.
-
+    
     Attributes:
-        - XML: XMLBladeOutputs 
-        - LoadOutputs: BladeLoadOutputs 
-        - MotionOutputs: BladeMotionOutputs 
-        - AerodynamicOutputs: BladeAerodynamicOutputs 
-        - HydrodynamicOutputs: BladeHydrodynamicOutputs 
-        - OutputLocations: List[BladeOutputsForLocation] => List of BladeOutputsForLocation, originally enumerated by Fortran.OUTPUT.AEROD_NSTS
+    ----------
+    XML : XMLBladeOutputs
+    LoadOutputs : BladeLoadOutputs
+    MotionOutputs : BladeMotionOutputs
+    AerodynamicOutputs : BladeAerodynamicOutputs
+    HydrodynamicOutputs : BladeHydrodynamicOutputs
+    OutputLocations : List[BladeOutputsForLocation], default=list()
+        List of BladeOutputsForLocation, originally enumerated by Fortran.OUTPUT.AEROD_NSTS
+    
     """
 
     XML: Optional[XMLBladeOutputs] = Field(alias="XML", default=None)
     LoadOutputs: Optional[BladeLoadOutputs] = Field(alias="LoadOutputs", default=None)
     MotionOutputs: Optional[BladeMotionOutputs] = Field(alias="MotionOutputs", default=None)
     AerodynamicOutputs: Optional[BladeAerodynamicOutputs] = Field(alias="AerodynamicOutputs", default=None)
     HydrodynamicOutputs: Optional[BladeHydrodynamicOutputs] = Field(alias="HydrodynamicOutputs", default=None)
@@ -50,15 +54,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_outputs_for_location.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_outputs_for_location.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeOutputsForLocation_BladeOutputsForLocationTypeEnum(str, Enum):
     OUTPUTS_FOR_SECTION = "OutputsForSection"
     OUTPUTS_FOR_POSITION = "OutputsForPosition"
 
 class BladeOutputsForLocation(Dnv):
     """BladeOutputsForLocation - 
-
+    
     Attributes:
-        - Loads: bool =>  An array of blade station indices to output loads for (exclusive with BLOADS_POS).
-        - Motion: bool =>  An array of blade station indices to output deflections for (exclusive with BDEFLS_POS).
-        - Aerodynamics: bool => Whether to output loads on this node
-        - Hydrodynamics: bool =>  An array of blade radii to output water kinematics for (tidal only).
-        - BladeOutputsForLocationType: BladeOutputsForLocation_BladeOutputsForLocationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    Loads : bool, default=False
+         An array of blade station indices to output loads for (exclusive with BLOADS_POS).
+    Motion : bool, default=False
+         An array of blade station indices to output deflections for (exclusive with BDEFLS_POS).
+    Aerodynamics : bool
+        Whether to output loads on this node
+    Hydrodynamics : bool, default=False
+         An array of blade radii to output water kinematics for (tidal only).
+    BladeOutputsForLocationType : BladeOutputsForLocation_BladeOutputsForLocationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - BladeOutputsForPosition
-        - BladeOutputsForSection
+        BladeOutputsForPosition
+        BladeOutputsForSection
+    
     """
 
     Loads: Optional[bool] = Field(alias="Loads", default=False)
     Motion: Optional[bool] = Field(alias="Motion", default=False)
     Aerodynamics: Optional[bool] = Field(alias="Aerodynamics", default=None)
     Hydrodynamics: Optional[bool] = Field(alias="Hydrodynamics", default=False)
     BladeOutputsForLocationType: Optional[BladeOutputsForLocation_BladeOutputsForLocationTypeEnum] = Field(alias="BladeOutputsForLocationType", default=None)
@@ -70,15 +78,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'BladeOutputsForLocation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_outputs_for_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_outputs_for_position.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_outputs_for_location import BladeOutputsForLocation
 
 
 class BladeOutputsForPosition(BladeOutputsForLocation, BladeOutputsForLocationType='OutputsForPosition'):
     """BladeOutputsForPosition - 
-
+    
     Attributes:
-        - BladePosition: float => The distance along the blade station these outputs apply to
-        - BladeOutputsForLocationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;OutputsForPosition&#39;
+    ----------
+    BladePosition : float
+        The distance along the blade station these outputs apply to
+    BladeOutputsForLocationType : str, readonly, default='OutputsForPosition'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'OutputsForPosition'
+    
     """
 
     BladePosition: Optional[float] = Field(alias="BladePosition", default=None)
     BladeOutputsForLocationType: Optional[str] = Field(alias="BladeOutputsForLocationType", default='OutputsForPosition', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_outputs_for_section.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_outputs_for_section.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_outputs_for_location import BladeOutputsForLocation
 
 
 class BladeOutputsForSection(BladeOutputsForLocation, BladeOutputsForLocationType='OutputsForSection'):
     """BladeOutputsForSection - 
-
+    
     Attributes:
-        - SectionReference: str => This is a JSON $ref style reference to another element in the JSON model.
-        - BladeOutputsForLocationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;OutputsForSection&#39;
+    ----------
+    SectionReference : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    BladeOutputsForLocationType : str, readonly, default='OutputsForSection'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'OutputsForSection'
+    
     """
 
     SectionReference: Optional[str] = Field(alias="SectionReference", default=None)
     BladeOutputsForLocationType: Optional[str] = Field(alias="BladeOutputsForLocationType", default='OutputsForSection', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_point_loading.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_point_loading.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.applied_load import AppliedLoad
 
 
 class BladePointLoading(AppliedLoad, AppliedLoadType='BladePointLoading'):
     """BladePointLoading - A time history of point loading applied to a blade.
-
+    
     Attributes:
-        - DistanceAlongBlade: float => A number representing a length.  The SI units for length are metres.
-        - AppliedLoadType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;BladePointLoading&#39;
+    ----------
+    DistanceAlongBlade : float
+        A number representing a length.  The SI units for length are metres.
+    AppliedLoadType : str, readonly, default='BladePointLoading'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'BladePointLoading'
+    
     """
 
     DistanceAlongBlade: Optional[float] = Field(alias="DistanceAlongBlade", default=None)
     AppliedLoadType: Optional[str] = Field(alias="AppliedLoadType", default='BladePointLoading', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_point_mass.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_point_mass.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.vector3_d import Vector3D
 
 
 class BladePointMass(Dnv):
     """BladePointMass - A point mass with rotational inertia on the blade.  This is defined in the blade root axes.
-
+    
     Attributes:
-        - LocationInRootAxes: Vector3D 
-        - Mass: float => A number representing a mass.  The SI units for mass are kilograms.
+    ----------
+    LocationInRootAxes : Vector3D
+    Mass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    
     """
 
     LocationInRootAxes: Optional[Vector3D] = Field(alias="LocationInRootAxes", default=None)
     Mass: Optional[float] = Field(alias="Mass", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_aerodynamic_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_aerodynamic_properties.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeSectionAerodynamicProperties(Dnv):
     """BladeSectionAerodynamicProperties - The aerodynamic properties of the blade section.  These are all provided in the QuarterChordCoordinateSystem, which both locates and orients the aerofoil section.
-
+    
     Attributes:
-        - Chord: float => A number representing a length.  The SI units for length are metres.
-        - ThicknessToChordRatio: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AerodynamicSection: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    Chord : float
+        A number representing a length.  The SI units for length are metres.
+    ThicknessToChordRatio : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AerodynamicSection : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     Chord: Optional[float] = Field(alias="Chord", default=None)
     ThicknessToChordRatio: Optional[float] = Field(alias="ThicknessToChordRatio", default=None)
     AerodynamicSection: Optional[str] = Field(alias="AerodynamicSection", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_axes_and_coordinate_systems.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_axes_and_coordinate_systems.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_mass_coordinate_system import BladeSectionMassCoordinateSystem
 
 from dnv_bladed_models.blade_section_neutral_axis_coordinate_system import BladeSectionNeutralAxisCoordinateSystem
 
 from dnv_bladed_models.blade_section_output_coordinate_system import BladeSectionOutputCoordinateSystem
 
 from dnv_bladed_models.blade_section_quarter_chord_coordinate_system import BladeSectionQuarterChordCoordinateSystem
@@ -26,22 +27,24 @@
 from dnv_bladed_models.blade_section_shear_axis import BladeSectionShearAxis
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeSectionAxesAndCoordinateSystems(Dnv):
     """BladeSectionAxesAndCoordinateSystems - Definitions of the various axis systems used on the blade.  Any coordinate system which is omitted will be assumed to be the same as the reference one.  This means that any of the coordinate systems could be used as the reference if wished.
-
+    
     Attributes:
-        - ReferenceCoordinateSystem: BladeSectionReferenceCoordinateSystem 
-        - NeutralAxisCoordinateSystem: BladeSectionNeutralAxisCoordinateSystem 
-        - QuarterChordCoordinateSystem: BladeSectionQuarterChordCoordinateSystem 
-        - MassCoordinateSystem: BladeSectionMassCoordinateSystem 
-        - ShearAxis: BladeSectionShearAxis 
-        - OutputCoordinateSystem: BladeSectionOutputCoordinateSystem 
+    ----------
+    ReferenceCoordinateSystem : BladeSectionReferenceCoordinateSystem
+    NeutralAxisCoordinateSystem : BladeSectionNeutralAxisCoordinateSystem
+    QuarterChordCoordinateSystem : BladeSectionQuarterChordCoordinateSystem
+    MassCoordinateSystem : BladeSectionMassCoordinateSystem
+    ShearAxis : BladeSectionShearAxis
+    OutputCoordinateSystem : BladeSectionOutputCoordinateSystem
+    
     """
 
     ReferenceCoordinateSystem: Optional[BladeSectionReferenceCoordinateSystem] = Field(alias="ReferenceCoordinateSystem", default=None)
     NeutralAxisCoordinateSystem: Optional[BladeSectionNeutralAxisCoordinateSystem] = Field(alias="NeutralAxisCoordinateSystem", default=None)
     QuarterChordCoordinateSystem: Optional[BladeSectionQuarterChordCoordinateSystem] = Field(alias="QuarterChordCoordinateSystem", default=None)
     MassCoordinateSystem: Optional[BladeSectionMassCoordinateSystem] = Field(alias="MassCoordinateSystem", default=None)
     ShearAxis: Optional[BladeSectionShearAxis] = Field(alias="ShearAxis", default=None)
@@ -50,15 +53,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_definition.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_definition.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_aerodynamic_properties import BladeSectionAerodynamicProperties
 
 from dnv_bladed_models.blade_section_axes_and_coordinate_systems import BladeSectionAxesAndCoordinateSystems
 
 from dnv_bladed_models.blade_section_mass_properties import BladeSectionMassProperties
 
 from dnv_bladed_models.blade_section_structural_properties import BladeSectionStructuralProperties
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeSectionDefinition(Dnv):
     """BladeSectionDefinition - A blade section definition which define the geometric, structural, aerodynamic, and mass properties of the blade.
-
+    
     Attributes:
-        - IsBladePartBoundaryAtThisSection: bool => If true, the blade will be split into parts at this section.
-        - AxesAndCoordinateSystems: BladeSectionAxesAndCoordinateSystems 
-        - StructuralProperties: BladeSectionStructuralProperties 
-        - AerodynamicProperties: BladeSectionAerodynamicProperties 
-        - MassProperties: BladeSectionMassProperties 
+    ----------
+    IsBladePartBoundaryAtThisSection : bool
+        If true, the blade will be split into parts at this section.
+    AxesAndCoordinateSystems : BladeSectionAxesAndCoordinateSystems
+    StructuralProperties : BladeSectionStructuralProperties
+    AerodynamicProperties : BladeSectionAerodynamicProperties
+    MassProperties : BladeSectionMassProperties
+    
     """
 
     IsBladePartBoundaryAtThisSection: Optional[bool] = Field(alias="IsBladePartBoundaryAtThisSection", default=None)
     AxesAndCoordinateSystems: Optional[BladeSectionAxesAndCoordinateSystems] = Field(alias="AxesAndCoordinateSystems", default=None)
     StructuralProperties: Optional[BladeSectionStructuralProperties] = Field(alias="StructuralProperties", default=None)
     AerodynamicProperties: Optional[BladeSectionAerodynamicProperties] = Field(alias="AerodynamicProperties", default=None)
     MassProperties: Optional[BladeSectionMassProperties] = Field(alias="MassProperties", default=None)
@@ -44,15 +48,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_mass_coordinate_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_mass_coordinate_system.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_offset_and_rotated_axes_system import BladeSectionOffsetAndRotatedAxesSystem
 
 
 class BladeSectionMassCoordinateSystem(BladeSectionOffsetAndRotatedAxesSystem):
     """BladeSectionMassCoordinateSystem - This axes system in which the mass properties are defined.  The origin is the centre of gravity of the section.  If omitted, the reference axes will be used as the mass axes.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_mass_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_mass_properties.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeSectionMassProperties(Dnv):
     """BladeSectionMassProperties - The mass properties at the section, expressed per unit length where appropriate.  These are defined in the mass axis system.
-
+    
     Attributes:
-        - MassPerUnitLength: float => A number representing a mass per unit length.  The SI units for mass per unit length are kilograms per metre.
-        - MomentOfInertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram  radian squareds.
-        - RadiusOfGyration: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    MassPerUnitLength : float
+        A number representing a mass per unit length.  The SI units for mass per unit length are kilograms per metre.
+    MomentOfInertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram  radian squareds.
+    RadiusOfGyration : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     MassPerUnitLength: Optional[float] = Field(alias="MassPerUnitLength", default=None)
     MomentOfInertia: Optional[float] = Field(alias="MomentOfInertia", default=None)
     RadiusOfGyration: Optional[float] = Field(alias="RadiusOfGyration", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_neutral_axis_coordinate_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_neutral_axis_coordinate_system.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_offset_and_rotated_axes_system import BladeSectionOffsetAndRotatedAxesSystem
 
 
 class BladeSectionNeutralAxisCoordinateSystem(BladeSectionOffsetAndRotatedAxesSystem):
     """BladeSectionNeutralAxisCoordinateSystem - This axes system defines the neutral bending axes relative to the reference axes.  The origin is the point where the bending stresses are zero under either bending loads (https://www.wikipedia.org/wiki/Neutral_axis).  If omitted, the reference axes will be used as the neutral axes.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_offset_and_rotated_axes_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_offset_and_rotated_axes_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_offset_axes_system import BladeSectionOffsetAxesSystem
 
 
 class BladeSectionOffsetAndRotatedAxesSystem(BladeSectionOffsetAxesSystem):
     """BladeSectionOffsetAndRotatedAxesSystem - The common properties of axes systems that are offset from the reference axes.
-
+    
     Attributes:
-        - RotationAboutReferenceZ: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    RotationAboutReferenceZ : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     RotationAboutReferenceZ: Optional[float] = Field(alias="RotationAboutReferenceZ", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_offset_axes_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_offset_axes_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeSectionOffsetAxesSystem(Dnv):
     """BladeSectionOffsetAxesSystem - The common properties of axes systems that are offset from the reference axes.
-
+    
     Attributes:
-        - OffsetInReferenceX: float => A number representing a length.  The SI units for length are metres.
-        - OffsetInReferenceY: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    OffsetInReferenceX : float
+        A number representing a length.  The SI units for length are metres.
+    OffsetInReferenceY : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     OffsetInReferenceX: Optional[float] = Field(alias="OffsetInReferenceX", default=None)
     OffsetInReferenceY: Optional[float] = Field(alias="OffsetInReferenceY", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_output_coordinate_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_output_coordinate_system.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,41 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_offset_and_rotated_axes_system import BladeSectionOffsetAndRotatedAxesSystem
 
 
 class BladeSectionOutputCoordinateSystem_YAxisFollowsEnum(str, Enum):
     COMPONENT_AXIS = "COMPONENT_AXIS"
     REFERENCE_AXIS = "REFERENCE_AXIS"
     AERODYNAMIC_AXIS = "AERODYNAMIC_AXIS"
 
 class BladeSectionOutputCoordinateSystem(BladeSectionOffsetAndRotatedAxesSystem):
     """BladeSectionOutputCoordinateSystem - This axes system in which outputs will be produced.  This allows the output of loads in a different axis system to any of the physical properties.  If omitted, the reference axes will be used as the mass axes.
-
+    
     Attributes:
-        - YAxisFollows: BladeSectionOutputCoordinateSystem_YAxisFollowsEnum => The orientation of the x and y axes.  A rotation about the reference z axis can be specified instead.
-        - ZAxisFollowsNeutralAxis: bool => If true, the z axis is parallel to the line created by the origins of the neatural axes, and not the z axis of the reference plane.
+    ----------
+    YAxisFollows : BladeSectionOutputCoordinateSystem_YAxisFollowsEnum
+        The orientation of the x and y axes.  A rotation about the reference z axis can be specified instead.
+    ZAxisFollowsNeutralAxis : bool
+        If true, the z axis is parallel to the line created by the origins of the neatural axes, and not the z axis of the reference plane.
+    
     """
 
     YAxisFollows: Optional[BladeSectionOutputCoordinateSystem_YAxisFollowsEnum] = Field(alias="YAxisFollows", default=None)
     ZAxisFollowsNeutralAxis: Optional[bool] = Field(alias="ZAxisFollowsNeutralAxis", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_quarter_chord_coordinate_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_quarter_chord_coordinate_system.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_offset_and_rotated_axes_system import BladeSectionOffsetAndRotatedAxesSystem
 
 
 class BladeSectionQuarterChordCoordinateSystem(BladeSectionOffsetAndRotatedAxesSystem):
     """BladeSectionQuarterChordCoordinateSystem - This axes system positions the aerofoil relative to the reference axes.  The origin is at the quarter-chord position, a quarter of the way from the leading edge to the trailing edge along the chordline (https://www.wikipedia.org/wiki/Airfoil#Airfoil_terminology).  The X-axis of the system is parallel to the chordline, and the Y-axis perpendicular to it.  If omitted, the reference axes will be used as the quarter-chord axes.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_reference_coordinate_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_reference_coordinate_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,42 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.point_on_span import PointOnSpan
 
 from dnv_bladed_models.vector3_d import Vector3D
 
 
 class BladeSectionReferenceCoordinateSystem(Dnv):
     """BladeSectionReferenceCoordinateSystem - An definition of the blade section&#39;s reference axis system.
-
+    
     Attributes:
-        - Origin: PointOnSpan 
-        - ZAxis: Vector3D 
-        - YAxis: Vector3D 
+    ----------
+    Origin : PointOnSpan
+    ZAxis : Vector3D
+    YAxis : Vector3D
+    
     """
 
     Origin: Optional[PointOnSpan] = Field(alias="Origin", default=None)
     ZAxis: Optional[Vector3D] = Field(alias="ZAxis", default=None)
     YAxis: Optional[Vector3D] = Field(alias="YAxis", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_shear_axis.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_shear_axis.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_section_offset_axes_system import BladeSectionOffsetAxesSystem
 
 
 class BladeSectionShearAxis(BladeSectionOffsetAxesSystem):
     """BladeSectionShearAxis - This axes system defines the location of the shear centre relative to the reference axes.  The origin is the point where the if a load were applied there would be no moment applied to the beam (https://www.wikipedia.org/wiki/Shearing_(physics)).  If omitted, the shear axes will not be used in the analyses.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_section_structural_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_section_structural_properties.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,31 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.coupling_terms import CouplingTerms
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.shear_stiffnesses import ShearStiffnesses
 
 
 class BladeSectionStructuralProperties(Dnv):
     """BladeSectionStructuralProperties - The structural properties of the blade section.  These are all oriented in the NeutralAxesCoordinateSystem, but the ShearAxis is used to interpret the shear and torsion stiffnesses and cross-coupling terms.
-
+    
     Attributes:
-        - BendingStiffnessAboutX: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - BendingStiffnessAboutY: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - TorsionalStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - AxialStiffness: float => A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
-        - ShearStiffnesses: ShearStiffnesses 
-        - CouplingTerms: CouplingTerms 
+    ----------
+    BendingStiffnessAboutX : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    BendingStiffnessAboutY : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    TorsionalStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    AxialStiffness : float
+        A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
+    ShearStiffnesses : ShearStiffnesses
+    CouplingTerms : CouplingTerms
+    
     """
 
     BendingStiffnessAboutX: Optional[float] = Field(alias="BendingStiffnessAboutX", default=None)
     BendingStiffnessAboutY: Optional[float] = Field(alias="BendingStiffnessAboutY", default=None)
     TorsionalStiffness: Optional[float] = Field(alias="TorsionalStiffness", default=None)
     AxialStiffness: Optional[float] = Field(alias="AxialStiffness", default=None)
     ShearStiffnesses: Optional[ShearStiffnesses] = Field(alias="ShearStiffnesses", default=None)
@@ -42,15 +49,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_sensor.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_sensor.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,31 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class BladeSensor_BladeSensorTypeEnum(str, Enum):
     ACCELEROMETER = "Accelerometer"
     STRAIN_GAUGE = "StrainGauge"
 
 class BladeSensor(Dnv):
     """BladeSensor - The common properties of all sensors on the blade.
-
+    
     Attributes:
-        - DistanceFromBladeRoot: float => The distance along the blade, measured from the root of the blade component.
-        - BladeSensorType: BladeSensor_BladeSensorTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    DistanceFromBladeRoot : float
+        The distance along the blade, measured from the root of the blade component.
+    BladeSensorType : BladeSensor_BladeSensorTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - BladeAccelerometer
-        - BladeStrainGauge
+        BladeAccelerometer
+        BladeStrainGauge
+    
     """
 
     DistanceFromBladeRoot: Optional[float] = Field(alias="DistanceFromBladeRoot", default=None)
     BladeSensorType: Optional[BladeSensor_BladeSensorTypeEnum] = Field(alias="BladeSensorType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -64,15 +69,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'BladeSensor'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,48 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_stability_analysis_calculation import BladeStabilityAnalysisCalculation
 
 from dnv_bladed_models.blade_stability_analysis_mode_of_operation import BladeStabilityAnalysisModeOfOperation
 
 
 class BladeStabilityAnalysis(BladeStabilityAnalysisCalculation, SteadyCalculationType='BladeStabilityAnalysis'):
     """BladeStabilityAnalysis - Defines a calculation which produces the damping and frequency of all the coupled rotor or blade modes plotted against wind speed for a single rotor.  This calculation allows analysis over a wider range of inflow conditions than the Cambell diagram calculation, rather than being constrained to normal operating conditions.
-
+    
     Attributes:
-        - ModeOfOperation: BladeStabilityAnalysisModeOfOperation 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;BladeStabilityAnalysis&#39;
+    ----------
+    ModeOfOperation : BladeStabilityAnalysisModeOfOperation, abstract
+    SteadyCalculationType : str, readonly, default='BladeStabilityAnalysis'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'BladeStabilityAnalysis'
+    
+    Notes:
+    ---------
+    ModeOfOperation has the following concrete types:
+        - BladeStabilityAnalysisIdling
+        - BladeStabilityAnalysisParked
+    
     """
 
     ModeOfOperation: Optional[BladeStabilityAnalysisModeOfOperation] = Field(alias="ModeOfOperation", default=None)
     SteadyCalculationType: Optional[str] = Field(alias="SteadyCalculationType", default='BladeStabilityAnalysis', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_calculation.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,44 +9,49 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_stability_perturbations import BladeStabilityPerturbations
 
 from dnv_bladed_models.custom_linear_model_dll import CustomLinearModelDll
 
 from dnv_bladed_models.linearisation_calculation import LinearisationCalculation
 
 
 class BladeStabilityAnalysisCalculation(LinearisationCalculation):
     """BladeStabilityAnalysisCalculation - 
-
+    
     Attributes:
-        - AzimuthAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - MaximumPlotFrequency: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - CustomLinearModelDll: CustomLinearModelDll 
-        - Perturbations: BladeStabilityPerturbations 
+    ----------
+    AzimuthAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    MaximumPlotFrequency : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    CustomLinearModelDll : CustomLinearModelDll
+    Perturbations : BladeStabilityPerturbations
+    
     """
 
     AzimuthAngle: Optional[float] = Field(alias="AzimuthAngle", default=None)
     MaximumPlotFrequency: Optional[float] = Field(alias="MaximumPlotFrequency", default=None)
     CustomLinearModelDll: Optional[CustomLinearModelDll] = Field(alias="CustomLinearModelDll", default=None)
     Perturbations: Optional[BladeStabilityPerturbations] = Field(alias="Perturbations", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_idling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_idling.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_stability_analysis_mode_of_operation import BladeStabilityAnalysisModeOfOperation
 
 
 class BladeStabilityAnalysisIdling(BladeStabilityAnalysisModeOfOperation, BladeStabilityAnalysisModeOfOperationType='Idling'):
     """BladeStabilityAnalysisIdling - 
-
+    
     Attributes:
-        - TorqueSpeedGain: float => A number representing the moment per angular velocity squared.  This is always provided in SI units, using radians.
-        - BladeStabilityAnalysisModeOfOperationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Idling&#39;
+    ----------
+    TorqueSpeedGain : float
+        A number representing the moment per angular velocity squared.  This is always provided in SI units, using radians.
+    BladeStabilityAnalysisModeOfOperationType : str, readonly, default='Idling'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Idling'
+    
     """
 
     TorqueSpeedGain: Optional[float] = Field(alias="TorqueSpeedGain", default=None)
     BladeStabilityAnalysisModeOfOperationType: Optional[str] = Field(alias="BladeStabilityAnalysisModeOfOperationType", default='Idling', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_mode_of_operation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_mode_of_operation.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,34 +9,39 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.velocity_range import VelocityRange
 
 
 class BladeStabilityAnalysisModeOfOperation_BladeStabilityAnalysisModeOfOperationTypeEnum(str, Enum):
     IDLING = "Idling"
     PARKED = "Parked"
 
 class BladeStabilityAnalysisModeOfOperation(Dnv):
     """BladeStabilityAnalysisModeOfOperation - 
-
+    
     Attributes:
-        - PitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - WindSpeedRange: VelocityRange 
-        - BladeStabilityAnalysisModeOfOperationType: BladeStabilityAnalysisModeOfOperation_BladeStabilityAnalysisModeOfOperationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    PitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    WindSpeedRange : VelocityRange
+    BladeStabilityAnalysisModeOfOperationType : BladeStabilityAnalysisModeOfOperation_BladeStabilityAnalysisModeOfOperationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - BladeStabilityAnalysisIdling
-        - BladeStabilityAnalysisParked
+        BladeStabilityAnalysisIdling
+        BladeStabilityAnalysisParked
+    
     """
 
     PitchAngle: Optional[float] = Field(alias="PitchAngle", default=None)
     WindSpeedRange: Optional[VelocityRange] = Field(alias="WindSpeedRange", default=None)
     BladeStabilityAnalysisModeOfOperationType: Optional[BladeStabilityAnalysisModeOfOperation_BladeStabilityAnalysisModeOfOperationTypeEnum] = Field(alias="BladeStabilityAnalysisModeOfOperationType", default=None)
 
     class Config:
@@ -68,15 +73,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'BladeStabilityAnalysisModeOfOperation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_analysis_parked.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_analysis_parked.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_stability_analysis_mode_of_operation import BladeStabilityAnalysisModeOfOperation
 
 
 class BladeStabilityAnalysisParked(BladeStabilityAnalysisModeOfOperation, BladeStabilityAnalysisModeOfOperationType='Parked'):
     """BladeStabilityAnalysisParked - 
-
+    
     Attributes:
-        - WindDirection: float => A number representing an angle.  The SI units for angles are radians.
-        - BladeStabilityAnalysisModeOfOperationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Parked&#39;
+    ----------
+    WindDirection : float
+        A number representing an angle.  The SI units for angles are radians.
+    BladeStabilityAnalysisModeOfOperationType : str, readonly, default='Parked'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Parked'
+    
     """
 
     WindDirection: Optional[float] = Field(alias="WindDirection", default=None)
     BladeStabilityAnalysisModeOfOperationType: Optional[str] = Field(alias="BladeStabilityAnalysisModeOfOperationType", default='Parked', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_stability_perturbations.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_stability_perturbations.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.perturbation_settings import PerturbationSettings
 
 
 class BladeStabilityPerturbations(PerturbationSettings):
     """BladeStabilityPerturbations - 
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/blade_strain_gauge.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_strain_gauge.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_sensor import BladeSensor
 
 
 class BladeStrainGauge(BladeSensor, BladeSensorType='StrainGauge'):
     """BladeStrainGauge - A strain gauge on the blade.
-
+    
     Attributes:
-        - BladeSensorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;StrainGauge&#39;
+    ----------
+    BladeSensorType : str, readonly, default='StrainGauge'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'StrainGauge'
+    
     """
 
     BladeSensorType: Optional[str] = Field(alias="BladeSensorType", default='StrainGauge', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/bladed_analysis.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/bladed_analysis.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.constants import Constants
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.settings import Settings
@@ -26,22 +27,37 @@
 from dnv_bladed_models.time_domain_simulation import TimeDomainSimulation
 
 from dnv_bladed_models.turbine import Turbine
 
 
 class BladedAnalysis(Dnv):
     """BladedAnalysis - The definition of a single Bladed analysis.
-
+    
     Attributes:
-        - TimeDomainSimulation: TimeDomainSimulation 
-        - SteadyCalculation: SteadyCalculation 
-        - Settings: Settings 
-        - Constants: Constants 
-        - Turbine: Turbine 
-        - ComponentDefinitions: Dict[str, Component] => A repository to manage Component definitions.
+    ----------
+    TimeDomainSimulation : TimeDomainSimulation
+    SteadyCalculation : SteadyCalculation, abstract
+    Settings : Settings
+    Constants : Constants
+    Turbine : Turbine
+    ComponentDefinitions : Dict[str, Component], default=dict()
+        A repository to manage Component definitions.
+    
+    Notes:
+    ---------
+    SteadyCalculation has the following concrete types:
+        - AerodynamicInformationCalculation
+        - BladeStabilityAnalysis
+        - CampbellDiagram
+        - ModelLinearisation
+        - PerformanceCoefficientsCalculation
+        - SteadyOperationalLoadsCalculation
+        - SteadyParkedLoadsCalculation
+        - SteadyPowerCurveCalculation
+    
     """
 
     TimeDomainSimulation: Optional[TimeDomainSimulation] = Field(alias="TimeDomainSimulation", default=None)
     SteadyCalculation: Optional[SteadyCalculation] = Field(alias="SteadyCalculation", default=None)
     Settings: Optional[Settings] = Field(alias="Settings", default=None)
     Constants: Optional[Constants] = Field(alias="Constants", default=None)
     Turbine: Optional[Turbine] = Field(alias="Turbine", default=None)
@@ -50,15 +66,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/bladed_control.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/bladed_control.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.control_strategy import ControlStrategy
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.external_controller import ExternalController
 
 from dnv_bladed_models.measured_signal_properties import MeasuredSignalProperties
@@ -24,22 +25,32 @@
 from dnv_bladed_models.safety_system import SafetySystem
 
 from dnv_bladed_models.sensors_used import SensorsUsed
 
 
 class BladedControl(Dnv):
     """BladedControl - A definition of how the turbine is controlled.
-
+    
     Attributes:
-        - SignalProperties: MeasuredSignalProperties 
-        - ControlDiscreteTimeStep: float => A number representing a time.  The SI units for time are seconds.
-        - Controllers: List[ExternalController] => A list of one or more controllers.  These will be run in the order they are specified, but can be run at different multiples of the timestep set above.
-        - ControlStrategy: ControlStrategy 
-        - SafetySystem: SafetySystem 
-        - SensorsUsed: SensorsUsed 
+    ----------
+    SignalProperties : MeasuredSignalProperties
+    ControlDiscreteTimeStep : float
+        A number representing a time.  The SI units for time are seconds.
+    Controllers : List[ExternalController], default=list()
+        A list of one or more controllers.  These will be run in the order they are specified, but can be run at different multiples of the timestep set above.
+    ControlStrategy : ControlStrategy
+    SafetySystem : SafetySystem
+    SensorsUsed : SensorsUsed, abstract
+    
+    Notes:
+    ---------
+    SensorsUsed has the following concrete types:
+        - LegacySensorMappings
+        - SensorMapping
+    
     """
 
     SignalProperties: Optional[MeasuredSignalProperties] = Field(alias="SignalProperties", default=None)
     ControlDiscreteTimeStep: Optional[float] = Field(alias="ControlDiscreteTimeStep", default=None)
     Controllers: Optional[List[ExternalController]] = Field(alias="Controllers", default=list())
     ControlStrategy: Optional[ControlStrategy] = Field(alias="ControlStrategy", default=None)
     SafetySystem: Optional[SafetySystem] = Field(alias="SafetySystem", default=None)
@@ -48,15 +59,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/brake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/brake.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Brake_BrakePositionEnum(str, Enum):
     LOW_SPEED_SHAFT_ROTOR_END = "LOW_SPEED_SHAFT_ROTOR_END"
     LOW_SPEED_SHAFT_GEARBOX_END = "LOW_SPEED_SHAFT_GEARBOX_END"
     HIGH_SPEED_SHAFT_GEARBOX_END = "HIGH_SPEED_SHAFT_GEARBOX_END"
@@ -24,22 +25,26 @@
 
 class Brake_BrakeTypeEnum(str, Enum):
     SIMPLE_SHAFT_BRAKE = "SimpleShaftBrake"
     NON_LINEAR_SHAFT_BRAKE = "NonLinearShaftBrake"
 
 class Brake(Dnv):
     """Brake - The common properties of all brakes in the drivetrain.
-
+    
     Attributes:
-        - BrakePosition: Brake_BrakePositionEnum => Which shaft the brake is acting on.
-        - BrakeType: Brake_BrakeTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    BrakePosition : Brake_BrakePositionEnum
+        Which shaft the brake is acting on.
+    BrakeType : Brake_BrakeTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - NonLinearShaftBrake
-        - SimpleShaftBrake
+        NonLinearShaftBrake
+        SimpleShaftBrake
+    
     """
 
     BrakePosition: Optional[Brake_BrakePositionEnum] = Field(alias="BrakePosition", default=None)
     BrakeType: Optional[Brake_BrakeTypeEnum] = Field(alias="BrakeType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -70,15 +75,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Brake'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_diagram.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_diagram.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,49 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_stability_analysis_calculation import BladeStabilityAnalysisCalculation
 
 from dnv_bladed_models.campbell_diagram_mode_of_operation import CampbellDiagramModeOfOperation
 
 
 class CampbellDiagram(BladeStabilityAnalysisCalculation, SteadyCalculationType='CampbellDiagram'):
     """CampbellDiagram - Defines a calculation which produces a Campbell diagram, showing how the system response frequencies change with the rotor&#39;s rotational speed.
-
+    
     Attributes:
-        - ModeOfOperation: CampbellDiagramModeOfOperation 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;CampbellDiagram&#39;
+    ----------
+    ModeOfOperation : CampbellDiagramModeOfOperation, abstract
+    SteadyCalculationType : str, readonly, default='CampbellDiagram'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'CampbellDiagram'
+    
+    Notes:
+    ---------
+    ModeOfOperation has the following concrete types:
+        - CampbellIdling
+        - CampbellParked
+        - CampbellPowerProduction
+    
     """
 
     ModeOfOperation: Optional[CampbellDiagramModeOfOperation] = Field(alias="ModeOfOperation", default=None)
     SteadyCalculationType: Optional[str] = Field(alias="SteadyCalculationType", default='CampbellDiagram', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_diagram_mode_of_operation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_diagram_mode_of_operation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,35 +9,39 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.velocity_range import VelocityRange
 
 
 class CampbellDiagramModeOfOperation_CampbellDiagramModeOfOperationTypeEnum(str, Enum):
     PARKED = "Parked"
     IDLING = "Idling"
     POWER_PRODUCTION = "PowerProduction"
 
 class CampbellDiagramModeOfOperation(Dnv):
     """CampbellDiagramModeOfOperation - The common properties of the operating mode of a Campbell diagram calculation.
-
+    
     Attributes:
-        - WindSpeedRange: VelocityRange 
-        - CampbellDiagramModeOfOperationType: CampbellDiagramModeOfOperation_CampbellDiagramModeOfOperationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    WindSpeedRange : VelocityRange
+    CampbellDiagramModeOfOperationType : CampbellDiagramModeOfOperation_CampbellDiagramModeOfOperationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - CampbellIdling
-        - CampbellParked
-        - CampbellPowerProduction
+        CampbellIdling
+        CampbellParked
+        CampbellPowerProduction
+    
     """
 
     WindSpeedRange: Optional[VelocityRange] = Field(alias="WindSpeedRange", default=None)
     CampbellDiagramModeOfOperationType: Optional[CampbellDiagramModeOfOperation_CampbellDiagramModeOfOperationTypeEnum] = Field(alias="CampbellDiagramModeOfOperationType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -68,15 +72,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'CampbellDiagramModeOfOperation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_idling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_idling.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.campbell_diagram_mode_of_operation import CampbellDiagramModeOfOperation
 
 
 class CampbellIdling(CampbellDiagramModeOfOperation, CampbellDiagramModeOfOperationType='Idling'):
     """CampbellIdling - 
-
+    
     Attributes:
-        - CampbellDiagramModeOfOperationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Idling&#39;
+    ----------
+    CampbellDiagramModeOfOperationType : str, readonly, default='Idling'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Idling'
+    
     """
 
     CampbellDiagramModeOfOperationType: Optional[str] = Field(alias="CampbellDiagramModeOfOperationType", default='Idling', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_parked.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_parked.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.campbell_diagram_mode_of_operation import CampbellDiagramModeOfOperation
 
 
 class CampbellParked(CampbellDiagramModeOfOperation, CampbellDiagramModeOfOperationType='Parked'):
     """CampbellParked - 
-
+    
     Attributes:
-        - CampbellDiagramModeOfOperationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Parked&#39;
+    ----------
+    CampbellDiagramModeOfOperationType : str, readonly, default='Parked'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Parked'
+    
     """
 
     CampbellDiagramModeOfOperationType: Optional[str] = Field(alias="CampbellDiagramModeOfOperationType", default='Parked', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/campbell_power_production.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/campbell_power_production.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.campbell_diagram_mode_of_operation import CampbellDiagramModeOfOperation
 
 
 class CampbellPowerProduction(CampbellDiagramModeOfOperation, CampbellDiagramModeOfOperationType='PowerProduction'):
     """CampbellPowerProduction - 
-
+    
     Attributes:
-        - CampbellDiagramModeOfOperationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PowerProduction&#39;
+    ----------
+    CampbellDiagramModeOfOperationType : str, readonly, default='PowerProduction'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PowerProduction'
+    
     """
 
     CampbellDiagramModeOfOperationType: Optional[str] = Field(alias="CampbellDiagramModeOfOperationType", default='PowerProduction', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/circular_lidar_scan.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/circular_lidar_scan.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.preset_lidar_scan import PresetLidarScan
 
 
 class CircularLidarScan(PresetLidarScan, ScanningPatternType='CircularScan'):
     """CircularLidarScan - A simple circular scan pattern.
-
+    
     Attributes:
-        - HalfAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - ScanningPatternType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;CircularScan&#39;
+    ----------
+    HalfAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    ScanningPatternType : str, readonly, default='CircularScan'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'CircularScan'
+    
     """
 
     HalfAngle: Optional[float] = Field(alias="HalfAngle", default=None)
     ScanningPatternType: Optional[str] = Field(alias="ScanningPatternType", default='CircularScan', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/component.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/component.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Component_ComponentTypeEnum(str, Enum):
     TOWER = "Tower"
     LINEAR_FOUNDATION = "LinearFoundation"
     SIMPLIFIED_LINEAR_FOUNDATION = "SimplifiedLinearFoundation"
@@ -41,44 +42,47 @@
     VARIABLE_SPEED_ACTIVE_DAMPER = "VariableSpeedActiveDamper"
     PENDULUM_DAMPER = "PendulumDamper"
     LINEAR_PASSIVE_DAMPER = "LinearPassiveDamper"
     EXTERNAL_MODULE_COMPONENT = "ExternalModuleComponent"
 
 class Component(Dnv):
     """Component - The common properties of all components.
-
+    
     Attributes:
-        - ComponentType: Component_ComponentTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    ComponentType : Component_ComponentTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - ActuatedYawSystem
-        - Blade
-        - CounterweightHub
-        - Drivetrain
-        - ExternalModuleComponent
-        - FixedSpeedActiveDamper
-        - FixedSpeedGenerator
-        - HydraulicAccumulatorYawSystem
-        - IndependentPitchHub
-        - Lidar
-        - LinearFoundation
-        - LinearPassiveDamper
-        - LinearSpringYawSystem
-        - Nacelle
-        - PassiveYawSystem
-        - PendulumDamper
-        - PitchSystem
-        - SimplifiedLinearFoundation
-        - SingleActuatorCollectiveHub
-        - Superelement
-        - TorqueControlledYawSystem
-        - Tower
-        - VariableSlipGenerator
-        - VariableSpeedActiveDamper
-        - VariableSpeedGenerator
+        ActuatedYawSystem
+        Blade
+        CounterweightHub
+        Drivetrain
+        ExternalModuleComponent
+        FixedSpeedActiveDamper
+        FixedSpeedGenerator
+        HydraulicAccumulatorYawSystem
+        IndependentPitchHub
+        Lidar
+        LinearFoundation
+        LinearPassiveDamper
+        LinearSpringYawSystem
+        Nacelle
+        PassiveYawSystem
+        PendulumDamper
+        PitchSystem
+        SimplifiedLinearFoundation
+        SingleActuatorCollectiveHub
+        Superelement
+        TorqueControlledYawSystem
+        Tower
+        VariableSlipGenerator
+        VariableSpeedActiveDamper
+        VariableSpeedGenerator
+    
     """
 
     ComponentType: Optional[Component_ComponentTypeEnum] = Field(alias="ComponentType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -108,15 +112,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Component'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/compressible_beddoes_leishman_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/compressible_beddoes_leishman_model.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.beddoes_leishman_model import BeddoesLeishmanModel
 
 
 class CompressibleBeddoesLeishmanModel(BeddoesLeishmanModel, DynamicStallType='CompressibleBeddoesLeishmanModel'):
     """CompressibleBeddoesLeishmanModel - The compressible Beddoes Leishman dynamic stall model.
-
+    
     Attributes:
-        - DynamicStallType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;CompressibleBeddoesLeishmanModel&#39;
+    ----------
+    DynamicStallType : str, readonly, default='CompressibleBeddoesLeishmanModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'CompressibleBeddoesLeishmanModel'
+    
     """
 
     DynamicStallType: Optional[str] = Field(alias="DynamicStallType", default='CompressibleBeddoesLeishmanModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/conditional_event.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/conditional_event.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.event import Event
 
 
 class ConditionalEvent(Event):
     """ConditionalEvent - An event which may occur during the simulation if its conditions are met.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/connectable_node.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/connectable_node.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,43 +9,48 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.vector3_d import Vector3D
 
 
 class ConnectableNode_ComponentTypesAllowedEnum(str, Enum):
     COMPONENTS = "COMPONENTS"
 
 class ConnectableNode(Dnv):
     """ConnectableNode - An object declaring a distal node for a component.
-
+    
     Attributes:
-        - ComponentTypesAllowed: ConnectableNode_ComponentTypesAllowedEnum => Optional: A list of the components that could be attached to the node.  If not specified, this will be governed by the component&#39;s connectivity.
-        - RelativeOrientation: List[float] => The relative orientation of the attached component to the current component (e.g. the axle to the nacelle, or the blade assembly to the hub).  Required if encrypted and not the same as the parent (e.g. for a nacelle&#39;s axle node)
-        - OffsetIfEncrypted: Vector3D 
+    ----------
+    ComponentTypesAllowed : ConnectableNode_ComponentTypesAllowedEnum
+        Optional: A list of the components that could be attached to the node.  If not specified, this will be governed by the component's connectivity.
+    RelativeOrientation : List[float], default=list()
+        The relative orientation of the attached component to the current component (e.g. the axle to the nacelle, or the blade assembly to the hub).  Required if encrypted and not the same as the parent (e.g. for a nacelle's axle node)
+    OffsetIfEncrypted : Vector3D
+    
     """
 
     ComponentTypesAllowed: Optional[ConnectableNode_ComponentTypesAllowedEnum] = Field(alias="ComponentTypesAllowed", default=None)
     RelativeOrientation: Optional[List[float]] = Field(alias="RelativeOrientation", default=list())
     OffsetIfEncrypted: Optional[Vector3D] = Field(alias="OffsetIfEncrypted", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/constants.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/constants.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,42 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.air_characteristics import AirCharacteristics
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.water_characteristics import WaterCharacteristics
 
 
 class Constants(Dnv):
     """Constants - The physical constants used in the analysis.  These can be overridden in specific analyses using $insertAndOverride.
-
+    
     Attributes:
-        - GravitationalConstant: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - AirCharacteristics: AirCharacteristics 
-        - WaterCharacteristics: WaterCharacteristics 
+    ----------
+    GravitationalConstant : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    AirCharacteristics : AirCharacteristics
+    WaterCharacteristics : WaterCharacteristics
+    
     """
 
     GravitationalConstant: Optional[float] = Field(alias="GravitationalConstant", default=None)
     AirCharacteristics: Optional[AirCharacteristics] = Field(alias="AirCharacteristics", default=None)
     WaterCharacteristics: Optional[WaterCharacteristics] = Field(alias="WaterCharacteristics", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/control_strategy.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/control_strategy.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ControlStrategy_RegulationMethodEnum(str, Enum):
     FIXED_SPEED_STALL_REGULATED = "FIXED_SPEED_STALL_REGULATED"
     FIXED_SPEED_PITCH_REGULATED = "FIXED_SPEED_PITCH_REGULATED"
     VARIABLE_SPEED_STALL_REGULATED = "VARIABLE_SPEED_STALL_REGULATED"
@@ -29,22 +30,30 @@
 
 class ControlStrategy_PitchingDirectionToReduceLiftEnum(str, Enum):
     FEATHER = "PITCH_TOWARDS_FEATHER"
     FINE = "PITCH_TOWARDS_FINE"
 
 class ControlStrategy(Dnv):
     """ControlStrategy - The high-level description of the control system.
-
+    
     Attributes:
-        - RegulationMethod: ControlStrategy_RegulationMethodEnum => The overall principles by which the turbine is controlled.
-        - PitchingMechanism: ControlStrategy_PitchingMechanismEnum => The pitching mechanism used to control the blades.
-        - CutInFlowSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - CutOutFlowSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - UseControllerDynamics: bool => If true, controller dynamics will be included in the simulation.
-        - PitchingDirectionToReduceLift: ControlStrategy_PitchingDirectionToReduceLiftEnum => The direction that the controller will pitch the blades in order to reduce the rotor speed.  Most modern turbines feather the blades to reduce the load, but another strategy (usually found in older turbines) is to increase the angle of attack to deliberately stall the blades.
+    ----------
+    RegulationMethod : ControlStrategy_RegulationMethodEnum
+        The overall principles by which the turbine is controlled.
+    PitchingMechanism : ControlStrategy_PitchingMechanismEnum
+        The pitching mechanism used to control the blades.
+    CutInFlowSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    CutOutFlowSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    UseControllerDynamics : bool, default=True
+        If true, controller dynamics will be included in the simulation.
+    PitchingDirectionToReduceLift : ControlStrategy_PitchingDirectionToReduceLiftEnum, default='PITCH_TOWARDS_FEATHER'
+        The direction that the controller will pitch the blades in order to reduce the rotor speed.  Most modern turbines feather the blades to reduce the load, but another strategy (usually found in older turbines) is to increase the angle of attack to deliberately stall the blades.
+    
     """
 
     RegulationMethod: Optional[ControlStrategy_RegulationMethodEnum] = Field(alias="RegulationMethod", default=None)
     PitchingMechanism: Optional[ControlStrategy_PitchingMechanismEnum] = Field(alias="PitchingMechanism", default=None)
     CutInFlowSpeed: Optional[float] = Field(alias="CutInFlowSpeed", default=None)
     CutOutFlowSpeed: Optional[float] = Field(alias="CutOutFlowSpeed", default=None)
     UseControllerDynamics: Optional[bool] = Field(alias="UseControllerDynamics", default=True)
@@ -53,15 +62,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controlled_operation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_control_state.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.timed_event import TimedEvent
 
+from dnv_bladed_models.initial_condition import InitialCondition
 
-class ControlledOperation(TimedEvent):
-    """ControlledOperation - An operation occuring at a specified time.  This will be implemented by the controller, and Bladed will send it a signal at the time it should occur.
 
+class InitialControlState(InitialCondition):
+    """InitialControlState - The initial operational state of the turbine.
+    
     Attributes:
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
+    ----------
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    
     """
 
     OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +112,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        ControlledOperation
+        InitialControlState
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = ControlledOperation.from_file('/path/to/file')
+        >>> model = InitialControlState.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +142,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        ControlledOperation
+        InitialControlState
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = ControlledOperation.from_json('{ ... }')
+        >>> model = InitialControlState.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +174,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        ControlledOperation
+        InitialControlState
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +206,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-ControlledOperation.update_forward_refs()
+InitialControlState.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controlled_stop.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/controlled_stop.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.controlled_operation import ControlledOperation
 
 
 class ControlledStop(ControlledOperation):
     """ControlledStop - A stop operation  occuring at a specified time.
-
+    
     Attributes:
-        - ExtraSimulationTimeAfterFullStop: float => A number representing a time.  The SI units for time are seconds.
+    ----------
+    ExtraSimulationTimeAfterFullStop : float
+        A number representing a time.  The SI units for time are seconds.
+    
     """
 
     ExtraSimulationTimeAfterFullStop: Optional[float] = Field(alias="ExtraSimulationTimeAfterFullStop", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controller_fault.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/controller_fault.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fault import Fault
 
 
 class ControllerFault(Fault, EventType='ControllerFault'):
     """ControllerFault - A controller fault occuring at a specified time.  A signal will be sent to the controller, and the controller will be expected to implement the fault behaviour.
-
+    
     Attributes:
-        - FaultCode: int => A fault number that will be provided to the controller.  It is for the controller to decide how to react to this number.
-        - FaultParameters: Dict[str, Any] => Further additional parameters to be sent to the controller.  This should be in JSON format.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ControllerFault&#39;
+    ----------
+    FaultCode : int
+        A fault number that will be provided to the controller.  It is for the controller to decide how to react to this number.
+    FaultParameters : Dict[str, Any]
+        Further additional parameters to be sent to the controller.  This should be in JSON format.
+    EventType : str, readonly, default='ControllerFault'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ControllerFault'
+    
     """
 
     FaultCode: Optional[int] = Field(alias="FaultCode", default=None)
     FaultParameters: Optional[Dict[str, Any]] = Field(alias="FaultParameters", default=None)
     EventType: Optional[str] = Field(alias="EventType", default='ControllerFault', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/controller_lidar_settings.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/controller_lidar_settings.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.lidar_focal_distance_control import LidarFocalDistanceControl
 
 
 class ControllerLidarSettings(LidarFocalDistanceControl, FocalDistanceControlType='Controller'):
     """ControllerLidarSettings - The focal distance is controlled by the external controller.
-
+    
     Attributes:
-        - FocalDistanceControlType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Controller&#39;
+    ----------
+    FocalDistanceControlType : str, readonly, default='Controller'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Controller'
+    
     """
 
     FocalDistanceControlType: Optional[str] = Field(alias="FocalDistanceControlType", default='Controller', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/counterweight_hub.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/counterweight_hub.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.hub import Hub
 
 
 class CounterweightHub(Hub, ComponentType='CounterweightHub'):
     """CounterweightHub - A one-bladed hub with a counterweight diametrically opposite to the blade mounting.
-
+    
     Attributes:
-        - CounterweightMass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - CounterweightInertia: float => A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;CounterweightHub&#39;
+    ----------
+    CounterweightMass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    CounterweightInertia : float
+        A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
+    ComponentType : str, readonly, default='CounterweightHub'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'CounterweightHub'
+    
     """
 
     CounterweightMass: Optional[float] = Field(alias="CounterweightMass", default=None)
     CounterweightInertia: Optional[float] = Field(alias="CounterweightInertia", default=None)
     ComponentType: Optional[str] = Field(alias="ComponentType", default='CounterweightHub', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/coupling_terms.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/coupling_terms.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class CouplingTerms(Dnv):
     """CouplingTerms - Any coupling terms between bending and torsion.
-
+    
     Attributes:
-        - BendingXYCoupling: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - TorsionBendingXCoupling: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - TorsionBendingYCoupling: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    ----------
+    BendingXYCoupling : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    TorsionBendingXCoupling : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    TorsionBendingYCoupling : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    
     """
 
     BendingXYCoupling: Optional[float] = Field(alias="BendingXYCoupling", default=None)
     TorsionBendingXCoupling: Optional[float] = Field(alias="TorsionBendingXCoupling", default=None)
     TorsionBendingYCoupling: Optional[float] = Field(alias="TorsionBendingYCoupling", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/cuboid_nacelle_cover.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/cuboid_nacelle_cover.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.nacelle_cover import NacelleCover
 
 
 class CuboidNacelleCover(NacelleCover, NacelleCoverType='Cuboid'):
     """CuboidNacelleCover - A nacelle cover best represented by a cuboid.
-
+    
     Attributes:
-        - Width: float => A number representing a length.  The SI units for length are metres.
-        - Length: float => A number representing a length.  The SI units for length are metres.
-        - Height: float => A number representing a length.  The SI units for length are metres.
-        - CoefficientOfDrag: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - NacelleCoverType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Cuboid&#39;
+    ----------
+    Width : float
+        A number representing a length.  The SI units for length are metres.
+    Length : float
+        A number representing a length.  The SI units for length are metres.
+    Height : float
+        A number representing a length.  The SI units for length are metres.
+    CoefficientOfDrag : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    NacelleCoverType : str, readonly, default='Cuboid'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Cuboid'
+    
     """
 
     Width: Optional[float] = Field(alias="Width", default=None)
     Length: Optional[float] = Field(alias="Length", default=None)
     Height: Optional[float] = Field(alias="Height", default=None)
     CoefficientOfDrag: Optional[float] = Field(alias="CoefficientOfDrag", default=None)
     NacelleCoverType: Optional[str] = Field(alias="NacelleCoverType", default='Cuboid', allow_mutation=False)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,32 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Current_CurrentTypeEnum(str, Enum):
     TURBULENT_CURRENT = "TurbulentCurrent"
     LAMINAR_FLOW = "LaminarFlow"
     CURRENTS = "Currents"
 
 class Current(Dnv):
     """Current - The definition of the currents to be considered for the analysis.
-
+    
     Attributes:
-        - CurrentType: Current_CurrentTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    CurrentType : Current_CurrentTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - Currents
-        - LaminarFlowCurrent
-        - TurbulentCurrent
+        Currents
+        LaminarFlowCurrent
+        TurbulentCurrent
+    
     """
 
     CurrentType: Optional[Current_CurrentTypeEnum] = Field(alias="CurrentType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -64,15 +68,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Current'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_shear_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_shear_time_history.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_direction_shear_variation import CurrentDirectionShearVariation
 
 from dnv_bladed_models.time_vs_current_direction_shear import TimeVsCurrentDirectionShear
 
 
 class CurrentDirectionShearTimeHistory(CurrentDirectionShearVariation, DirectionShearVariationType='TimeHistory'):
     """CurrentDirectionShearTimeHistory - A time history of the vertical direction shear (veer).  This is a linear relationship between the height and the local direction, with the direction being its nominal value at the reference height.
-
+    
     Attributes:
-        - TimeVsDirectionShear: List[TimeVsCurrentDirectionShear] => A list of points specifying the wind veer at the corresponding time.  The wind veer will be interpolated between these points; the wind veer before the first point will be constant at the first point&#39;s value; and the wind veer after the last point will remain constant at the last point&#39;s value
-        - DirectionShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsDirectionShear : List[TimeVsCurrentDirectionShear], default=list()
+        A list of points specifying the wind veer at the corresponding time.  The wind veer will be interpolated between these points; the wind veer before the first point will be constant at the first point's value; and the wind veer after the last point will remain constant at the last point's value
+    DirectionShearVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsDirectionShear: Optional[List[TimeVsCurrentDirectionShear]] = Field(alias="TimeVsDirectionShear", default=list())
     DirectionShearVariationType: Optional[str] = Field(alias="DirectionShearVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_shear_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_shear_variation.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class CurrentDirectionShearVariation_DirectionShearVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class CurrentDirectionShearVariation(Dnv):
     """CurrentDirectionShearVariation - A defined variation in the direction wind shear.  This is a linear relationship between the height and the local direction, with the direction being its nominal value at the reference height.
-
+    
     Attributes:
-        - DirectionShearVariationType: CurrentDirectionShearVariation_DirectionShearVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    DirectionShearVariationType : CurrentDirectionShearVariation_DirectionShearVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetCurrentDirectionShearTransient
-        - CurrentDirectionShearTimeHistory
+        PresetCurrentDirectionShearTransient
+        CurrentDirectionShearTimeHistory
+    
     """
 
     DirectionShearVariationType: Optional[CurrentDirectionShearVariation_DirectionShearVariationTypeEnum] = Field(alias="DirectionShearVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'CurrentDirectionShearVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_time_history.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_direction_variation import CurrentDirectionVariation
 
 from dnv_bladed_models.time_vs_current_direction import TimeVsCurrentDirection
 
 
 class CurrentDirectionTimeHistory(CurrentDirectionVariation, DirectionVariationType='TimeHistory'):
     """CurrentDirectionTimeHistory - A time history of the current&#39;s direction.
-
+    
     Attributes:
-        - TimeVsDirection: List[TimeVsCurrentDirection] => A list of points specifying the direction at the corresponding time.  The direction will be interpolated between these points; the direction before the first point will be constant at the first point&#39;s value; and the direction after the last point will remain constant at the last point&#39;s value
-        - DirectionVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsDirection : List[TimeVsCurrentDirection], default=list()
+        A list of points specifying the direction at the corresponding time.  The direction will be interpolated between these points; the direction before the first point will be constant at the first point's value; and the direction after the last point will remain constant at the last point's value
+    DirectionVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsDirection: Optional[List[TimeVsCurrentDirection]] = Field(alias="TimeVsDirection", default=list())
     DirectionVariationType: Optional[str] = Field(alias="DirectionVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_direction_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_direction_variation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class CurrentDirectionVariation_DirectionVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class CurrentDirectionVariation(Dnv):
     """CurrentDirectionVariation - A defined variation in the current&#39;s direction.
-
+    
     Attributes:
-        - DirectionVariationType: CurrentDirectionVariation_DirectionVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    DirectionVariationType : CurrentDirectionVariation_DirectionVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetCurrentDirectionTransient
-        - CurrentDirectionTimeHistory
+        PresetCurrentDirectionTransient
+        CurrentDirectionTimeHistory
+    
     """
 
     DirectionVariationType: Optional[CurrentDirectionVariation_DirectionVariationTypeEnum] = Field(alias="DirectionVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'CurrentDirectionVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_horizontal_shear_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_horizontal_shear_time_history.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_horizontal_shear_variation import CurrentHorizontalShearVariation
 
 from dnv_bladed_models.time_vs_current_horizontal_shear import TimeVsCurrentHorizontalShear
 
 
 class CurrentHorizontalShearTimeHistory(CurrentHorizontalShearVariation, HorizontalShearVariationType='TimeHistory'):
     """CurrentHorizontalShearTimeHistory - A time history of the current &#39;s horizontal shear.
-
+    
     Attributes:
-        - TimeVsHorizontalShear: List[TimeVsCurrentHorizontalShear] => A list of points specifying the current&#39;s shear at the corresponding time.  The shear will be interpolated between these points; the shear before the first point will be constant at the first point&#39;s value; and the shear after the last point will remain constant at the last point&#39;s value
-        - HorizontalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsHorizontalShear : List[TimeVsCurrentHorizontalShear], default=list()
+        A list of points specifying the current's shear at the corresponding time.  The shear will be interpolated between these points; the shear before the first point will be constant at the first point's value; and the shear after the last point will remain constant at the last point's value
+    HorizontalShearVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsHorizontalShear: Optional[List[TimeVsCurrentHorizontalShear]] = Field(alias="TimeVsHorizontalShear", default=list())
     HorizontalShearVariationType: Optional[str] = Field(alias="HorizontalShearVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_horizontal_shear_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_horizontal_shear_variation.py`

 * *Files 0% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class CurrentHorizontalShearVariation_HorizontalShearVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class CurrentHorizontalShearVariation(Dnv):
     """CurrentHorizontalShearVariation - A defined variation in the current&#39;s horizontal shear.
-
+    
     Attributes:
-        - HorizontalShearVariationType: CurrentHorizontalShearVariation_HorizontalShearVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    HorizontalShearVariationType : CurrentHorizontalShearVariation_HorizontalShearVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetCurrentHorizontalShearTransient
-        - CurrentHorizontalShearTimeHistory
+        PresetCurrentHorizontalShearTransient
+        CurrentHorizontalShearTimeHistory
+    
     """
 
     HorizontalShearVariationType: Optional[CurrentHorizontalShearVariation_HorizontalShearVariationTypeEnum] = Field(alias="HorizontalShearVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'CurrentHorizontalShearVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_mean_speed_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_mean_speed_time_history.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_mean_speed_variation import CurrentMeanSpeedVariation
 
 from dnv_bladed_models.time_vs_current_mean_speed import TimeVsCurrentMeanSpeed
 
 
 class CurrentMeanSpeedTimeHistory(CurrentMeanSpeedVariation, MeanSpeedVariationType='TimeHistory'):
     """CurrentMeanSpeedTimeHistory - A time history of the current&#39;s speed.
-
+    
     Attributes:
-        - TimeVsMeanSpeed: List[TimeVsCurrentMeanSpeed] => A list of points specifying the current&#39;s speed at the corresponding time.  The speed will be interpolated between these points; the speed before the first point will be constant at the first point&#39;s value; and the speed after the last point will remain constant at the last point&#39;s value
-        - MeanSpeedVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsMeanSpeed : List[TimeVsCurrentMeanSpeed], default=list()
+        A list of points specifying the current's speed at the corresponding time.  The speed will be interpolated between these points; the speed before the first point will be constant at the first point's value; and the speed after the last point will remain constant at the last point's value
+    MeanSpeedVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsMeanSpeed: Optional[List[TimeVsCurrentMeanSpeed]] = Field(alias="TimeVsMeanSpeed", default=list())
     MeanSpeedVariationType: Optional[str] = Field(alias="MeanSpeedVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_mean_speed_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_mean_speed_variation.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class CurrentMeanSpeedVariation_MeanSpeedVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class CurrentMeanSpeedVariation(Dnv):
     """CurrentMeanSpeedVariation - A defined variation in the current&#39;s mean speed.
-
+    
     Attributes:
-        - MeanSpeedVariationType: CurrentMeanSpeedVariation_MeanSpeedVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    MeanSpeedVariationType : CurrentMeanSpeedVariation_MeanSpeedVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetCurrentMeanSpeedTransient
-        - CurrentMeanSpeedTimeHistory
+        PresetCurrentMeanSpeedTransient
+        CurrentMeanSpeedTimeHistory
+    
     """
 
     MeanSpeedVariationType: Optional[CurrentMeanSpeedVariation_MeanSpeedVariationTypeEnum] = Field(alias="MeanSpeedVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'CurrentMeanSpeedVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_vertical_shear_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_vertical_shear_time_history.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_vertical_shear_variation import CurrentVerticalShearVariation
 
 from dnv_bladed_models.time_vs_current_vertical_shear import TimeVsCurrentVerticalShear
 
 
 class CurrentVerticalShearTimeHistory(CurrentVerticalShearVariation, VerticalShearVariationType='TimeHistory'):
     """CurrentVerticalShearTimeHistory - A time history of the current &#39;s vertical shear.
-
+    
     Attributes:
-        - TimeVsVerticalShear: List[TimeVsCurrentVerticalShear] => A list of points specifying the current&#39;s shear at the corresponding time.  The shear will be interpolated between these points; the shear before the first point will be constant at the first point&#39;s value; and the shear after the last point will remain constant at the last point&#39;s value
-        - VerticalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsVerticalShear : List[TimeVsCurrentVerticalShear], default=list()
+        A list of points specifying the current's shear at the corresponding time.  The shear will be interpolated between these points; the shear before the first point will be constant at the first point's value; and the shear after the last point will remain constant at the last point's value
+    VerticalShearVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsVerticalShear: Optional[List[TimeVsCurrentVerticalShear]] = Field(alias="TimeVsVerticalShear", default=list())
     VerticalShearVariationType: Optional[str] = Field(alias="VerticalShearVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/current_vertical_shear_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/current_vertical_shear_variation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class CurrentVerticalShearVariation_VerticalShearVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class CurrentVerticalShearVariation(Dnv):
     """CurrentVerticalShearVariation - A defined variation in the current&#39;s vertical shear.
-
+    
     Attributes:
-        - VerticalShearVariationType: CurrentVerticalShearVariation_VerticalShearVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    VerticalShearVariationType : CurrentVerticalShearVariation_VerticalShearVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetCurrentVerticalShearTransient
-        - CurrentVerticalShearTimeHistory
+        PresetCurrentVerticalShearTransient
+        CurrentVerticalShearTimeHistory
+    
     """
 
     VerticalShearVariationType: Optional[CurrentVerticalShearVariation_VerticalShearVariationTypeEnum] = Field(alias="VerticalShearVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'CurrentVerticalShearVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/currents.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/currents.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,46 +9,50 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.near_shore_current import NearShoreCurrent
 
 from dnv_bladed_models.near_surface_current import NearSurfaceCurrent
 
 from dnv_bladed_models.sub_surface_current import SubSurfaceCurrent
 
 from dnv_bladed_models.time_domain_current import TimeDomainCurrent
 
 
 class Currents(TimeDomainCurrent, CurrentType='Currents'):
     """Currents - The definition of the current using simple numerical models.
-
+    
     Attributes:
-        - NearSurfaceCurrent: NearSurfaceCurrent 
-        - SubSurfaceCurrent: SubSurfaceCurrent 
-        - NearShoreCurrent: NearShoreCurrent 
-        - CurrentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Currents&#39;
+    ----------
+    NearSurfaceCurrent : NearSurfaceCurrent
+    SubSurfaceCurrent : SubSurfaceCurrent
+    NearShoreCurrent : NearShoreCurrent
+    CurrentType : str, readonly, default='Currents'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Currents'
+    
     """
 
     NearSurfaceCurrent: Optional[NearSurfaceCurrent] = Field(alias="NearSurfaceCurrent", default=None)
     SubSurfaceCurrent: Optional[SubSurfaceCurrent] = Field(alias="SubSurfaceCurrent", default=None)
     NearShoreCurrent: Optional[NearShoreCurrent] = Field(alias="NearShoreCurrent", default=None)
     CurrentType: Optional[str] = Field(alias="CurrentType", default='Currents', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/custom_linear_model_dll.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/custom_linear_model_dll.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class CustomLinearModelDll(Dnv):
     """CustomLinearModelDll - The definition of a custom library (a dynamic link library (dll) on Windows, or a shared object (so) on Linux) to use for linearisation calculations.
-
+    
     Attributes:
-        - Filepath: str => The linearModel.dll performs linear analysis on model linearisation data produced by dtbladed.exe and creates the Campbell diagram. The user may choose a custom LinearModel.dll rather than the one in the same folder as the dtbladed.exe.
+    ----------
+    Filepath : str, default='LinearModel.dll'
+        The linearModel.dll performs linear analysis on model linearisation data produced by dtbladed.exe and creates the Campbell diagram. The user may choose a custom LinearModel.dll rather than the one in the same folder as the dtbladed.exe.
+    
     """
 
     Filepath: Optional[str] = Field(alias="Filepath", default='LinearModel.dll')
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/damper.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 
 class Damper(Component):
     """Damper - The common properties of all support structure dampers.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/damper_force_or_torque_vs_rate.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/damper_force_or_torque_vs_rate.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class DamperForceOrTorqueVsRate(Dnv):
     """DamperForceOrTorqueVsRate - A line or row in a look-up table, specifying a value of Rate for a specified DamperForceOrTorque.
-
+    
     Attributes:
-        - DamperForceOrTorque: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Rate: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    DamperForceOrTorque : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Rate : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     DamperForceOrTorque: Optional[float] = Field(alias="DamperForceOrTorque", default=None)
     Rate: Optional[float] = Field(alias="Rate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dimensionless_range.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/dimensionless_range.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class DimensionlessRange(Dnv):
     """DimensionlessRange - The range of tip speed ratios to consider.
-
+    
     Attributes:
-        - Minimum: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Maximum: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Interval: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Minimum : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Maximum : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Interval : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Minimum: Optional[float] = Field(alias="Minimum", default=None)
     Maximum: Optional[float] = Field(alias="Maximum", default=None)
     Interval: Optional[float] = Field(alias="Interval", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dnv.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/dnv.py`

 * *Files 2% similar despite different names*

```diff
@@ -10,37 +10,44 @@
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
 
+
 class Dnv(BaseModel):
     """Dnv - The base schema for all DNV objects, allowing $insert and $insertAndOverride behaviour.
-
+    
     Attributes:
-        - DerivedType: str => Not used
-        - insert: str => If the $insert keyword is provided, ALL properties will be taken from the referenced object, and no additional properties can be specified.
-        - insertAndOverride: str => If the $insertAndOverride keyword is provided, properties will be taken from the referenced object, but any properties specified alongside it will be taken in preference to it.
-        - Schema: str => Every JSON object can explicitly specify the schema that it is conforming to.  This can be helpful in order to validate your JSON.
+    ----------
+    DerivedType : str
+        Not used
+    insert : str
+        If the $insert keyword is provided, ALL properties will be taken from the referenced object, and no additional properties can be specified.
+    insertAndOverride : str
+        If the $insertAndOverride keyword is provided, properties will be taken from the referenced object, but any properties specified alongside it will be taken in preference to it.
+    Schema : str
+        Every JSON object can explicitly specify the schema that it is conforming to.  This can be helpful in order to validate your JSON.
+    
     """
 
     DerivedType: Optional[str] = Field(alias="DerivedType", default=None)
     insert: Optional[str] = Field(alias="$insert", default=None)
     insertAndOverride: Optional[str] = Field(alias="$insertAndOverride", default=None)
     Schema: Optional[str] = Field(alias="$schema", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/drivetrain.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/drivetrain.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.brake import Brake
 
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 from dnv_bladed_models.drivetrain_output_group import DrivetrainOutputGroup
@@ -28,27 +29,42 @@
 from dnv_bladed_models.pallet_mounting_flexibility import PalletMountingFlexibility
 
 from dnv_bladed_models.slipping_clutch import SlippingClutch
 
 
 class Drivetrain(Component, ComponentType='Drivetrain'):
     """Drivetrain - A drivetrain component, typically connecting the nacelle (at it&#39;s main bearing) to a generator.
-
+    
     Attributes:
-        - ShaftBrakes: List[Brake] => Definitions for the brakes on the various shafts of the drivetrain.
-        - GearboxRatio: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - GearboxInertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - HighSpeedShaftInertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - SlippingClutch: SlippingClutch 
-        - HighSpeedShaftTorsion: HighSpeedShaftFlexibility 
-        - MountingFlexibility: PalletMountingFlexibility 
-        - Losses: MechanicalLosses 
-        - OutputGroups: Dict[str, DrivetrainOutputGroup] => A library which contains any number of named output groups.  These can be referenced from &#39;SelectedOutputGroup&#39;
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Drivetrain&#39;
+    ----------
+    ShaftBrakes : List[Brake], default=list()
+        Definitions for the brakes on the various shafts of the drivetrain.
+    GearboxRatio : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    GearboxInertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    HighSpeedShaftInertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    SlippingClutch : SlippingClutch
+    HighSpeedShaftTorsion : HighSpeedShaftFlexibility
+    MountingFlexibility : PalletMountingFlexibility
+    Losses : MechanicalLosses, abstract
+    OutputGroups : Dict[str, DrivetrainOutputGroup], default=dict()
+        A library which contains any number of named output groups.  These can be referenced from 'SelectedOutputGroup'
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    ComponentType : str, readonly, default='Drivetrain'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Drivetrain'
+    
+    Notes:
+    ---------
+    Losses has the following concrete types:
+        - PowerScalingLosses
+        - TorqueScalingLosses
+    
     """
 
     ShaftBrakes: Optional[List[Brake]] = Field(alias="ShaftBrakes", default=list())
     GearboxRatio: Optional[float] = Field(alias="GearboxRatio", default=None)
     GearboxInertia: Optional[float] = Field(alias="GearboxInertia", default=None)
     HighSpeedShaftInertia: Optional[float] = Field(alias="HighSpeedShaftInertia", default=None)
     SlippingClutch: Optional[SlippingClutch] = Field(alias="SlippingClutch", default=None)
@@ -62,15 +78,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/drivetrain_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/drivetrain_output_group.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class DrivetrainOutputGroup(Dnv):
     """DrivetrainOutputGroup - An output group definition for the drivetrain.
-
+    
     Attributes:
-        - OutputDrivetrainData: bool => If true, output drivetrain output groups
+    ----------
+    OutputDrivetrainData : bool, default=False
+        If true, output drivetrain output groups
+    
     """
 
     OutputDrivetrainData: Optional[bool] = Field(alias="OutputDrivetrainData", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dynamic_stall.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/dynamic_stall.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,37 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class DynamicStall_DynamicStallTypeEnum(str, Enum):
     OYE_MODEL = "OyeModel"
     INCOMPRESSIBLE_BEDDOES_LEISHMAN_MODEL = "IncompressibleBeddoesLeishmanModel"
     COMPRESSIBLE_BEDDOES_LEISHMAN_MODEL = "CompressibleBeddoesLeishmanModel"
 
 class DynamicStall(Dnv):
     """DynamicStall - The common properties of all of the dynamic stall models.
-
+    
     Attributes:
-        - UseDynamicPitchingMomentCoefficient: bool => If true, the dynamic pitching moment coefficient will be used.  This option is enabled by default.  It is not recommended to disable this option for blades with a torsional degree of freedom because the so-called &#39;pitch- rate damping&#39; term of the moment coefficient is typically important to keep the blade torsional mode stable.
-        - StartingRadius: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - EndingRadius: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - SeparationTimeConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - UseAveragedLocalTangentMethod: bool => If true, the averaged polar gradient is used to reconstruct the inviscid polar data.  The averaging is performed between AoA&#x3D;-2 deg to AoA&#x3D;7 deg at 0.5 deg intervals.  This approach is more suitable for polar data sets where the lift coefficient slope is not straight around 0 deg angle of attack.  It is recommended to activate this option for more accurate computations.  This option is turned on by default. Switch off for obtaining consistent results with Bladed 4.12 and earlier.
-        - DynamicStallType: DynamicStall_DynamicStallTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    UseDynamicPitchingMomentCoefficient : bool, default=True
+        If true, the dynamic pitching moment coefficient will be used.  This option is enabled by default.  It is not recommended to disable this option for blades with a torsional degree of freedom because the so-called 'pitch- rate damping' term of the moment coefficient is typically important to keep the blade torsional mode stable.
+    StartingRadius : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    EndingRadius : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    SeparationTimeConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    UseAveragedLocalTangentMethod : bool, default=True
+        If true, the averaged polar gradient is used to reconstruct the inviscid polar data.  The averaging is performed between AoA=-2 deg to AoA=7 deg at 0.5 deg intervals.  This approach is more suitable for polar data sets where the lift coefficient slope is not straight around 0 deg angle of attack.  It is recommended to activate this option for more accurate computations.  This option is turned on by default. Switch off for obtaining consistent results with Bladed 4.12 and earlier.
+    DynamicStallType : DynamicStall_DynamicStallTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - CompressibleBeddoesLeishmanModel
-        - IncompressibleBeddoesLeishmanModel
-        - OyeModel
+        CompressibleBeddoesLeishmanModel
+        IncompressibleBeddoesLeishmanModel
+        OyeModel
+    
     """
 
     UseDynamicPitchingMomentCoefficient: Optional[bool] = Field(alias="UseDynamicPitchingMomentCoefficient", default=True)
     StartingRadius: Optional[float] = Field(alias="StartingRadius", default=None)
     EndingRadius: Optional[float] = Field(alias="EndingRadius", default=None)
     SeparationTimeConstant: Optional[float] = Field(alias="SeparationTimeConstant", default=None)
     UseAveragedLocalTangentMethod: Optional[bool] = Field(alias="UseAveragedLocalTangentMethod", default=True)
@@ -74,15 +83,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'DynamicStall'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dynamic_upstream_wake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/dynamic_upstream_wake.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.meandering_wake import MeanderingWake
 
 from dnv_bladed_models.position_of_upwind_turbine import PositionOfUpwindTurbine
 
 from dnv_bladed_models.wake_properties_of_upstream_turbine import WakePropertiesOfUpstreamTurbine
 
 
 class DynamicUpstreamWake(Dnv):
     """DynamicUpstreamWake - 
-
+    
     Attributes:
-        - RadialStepSize: float => A number representing a length.  The SI units for length are metres.
-        - NumberOfRadialPoints: int => The total number of points in the radial wake deficit profile.
-        - StreamwiseStep: float => A number representing a length.  The SI units for length are metres.
-        - MixingLength: float => A number representing a length.  The SI units for length are metres.
-        - ShearCalibrationConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AmbientCalibrationConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - PositionOfUpwindTurbine: PositionOfUpwindTurbine 
-        - WakePropertiesOfUpstreamTurbine: WakePropertiesOfUpstreamTurbine 
-        - MeanderingWake: MeanderingWake 
+    ----------
+    RadialStepSize : float
+        A number representing a length.  The SI units for length are metres.
+    NumberOfRadialPoints : int, default=50
+        The total number of points in the radial wake deficit profile.
+    StreamwiseStep : float
+        A number representing a length.  The SI units for length are metres.
+    MixingLength : float
+        A number representing a length.  The SI units for length are metres.
+    ShearCalibrationConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AmbientCalibrationConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    PositionOfUpwindTurbine : PositionOfUpwindTurbine
+    WakePropertiesOfUpstreamTurbine : WakePropertiesOfUpstreamTurbine
+    MeanderingWake : MeanderingWake
+    
     """
 
     RadialStepSize: Optional[float] = Field(alias="RadialStepSize", default=None)
     NumberOfRadialPoints: Optional[int] = Field(alias="NumberOfRadialPoints", default=50)
     StreamwiseStep: Optional[float] = Field(alias="StreamwiseStep", default=None)
     MixingLength: Optional[float] = Field(alias="MixingLength", default=None)
     ShearCalibrationConstant: Optional[float] = Field(alias="ShearCalibrationConstant", default=None)
@@ -50,15 +59,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/dynamic_wake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/dynamic_wake.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,39 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class DynamicWake_AreaAveragingMethodEnum(str, Enum):
     OVER_ANNULUS = "OVER_ANNULUS"
     NONE = "NONE"
 
 class DynamicWake_DynamicWakeTypeEnum(str, Enum):
     OYE_DYNAMIC_WAKE = "OyeDynamicWake"
     PITT_AND_PETERS_MODEL = "PittAndPetersModel"
     FREE_FLOW_MODEL = "FreeFlowModel"
     EQUILIBRIUM_WAKE_MODEL = "EquilibriumWakeModel"
+    FROZEN_WAKE_MODEL = "FrozenWakeModel"
 
 class DynamicWake(Dnv):
     """DynamicWake - Common properties for all dynamic wake models.
-
+    
     Attributes:
-        - AreaAveragingMethod: DynamicWake_AreaAveragingMethodEnum => With the \&quot;over annulus\&quot; method, the dynamic wake is calculated over the entire annular ring.  Induced velocities are averaged over the number of blades.  If \&quot;none\&quot; is selected, the annulus is divided into segments to which separate dynamic wakes are applied.
-        - DynamicWakeType: DynamicWake_DynamicWakeTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    AreaAveragingMethod : DynamicWake_AreaAveragingMethodEnum, default='OVER_ANNULUS'
+        With the \"over annulus\" method, the dynamic wake is calculated over the entire annular ring.  Induced velocities are averaged over the number of blades.  If \"none\" is selected, the annulus is divided into segments to which separate dynamic wakes are applied.
+    DynamicWakeType : DynamicWake_DynamicWakeTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - EquilibriumWakeModel
-        - FreeFlowModel
-        - OyeDynamicWake
-        - PittAndPetersModel
+        EquilibriumWakeModel
+        FreeFlowModel
+        FrozenWakeModel
+        OyeDynamicWake
+        PittAndPetersModel
+    
     """
 
     AreaAveragingMethod: Optional[DynamicWake_AreaAveragingMethodEnum] = Field(alias="AreaAveragingMethod", default='OVER_ANNULUS')
     DynamicWakeType: Optional[DynamicWake_DynamicWakeTypeEnum] = Field(alias="DynamicWakeType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -72,15 +79,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'DynamicWake'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/earthquake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/earthquake.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Earthquake(Dnv):
     """Earthquake - The definition of a seismic event, or earthquake.
-
+    
     Attributes:
-        - QuakeFilepath: str => The filepath or URI providing a time history of the earthquake accelerations.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DirectionClockwiseFromNorth: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    QuakeFilepath : str
+        The filepath or URI providing a time history of the earthquake accelerations.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DirectionClockwiseFromNorth : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     QuakeFilepath: Optional[str] = Field(alias="QuakeFilepath", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DirectionClockwiseFromNorth: Optional[float] = Field(alias="DirectionClockwiseFromNorth", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/electrical_grid.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/electrical_grid.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,30 +9,40 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.grid_loss_energy_sink import GridLossEnergySink
 
 
 class ElectricalGrid(Dnv):
     """ElectricalGrid - The definition of the electrical grid that the turbine is connected to.
-
+    
     Attributes:
-        - NetworkVoltage: float => A number representing an electrical voltage [https://ww.wikipedia.org/wiki/Voltage].  The SI units for volatage are Volts.
-        - ConnectingLineResistance: float => A number representing an electrical resistance [https://www.wikipedia.org/wiki/Electrical_resistance_and_conductance].  The SI units for resistance are Ohms, ℧.
-        - ConnectingLineInductance: float => A number representing an electrical inductance [https://www.wikipedia.org/wiki/Inductance].  The SI units for inductance are henrys (H).
-        - NetworkResistance: float => A number representing an electrical resistance [https://www.wikipedia.org/wiki/Electrical_resistance_and_conductance].  The SI units for resistance are Ohms, ℧.
-        - NetworkInductance: float => A number representing an electrical inductance [https://www.wikipedia.org/wiki/Inductance].  The SI units for inductance are henrys (H).
-        - NumberOfTurbinesOnFarm: int => The number of turbines on the farm that share the same grid connection.
-        - GridLossEnergySinks: List[GridLossEnergySink] => A list of energy sinks available to the turbine.
+    ----------
+    NetworkVoltage : float
+        A number representing an electrical voltage [https://ww.wikipedia.org/wiki/Voltage].  The SI units for volatage are Volts.
+    ConnectingLineResistance : float
+        A number representing an electrical resistance [https://www.wikipedia.org/wiki/Electrical_resistance_and_conductance].  The SI units for resistance are Ohms, ℧.
+    ConnectingLineInductance : float
+        A number representing an electrical inductance [https://www.wikipedia.org/wiki/Inductance].  The SI units for inductance are henrys (H).
+    NetworkResistance : float
+        A number representing an electrical resistance [https://www.wikipedia.org/wiki/Electrical_resistance_and_conductance].  The SI units for resistance are Ohms, ℧.
+    NetworkInductance : float
+        A number representing an electrical inductance [https://www.wikipedia.org/wiki/Inductance].  The SI units for inductance are henrys (H).
+    NumberOfTurbinesOnFarm : int
+        The number of turbines on the farm that share the same grid connection.
+    GridLossEnergySinks : List[GridLossEnergySink], default=list()
+        A list of energy sinks available to the turbine.
+    
     """
 
     NetworkVoltage: Optional[float] = Field(alias="NetworkVoltage", default=None)
     ConnectingLineResistance: Optional[float] = Field(alias="ConnectingLineResistance", default=None)
     ConnectingLineInductance: Optional[float] = Field(alias="ConnectingLineInductance", default=None)
     NetworkResistance: Optional[float] = Field(alias="NetworkResistance", default=None)
     NetworkInductance: Optional[float] = Field(alias="NetworkInductance", default=None)
@@ -42,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/electrical_losses.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/electrical_losses.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ElectricalLosses_ElectricalLossesTypeEnum(str, Enum):
     LINEAR_ELECTRICAL_LOSSES = "LinearElectricalLosses"
     NON_LINEAR_ELECTRICAL_LOSSES = "NonLinearElectricalLosses"
 
 class ElectricalLosses(Dnv):
     """ElectricalLosses - The electrical losses in the generator.
-
+    
     Attributes:
-        - ElectricalLossesType: ElectricalLosses_ElectricalLossesTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    ElectricalLossesType : ElectricalLosses_ElectricalLossesTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - LinearElectricalLosses
-        - NonLinearElectricalLosses
+        LinearElectricalLosses
+        NonLinearElectricalLosses
+    
     """
 
     ElectricalLossesType: Optional[ElectricalLosses_ElectricalLossesTypeEnum] = Field(alias="ElectricalLossesType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'ElectricalLosses'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/emergency_stop_button.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/emergency_stop_button.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class EmergencyStopButton(Dnv):
     """EmergencyStopButton - The definition of the safety system behaviour when the emergency stop button has been pressed.
-
+    
     Attributes:
-        - CircuitToActivate: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    CircuitToActivate : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     CircuitToActivate: Optional[str] = Field(alias="CircuitToActivate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/emergency_stop_operation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/emergency_stop_operation.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.controlled_stop import ControlledStop
 
 
 class EmergencyStopOperation(ControlledStop, EventType='EmergencyStopOperation'):
     """EmergencyStopOperation - A request for the controller to stop the turbine or rotor at a specified time in the simulation.  The stop will be performed by the safety system.
-
+    
     Attributes:
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;EmergencyStopOperation&#39;
+    ----------
+    EventType : str, readonly, default='EmergencyStopOperation'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'EmergencyStopOperation'
+    
     """
 
     EventType: Optional[str] = Field(alias="EventType", default='EmergencyStopOperation', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/environment.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/environment.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,44 +9,54 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.earthquake import Earthquake
 
 from dnv_bladed_models.sea_state import SeaState
 
 from dnv_bladed_models.wind import Wind
 
 
 class Environment(Dnv):
     """Environment - The definition of the environment conditions affecting the turbine location during this simulation.
-
+    
     Attributes:
-        - Wind: Wind 
-        - SeaState: SeaState 
-        - Earthquake: Earthquake 
+    ----------
+    Wind : Wind, abstract
+    SeaState : SeaState
+    Earthquake : Earthquake
+    
+    Notes:
+    ---------
+    Wind has the following concrete types:
+        - ExternalFlowSourceForWind
+        - LaminarFlowWind
+        - TurbulentWind
+    
     """
 
     Wind: Optional[Wind] = Field(alias="Wind", default=None)
     SeaState: Optional[SeaState] = Field(alias="SeaState", default=None)
     Earthquake: Optional[Earthquake] = Field(alias="Earthquake", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/equilibrium_wake_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/equilibrium_wake_model.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dynamic_wake import DynamicWake
 
 
 class EquilibriumWakeModel(DynamicWake, DynamicWakeType='EquilibriumWakeModel'):
-    """EquilibriumWakeModel - The equilibrium wake dynamic wake model.
-
+    """EquilibriumWakeModel - The equilibrium wake model instantaneously updates the induction in response to changes operating condition.  This is represents the steady state solution.  It should not be used in linearisation as it would represent an instantaneous response of the wake, where in reality the response is significantly lagged.  It is also not a good approach for time domain as the lagged wake response is not captured.
+    
     Attributes:
-        - DynamicWakeType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;EquilibriumWakeModel&#39;
+    ----------
+    DynamicWakeType : str, readonly, default='EquilibriumWakeModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'EquilibriumWakeModel'
+    
     """
 
     DynamicWakeType: Optional[str] = Field(alias="DynamicWakeType", default='EquilibriumWakeModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/event.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/event.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Event_EventTypeEnum(str, Enum):
     YAW_FAULT_CONSTANT_RATE = "YawFaultConstantRate"
     YAW_FAULT_CONSTANT_TORQUE = "YawFaultConstantTorque"
     YAW_FAULT_LIMP = "YawFaultLimp"
@@ -34,37 +35,40 @@
     NORMAL_STOP_OPERATION = "NormalStopOperation"
     START_UP_OPERATION = "StartUpOperation"
     PITCH_FAULT_SEIZURE_AT_ANGLE = "PitchFaultSeizureAtAngle"
     PERMANENTLY_STUCK_PITCH_SYSTEM = "PermanentlyStuckPitchSystem"
 
 class Event(Dnv):
     """Event - An event which may occur during the simulation.  These can either be timed events, or based on a set of conditions which may or may not occur.
-
+    
     Attributes:
-        - EventType: Event_EventTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    EventType : Event_EventTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - ControllerFault
-        - EmergencyStopOperation
-        - GridLoss
-        - NetworkFrequencyDisturbance
-        - NetworkVoltageDisturbance
-        - NormalStopOperation
-        - PermanentlyStuckPitchSystem
-        - PitchFaultConstantRate
-        - PitchFaultConstantTorque
-        - PitchFaultLimp
-        - PitchFaultSeizure
-        - PitchFaultSeizureAtAngle
-        - ShortCircuit
-        - StartUpOperation
-        - YawFaultConstantRate
-        - YawFaultConstantTorque
-        - YawFaultLimp
-        - YawManoever
+        ControllerFault
+        EmergencyStopOperation
+        GridLoss
+        NetworkFrequencyDisturbance
+        NetworkVoltageDisturbance
+        NormalStopOperation
+        PermanentlyStuckPitchSystem
+        PitchFaultConstantRate
+        PitchFaultConstantTorque
+        PitchFaultLimp
+        PitchFaultSeizure
+        PitchFaultSeizureAtAngle
+        ShortCircuit
+        StartUpOperation
+        YawFaultConstantRate
+        YawFaultConstantTorque
+        YawFaultLimp
+        YawManoever
+    
     """
 
     EventType: Optional[Event_EventTypeEnum] = Field(alias="EventType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -94,15 +98,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Event'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/evolving_current_turbulence.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/evolving_current_turbulence.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class EvolvingCurrentTurbulence_MethodEnum(str, Enum):
     KRISTENSEN = "KRISTENSEN"
     EXPONENTIAL = "EXPONENTIAL"
 
 class EvolvingCurrentTurbulence(Dnv):
     """EvolvingCurrentTurbulence - The settings for evolving turbulence, if required.  In the case of a normal turbulent wind field, the turbulence is frozen and approaches the turbine at a constant block.  Although this doesn&#39;t match physical reality, it is a particular problem for Lidar - as it gives them a perfect insight into the oncoming wind field.  In order to represent the nature of real turbulence, a second turbulence file is superimposed on the windfield so that it evolves as it moves forward.  This is computationally expensive, and is usually applied only to the Lidar readings - although it can be applied to all the wind values in a simulation.
-
+    
     Attributes:
-        - Method: EvolvingCurrentTurbulence_MethodEnum => The method used to combine the two wind fields.
-        - ExponentialFactor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Method : EvolvingCurrentTurbulence_MethodEnum
+        The method used to combine the two wind fields.
+    ExponentialFactor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Method: Optional[EvolvingCurrentTurbulence_MethodEnum] = Field(alias="Method", default=None)
     ExponentialFactor: Optional[float] = Field(alias="ExponentialFactor", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/evolving_wind_turbulence.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/evolving_wind_turbulence.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,49 +9,56 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class EvolvingWindTurbulence_MethodEnum(str, Enum):
     KRISTENSEN = "KRISTENSEN"
     EXPONENTIAL = "EXPONENTIAL"
 
 class EvolvingWindTurbulence_ApplyToEnum(str, Enum):
     OFF = "OFF"
     LIDAR_ONLY = "LIDAR_ONLY"
     ALL_WIND_CALCULATIONS = "ALL_WIND_CALCULATIONS"
 
 class EvolvingWindTurbulence(Dnv):
     """EvolvingWindTurbulence - The settings for evolving turbulence.  In the case of a normal turbulent wind field, the turbulence is frozen and approaches the turbine at a constant block.  Although this doesn&#39;t match physical reality, it is a particular problem for Lidar - as it gives them a perfect insight into the oncoming wind field.  In order to represent the nature of real turbulence, a second turbulence file is superimposed on the windfield so that it evolves as it moves forward.  This is computationally expensive, and is usually applied only to the Lidar readings - although it can be applied to all the wind values in a simulation.
-
+    
     Attributes:
-        - SecondTurbulenceFilepath: str => The filepath or URI of the second turbulence file.  The turbulence in this file will be used to simulate an evolving turbulence field.
-        - Method: EvolvingWindTurbulence_MethodEnum => The method used to combine the turbulence in the two turbulence files.
-        - ExponentialFactor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - ApplyTo: EvolvingWindTurbulence_ApplyToEnum => Evolving turbulence is usually only applied to Lidar readings, but it can be applied to all wind values.
+    ----------
+    SecondTurbulenceFilepath : str
+        The filepath or URI of the second turbulence file.  The turbulence in this file will be used to simulate an evolving turbulence field.
+    Method : EvolvingWindTurbulence_MethodEnum
+        The method used to combine the turbulence in the two turbulence files.
+    ExponentialFactor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    ApplyTo : EvolvingWindTurbulence_ApplyToEnum
+        Evolving turbulence is usually only applied to Lidar readings, but it can be applied to all wind values.
+    
     """
 
     SecondTurbulenceFilepath: Optional[str] = Field(alias="SecondTurbulenceFilepath", default=None)
     Method: Optional[EvolvingWindTurbulence_MethodEnum] = Field(alias="Method", default=None)
     ExponentialFactor: Optional[float] = Field(alias="ExponentialFactor", default=None)
     ApplyTo: Optional[EvolvingWindTurbulence_ApplyToEnum] = Field(alias="ApplyTo", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/explicit_newmark_beta_fixed_step.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/explicit_newmark_beta_fixed_step.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fixed_step_integrator import FixedStepIntegrator
 
 
 class ExplicitNewmarkBetaFixedStep(FixedStepIntegrator, IntegratorType='ExplicitNewmarkBetaFixedStep'):
     """ExplicitNewmarkBetaFixedStep - Settings for the Explicit Newmark Beta Fixed Step integrator.
-
+    
     Attributes:
-        - MaximumNumberOfIterations: int => The maximum number of iterations for prescribed freedoms and first order states (e.g. dynamic stall &amp; wake).  A value of 1 may sometimes inprecisely integrate first order states
-        - Beta: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Gamma: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - IntegratorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ExplicitNewmarkBetaFixedStep&#39;
+    ----------
+    MaximumNumberOfIterations : int, default=1
+        The maximum number of iterations for prescribed freedoms and first order states (e.g. dynamic stall & wake).  A value of 1 may sometimes inprecisely integrate first order states
+    Beta : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Gamma : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    IntegratorType : str, readonly, default='ExplicitNewmarkBetaFixedStep'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ExplicitNewmarkBetaFixedStep'
+    
     """
 
     MaximumNumberOfIterations: Optional[int] = Field(alias="MaximumNumberOfIterations", default=1)
     Beta: Optional[float] = Field(alias="Beta", default=None)
     Gamma: Optional[float] = Field(alias="Gamma", default=None)
     IntegratorType: Optional[str] = Field(alias="IntegratorType", default='ExplicitNewmarkBetaFixedStep', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/explicit_tower_can.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/explicit_tower_can.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,42 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_can import TowerCan
 
 from dnv_bladed_models.tower_can_properties_explicit import TowerCanPropertiesExplicit
 
 from dnv_bladed_models.tower_can_properties_explicit_where_different import TowerCanPropertiesExplicitWhereDifferent
 
 
 class ExplicitTowerCan(TowerCan, TowerCanType='ExplicitTowerCan'):
     """ExplicitTowerCan - A tower can where all of the physical properties are explicitly provided.
-
+    
     Attributes:
-        - BaseSection: TowerCanPropertiesExplicit 
-        - TopSection: TowerCanPropertiesExplicitWhereDifferent 
-        - TowerCanType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ExplicitTowerCan&#39;
+    ----------
+    BaseSection : TowerCanPropertiesExplicit
+    TopSection : TowerCanPropertiesExplicitWhereDifferent
+    TowerCanType : str, readonly, default='ExplicitTowerCan'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ExplicitTowerCan'
+    
     """
 
     BaseSection: Optional[TowerCanPropertiesExplicit] = Field(alias="BaseSection", default=None)
     TopSection: Optional[TowerCanPropertiesExplicitWhereDifferent] = Field(alias="TopSection", default=None)
     TowerCanType: Optional[str] = Field(alias="TowerCanType", default='ExplicitTowerCan', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/exponential_shear_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/exponential_shear_model.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.vertical_shear import VerticalShear
 
 
 class ExponentialShearModel(VerticalShear, VerticalShearType='ExponentialShearModel'):
     """ExponentialShearModel - The vertical wind shear to apply to the wind field.  This will vary the wind speed from 100% at the reference height (usually the hub height) down to 0% at ground level on an exponential basis.
-
+    
     Attributes:
-        - ShearExponent: float => The exponential factor governing the shape of the wind shear variation.  This is typically in the order of 0.2.
-        - VerticalShearType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ExponentialShearModel&#39;
+    ----------
+    ShearExponent : float
+        The exponential factor governing the shape of the wind shear variation.  This is typically in the order of 0.2.
+    VerticalShearType : str, readonly, default='ExponentialShearModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ExponentialShearModel'
+    
     """
 
     ShearExponent: Optional[float] = Field(alias="ShearExponent", default=None)
     VerticalShearType: Optional[str] = Field(alias="VerticalShearType", default='ExponentialShearModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/extension_pieces.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/extension_pieces.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ExtensionPieces(Dnv):
     """ExtensionPieces - The (typically cylindrical) extension connecting the pitch drive to the blade root.  These can be provided if the drag on this part of the hub needs to be modelled.
-
+    
     Attributes:
-        - ExtensionPieceDiameter: float => A number representing a length.  The SI units for length are metres.
-        - CoefficientOfDrag: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    ExtensionPieceDiameter : float
+        A number representing a length.  The SI units for length are metres.
+    CoefficientOfDrag : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     ExtensionPieceDiameter: Optional[float] = Field(alias="ExtensionPieceDiameter", default=None)
     CoefficientOfDrag: Optional[float] = Field(alias="CoefficientOfDrag", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_controller.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_controller.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ExternalController_CallingConventionEnum(str, Enum):
     CDECL = "__cdecl"
     STDCALL = "__stdcall"
 
@@ -30,24 +31,34 @@
     SEVENTH = "Seventh"
     EIGTH = "Eigth"
     NINTH = "Ninth"
     TENTH = "Tenth"
 
 class ExternalController(Dnv):
     """ExternalController - A definition of a single controller for the turbine.
-
+    
     Attributes:
-        - Filepath: str => The location of the external controller dll.
-        - CallingConvention: ExternalController_CallingConventionEnum => The calling convention to be used when calling the external controller.  The default for all C-family languages is __cdecl.  The default for FORTRAN is __stdcall unless the [C] qualifier is specfied immediately after the function name.  Specifying the wrong calling convention can lead to unexplained system exceptions when attempting to call the external controller.
-        - FunctionName: str => The name of the function in the dll to run.  This must satisfy the standard external controller typedef, found in the ExternalControllerApi.h.
-        - PassParametersByFile: bool => If true, a file will be written containing the parameters in the above box.  The location of this file can be obtained in the external controller using the function GetInfileFilepath.  The name of this file will be \&quot;DISCON.IN\&quot; if there is only one controller, or of the pattern \&quot;DISCONn.IN\&quot;, where &#39;n&#39; is the number of the controller.  If not checked (the default), this string will be directly available using the function GetUserParameters.
-        - ForceLegacy: bool => If true, only the old-style &#39;DISCON&#39; function will be looked for in the controller, and raise an error if it cannot be found.  This is only used for testing legacy controllers where both CONTROLLER and DISCON functions are both defined, but the DISCON function is required.
-        - TimeStepMultiplier: ExternalController_TimeStepMultiplierEnum => Whether the controller should be called on every discrete timestep, set above.
-        - Parameters: Dict[str, Any] => JSON data that will be passed to the constructor of the external module.
-        - UseFloatingPointProtection: bool => If true, this will apply floating point protection when calling the external controllers.  When the protection is on, any floating point errors are trapped and reported.  When this is switched off, the behaviour will default to that of the computer&#39;s floating point machine, but this can often be to not report the error, and to use a semi-random (but often very large) number instead of the correct result.  This can lead to unrepeatable results and numeric errors.
+    ----------
+    Filepath : str
+        The location of the external controller dll.
+    CallingConvention : ExternalController_CallingConventionEnum, default='__cdecl'
+        The calling convention to be used when calling the external controller.  The default for all C-family languages is __cdecl.  The default for FORTRAN is __stdcall unless the [C] qualifier is specfied immediately after the function name.  Specifying the wrong calling convention can lead to unexplained system exceptions when attempting to call the external controller.
+    FunctionName : str, default='ExternalController'
+        The name of the function in the dll to run.  This must satisfy the standard external controller typedef, found in the ExternalControllerApi.h.
+    PassParametersByFile : bool, default=False
+        If true, a file will be written containing the parameters in the above box.  The location of this file can be obtained in the external controller using the function GetInfileFilepath.  The name of this file will be \"DISCON.IN\" if there is only one controller, or of the pattern \"DISCONn.IN\", where 'n' is the number of the controller.  If not checked (the default), this string will be directly available using the function GetUserParameters.
+    ForceLegacy : bool, default=False
+        If true, only the old-style 'DISCON' function will be looked for in the controller, and raise an error if it cannot be found.  This is only used for testing legacy controllers where both CONTROLLER and DISCON functions are both defined, but the DISCON function is required.
+    TimeStepMultiplier : ExternalController_TimeStepMultiplierEnum, default='Every'
+        Whether the controller should be called on every discrete timestep, set above.
+    Parameters : Dict[str, Any]
+        JSON data that will be passed to the constructor of the external module.
+    UseFloatingPointProtection : bool, default=True
+        If true, this will apply floating point protection when calling the external controllers.  When the protection is on, any floating point errors are trapped and reported.  When this is switched off, the behaviour will default to that of the computer's floating point machine, but this can often be to not report the error, and to use a semi-random (but often very large) number instead of the correct result.  This can lead to unrepeatable results and numeric errors.
+    
     """
 
     Filepath: Optional[str] = Field(alias="Filepath", default=None)
     CallingConvention: Optional[ExternalController_CallingConventionEnum] = Field(alias="CallingConvention", default='__cdecl')
     FunctionName: Optional[str] = Field(alias="FunctionName", default='ExternalController')
     PassParametersByFile: Optional[bool] = Field(alias="PassParametersByFile", default=False)
     ForceLegacy: Optional[bool] = Field(alias="ForceLegacy", default=False)
@@ -58,15 +69,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_flow_source_for_wind.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_flow_source_for_wind.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wind import Wind
 
 
 class ExternalFlowSourceForWind(Wind, WindType='ExternalFlowSourceForWind'):
     """ExternalFlowSourceForWind - The definition of a wind field that is provided by an external library - a dynamic link library (dll) on Windows, or a shared object (so) on Linux.
-
+    
     Attributes:
-        - LibraryFilepath: str => The filepath of the external library object.
-        - Parameters: Dict[str, Any] => JSON data to be passed to the external library during instantiation.
-        - WindType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ExternalFlowSourceForWind&#39;
+    ----------
+    LibraryFilepath : str
+        The filepath of the external library object.
+    Parameters : Dict[str, Any]
+        JSON data to be passed to the external library during instantiation.
+    WindType : str, readonly, default='ExternalFlowSourceForWind'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ExternalFlowSourceForWind'
+    
     """
 
     LibraryFilepath: Optional[str] = Field(alias="LibraryFilepath", default=None)
     Parameters: Optional[Dict[str, Any]] = Field(alias="Parameters", default=None)
     WindType: Optional[str] = Field(alias="WindType", default='ExternalFlowSourceForWind', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_module.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_module.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ExternalModule(Dnv):
     """ExternalModule - A specialised External Module component.  This is in the form of an external library (a dynamic link library on Windows, or a shared object on Linux).  This acts as a component in the Bladed assembly, and creates multibody objects to attach to the previous component. 
-
+    
     Attributes:
-        - Filepath: str => The absolute filepath to the dynamic linked library (Windows) or the shared object (Linux).
-        - Parameters: Dict[str, Any] => JSON data that will be passed to the constructor of the external module.
+    ----------
+    Filepath : str
+        The absolute filepath to the dynamic linked library (Windows) or the shared object (Linux).
+    Parameters : Dict[str, Any]
+        JSON data that will be passed to the constructor of the external module.
+    
     """
 
     Filepath: Optional[str] = Field(alias="Filepath", default=None)
     Parameters: Optional[Dict[str, Any]] = Field(alias="Parameters", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/external_module_component.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/external_module_component.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,42 +9,49 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 
 class ExternalModuleComponent(Component, ComponentType='ExternalModuleComponent'):
     """ExternalModuleComponent - A specialised External Module component.  This is in the form of an external library (a dynamic link library on Windows, or a shared object on Linux).  This acts as a component in the Bladed assembly, and creates multibody objects to attach to the previous component. 
-
+    
     Attributes:
-        - Filepath: str => The absolute filepath to the dynamic linked library (Windows) or the shared object (Linux).
-        - Parameters: Dict[str, Any] => JSON data that will be passed to the constructor of the external module.
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ExternalModuleComponent&#39;
+    ----------
+    Filepath : str
+        The absolute filepath to the dynamic linked library (Windows) or the shared object (Linux).
+    Parameters : Dict[str, Any]
+        JSON data that will be passed to the constructor of the external module.
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    ComponentType : str, readonly, default='ExternalModuleComponent'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ExternalModuleComponent'
+    
     """
 
     Filepath: Optional[str] = Field(alias="Filepath", default=None)
     Parameters: Optional[Dict[str, Any]] = Field(alias="Parameters", default=None)
     ConnectableNodes: Optional[Dict[str, ConnectableNode]] = Field(alias="ConnectableNodes", default=dict())
     ComponentType: Optional[str] = Field(alias="ComponentType", default='ExternalModuleComponent', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/externally_stepped_simulation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/externally_stepped_simulation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,42 +9,48 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.hardware_tracking import HardwareTracking
 
 
 class ExternallySteppedSimulation(Dnv):
     """ExternallySteppedSimulation - The definition of a Bladed real time simulation.  This will be run with Bladed as an external library (i.e. a dynamic link library (dll) on Windows, or a shared object (so) on Linux).  An external process must call functions within the Bladed external library to step the simulation, and thereby keep it synchronised with the external controlling process.
-
+    
     Attributes:
-        - TimeStep: float => A number representing a time.  The SI units for time are seconds.
-        - TimeStepMultiplier: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - HardwareTracking: HardwareTracking 
-        - OutputRealTimeDataFromStart: bool => If true, realtime data will be output as soon as Bladed is running.
+    ----------
+    TimeStep : float
+        A number representing a time.  The SI units for time are seconds.
+    TimeStepMultiplier : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    HardwareTracking : HardwareTracking
+    OutputRealTimeDataFromStart : bool
+        If true, realtime data will be output as soon as Bladed is running.
+    
     """
 
     TimeStep: Optional[float] = Field(alias="TimeStep", default=None)
     TimeStepMultiplier: Optional[float] = Field(alias="TimeStepMultiplier", default=None)
     HardwareTracking: Optional[HardwareTracking] = Field(alias="HardwareTracking", default=None)
     OutputRealTimeDataFromStart: Optional[bool] = Field(alias="OutputRealTimeDataFromStart", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fault.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/fault.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.timed_event import TimedEvent
 
 
 class Fault(TimedEvent):
     """Fault - A fault which may occur during the simulation.  These can either be timed events, or based on a set of conditions which may or may not occur.
-
+    
     Attributes:
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
+    ----------
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    
     """
 
     OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/finite_element_blade_modelling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/finite_element_blade_modelling.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_modelling import BladeModelling
 
 
 class FiniteElementBladeModelling(BladeModelling, BladeModellingType='FiniteElementBladeModelling'):
     """FiniteElementBladeModelling - The properties for the structural modelling of the blade using finite element analysis without modal reduction.
-
+    
     Attributes:
-        - BladeModellingType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;FiniteElementBladeModelling&#39;
+    ----------
+    BladeModellingType : str, readonly, default='FiniteElementBladeModelling'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'FiniteElementBladeModelling'
+    
     """
 
     BladeModellingType: Optional[str] = Field(alias="BladeModellingType", default='FiniteElementBladeModelling', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/first_order_response.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/first_order_response.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class FirstOrderResponse(ResponseToDemand, ResponseToDemandType='FirstOrderResponse'):
     """FirstOrderResponse - Defines a first-order delay response to the controller&#39;s demands.
-
+    
     Attributes:
-        - LagTime: float => A number representing a time.  The SI units for time are seconds.
-        - ResponseToDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;FirstOrderResponse&#39;
+    ----------
+    LagTime : float
+        A number representing a time.  The SI units for time are seconds.
+    ResponseToDemandType : str, readonly, default='FirstOrderResponse'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'FirstOrderResponse'
+    
     """
 
     LagTime: Optional[float] = Field(alias="LagTime", default=None)
     ResponseToDemandType: Optional[str] = Field(alias="ResponseToDemandType", default='FirstOrderResponse', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_speed_active_damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_speed_active_damper.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.active_damper import ActiveDamper
 
 
 class FixedSpeedActiveDamper(ActiveDamper, ComponentType='FixedSpeedActiveDamper'):
     """FixedSpeedActiveDamper - A fixed speed active damper for the support structure.
-
+    
     Attributes:
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;FixedSpeedActiveDamper&#39;
+    ----------
+    ComponentType : str, readonly, default='FixedSpeedActiveDamper'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'FixedSpeedActiveDamper'
+    
     """
 
     ComponentType: Optional[str] = Field(alias="ComponentType", default='FixedSpeedActiveDamper', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_speed_generator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_speed_generator.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,40 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fixed_speed_induction_generator import FixedSpeedInductionGenerator
 
 from dnv_bladed_models.generator import Generator
 
 
 class FixedSpeedGenerator(Generator, ComponentType='FixedSpeedGenerator'):
     """FixedSpeedGenerator - A generator that is designed to operate at a fixed speed.
-
+    
     Attributes:
-        - MainGenerator: FixedSpeedInductionGenerator 
-        - LowSpeedGenerator: FixedSpeedInductionGenerator 
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;FixedSpeedGenerator&#39;
+    ----------
+    MainGenerator : FixedSpeedInductionGenerator
+    LowSpeedGenerator : FixedSpeedInductionGenerator
+    ComponentType : str, readonly, default='FixedSpeedGenerator'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'FixedSpeedGenerator'
+    
     """
 
     MainGenerator: Optional[FixedSpeedInductionGenerator] = Field(alias="MainGenerator", default=None)
     LowSpeedGenerator: Optional[FixedSpeedInductionGenerator] = Field(alias="LowSpeedGenerator", default=None)
     ComponentType: Optional[str] = Field(alias="ComponentType", default='FixedSpeedGenerator', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_speed_induction_generator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_speed_induction_generator.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class FixedSpeedInductionGenerator(Dnv):
     """FixedSpeedInductionGenerator - The properties of a fixed speed induction generator.
-
+    
     Attributes:
-        - RatedPower: float => A number representing a power.  The SI units for enery are Watts.
-        - SynchronousSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - TimeConstantOfTorqueResponse: float => A number representing a time.  The SI units for time are seconds.
-        - SlipAtRatedPower: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    RatedPower : float
+        A number representing a power.  The SI units for enery are Watts.
+    SynchronousSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    TimeConstantOfTorqueResponse : float
+        A number representing a time.  The SI units for time are seconds.
+    SlipAtRatedPower : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     RatedPower: Optional[float] = Field(alias="RatedPower", default=None)
     SynchronousSpeed: Optional[float] = Field(alias="SynchronousSpeed", default=None)
     TimeConstantOfTorqueResponse: Optional[float] = Field(alias="TimeConstantOfTorqueResponse", default=None)
     SlipAtRatedPower: Optional[float] = Field(alias="SlipAtRatedPower", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/fixed_step_integrator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/fixed_step_integrator.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.integrator import Integrator
 
 
 class FixedStepIntegrator(Integrator):
     """FixedStepIntegrator - Common settings for the fixed step integrators.
-
+    
     Attributes:
-        - TimeStep: float => A number representing a time.  The SI units for time are seconds.
-        - Tolerance: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    TimeStep : float
+        A number representing a time.  The SI units for time are seconds.
+    Tolerance : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     TimeStep: Optional[float] = Field(alias="TimeStep", default=None)
     Tolerance: Optional[float] = Field(alias="Tolerance", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/flex_body.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/flex_body.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,39 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.structural_modelling import StructuralModelling
 
 
 class FlexBody(Component):
     """FlexBody - The common properties of components modelled using multibody flex bodies.
-
+    
     Attributes:
-        - Modelling: StructuralModelling 
+    ----------
+    Modelling : StructuralModelling
+    
     """
 
     Modelling: Optional[StructuralModelling] = Field(alias="Modelling", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/flexible_joint.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/flexible_joint.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class FlexibleJoint(Dnv):
     """FlexibleJoint - The common properties of a linear spring damper.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/flow_obstruction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/flow_obstruction.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,40 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.tower_shadow_potential_flow import TowerShadowPotentialFlow
 
 from dnv_bladed_models.tower_shadow_powles import TowerShadowPowles
 
 
 class FlowObstruction(Dnv):
     """FlowObstruction - The properties of the tower shadow, which modifies the flow in the region around the tower.  This has a direct affect on the flow as the blade sees it.
-
+    
     Attributes:
-        - PotentialFlow: TowerShadowPotentialFlow 
-        - Powles: TowerShadowPowles 
+    ----------
+    PotentialFlow : TowerShadowPotentialFlow
+    Powles : TowerShadowPowles
+    
     """
 
     PotentialFlow: Optional[TowerShadowPotentialFlow] = Field(alias="PotentialFlow", default=None)
     Powles: Optional[TowerShadowPowles] = Field(alias="Powles", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/foundation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/foundation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 
 class Foundation(Component):
     """Foundation - FOUNDATION
-
+    
     Attributes:
-        - UseFiniteElementDeflectionsForFoundationLoads: bool => When this feature is enabled, support structure deflections calculated from the underlying finite element model are used to evaluate the foundation applied loads. The effect of these foundation loads is included when evaluating the turbine dynamic response.  This model is only active for time domain simulations. The foundation applied loads are calculated from the finite element deflections from the previous time step. This means that time step convergence studies may be required to ensure the accuracy of this model.  When this feature is enabled, the support structure node deflection outputs will be based on the finite element model rather than the modal deflections that are normally used. This ensures consistency between the foundation applied loads and the support structure node deflections.
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
+    ----------
+    UseFiniteElementDeflectionsForFoundationLoads : bool, default=False
+        When this feature is enabled, support structure deflections calculated from the underlying finite element model are used to evaluate the foundation applied loads. The effect of these foundation loads is included when evaluating the turbine dynamic response.  This model is only active for time domain simulations. The foundation applied loads are calculated from the finite element deflections from the previous time step. This means that time step convergence studies may be required to ensure the accuracy of this model.  When this feature is enabled, the support structure node deflection outputs will be based on the finite element model rather than the modal deflections that are normally used. This ensures consistency between the foundation applied loads and the support structure node deflections.
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    
     """
 
     UseFiniteElementDeflectionsForFoundationLoads: Optional[bool] = Field(alias="UseFiniteElementDeflectionsForFoundationLoads", default=False)
     ConnectableNodes: Optional[Dict[str, ConnectableNode]] = Field(alias="ConnectableNodes", default=dict())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/free_flow_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/free_flow_model.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dynamic_wake import DynamicWake
 
 
 class FreeFlowModel(DynamicWake, DynamicWakeType='FreeFlowModel'):
-    """FreeFlowModel - The free flow dynamic wake model.
-
+    """FreeFlowModel - The Free-flow dynamic wake model represents a case where there is no induction, and so no momentum calculation.  The lift, drag and moment polars are used directly.  This could be appropriate to use in e.g. parked or idling.  This option might be removed in a future re-design of the aerodynamic settings.
+    
     Attributes:
-        - DynamicWakeType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;FreeFlowModel&#39;
+    ----------
+    DynamicWakeType : str, readonly, default='FreeFlowModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'FreeFlowModel'
+    
     """
 
     DynamicWakeType: Optional[str] = Field(alias="DynamicWakeType", default='FreeFlowModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/frequency_vs_spectral_density.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/frequency_vs_spectral_density.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class FrequencyVsSpectralDensity(Dnv):
     """FrequencyVsSpectralDensity - A line or row in a look-up table, specifying a value of SpectralDensity for a specified Frequency.
-
+    
     Attributes:
-        - Frequency: float => A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
-        - SpectralDensity: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Frequency : float
+        A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
+    SpectralDensity : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Frequency: Optional[float] = Field(alias="Frequency", default=None)
     SpectralDensity: Optional[float] = Field(alias="SpectralDensity", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/friction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/friction.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,40 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.pitch_bearing_kinetic_friction import PitchBearingKineticFriction
 
 from dnv_bladed_models.pitch_bearing_stiction import PitchBearingStiction
 
 
 class Friction(Dnv):
     """Friction - The friction characteristics of the pitch system&#39;s bearing.
-
+    
     Attributes:
-        - BearingFriction: PitchBearingKineticFriction 
-        - BearingStiction: PitchBearingStiction 
+    ----------
+    BearingFriction : PitchBearingKineticFriction
+    BearingStiction : PitchBearingStiction
+    
     """
 
     BearingFriction: Optional[PitchBearingKineticFriction] = Field(alias="BearingFriction", default=None)
     BearingStiction: Optional[PitchBearingStiction] = Field(alias="BearingStiction", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/friction_torque_vs_stiction_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/friction_torque_vs_stiction_torque.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class FrictionTorqueVsStictionTorque(Dnv):
     """FrictionTorqueVsStictionTorque - A line or row in a look-up table, specifying a value of StictionTorque for a specified FrictionTorque.
-
+    
     Attributes:
-        - FrictionTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - StictionTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    FrictionTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    StictionTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     FrictionTorque: Optional[float] = Field(alias="FrictionTorque", default=None)
     StictionTorque: Optional[float] = Field(alias="StictionTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gaussian.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/gaussian.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.steady_wake_deficit import SteadyWakeDeficit
 
 
 class Gaussian(SteadyWakeDeficit, SteadyWakeDeficitType='Gaussian'):
     """Gaussian - A simple model for the reduced velocity of the wind behind another turbine.  This deficit will be applied across a certain region, and this region will not move around during the simulation.  The velocity deficit profile will be based on a Gaussian distribution, scaled by a maximum deficit and the radius of the region affected.
-
+    
     Attributes:
-        - CentrelineVelocityDeficit: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - WidthOfDeficit: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - SteadyWakeDeficitType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Gaussian&#39;
+    ----------
+    CentrelineVelocityDeficit : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    WidthOfDeficit : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    SteadyWakeDeficitType : str, readonly, default='Gaussian'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Gaussian'
+    
     """
 
     CentrelineVelocityDeficit: Optional[float] = Field(alias="CentrelineVelocityDeficit", default=None)
     WidthOfDeficit: Optional[float] = Field(alias="WidthOfDeficit", default=None)
     SteadyWakeDeficitType: Optional[str] = Field(alias="SteadyWakeDeficitType", default='Gaussian', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gearbox.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/gearbox.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Gearbox(Dnv):
     """Gearbox - The properties of the yaw system&#39;s gearbox.
-
+    
     Attributes:
-        - GearboxRatio: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
-        - Friction: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Inertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - LockGearbox: bool => If true, the gearbox itself will be locked and be unable to rotate.
+    ----------
+    GearboxRatio : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    Friction : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Inertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    LockGearbox : bool
+        If true, the gearbox itself will be locked and be unable to rotate.
+    
     """
 
     GearboxRatio: Optional[float] = Field(alias="GearboxRatio", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
     Friction: Optional[float] = Field(alias="Friction", default=None)
     Inertia: Optional[float] = Field(alias="Inertia", default=None)
     LockGearbox: Optional[bool] = Field(alias="LockGearbox", default=None)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gearbox_mounting_rolling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/gearbox_mounting_rolling.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class GearboxMountingRolling(Dnv):
     """GearboxMountingRolling - The flexibility of the rolling degree of freedom of the whole mounting.
-
+    
     Attributes:
-        - Stiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
-        - MomentOfInertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    ----------
+    Stiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    MomentOfInertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    
     """
 
     Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
     MomentOfInertia: Optional[float] = Field(alias="MomentOfInertia", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generalised_alpha_fixed_step.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/generalised_alpha_fixed_step.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fixed_step_integrator import FixedStepIntegrator
 
 
 class GeneralisedAlphaFixedStep(FixedStepIntegrator, IntegratorType='GeneralisedAlphaFixedStep'):
     """GeneralisedAlphaFixedStep - Settings for the Generalised Alpha Fixed Step integrator.
-
+    
     Attributes:
-        - MaximumNumberOfIterations: int => The maximum number of iterations for prescribed freedoms and first order states (e.g. dynamic stall &amp; wake).  A value of 1 may sometimes inprecisely integrate first order states
-        - SpectralRadiusAtInfiniteFrequency: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - IntegratorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;GeneralisedAlphaFixedStep&#39;
+    ----------
+    MaximumNumberOfIterations : int, default=1
+        The maximum number of iterations for prescribed freedoms and first order states (e.g. dynamic stall & wake).  A value of 1 may sometimes inprecisely integrate first order states
+    SpectralRadiusAtInfiniteFrequency : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    IntegratorType : str, readonly, default='GeneralisedAlphaFixedStep'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'GeneralisedAlphaFixedStep'
+    
     """
 
     MaximumNumberOfIterations: Optional[int] = Field(alias="MaximumNumberOfIterations", default=1)
     SpectralRadiusAtInfiniteFrequency: Optional[float] = Field(alias="SpectralRadiusAtInfiniteFrequency", default=None)
     IntegratorType: Optional[str] = Field(alias="IntegratorType", default='GeneralisedAlphaFixedStep', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,42 +9,53 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.electrical_losses import ElectricalLosses
 
 from dnv_bladed_models.generator_output_group import GeneratorOutputGroup
 
 
 class Generator(Component):
     """Generator - The common properties for all types of generator.
-
+    
     Attributes:
-        - GeneratorInertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - Losses: ElectricalLosses 
-        - OutputGroups: Dict[str, GeneratorOutputGroup] => A library which contains any number of named output groups.  These can be referenced from &#39;SelectedOutputGroup&#39;
+    ----------
+    GeneratorInertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    Losses : ElectricalLosses, abstract
+    OutputGroups : Dict[str, GeneratorOutputGroup], default=dict()
+        A library which contains any number of named output groups.  These can be referenced from 'SelectedOutputGroup'
+    
+    Notes:
+    ---------
+    Losses has the following concrete types:
+        - LinearElectricalLosses
+        - NonLinearElectricalLosses
+    
     """
 
     GeneratorInertia: Optional[float] = Field(alias="GeneratorInertia", default=None)
     Losses: Optional[ElectricalLosses] = Field(alias="Losses", default=None)
     OutputGroups: Optional[Dict[str, GeneratorOutputGroup]] = Field(alias="OutputGroups", default=dict())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_output_group.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class GeneratorOutputGroup(Dnv):
     """GeneratorOutputGroup - An available output group for generators.  This can be referenced in the time domain simulations SelectedComponentOutputGroups.
-
+    
     Attributes:
-        - OutputGeneratorInformation: bool => If true, the output group for generators will be produced.
+    ----------
+    OutputGeneratorInformation : bool, default=False
+        If true, the output group for generators will be produced.
+    
     """
 
     OutputGeneratorInformation: Optional[bool] = Field(alias="OutputGeneratorInformation", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_overpower.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_overpower.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class GeneratorOverpower(Dnv):
     """GeneratorOverpower - The definition of the safety system behaviour when the generator is producing too much power.
-
+    
     Attributes:
-        - TripValue: float => A number representing a power.  The SI units for enery are Watts.
-        - Delay: float => A number representing a time.  The SI units for time are seconds.
-        - CircuitToActivate: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    TripValue : float
+        A number representing a power.  The SI units for enery are Watts.
+    Delay : float
+        A number representing a time.  The SI units for time are seconds.
+    CircuitToActivate : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     TripValue: Optional[float] = Field(alias="TripValue", default=None)
     Delay: Optional[float] = Field(alias="Delay", default=None)
     CircuitToActivate: Optional[str] = Field(alias="CircuitToActivate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_overspeed.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_overspeed.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class GeneratorOverspeed(Dnv):
     """GeneratorOverspeed - The definition of the safety system behaviour when the generator is running too fast
-
+    
     Attributes:
-        - TripValue: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Delay: float => A number representing a time.  The SI units for time are seconds.
-        - CircuitToActivate: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    TripValue : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Delay : float
+        A number representing a time.  The SI units for time are seconds.
+    CircuitToActivate : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     TripValue: Optional[float] = Field(alias="TripValue", default=None)
     Delay: Optional[float] = Field(alias="Delay", default=None)
     CircuitToActivate: Optional[str] = Field(alias="CircuitToActivate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/generator_short_circuit.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/generator_short_circuit.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class GeneratorShortCircuit(Dnv):
     """GeneratorShortCircuit - The definition of the safety system behaviour when there is a short circuit.
-
+    
     Attributes:
-        - Delay: float => A number representing a time.  The SI units for time are seconds.
-        - CircuitToActivate: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    Delay : float
+        A number representing a time.  The SI units for time are seconds.
+    CircuitToActivate : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     Delay: Optional[float] = Field(alias="Delay", default=None)
     CircuitToActivate: Optional[str] = Field(alias="CircuitToActivate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/gl2010_icing.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/gl2010_icing.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.iced_condition import IcedCondition
 
 
 class GL2010Icing(IcedCondition, InitialConditionType='GL2010Icing'):
     """GL2010Icing - The initial condition of a blade being iced according to the GL2010 standards.  This will remain unchanged throughout the simulation.
-
+    
     Attributes:
-        - ReferenceTipChord: float => A number representing a length.  The SI units for length are metres.
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;GL2010Icing&#39;
+    ----------
+    ReferenceTipChord : float
+        A number representing a length.  The SI units for length are metres.
+    InitialConditionType : str, readonly, default='GL2010Icing'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'GL2010Icing'
+    
     """
 
     ReferenceTipChord: Optional[float] = Field(alias="ReferenceTipChord", default=None)
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='GL2010Icing', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/glauert_skew_wake_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/glauert_skew_wake_model.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.skew_wake_model import SkewWakeModel
 
 
 class GlauertSkewWakeModel(SkewWakeModel):
     """GlauertSkewWakeModel - Settings for the Glauert skew wake model.
-
+    
     Attributes:
-        - VelocityCorrectionFactor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TimeCorrectionFactor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    VelocityCorrectionFactor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TimeCorrectionFactor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     VelocityCorrectionFactor: Optional[float] = Field(alias="VelocityCorrectionFactor", default=None)
     TimeCorrectionFactor: Optional[float] = Field(alias="TimeCorrectionFactor", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/grid_loss.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/grid_loss.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fault import Fault
 
 
 class GridLoss(Fault, EventType='GridLoss'):
     """GridLoss - A fault in the grid where all connectivity is lost at a specified time.
-
+    
     Attributes:
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;GridLoss&#39;
+    ----------
+    EventType : str, readonly, default='GridLoss'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'GridLoss'
+    
     """
 
     EventType: Optional[str] = Field(alias="EventType", default='GridLoss', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/grid_loss_energy_sink.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/grid_loss_energy_sink.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class GridLossEnergySink(Dnv):
     """GridLossEnergySink - An energy sink used to absorb the power from the generator in the case of a grid loss where generator braking is used.
-
+    
     Attributes:
-        - MaximumGeneratorTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - EnergyCapacityOfSink: float => A number representing an energy.  The SI units for enery are Joules.
+    ----------
+    MaximumGeneratorTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    EnergyCapacityOfSink : float
+        A number representing an energy.  The SI units for enery are Joules.
+    
     """
 
     MaximumGeneratorTorque: Optional[float] = Field(alias="MaximumGeneratorTorque", default=None)
     EnergyCapacityOfSink: Optional[float] = Field(alias="EnergyCapacityOfSink", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hardware_tracking.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/hardware_tracking.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HardwareTracking(Dnv):
     """HardwareTracking - Options for tracking external hardware using BHTM (Bladed Hardware Test Module) or similar external package.
-
+    
     Attributes:
-        - TrackPitchActuator: bool => If true, Bladed will accept data about the pitch system from an external source, and simulate the rest of the turbine in Bladed.
-        - TrackGenerator: bool => If true, Bladed will accept data about the generator from an external source, and simulate the rest of the turbine in Bladed.
+    ----------
+    TrackPitchActuator : bool, default=False
+        If true, Bladed will accept data about the pitch system from an external source, and simulate the rest of the turbine in Bladed.
+    TrackGenerator : bool
+        If true, Bladed will accept data about the generator from an external source, and simulate the rest of the turbine in Bladed.
+    
     """
 
     TrackPitchActuator: Optional[bool] = Field(alias="TrackPitchActuator", default=False)
     TrackGenerator: Optional[bool] = Field(alias="TrackGenerator", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/height_up_tower_vs_cd_cm.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/height_up_tower_vs_cd_cm.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HeightUpTowerVsCdCm(Dnv):
     """HeightUpTowerVsCdCm - A look-up table for the hydrodynamic properties of the tower at different heights.
-
+    
     Attributes:
-        - HeightUpTower: float => A number representing a length.  The SI units for length are metres.
-        - Cd: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Cm: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    HeightUpTower : float
+        A number representing a length.  The SI units for length are metres.
+    Cd : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Cm : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     HeightUpTower: Optional[float] = Field(alias="HeightUpTower", default=None)
     Cd: Optional[float] = Field(alias="Cd", default=None)
     Cm: Optional[float] = Field(alias="Cm", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/height_vs_shear_factor.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/height_vs_shear_factor.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HeightVsShearFactor(Dnv):
     """HeightVsShearFactor - A single height vs shear factor point.
-
+    
     Attributes:
-        - Height: float => The height above the ground.  This should vary from 0.0 up to the reference height, above which normal flow resumes.
-        - ShearFactor: float => The shear factor, varying from 0.0 (no wind) up to 1.0 (full flow).
+    ----------
+    Height : float
+        The height above the ground.  This should vary from 0.0 up to the reference height, above which normal flow resumes.
+    ShearFactor : float
+        The shear factor, varying from 0.0 (no wind) up to 1.0 (full flow).
+    
     """
 
     Height: Optional[float] = Field(alias="Height", default=None)
     ShearFactor: Optional[float] = Field(alias="ShearFactor", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/high_speed_shaft.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/high_speed_shaft.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HighSpeedShaft(Dnv):
     """HighSpeedShaft - The properties of the yaw system&#39;s high speed shaft.
-
+    
     Attributes:
-        - Stiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    ----------
+    Stiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    
     """
 
     Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/high_speed_shaft_flexibility.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/high_speed_shaft_flexibility.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HighSpeedShaftFlexibility(Dnv):
     """HighSpeedShaftFlexibility - The parameters defining the high-speed shaft torsional flexibility.
-
+    
     Attributes:
-        - TorsionalStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - TorsionalDamping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    ----------
+    TorsionalStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    TorsionalDamping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    
     """
 
     TorsionalStiffness: Optional[float] = Field(alias="TorsionalStiffness", default=None)
     TorsionalDamping: Optional[float] = Field(alias="TorsionalDamping", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hub.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/hub.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_mounting_on_rotor import BladeMountingOnRotor
 
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 from dnv_bladed_models.extension_pieces import ExtensionPieces
@@ -36,25 +37,31 @@
 
 class Hub_UpwindOrDownwindEnum(str, Enum):
     UPWIND = "UPWIND"
     DOWNWIND = "DOWNWIND"
 
 class Hub(Component):
     """Hub - The common properties shared by all hubs.
-
+    
     Attributes:
-        - RotationDirection: Hub_RotationDirectionEnum => The rotational sense of rotor when viewed from upwind.
-        - UpwindOrDownwind: Hub_UpwindOrDownwindEnum => Whether the turbine is an upwind turbine (where the rotor is upwind of the nacelle) or a downwind turbine (where the rotor is downwind of the nacelle).
-        - BladeMounting: BladeMountingOnRotor 
-        - LowSpeedShaft: LowSpeedShaft 
-        - ExtensionPieces: ExtensionPieces 
-        - MassProperties: HubMassProperties 
-        - Spinner: Spinner 
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
-        - OutputGroups: Dict[str, HubOutputGroup] => The hub loads to be output in the simulations.
+    ----------
+    RotationDirection : Hub_RotationDirectionEnum
+        The rotational sense of rotor when viewed from upwind.
+    UpwindOrDownwind : Hub_UpwindOrDownwindEnum
+        Whether the turbine is an upwind turbine (where the rotor is upwind of the nacelle) or a downwind turbine (where the rotor is downwind of the nacelle).
+    BladeMounting : BladeMountingOnRotor
+    LowSpeedShaft : LowSpeedShaft
+    ExtensionPieces : ExtensionPieces
+    MassProperties : HubMassProperties
+    Spinner : Spinner
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    OutputGroups : Dict[str, HubOutputGroup], default=dict()
+        The hub loads to be output in the simulations.
+    
     """
 
     RotationDirection: Optional[Hub_RotationDirectionEnum] = Field(alias="RotationDirection", default=None)
     UpwindOrDownwind: Optional[Hub_UpwindOrDownwindEnum] = Field(alias="UpwindOrDownwind", default=None)
     BladeMounting: Optional[BladeMountingOnRotor] = Field(alias="BladeMounting", default=None)
     LowSpeedShaft: Optional[LowSpeedShaft] = Field(alias="LowSpeedShaft", default=None)
     ExtensionPieces: Optional[ExtensionPieces] = Field(alias="ExtensionPieces", default=None)
@@ -66,15 +73,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hub_mass_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/hub_mass_properties.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HubMassProperties(Dnv):
     """HubMassProperties - The mass properties of the hub.
-
+    
     Attributes:
-        - Mass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - AxialPositionOfCentreOfGravity: float => A number representing a length.  The SI units for length are metres.
-        - MomentOfInertiaAboutShaft: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - MomentOfInertiaPerpendicularToShaft: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    ----------
+    Mass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    AxialPositionOfCentreOfGravity : float
+        A number representing a length.  The SI units for length are metres.
+    MomentOfInertiaAboutShaft : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    MomentOfInertiaPerpendicularToShaft : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    
     """
 
     Mass: Optional[float] = Field(alias="Mass", default=None)
     AxialPositionOfCentreOfGravity: Optional[float] = Field(alias="AxialPositionOfCentreOfGravity", default=None)
     MomentOfInertiaAboutShaft: Optional[float] = Field(alias="MomentOfInertiaAboutShaft", default=None)
     MomentOfInertiaPerpendicularToShaft: Optional[float] = Field(alias="MomentOfInertiaPerpendicularToShaft", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hub_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/hub_output_group.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HubOutputGroup(Dnv):
     """HubOutputGroup - The hub loads to be output in the simulations.
-
+    
     Attributes:
-        - RotatingFrameOutputs: bool => If True, hub loads in the rotating frame will be output.
-        - FixedFrameOutputs: bool => If True, hub loads in the stationary frame will be output.
+    ----------
+    RotatingFrameOutputs : bool, default=False
+        If True, hub loads in the rotating frame will be output.
+    FixedFrameOutputs : bool, default=False
+        If True, hub loads in the stationary frame will be output.
+    
     """
 
     RotatingFrameOutputs: Optional[bool] = Field(alias="RotatingFrameOutputs", default=False)
     FixedFrameOutputs: Optional[bool] = Field(alias="FixedFrameOutputs", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hydraulic_accumulator_yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/hydraulic_accumulator_yaw_system.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.active_yaw_system import ActiveYawSystem
 
 
 class HydraulicAccumulatorYawSystem(ActiveYawSystem, ComponentType='HydraulicAccumulatorYawSystem'):
     """HydraulicAccumulatorYawSystem - A yaw system with a modelled hydraulic actuator system.
-
+    
     Attributes:
-        - GasVolume: float => A number representing a volume.  The SI units for volume are cubic metres.
-        - NominalPressure: float => A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
-        - PumpFlowPerUnitYaw: float => A number representing the flow rate of a gas or liquid per unit of angle passed.  The SI units for flow rate per unit angle are metre cubed per second per radian.
-        - YawTorquePerUnitPressure: float => A number representing a length.  The SI units for length are metres.
-        - GasLawConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;HydraulicAccumulatorYawSystem&#39;
+    ----------
+    GasVolume : float
+        A number representing a volume.  The SI units for volume are cubic metres.
+    NominalPressure : float
+        A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    PumpFlowPerUnitYaw : float
+        A number representing the flow rate of a gas or liquid per unit of angle passed.  The SI units for flow rate per unit angle are metre cubed per second per radian.
+    YawTorquePerUnitPressure : float
+        A number representing a length.  The SI units for length are metres.
+    GasLawConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    ComponentType : str, readonly, default='HydraulicAccumulatorYawSystem'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'HydraulicAccumulatorYawSystem'
+    
     """
 
     GasVolume: Optional[float] = Field(alias="GasVolume", default=None)
     NominalPressure: Optional[float] = Field(alias="NominalPressure", default=None)
     PumpFlowPerUnitYaw: Optional[float] = Field(alias="PumpFlowPerUnitYaw", default=None)
     YawTorquePerUnitPressure: Optional[float] = Field(alias="YawTorquePerUnitPressure", default=None)
     GasLawConstant: Optional[float] = Field(alias="GasLawConstant", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/hydrodynamic_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/hydrodynamic_calculation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,41 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class HydrodynamicCalculation_HydrostaticsMorisonMethodEnum(str, Enum):
     VARIABLE_INTERFACE = "VARIABLE_INTERFACE"
     EQUALLY_SPACED_INTERFACE = "EQUALLY_SPACED_INTERFACE"
     NOT_USED = "NOT_USED"
 
 class HydrodynamicCalculation(Dnv):
     """HydrodynamicCalculation - Settings for the hydrodynamic modelling within Bladed.  Note that many hydrodynamic modelling options are specific to the components.  These options can be found within the componentss definitions themselves.
-
+    
     Attributes:
-        - MinimumExcitationForceTimeStep: float => A number representing a time.  The SI units for time are seconds.
-        - HydrostaticsMorisonMethod: HydrodynamicCalculation_HydrostaticsMorisonMethodEnum => The method to use for the hydrostatics Morison calculations.
+    ----------
+    MinimumExcitationForceTimeStep : float
+        A number representing a time.  The SI units for time are seconds.
+    HydrostaticsMorisonMethod : HydrodynamicCalculation_HydrostaticsMorisonMethodEnum, default='NOT_USED'
+        The method to use for the hydrostatics Morison calculations.
+    
     """
 
     MinimumExcitationForceTimeStep: Optional[float] = Field(alias="MinimumExcitationForceTimeStep", default=None)
     HydrostaticsMorisonMethod: Optional[HydrodynamicCalculation_HydrostaticsMorisonMethodEnum] = Field(alias="HydrostaticsMorisonMethod", default='NOT_USED')
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/iced_condition.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/iced_condition.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_condition import InitialCondition
 
 
 class IcedCondition(InitialCondition):
     """IcedCondition - The initial condition of ice being on a specific blade.  This will remain unchanged throughout the simulation.
-
+    
     Attributes:
-        - IceDensity: float => A number representing a material&#39;s density.  The SI units for volume are kilograms per cubic metre.
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
+    ----------
+    IceDensity : float
+        A number representing a material's density.  The SI units for volume are kilograms per cubic metre.
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    
     """
 
     IceDensity: Optional[float] = Field(alias="IceDensity", default=None)
     OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/idealised_pitch_actuator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/idealised_pitch_actuator.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_actuator import PitchActuator
 
 
 class IdealisedPitchActuator(PitchActuator, ActuatorDriveType='IdealisedActuator'):
     """IdealisedPitchActuator - An idealised pitch actuation system which achieves the specified positions without considering any of the physical realities of the system.
-
+    
     Attributes:
-        - ActuatorDriveType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;IdealisedActuator&#39;
+    ----------
+    ActuatorDriveType : str, readonly, default='IdealisedActuator'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'IdealisedActuator'
+    
     """
 
     ActuatorDriveType: Optional[str] = Field(alias="ActuatorDriveType", default='IdealisedActuator', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/idealised_yaw_manoever.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/idealised_yaw_manoever.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class IdealisedYawManoever(Dnv):
     """IdealisedYawManoever - If provided, the controller will be ignored for this operation, and the yaw manoever will be performed at a constant rate.
-
+    
     Attributes:
-        - Rate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    ----------
+    Rate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    
     """
 
     Rate: Optional[float] = Field(alias="Rate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/iec4_icing.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/iec4_icing.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.iced_condition import IcedCondition
 
 
 class IEC4Icing(IcedCondition, InitialConditionType='IEC4Icing'):
     """IEC4Icing - The initial condition of a blade being iced according to the IEC4 standards.  This will remain unchanged throughout the simulation.
-
+    
     Attributes:
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;IEC4Icing&#39;
+    ----------
+    InitialConditionType : str, readonly, default='IEC4Icing'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'IEC4Icing'
+    
     """
 
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='IEC4Icing', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/implicit_newmark_beta_fixed_step.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/implicit_newmark_beta_fixed_step.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fixed_step_integrator import FixedStepIntegrator
 
 
 class ImplicitNewmarkBetaFixedStep(FixedStepIntegrator, IntegratorType='ImplicitNewmarkBetaFixedStep'):
     """ImplicitNewmarkBetaFixedStep - Settings for the Implicit Newmark Beta Fixed Step integrator.
-
+    
     Attributes:
-        - MaximumNumberOfIterations: int => The maximum number of iterations for prescribed freedoms and first order states (e.g. dynamic stall &amp; wake).  A value of 1 may sometimes inprecisely integrate first order states
-        - Beta: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Gamma: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - ToleranceMultiplier: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - IntegratorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ImplicitNewmarkBetaFixedStep&#39;
+    ----------
+    MaximumNumberOfIterations : int, default=1
+        The maximum number of iterations for prescribed freedoms and first order states (e.g. dynamic stall & wake).  A value of 1 may sometimes inprecisely integrate first order states
+    Beta : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Gamma : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    ToleranceMultiplier : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    IntegratorType : str, readonly, default='ImplicitNewmarkBetaFixedStep'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ImplicitNewmarkBetaFixedStep'
+    
     """
 
     MaximumNumberOfIterations: Optional[int] = Field(alias="MaximumNumberOfIterations", default=1)
     Beta: Optional[float] = Field(alias="Beta", default=None)
     Gamma: Optional[float] = Field(alias="Gamma", default=None)
     ToleranceMultiplier: Optional[float] = Field(alias="ToleranceMultiplier", default=None)
     IntegratorType: Optional[str] = Field(alias="IntegratorType", default='ImplicitNewmarkBetaFixedStep', allow_mutation=False)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/incompressible_beddoes_leishman_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/incompressible_beddoes_leishman_model.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.beddoes_leishman_model import BeddoesLeishmanModel
 
 
 class IncompressibleBeddoesLeishmanModel(BeddoesLeishmanModel, DynamicStallType='IncompressibleBeddoesLeishmanModel'):
     """IncompressibleBeddoesLeishmanModel - The incompressible Beddoes Leishman dynamic stall model.
-
+    
     Attributes:
-        - DynamicStallType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;IncompressibleBeddoesLeishmanModel&#39;
+    ----------
+    DynamicStallType : str, readonly, default='IncompressibleBeddoesLeishmanModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'IncompressibleBeddoesLeishmanModel'
+    
     """
 
     DynamicStallType: Optional[str] = Field(alias="DynamicStallType", default='IncompressibleBeddoesLeishmanModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/independent_pitch_hub.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/independent_pitch_hub.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.standard_hub import StandardHub
 
 
 class IndependentPitchHub(StandardHub, ComponentType='IndependentPitchHub'):
     """IndependentPitchHub - A standard hub with two or more blades, and one pitch system for each blade.
-
+    
     Attributes:
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;IndependentPitchHub&#39;
+    ----------
+    ComponentType : str, readonly, default='IndependentPitchHub'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'IndependentPitchHub'
+    
     """
 
     ComponentType: Optional[str] = Field(alias="ComponentType", default='IndependentPitchHub', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_azimuth_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_azimuth_position.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_position import InitialPosition
 
 
 class InitialAzimuthPosition(InitialPosition, InitialConditionType='InitialAzimuthPosition'):
     """InitialAzimuthPosition - The initial position of a rotor.  If there is only one rotor, then AssemblyReference can be omitted.
-
+    
     Attributes:
-        - AngleOfFirstBladeToVertical: float => A number representing an angle.  The SI units for angles are radians.
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;InitialAzimuthPosition&#39;
+    ----------
+    AngleOfFirstBladeToVertical : float
+        A number representing an angle.  The SI units for angles are radians.
+    InitialConditionType : str, readonly, default='InitialAzimuthPosition'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'InitialAzimuthPosition'
+    
     """
 
     AngleOfFirstBladeToVertical: Optional[float] = Field(alias="AngleOfFirstBladeToVertical", default=None)
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='InitialAzimuthPosition', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_condition.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_condition.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class InitialCondition_InitialConditionTypeEnum(str, Enum):
     INITIAL_AZIMUTH_POSITION = "InitialAzimuthPosition"
     INITIAL_MACHINE_POSITION = "InitialMachinePosition"
     MOORING_INITIAL_POSITION = "MooringInitialPosition"
@@ -24,27 +25,30 @@
     GL2010_ICING = "GL2010Icing"
     ROTOR_IN_POWER_PRODUCTION = "RotorInPowerProduction"
     ROTOR_IDLING = "RotorIdling"
     ROTOR_PARKED = "RotorParked"
 
 class InitialCondition(Dnv):
     """InitialCondition - A condition of the turbine or a component at the beginning of the simulation.  This may change during the simulation.
-
+    
     Attributes:
-        - InitialConditionType: InitialCondition_InitialConditionTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    InitialConditionType : InitialCondition_InitialConditionTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - GL2010Icing
-        - IEC4Icing
-        - InitialAzimuthPosition
-        - InitialMachinePosition
-        - MooringInitialPosition
-        - RotorIdlingControlState
-        - RotorInPowerProduction
-        - RotorParkedControlState
+        GL2010Icing
+        IEC4Icing
+        InitialAzimuthPosition
+        InitialMachinePosition
+        MooringInitialPosition
+        RotorIdlingControlState
+        RotorInPowerProduction
+        RotorParkedControlState
+    
     """
 
     InitialConditionType: Optional[InitialCondition_InitialConditionTypeEnum] = Field(alias="InitialConditionType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -74,15 +78,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'InitialCondition'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_control_state.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_position.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.initial_condition import InitialCondition
 
+from dnv_bladed_models.initial_condition import InitialCondition
 
-class InitialControlState(InitialCondition):
-    """InitialControlState - The initial operational state of the turbine.
 
+class InitialPosition(InitialCondition):
+    """InitialPosition - The initial condition of a component starting in a given position.
+    
     Attributes:
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
+    ----------
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    
     """
 
     OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +112,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        InitialControlState
+        InitialPosition
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = InitialControlState.from_file('/path/to/file')
+        >>> model = InitialPosition.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +142,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        InitialControlState
+        InitialPosition
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = InitialControlState.from_json('{ ... }')
+        >>> model = InitialPosition.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +174,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        InitialControlState
+        InitialPosition
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +206,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-InitialControlState.update_forward_refs()
+InitialPosition.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_inactive_control_state.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_inactive_control_state.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_control_state import InitialControlState
 
 
 class InitialInactiveControlState(InitialControlState):
     """InitialInactiveControlState - Base class for all initial control states.
-
+    
     Attributes:
-        - IsControllerActive: bool => If true, the controller is active and can change the state of the turbine.
-        - PitchAngle: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    IsControllerActive : bool
+        If true, the controller is active and can change the state of the turbine.
+    PitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     IsControllerActive: Optional[bool] = Field(alias="IsControllerActive", default=None)
     PitchAngle: Optional[float] = Field(alias="PitchAngle", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_machine_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/initial_machine_position.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_position import InitialPosition
 
 
 class InitialMachinePosition(InitialPosition, InitialConditionType='InitialMachinePosition'):
     """InitialMachinePosition - The initial position of the yaw system.
-
+    
     Attributes:
-        - YawAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;InitialMachinePosition&#39;
+    ----------
+    YawAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    InitialConditionType : str, readonly, default='InitialMachinePosition'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'InitialMachinePosition'
+    
     """
 
     YawAngle: Optional[float] = Field(alias="YawAngle", default=None)
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='InitialMachinePosition', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/initial_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/spring_force_or_torque_vs_position.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,53 +9,54 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.initial_condition import InitialCondition
 
+from dnv_bladed_models.dnv import Dnv
 
-class InitialPosition(InitialCondition):
-    """InitialPosition - The initial condition of a component starting in a given position.
 
+class SpringForceOrTorqueVsPosition(Dnv):
+    """SpringForceOrTorqueVsPosition - A line or row in a look-up table, specifying a value of Position for a specified SpringForceOrTorque.
+    
     Attributes:
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
+    ----------
+    SpringForceOrTorque : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Position : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
-    OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
+    SpringForceOrTorque: Optional[float] = Field(alias="SpringForceOrTorque", default=None)
+    Position: Optional[float] = Field(alias="Position", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
                             allowed_child_vals[child_key] = child_val
                     allowed_vals[key] = allowed_child_vals
                 else:
                     allowed_vals[key] = val
         return allowed_vals
 
-    @validator("OnComponentInAssembly")
-    def OnComponentInAssembly_pattern(cls, value):
-        if value is not None and not re.match(r"^#(\/.+)+$", value):
-            raise ValueError(f"OnComponentInAssembly did not match the expected format (found {value})")
-        return value
-
     def to_json(
         self,
         *, 
         include: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         exclude: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         by_alias: bool = True, 
         skip_defaults: Optional[bool] = None, 
@@ -108,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        InitialPosition
+        SpringForceOrTorqueVsPosition
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = InitialPosition.from_file('/path/to/file')
+        >>> model = SpringForceOrTorqueVsPosition.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        InitialPosition
+        SpringForceOrTorqueVsPosition
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = InitialPosition.from_json('{ ... }')
+        >>> model = SpringForceOrTorqueVsPosition.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        InitialPosition
+        SpringForceOrTorqueVsPosition
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-InitialPosition.update_forward_refs()
+SpringForceOrTorqueVsPosition.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/input_power_vs_loss.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/input_power_vs_loss.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class InputPowerVsLoss(Dnv):
     """InputPowerVsLoss - A line or row in a look-up table, specifying a value of Loss for a specified InputPower.
-
+    
     Attributes:
-        - InputPower: float => A number representing a power.  The SI units for enery are Watts.
-        - Loss: float => A number representing a power.  The SI units for enery are Watts.
+    ----------
+    InputPower : float
+        A number representing a power.  The SI units for enery are Watts.
+    Loss : float
+        A number representing a power.  The SI units for enery are Watts.
+    
     """
 
     InputPower: Optional[float] = Field(alias="InputPower", default=None)
     Loss: Optional[float] = Field(alias="Loss", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/instantaneous.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/instantaneous.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class Instantaneous(ResponseToDemand, ResponseToDemandType='Instantaneous'):
     """Instantaneous - Defines an instantaneous response to the controller&#39;s demands - effectively ignoring any pitch controller dynamics.
-
+    
     Attributes:
-        - ResponseToDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Instantaneous&#39;
+    ----------
+    ResponseToDemandType : str, readonly, default='Instantaneous'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Instantaneous'
+    
     """
 
     ResponseToDemandType: Optional[str] = Field(alias="ResponseToDemandType", default='Instantaneous', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/integrator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/integrator.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,38 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Integrator_IntegratorTypeEnum(str, Enum):
     RUNGE_KUTTA_VARIABLE_STEP = "RungeKuttaVariableStep"
     GENERALISED_ALPHA_FIXED_STEP = "GeneralisedAlphaFixedStep"
     EXPLICIT_NEWMARK_BETA_FIXED_STEP = "ExplicitNewmarkBetaFixedStep"
     RUNGE_KUTTA4TH_ORDER_FIXED_STEP = "RungeKutta4thOrderFixedStep"
     MIDPOINT_METHOD_FIXED_STEP = "MidpointMethodFixedStep"
     IMPLICIT_NEWMARK_BETA_FIXED_STEP = "ImplicitNewmarkBetaFixedStep"
 
 class Integrator(Dnv):
     """Integrator - Settings for the integrator.
-
+    
     Attributes:
-        - IntegratorType: Integrator_IntegratorTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    IntegratorType : Integrator_IntegratorTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - ExplicitNewmarkBetaFixedStep
-        - GeneralisedAlphaFixedStep
-        - ImplicitNewmarkBetaFixedStep
-        - MidpointMethodFixedStep
-        - RungeKutta4thOrderFixedStep
-        - RungeKuttaVariableStep
+        ExplicitNewmarkBetaFixedStep
+        GeneralisedAlphaFixedStep
+        ImplicitNewmarkBetaFixedStep
+        MidpointMethodFixedStep
+        RungeKutta4thOrderFixedStep
+        RungeKuttaVariableStep
+    
     """
 
     IntegratorType: Optional[Integrator_IntegratorTypeEnum] = Field(alias="IntegratorType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -70,15 +74,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Integrator'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/interface_load_file.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/interface_load_file.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class InterfaceLoadFile(Dnv):
     """InterfaceLoadFile - The definition of a second copy of the interface load file.
-
+    
     Attributes:
-        - DirectoryForSecondCopy: str => The directory for the second copy of the interface load file.  If omitted, this will be create in the output directory.
+    ----------
+    DirectoryForSecondCopy : str
+        The directory for the second copy of the interface load file.  If omitted, this will be create in the output directory.
+    
     """
 
     DirectoryForSecondCopy: Optional[str] = Field(alias="DirectoryForSecondCopy", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/irregular_waves.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/irregular_waves.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,44 +9,57 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.additional_constrained_wave import AdditionalConstrainedWave
 
 from dnv_bladed_models.wave_diffraction_approximation import WaveDiffractionApproximation
 
 from dnv_bladed_models.waves import Waves
 
 
 class IrregularWaves(Waves):
     """IrregularWaves - The definition of irregular waves.
-
+    
     Attributes:
-        - DirectionOfApproachClockwiseFromNorth: float => A number representing an angle.  The SI units for angles are radians.
-        - RandomNumberSeed: int => A arbitrary integer used to generate a realisation of the irregular waves.  This ensures that the &#39;randomness&#39; is consistent from simulation to simulation.
-        - WaveDiffractionApproximation: WaveDiffractionApproximation 
-        - AdditionalConstrainedWave: AdditionalConstrainedWave 
+    ----------
+    DirectionOfApproachClockwiseFromNorth : float
+        A number representing an angle.  The SI units for angles are radians.
+    RandomNumberSeed : int
+        A arbitrary integer used to generate a realisation of the irregular waves.  This ensures that the 'randomness' is consistent from simulation to simulation.
+    WaveDiffractionApproximation : WaveDiffractionApproximation, abstract
+    AdditionalConstrainedWave : AdditionalConstrainedWave
+    
+    Notes:
+    ---------
+    WaveDiffractionApproximation has the following concrete types:
+        - AutomaticMacCamyFuchs
+        - AutomaticSimpleCutoffFrequency
+        - MacCamyFuchs
+        - SimpleCutoffFrequency
+    
     """
 
     DirectionOfApproachClockwiseFromNorth: Optional[float] = Field(alias="DirectionOfApproachClockwiseFromNorth", default=None)
     RandomNumberSeed: Optional[int] = Field(alias="RandomNumberSeed", default=None)
     WaveDiffractionApproximation: Optional[WaveDiffractionApproximation] = Field(alias="WaveDiffractionApproximation", default=None)
     AdditionalConstrainedWave: Optional[AdditionalConstrainedWave] = Field(alias="AdditionalConstrainedWave", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/jonswap_pierson_moskowitz.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/jonswap_pierson_moskowitz.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.irregular_waves import IrregularWaves
 
 
 class JonswapPiersonMoskowitz(IrregularWaves, WavesType='JonswapPiersonMoskowitz'):
     """JonswapPiersonMoskowitz - The definition of Jonswap/Pierson-Moskowitz spectrum waves.
-
+    
     Attributes:
-        - SignificantWaveHeight: float => A number representing a length.  The SI units for length are metres.
-        - SpectralPeakPeriod: float => A number representing a time.  The SI units for time are seconds.
-        - Peakedness: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - WavesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;JonswapPiersonMoskowitz&#39;
+    ----------
+    SignificantWaveHeight : float
+        A number representing a length.  The SI units for length are metres.
+    SpectralPeakPeriod : float
+        A number representing a time.  The SI units for time are seconds.
+    Peakedness : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    WavesType : str, readonly, default='JonswapPiersonMoskowitz'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'JonswapPiersonMoskowitz'
+    
     """
 
     SignificantWaveHeight: Optional[float] = Field(alias="SignificantWaveHeight", default=None)
     SpectralPeakPeriod: Optional[float] = Field(alias="SpectralPeakPeriod", default=None)
     Peakedness: Optional[float] = Field(alias="Peakedness", default=None)
     WavesType: Optional[str] = Field(alias="WavesType", default='JonswapPiersonMoskowitz', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/laminar_flow_current.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/laminar_flow_current.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,35 +9,55 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_direction_shear_variation import CurrentDirectionShearVariation
 
 from dnv_bladed_models.current_horizontal_shear_variation import CurrentHorizontalShearVariation
 
 from dnv_bladed_models.current_mean_speed_variation import CurrentMeanSpeedVariation
 
 from dnv_bladed_models.current_vertical_shear_variation import CurrentVerticalShearVariation
 
 from dnv_bladed_models.time_domain_current import TimeDomainCurrent
 
 
 class LaminarFlowCurrent(TimeDomainCurrent, CurrentType='LaminarFlow'):
     """LaminarFlowCurrent - The definition of a current that varies throughout a time domain simulation, but does not have turbulence.
-
+    
     Attributes:
-        - MeanSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - MeanSpeedVariation: CurrentMeanSpeedVariation 
-        - VerticalShearVariation: CurrentVerticalShearVariation 
-        - HorizontalShearVariation: CurrentHorizontalShearVariation 
-        - DirectionShearVariation: CurrentDirectionShearVariation 
-        - CurrentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LaminarFlow&#39;
+    ----------
+    MeanSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    MeanSpeedVariation : CurrentMeanSpeedVariation, abstract
+    VerticalShearVariation : CurrentVerticalShearVariation, abstract
+    HorizontalShearVariation : CurrentHorizontalShearVariation, abstract
+    DirectionShearVariation : CurrentDirectionShearVariation, abstract
+    CurrentType : str, readonly, default='LaminarFlow'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LaminarFlow'
+    
+    Notes:
+    ---------
+    MeanSpeedVariation has the following concrete types:
+        - PresetCurrentMeanSpeedTransient
+        - CurrentMeanSpeedTimeHistory
+    VerticalShearVariation has the following concrete types:
+        - PresetCurrentVerticalShearTransient
+        - CurrentVerticalShearTimeHistory
+    HorizontalShearVariation has the following concrete types:
+        - PresetCurrentHorizontalShearTransient
+        - CurrentHorizontalShearTimeHistory
+    DirectionShearVariation has the following concrete types:
+        - PresetCurrentDirectionShearTransient
+        - CurrentDirectionShearTimeHistory
+    
     """
 
     MeanSpeed: Optional[float] = Field(alias="MeanSpeed", default=None)
     MeanSpeedVariation: Optional[CurrentMeanSpeedVariation] = Field(alias="MeanSpeedVariation", default=None)
     VerticalShearVariation: Optional[CurrentVerticalShearVariation] = Field(alias="VerticalShearVariation", default=None)
     HorizontalShearVariation: Optional[CurrentHorizontalShearVariation] = Field(alias="HorizontalShearVariation", default=None)
     DirectionShearVariation: Optional[CurrentDirectionShearVariation] = Field(alias="DirectionShearVariation", default=None)
@@ -46,15 +66,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/laminar_flow_wind.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/laminar_flow_wind.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,35 +9,55 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.time_domain_wind import TimeDomainWind
 
 from dnv_bladed_models.wind_direction_shear_variation import WindDirectionShearVariation
 
 from dnv_bladed_models.wind_horizontal_shear_variation import WindHorizontalShearVariation
 
 from dnv_bladed_models.wind_mean_speed_variation import WindMeanSpeedVariation
 
 from dnv_bladed_models.wind_vertical_shear_variation import WindVerticalShearVariation
 
 
 class LaminarFlowWind(TimeDomainWind, WindType='LaminarFlow'):
     """LaminarFlowWind - The definition of a wind field that varies throughout a time domain simulation, but does not have turbulence.
-
+    
     Attributes:
-        - MeanSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - MeanSpeedVariation: WindMeanSpeedVariation 
-        - VerticalShearVariation: WindVerticalShearVariation 
-        - HorizontalShearVariation: WindHorizontalShearVariation 
-        - DirectionShearVariation: WindDirectionShearVariation 
-        - WindType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LaminarFlow&#39;
+    ----------
+    MeanSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    MeanSpeedVariation : WindMeanSpeedVariation, abstract
+    VerticalShearVariation : WindVerticalShearVariation, abstract
+    HorizontalShearVariation : WindHorizontalShearVariation, abstract
+    DirectionShearVariation : WindDirectionShearVariation, abstract
+    WindType : str, readonly, default='LaminarFlow'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LaminarFlow'
+    
+    Notes:
+    ---------
+    MeanSpeedVariation has the following concrete types:
+        - PresetWindMeanSpeedTransient
+        - WindMeanSpeedTimeHistory
+    VerticalShearVariation has the following concrete types:
+        - PresetWindVerticalShearTransient
+        - WindVerticalShearTimeHistory
+    HorizontalShearVariation has the following concrete types:
+        - PresetWindHorizontalShearTransient
+        - WindHorizontalShearTimeHistory
+    DirectionShearVariation has the following concrete types:
+        - PresetWindDirectionShearTransient
+        - WindDirectionShearTimeHistory
+    
     """
 
     MeanSpeed: Optional[float] = Field(alias="MeanSpeed", default=None)
     MeanSpeedVariation: Optional[WindMeanSpeedVariation] = Field(alias="MeanSpeedVariation", default=None)
     VerticalShearVariation: Optional[WindVerticalShearVariation] = Field(alias="VerticalShearVariation", default=None)
     HorizontalShearVariation: Optional[WindHorizontalShearVariation] = Field(alias="HorizontalShearVariation", default=None)
     DirectionShearVariation: Optional[WindDirectionShearVariation] = Field(alias="DirectionShearVariation", default=None)
@@ -46,15 +66,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_blade_sensor_mapping.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_blade_sensor_mapping.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.legacy_sensor_mapping import LegacySensorMapping
 
 
 class LegacyBladeSensorMapping(LegacySensorMapping, LegacySensorMappingType='LegacyBladeSensorMapping'):
     """LegacyBladeSensorMapping - The controller interface used by 4.X compatible controllers need to have a mapping between how they are referenced now, and how they used to be referenced.
-
+    
     Attributes:
-        - RotorIndex: int => The index of the rotor the sensor is on.
-        - BladeIndex: int => The index of the blade the sensor is on.
-        - LegacySensorMappingType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LegacyBladeSensorMapping&#39;
+    ----------
+    RotorIndex : int
+        The index of the rotor the sensor is on.
+    BladeIndex : int
+        The index of the blade the sensor is on.
+    LegacySensorMappingType : str, readonly, default='LegacyBladeSensorMapping'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LegacyBladeSensorMapping'
+    
     """
 
     RotorIndex: Optional[int] = Field(alias="RotorIndex", default=None)
     BladeIndex: Optional[int] = Field(alias="BladeIndex", default=None)
     LegacySensorMappingType: Optional[str] = Field(alias="LegacySensorMappingType", default='LegacyBladeSensorMapping', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_nacelle_sensor_mapping.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_nacelle_sensor_mapping.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.legacy_sensor_mapping import LegacySensorMapping
 
 
 class LegacyNacelleSensorMapping(LegacySensorMapping, LegacySensorMappingType='LegacyNacelleSensorMapping'):
     """LegacyNacelleSensorMapping - The controller interface used by 4.X compatible controllers need to have a mapping between how they are referenced now, and how they used to be referenced.
-
+    
     Attributes:
-        - NacelleIndex: int => The index of the nacelle the sensor is on.
-        - LegacySensorMappingType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LegacyNacelleSensorMapping&#39;
+    ----------
+    NacelleIndex : int
+        The index of the nacelle the sensor is on.
+    LegacySensorMappingType : str, readonly, default='LegacyNacelleSensorMapping'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LegacyNacelleSensorMapping'
+    
     """
 
     NacelleIndex: Optional[int] = Field(alias="NacelleIndex", default=None)
     LegacySensorMappingType: Optional[str] = Field(alias="LegacySensorMappingType", default='LegacyNacelleSensorMapping', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_sensor_mapping.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_sensor_mapping.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class LegacySensorMapping_SensorTypeEnum(str, Enum):
     ACCELEROMETER = "ACCELEROMETER"
     INCLINOMETER = "INCLINOMETER"
     STRAIN_GAUGE = "STRAIN_GAUGE"
@@ -24,24 +25,29 @@
 class LegacySensorMapping_LegacySensorMappingTypeEnum(str, Enum):
     LEGACY_TOWER_SENSOR_MAPPING = "LegacyTowerSensorMapping"
     LEGACY_NACELLE_SENSOR_MAPPING = "LegacyNacelleSensorMapping"
     LEGACY_BLADE_SENSOR_MAPPING = "LegacyBladeSensorMapping"
 
 class LegacySensorMapping(Dnv):
     """LegacySensorMapping - The controller interface used by 4.X compatible controllers need to have a mapping between how they are referenced now, and how they used to be referenced.
-
+    
     Attributes:
-        - SensorType: LegacySensorMapping_SensorTypeEnum => The type of the sensor on the component.  These are stored in separate lists, and the mapping will need to know which list to look in.
-        - SensorIndex: int => The index of the sensor, as the controller would access it.
-        - LegacySensorMappingType: LegacySensorMapping_LegacySensorMappingTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    SensorType : LegacySensorMapping_SensorTypeEnum
+        The type of the sensor on the component.  These are stored in separate lists, and the mapping will need to know which list to look in.
+    SensorIndex : int
+        The index of the sensor, as the controller would access it.
+    LegacySensorMappingType : LegacySensorMapping_LegacySensorMappingTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - LegacyBladeSensorMapping
-        - LegacyNacelleSensorMapping
-        - LegacyTowerSensorMapping
+        LegacyBladeSensorMapping
+        LegacyNacelleSensorMapping
+        LegacyTowerSensorMapping
+    
     """
 
     SensorType: Optional[LegacySensorMapping_SensorTypeEnum] = Field(alias="SensorType", default=None)
     SensorIndex: Optional[int] = Field(alias="SensorIndex", default=None)
     LegacySensorMappingType: Optional[LegacySensorMapping_LegacySensorMappingTypeEnum] = Field(alias="LegacySensorMappingType", default=None)
 
     class Config:
@@ -73,15 +79,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'LegacySensorMapping'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_sensor_mappings.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_sensor_mappings.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.legacy_sensor_mapping import LegacySensorMapping
 
 from dnv_bladed_models.sensors_used import SensorsUsed
 
 
 class LegacySensorMappings(SensorsUsed, SensorsUsedType='LegacySensorMappings'):
     """LegacySensorMappings - This provides a mapping between the legacy external controller sensor interface, and the sensors defined on the tower, nacelle, and blade.
-
+    
     Attributes:
-        - Legacy: List[LegacySensorMapping] => The controller interface used by 4.X compatible controllers need to have a mapping between how they are referenced now, and how they used to be referenced.
-        - SensorsUsedType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LegacySensorMappings&#39;
+    ----------
+    Legacy : List[LegacySensorMapping], default=list()
+        The controller interface used by 4.X compatible controllers need to have a mapping between how they are referenced now, and how they used to be referenced.
+    SensorsUsedType : str, readonly, default='LegacySensorMappings'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LegacySensorMappings'
+    
     """
 
     Legacy: Optional[List[LegacySensorMapping]] = Field(alias="Legacy", default=list())
     SensorsUsedType: Optional[str] = Field(alias="SensorsUsedType", default='LegacySensorMappings', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/legacy_tower_sensor_mapping.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/legacy_tower_sensor_mapping.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.legacy_sensor_mapping import LegacySensorMapping
 
 
 class LegacyTowerSensorMapping(LegacySensorMapping, LegacySensorMappingType='LegacyTowerSensorMapping'):
     """LegacyTowerSensorMapping - The controller interface used by 4.X compatible controllers need to have a mapping between how they are referenced now, and how they used to be referenced.
-
+    
     Attributes:
-        - LegacySensorMappingType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LegacyTowerSensorMapping&#39;
+    ----------
+    LegacySensorMappingType : str, readonly, default='LegacyTowerSensorMapping'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LegacyTowerSensorMapping'
+    
     """
 
     LegacySensorMappingType: Optional[str] = Field(alias="LegacySensorMappingType", default='LegacyTowerSensorMapping', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/length_vs_radius.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/length_vs_radius.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class LengthVsRadius(Dnv):
     """LengthVsRadius - A line or row in a look-up table specifying the nacelle cover&#39;s radius for a given length.
-
+    
     Attributes:
-        - Length: float => A number representing a length.  The SI units for length are metres.
-        - Radius: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    Length : float
+        A number representing a length.  The SI units for length are metres.
+    Radius : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     Length: Optional[float] = Field(alias="Length", default=None)
     Radius: Optional[float] = Field(alias="Radius", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,36 +9,54 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.lidar_beam import LidarBeam
 
 from dnv_bladed_models.lidar_focal_distance_control import LidarFocalDistanceControl
 
 from dnv_bladed_models.lidar_scanning_pattern import LidarScanningPattern
 
 from dnv_bladed_models.look_up_table_element import LookUpTableElement
 
 
 class Lidar(Component, ComponentType='Lidar'):
     """Lidar - A Lidar sensor (https://www.wikipedia.org/wiki/Lidar) mounted on the scructure.
-
+    
     Attributes:
-        - LensArea: float => A number representing an area.  The SI units for area are square metres.
-        - LaserWavelength: float => A number representing a length.  The SI units for length are metres.
-        - WeightingFunction: List[LookUpTableElement] => The relationship between the distance from the focal point and the weighting to put on the sample.  Every Lidar sampling point has a finite width, where velocities are collected to either side of the nominal focal distance.  This relationship is used to put more weight on those samples closest to the nominal focal point.
-        - LidarBeams: LidarBeam 
-        - ScanningPattern: LidarScanningPattern 
-        - FocalDistanceControl: LidarFocalDistanceControl 
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Lidar&#39;
+    ----------
+    LensArea : float
+        A number representing an area.  The SI units for area are square metres.
+    LaserWavelength : float
+        A number representing a length.  The SI units for length are metres.
+    WeightingFunction : List[LookUpTableElement], default=list()
+        The relationship between the distance from the focal point and the weighting to put on the sample.  Every Lidar sampling point has a finite width, where velocities are collected to either side of the nominal focal distance.  This relationship is used to put more weight on those samples closest to the nominal focal point.
+    LidarBeams : LidarBeam
+    ScanningPattern : LidarScanningPattern, abstract
+    FocalDistanceControl : LidarFocalDistanceControl, abstract
+    ComponentType : str, readonly, default='Lidar'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Lidar'
+    
+    Notes:
+    ---------
+    ScanningPattern has the following concrete types:
+        - CircularLidarScan
+        - LidarControllerScan
+        - RosetteLidarScan
+    FocalDistanceControl has the following concrete types:
+        - ControllerLidarSettings
+        - MultipleLidarFocalDistances
+        - SingleLidarFocalDistance
+    
     """
 
     LensArea: Optional[float] = Field(alias="LensArea", default=None)
     LaserWavelength: Optional[float] = Field(alias="LaserWavelength", default=None)
     WeightingFunction: Optional[List[LookUpTableElement]] = Field(alias="WeightingFunction", default=list())
     LidarBeams: Optional[LidarBeam] = Field(alias="LidarBeams", default=None)
     ScanningPattern: Optional[LidarScanningPattern] = Field(alias="ScanningPattern", default=None)
@@ -48,15 +66,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_beam.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_beam.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,42 +9,48 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.lidar_mounting_position import LidarMountingPosition
 
 
 class LidarBeam(Dnv):
     """LidarBeam - The definition of a single Lidar beam.  A Lidar device may have multiple beams.
-
+    
     Attributes:
-        - Azimuth: float => A number representing an angle.  The SI units for angles are radians.
-        - AngleToCentreLine: float => A number representing an angle.  The SI units for angles are radians.
-        - CorrectForMountingNodeVelocity: bool => If true, the mounting node velocity will not be factored into the line-of-sight wind speed measurement.
-        - MountingPosition: LidarMountingPosition 
+    ----------
+    Azimuth : float
+        A number representing an angle.  The SI units for angles are radians.
+    AngleToCentreLine : float
+        A number representing an angle.  The SI units for angles are radians.
+    CorrectForMountingNodeVelocity : bool, default=False
+        If true, the mounting node velocity will not be factored into the line-of-sight wind speed measurement.
+    MountingPosition : LidarMountingPosition
+    
     """
 
     Azimuth: Optional[float] = Field(alias="Azimuth", default=None)
     AngleToCentreLine: Optional[float] = Field(alias="AngleToCentreLine", default=None)
     CorrectForMountingNodeVelocity: Optional[bool] = Field(alias="CorrectForMountingNodeVelocity", default=False)
     MountingPosition: Optional[LidarMountingPosition] = Field(alias="MountingPosition", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_controller_scan.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_controller_scan.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.lidar_scanning_pattern import LidarScanningPattern
 
 
 class LidarControllerScan(LidarScanningPattern, ScanningPatternType='ControllerScan'):
     """LidarControllerScan - Where the scan pattern is controlled by the turbine&#39;s controller.  The controller API will provide functions with which to move the beam.
-
+    
     Attributes:
-        - ScanningPatternType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ControllerScan&#39;
+    ----------
+    ScanningPatternType : str, readonly, default='ControllerScan'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ControllerScan'
+    
     """
 
     ScanningPatternType: Optional[str] = Field(alias="ScanningPatternType", default='ControllerScan', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_focal_distance_control.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_focal_distance_control.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,37 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class LidarFocalDistanceControl_FocussingModeEnum(str, Enum):
     PULSED = "Pulsed"
     CONTINUOUS = "Continuous"
 
 class LidarFocalDistanceControl_FocalDistanceControlTypeEnum(str, Enum):
     CONTROLLER = "Controller"
     SINGLE_FOCAL_DISTANCE = "SingleFocalDistance"
     MULTIPLE_FOCAL_DISTANCES = "MultipleFocalDistances"
 
 class LidarFocalDistanceControl(Dnv):
     """LidarFocalDistanceControl - The common properties of the different focal distance control methods.
-
+    
     Attributes:
-        - FocussingMode: LidarFocalDistanceControl_FocussingModeEnum => The focussing mode of the Lidar beam.  Continuous beam that focuses at single point or pulsed beam measures velocities at multiple distances
-        - FocalDistanceControlType: LidarFocalDistanceControl_FocalDistanceControlTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    FocussingMode : LidarFocalDistanceControl_FocussingModeEnum
+        The focussing mode of the Lidar beam.  Continuous beam that focuses at single point or pulsed beam measures velocities at multiple distances
+    FocalDistanceControlType : LidarFocalDistanceControl_FocalDistanceControlTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - ControllerLidarSettings
-        - MultipleLidarFocalDistances
-        - SingleLidarFocalDistance
+        ControllerLidarSettings
+        MultipleLidarFocalDistances
+        SingleLidarFocalDistance
+    
     """
 
     FocussingMode: Optional[LidarFocalDistanceControl_FocussingModeEnum] = Field(alias="FocussingMode", default=None)
     FocalDistanceControlType: Optional[LidarFocalDistanceControl_FocalDistanceControlTypeEnum] = Field(alias="FocalDistanceControlType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -70,15 +75,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'LidarFocalDistanceControl'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_mounting_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_mounting_position.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class LidarMountingPosition(Dnv):
     """LidarMountingPosition - The offset of the Lidar device from it&#39;s attachment point, in the component&#39;s axis system.
-
+    
     Attributes:
-        - X: float => A number representing a length.  The SI units for length are metres.
-        - Y: float => A number representing a length.  The SI units for length are metres.
-        - Z: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    X : float
+        A number representing a length.  The SI units for length are metres.
+    Y : float
+        A number representing a length.  The SI units for length are metres.
+    Z : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     X: Optional[float] = Field(alias="X", default=None)
     Y: Optional[float] = Field(alias="Y", default=None)
     Z: Optional[float] = Field(alias="Z", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/lidar_scanning_pattern.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/lidar_scanning_pattern.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class LidarScanningPattern_ScanningPatternTypeEnum(str, Enum):
     CONTROLLER_SCAN = "ControllerScan"
     ROSETTE_SCAN = "RosetteScan"
     CIRCULAR_SCAN = "CircularScan"
 
 class LidarScanningPattern(Dnv):
     """LidarScanningPattern - The common properties for Lidar scanning patterns.
-
+    
     Attributes:
-        - ScanningPatternType: LidarScanningPattern_ScanningPatternTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    ScanningPatternType : LidarScanningPattern_ScanningPatternTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - CircularLidarScan
-        - LidarControllerScan
-        - RosetteLidarScan
+        CircularLidarScan
+        LidarControllerScan
+        RosetteLidarScan
+    
     """
 
     ScanningPatternType: Optional[LidarScanningPattern_ScanningPatternTypeEnum] = Field(alias="ScanningPatternType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -64,15 +68,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'LidarScanningPattern'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_airy.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_airy.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.regular_waves import RegularWaves
 
 
 class LinearAiry(RegularWaves, WavesType='LinearAiry'):
     """LinearAiry - The definition of Linear Airy waves.
-
+    
     Attributes:
-        - WavesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LinearAiry&#39;
+    ----------
+    WavesType : str, readonly, default='LinearAiry'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LinearAiry'
+    
     """
 
     WavesType: Optional[str] = Field(alias="WavesType", default='LinearAiry', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_electrical_losses.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_electrical_losses.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.electrical_losses import ElectricalLosses
 
 
 class LinearElectricalLosses(ElectricalLosses, ElectricalLossesType='LinearElectricalLosses'):
     """LinearElectricalLosses - An electrical loss model that assumes a proportional relationship between the power and the losses. 
-
+    
     Attributes:
-        - PowerLossAtNoLoad: float => A number representing a power.  The SI units for enery are Watts.
-        - Efficiency: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - ElectricalLossesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LinearElectricalLosses&#39;
+    ----------
+    PowerLossAtNoLoad : float
+        A number representing a power.  The SI units for enery are Watts.
+    Efficiency : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    ElectricalLossesType : str, readonly, default='LinearElectricalLosses'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LinearElectricalLosses'
+    
     """
 
     PowerLossAtNoLoad: Optional[float] = Field(alias="PowerLossAtNoLoad", default=None)
     Efficiency: Optional[float] = Field(alias="Efficiency", default=None)
     ElectricalLossesType: Optional[str] = Field(alias="ElectricalLossesType", default='LinearElectricalLosses', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_foundation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_foundation.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.foundation import Foundation
 
 
 class LinearFoundation(Foundation, ComponentType='LinearFoundation'):
     """LinearFoundation - A foundation represented by a linear stiffness and mass matrix at a single point.
-
+    
     Attributes:
-        - StiffnessMatrix: List[List[float]] => A 6x6 matrix representing the linear and rotational stiffnesses of an object or joint.
-        - MassMatrix: List[List[float]] => A 6x6 matrix representing the mass and rotational inertias of an object.  These are symmetric matrices for all real-world objects.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LinearFoundation&#39;
+    ----------
+    StiffnessMatrix : List[List[float]], default=list()
+        A 6x6 matrix representing the linear and rotational stiffnesses of an object or joint.
+    MassMatrix : List[List[float]], default=list()
+        A 6x6 matrix representing the mass and rotational inertias of an object.  These are symmetric matrices for all real-world objects.
+    ComponentType : str, readonly, default='LinearFoundation'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LinearFoundation'
+    
     """
 
     StiffnessMatrix: Optional[List[List[float]]] = Field(alias="StiffnessMatrix", default=list())
     MassMatrix: Optional[List[List[float]]] = Field(alias="MassMatrix", default=list())
     ComponentType: Optional[str] = Field(alias="ComponentType", default='LinearFoundation', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_passive_damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_passive_damper.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,29 +9,37 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.damper import Damper
 
 from dnv_bladed_models.vector3_d import Vector3D
 
 
 class LinearPassiveDamper(Damper, ComponentType='LinearPassiveDamper'):
     """LinearPassiveDamper - A linear vibration damper to add to the support structure.
-
+    
     Attributes:
-        - Mass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - Frequency: float => A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
-        - Damping: float => A number representing a linear damping term.  The SI units for linear damping are Newton seconds per metre.
-        - IgnoreInModalAnalysis: bool => If true, the damper will be ignored in modal analyses.
-        - Direction: Vector3D 
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LinearPassiveDamper&#39;
+    ----------
+    Mass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    Frequency : float
+        A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
+    Damping : float
+        A number representing a linear damping term.  The SI units for linear damping are Newton seconds per metre.
+    IgnoreInModalAnalysis : bool
+        If true, the damper will be ignored in modal analyses.
+    Direction : Vector3D
+    ComponentType : str, readonly, default='LinearPassiveDamper'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LinearPassiveDamper'
+    
     """
 
     Mass: Optional[float] = Field(alias="Mass", default=None)
     Frequency: Optional[float] = Field(alias="Frequency", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
     IgnoreInModalAnalysis: Optional[bool] = Field(alias="IgnoreInModalAnalysis", default=None)
     Direction: Optional[Vector3D] = Field(alias="Direction", default=None)
@@ -40,15 +48,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_pitch_actuator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_pitch_actuator.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,33 +9,51 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_actuator import PitchActuator
 
 from dnv_bladed_models.pitch_force_limits import PitchForceLimits
 
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class LinearPitchActuator(PitchActuator, ActuatorDriveType='LinearActuator'):
     """LinearPitchActuator - A linear pitch actuation definition.
-
+    
     Attributes:
-        - ForceResponse: ResponseToDemand 
-        - PivotOffset: float => A number representing a length.  The SI units for length are metres.
-        - PitchAngleAtMaximumTorque: float => A number representing an angle.  The SI units for angles are radians.
-        - BrakeForce: float => A number representing a force.  The SI units for force are Newtons.
-        - ForceLimits: PitchForceLimits 
-        - BackupPowerForceLimits: PitchForceLimits 
-        - RadiusOfArm: float => A number representing a length.  The SI units for length are metres.
-        - ActuatorDriveType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LinearActuator&#39;
+    ----------
+    ForceResponse : ResponseToDemand, abstract
+    PivotOffset : float
+        A number representing a length.  The SI units for length are metres.
+    PitchAngleAtMaximumTorque : float
+        A number representing an angle.  The SI units for angles are radians.
+    BrakeForce : float
+        A number representing a force.  The SI units for force are Newtons.
+    ForceLimits : PitchForceLimits
+    BackupPowerForceLimits : PitchForceLimits
+    RadiusOfArm : float
+        A number representing a length.  The SI units for length are metres.
+    ActuatorDriveType : str, readonly, default='LinearActuator'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LinearActuator'
+    
+    Notes:
+    ---------
+    ForceResponse has the following concrete types:
+        - FirstOrderResponse
+        - Instantaneous
+        - Passive
+        - ProportionalIntegralDeriviative
+        - SecondOrderResponse
+        - SetpointTrajectoryPlanning
+    
     """
 
     ForceResponse: Optional[ResponseToDemand] = Field(alias="ForceResponse", default=None)
     PivotOffset: Optional[float] = Field(alias="PivotOffset", default=None)
     PitchAngleAtMaximumTorque: Optional[float] = Field(alias="PitchAngleAtMaximumTorque", default=None)
     BrakeForce: Optional[float] = Field(alias="BrakeForce", default=None)
     ForceLimits: Optional[PitchForceLimits] = Field(alias="ForceLimits", default=None)
@@ -46,15 +64,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_spring_damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_spring_damper.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.flexible_joint import FlexibleJoint
 
 
 class LinearSpringDamper(FlexibleJoint):
     """LinearSpringDamper - The definition of a linear spring damper.
-
+    
     Attributes:
-        - Stiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    ----------
+    Stiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    
     """
 
     Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linear_spring_yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linear_spring_yaw_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.active_yaw_system import ActiveYawSystem
 
 
 class LinearSpringYawSystem(ActiveYawSystem, ComponentType='LinearSpringYawSystem'):
     """LinearSpringYawSystem - The model known as &#39;Flexible Yaw&#39; in the UI.
-
+    
     Attributes:
-        - SpringStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LinearSpringYawSystem&#39;
+    ----------
+    SpringStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    ComponentType : str, readonly, default='LinearSpringYawSystem'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LinearSpringYawSystem'
+    
     """
 
     SpringStiffness: Optional[float] = Field(alias="SpringStiffness", default=None)
     ComponentType: Optional[str] = Field(alias="ComponentType", default='LinearSpringYawSystem', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/linearisation_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/linearisation_calculation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.steady_calculation import SteadyCalculation
 
 from dnv_bladed_models.steady_calculation_outputs import SteadyCalculationOutputs
 
 
 class LinearisationCalculation(SteadyCalculation):
     """LinearisationCalculation - The common properties of calculations which use small perturbations to generate system responses, from which the dynamics of the fully coupled system can be analysed.
-
+    
     Attributes:
-        - MinimumCorrelationCoefficient: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Outputs: SteadyCalculationOutputs 
-        - TransformRotorModesToNonRotating: bool => If true, a multi-blade coordinate (MBC) transform will be performed to transform the rotating modes into the stationary frame of reference.  This will generate forward and backward whirling modes in a Campbell diagram.
+    ----------
+    MinimumCorrelationCoefficient : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    Outputs : SteadyCalculationOutputs
+    TransformRotorModesToNonRotating : bool, default=False
+        If true, a multi-blade coordinate (MBC) transform will be performed to transform the rotating modes into the stationary frame of reference.  This will generate forward and backward whirling modes in a Campbell diagram.
+    
     """
 
     MinimumCorrelationCoefficient: Optional[float] = Field(alias="MinimumCorrelationCoefficient", default=None)
     Outputs: Optional[SteadyCalculationOutputs] = Field(alias="Outputs", default=None)
     TransformRotorModesToNonRotating: Optional[bool] = Field(alias="TransformRotorModesToNonRotating", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/loading_dependent_friction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/loading_dependent_friction.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.moment_vs_coulomb_friction import MomentVsCoulombFriction
 
 from dnv_bladed_models.thrust_vs_coulomb_friction import ThrustVsCoulombFriction
 
 
 class LoadingDependentFriction(Dnv):
     """LoadingDependentFriction - The friction of the yaw bearing, varying with the applied loads.
-
+    
     Attributes:
-        - ThrustVsCoulombFriction: List[ThrustVsCoulombFriction] => The relationship between the Coulomb friction in the yaw bearing and the applied thrust.
-        - MomentVsCoulombFriction: List[MomentVsCoulombFriction] => The relationship between the Coulomb friction in the yaw bearing and the applied moment.
+    ----------
+    ThrustVsCoulombFriction : List[ThrustVsCoulombFriction], default=list()
+        The relationship between the Coulomb friction in the yaw bearing and the applied thrust.
+    MomentVsCoulombFriction : List[MomentVsCoulombFriction], default=list()
+        The relationship between the Coulomb friction in the yaw bearing and the applied moment.
+    
     """
 
     ThrustVsCoulombFriction: Optional[List[ThrustVsCoulombFriction]] = Field(alias="ThrustVsCoulombFriction", default=list())
     MomentVsCoulombFriction: Optional[List[MomentVsCoulombFriction]] = Field(alias="MomentVsCoulombFriction", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/logarithmic_shear_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/logarithmic_shear_model.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.vertical_shear import VerticalShear
 
 
 class LogarithmicShearModel(VerticalShear, VerticalShearType='LogarithmicShearModel'):
     """LogarithmicShearModel - The vertical wind shear to apply to the wind field.  This will vary the wind speed from 100% at the reference height (usually the hub height) down to 0% at ground level on an logarithmic basis.
-
+    
     Attributes:
-        - SurfaceRoughness: float => The surface roughness, used to determin the shape of the wind shear variation.
-        - VerticalShearType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LogarithmicShearModel&#39;
+    ----------
+    SurfaceRoughness : float
+        The surface roughness, used to determin the shape of the wind shear variation.
+    VerticalShearType : str, readonly, default='LogarithmicShearModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LogarithmicShearModel'
+    
     """
 
     SurfaceRoughness: Optional[float] = Field(alias="SurfaceRoughness", default=None)
     VerticalShearType: Optional[str] = Field(alias="VerticalShearType", default='LogarithmicShearModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/look_up_shear_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/look_up_shear_model.py`

 * *Files 0% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.height_vs_shear_factor import HeightVsShearFactor
 
 from dnv_bladed_models.vertical_shear import VerticalShear
 
 
 class LookUpShearModel(VerticalShear, VerticalShearType='LookUpShearModel'):
     """LookUpShearModel - The vertical wind shear to apply to the wind field.  This will vary the wind speed from 100% at the reference height (usually the hub height) down to 0% at ground level according to a look-up table.
-
+    
     Attributes:
-        - HeightVsShearFactor: List[HeightVsShearFactor] => A series of height vs shear factors, including a point at the reference height (above which normal flow begins) and 0.0 (ground level).
-        - VerticalShearType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;LookUpShearModel&#39;
+    ----------
+    HeightVsShearFactor : List[HeightVsShearFactor], default=list()
+        A series of height vs shear factors, including a point at the reference height (above which normal flow begins) and 0.0 (ground level).
+    VerticalShearType : str, readonly, default='LookUpShearModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'LookUpShearModel'
+    
     """
 
     HeightVsShearFactor: Optional[List[HeightVsShearFactor]] = Field(alias="HeightVsShearFactor", default=list())
     VerticalShearType: Optional[str] = Field(alias="VerticalShearType", default='LookUpShearModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/look_up_table_element.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/look_up_table_element.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class LookUpTableElement(Dnv):
     """LookUpTableElement - 
-
+    
     Attributes:
-        - DistanceFromFocalPoint: float => A number representing a length.  The SI units for length are metres.
-        - ProportionOfSignal: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    DistanceFromFocalPoint : float
+        A number representing a length.  The SI units for length are metres.
+    ProportionOfSignal : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     DistanceFromFocalPoint: Optional[float] = Field(alias="DistanceFromFocalPoint", default=None)
     ProportionOfSignal: Optional[float] = Field(alias="ProportionOfSignal", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/low_speed_shaft.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_seizure_at_angle.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,57 +9,63 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.conditional_event import ConditionalEvent
 
-class LowSpeedShaft(Dnv):
-    """LowSpeedShaft - The low speed shaft bending definition.
 
+class PitchFaultSeizureAtAngle(ConditionalEvent, EventType='PitchFaultSeizureAtAngle'):
+    """PitchFaultSeizureAtAngle - The failure of a blade&#39;s pitch system when it passes through a specified angle that leaves it without actuation and free to move.  If the pitch system never passes through the specified angle, no seizure will occur.
+    
     Attributes:
-        - EffectiveShaftLength: float => A number representing a length.  The SI units for length are metres.
-        - HingePosition: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TorsionalStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - TorsionalDamping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
-        - BendingStiffness: float => A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
-        - BendingDamping: float => A number representing a linear damping term.  The SI units for linear damping are Newton seconds per metre.
+    ----------
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    SeizesAsItPassesAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    EventType : str, readonly, default='PitchFaultSeizureAtAngle'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PitchFaultSeizureAtAngle'
+    
     """
 
-    EffectiveShaftLength: Optional[float] = Field(alias="EffectiveShaftLength", default=None)
-    HingePosition: Optional[float] = Field(alias="HingePosition", default=None)
-    TorsionalStiffness: Optional[float] = Field(alias="TorsionalStiffness", default=None)
-    TorsionalDamping: Optional[float] = Field(alias="TorsionalDamping", default=None)
-    BendingStiffness: Optional[float] = Field(alias="BendingStiffness", default=None)
-    BendingDamping: Optional[float] = Field(alias="BendingDamping", default=None)
+    OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
+    SeizesAsItPassesAngle: Optional[float] = Field(alias="SeizesAsItPassesAngle", default=None)
+    EventType: Optional[str] = Field(alias="EventType", default='PitchFaultSeizureAtAngle', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
                             allowed_child_vals[child_key] = child_val
                     allowed_vals[key] = allowed_child_vals
                 else:
                     allowed_vals[key] = val
         return allowed_vals
 
+    @validator("OnComponentInAssembly")
+    def OnComponentInAssembly_pattern(cls, value):
+        if value is not None and not re.match(r"^#(\/.+)+$", value):
+            raise ValueError(f"OnComponentInAssembly did not match the expected format (found {value})")
+        return value
+
     def to_json(
         self,
         *, 
         include: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         exclude: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         by_alias: bool = True, 
         skip_defaults: Optional[bool] = None, 
@@ -112,25 +118,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        LowSpeedShaft
+        PitchFaultSeizureAtAngle
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = LowSpeedShaft.from_file('/path/to/file')
+        >>> model = PitchFaultSeizureAtAngle.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -142,25 +148,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        LowSpeedShaft
+        PitchFaultSeizureAtAngle
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = LowSpeedShaft.from_json('{ ... }')
+        >>> model = PitchFaultSeizureAtAngle.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -174,15 +180,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        LowSpeedShaft
+        PitchFaultSeizureAtAngle
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -206,8 +212,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-LowSpeedShaft.update_forward_refs()
+PitchFaultSeizureAtAngle.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/mac_camy_fuchs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/mac_camy_fuchs.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wave_diffraction_approximation import WaveDiffractionApproximation
 
 
 class MacCamyFuchs(WaveDiffractionApproximation, WaveDiffractionApproximationType='MacCamyFuchs'):
     """MacCamyFuchs - The MacCamy-Fuchs methodology will be used, with the member diameter automatically determined from the model.
-
+    
     Attributes:
-        - MemberDiameter: float => A number representing a length.  The SI units for length are metres.
-        - WaveDiffractionApproximationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;MacCamyFuchs&#39;
+    ----------
+    MemberDiameter : float
+        A number representing a length.  The SI units for length are metres.
+    WaveDiffractionApproximationType : str, readonly, default='MacCamyFuchs'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'MacCamyFuchs'
+    
     """
 
     MemberDiameter: Optional[float] = Field(alias="MemberDiameter", default=None)
     WaveDiffractionApproximationType: Optional[str] = Field(alias="WaveDiffractionApproximationType", default='MacCamyFuchs', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/main_bearing.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/vector3_d.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,40 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
 
-class MainBearing(Dnv):
-    """MainBearing - The location and properties of the hubmost main bearing that supports the low speed shaft.
-
+class Vector3D(BaseModel):
+    """Vector3D - A 3x1 vector representing a location or direction.
+    
     Attributes:
-        - RotorTilt: float => A number representing an angle.  The SI units for angles are radians.
-        - Overhang: float => A number representing a length.  The SI units for length are metres.
-        - HeightOffset: float => A number representing a length.  The SI units for length are metres.
-        - SideOffset: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    X : float
+        A number representing a length.  The SI units for length are metres.
+    Y : float
+        A number representing a length.  The SI units for length are metres.
+    Z : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
-    RotorTilt: Optional[float] = Field(alias="RotorTilt", default=None)
-    Overhang: Optional[float] = Field(alias="Overhang", default=None)
-    HeightOffset: Optional[float] = Field(alias="HeightOffset", default=None)
-    SideOffset: Optional[float] = Field(alias="SideOffset", default=None)
+    X: Optional[float] = Field(alias="X", default=None)
+    Y: Optional[float] = Field(alias="Y", default=None)
+    Z: Optional[float] = Field(alias="Z", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +110,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        MainBearing
+        Vector3D
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = MainBearing.from_file('/path/to/file')
+        >>> model = Vector3D.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +140,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        MainBearing
+        Vector3D
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = MainBearing.from_json('{ ... }')
+        >>> model = Vector3D.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +172,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        MainBearing
+        Vector3D
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +204,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-MainBearing.update_forward_refs()
+Vector3D.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/meandering_wake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/meandering_wake.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class MeanderingWake(Dnv):
     """MeanderingWake - The parameters governing the meandering of the upwind turbine wake.
-
+    
     Attributes:
-        - MeanderingWindFile: str => A filepath or URI containing low pass filtered wind data that is used to determine the meandering of the wake centreline.
-        - FractionalAmbientVelocity: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    MeanderingWindFile : str
+        A filepath or URI containing low pass filtered wind data that is used to determine the meandering of the wake centreline.
+    FractionalAmbientVelocity : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     MeanderingWindFile: Optional[str] = Field(alias="MeanderingWindFile", default=None)
     FractionalAmbientVelocity: Optional[float] = Field(alias="FractionalAmbientVelocity", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/measured_signal_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/measured_signal_properties.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.signal_properties_acceleration import SignalPropertiesAcceleration
 
 from dnv_bladed_models.signal_properties_angle import SignalPropertiesAngle
 
 from dnv_bladed_models.signal_properties_angular_acceleration import SignalPropertiesAngularAcceleration
@@ -32,73 +33,77 @@
 from dnv_bladed_models.signal_properties_power import SignalPropertiesPower
 
 from dnv_bladed_models.signal_properties_velocity import SignalPropertiesVelocity
 
 
 class MeasuredSignalProperties(Dnv):
     """MeasuredSignalProperties - The noise and transducer properties for those signals representing values coming from a physical sensor.
-
+    
     Attributes:
-        - RandomNumberSeed: int => A seed for the random number generator to ensure that subsequent runs have identical noise signatures.
-        - TurnOffNoise: bool => This allows the noise to be turned off globally.  Note: this turns off noise, but keeps discretisation, sampling time, faults and transducer behaviour.
-        - ShaftPowerSignals: SignalPropertiesPower 
-        - RotorSpeedSignals: SignalPropertiesAngularVelocity 
-        - ElectricalPowerOutputSignals: SignalPropertiesPower 
-        - GeneratorSpeedSignals: SignalPropertiesAngularVelocity 
-        - GeneratorTorqueSignals: SignalPropertiesMoment 
-        - YawBearingAngularPositionSignals: SignalPropertiesAngle 
-        - YawBearingAngularVelocitySignals: SignalPropertiesAngularVelocity 
-        - YawBearingAngularAccelerationSignals: SignalPropertiesAngularAcceleration 
-        - YawMotorRateSignals: SignalPropertiesAngularVelocity 
-        - YawErrorSignals: SignalPropertiesAngle 
-        - NacelleAngleFromNorthSignals: SignalPropertiesAngle 
-        - TowerTopForeAftAccelerationSignals: SignalPropertiesAcceleration 
-        - TowerTopSideSideAccelerationSignals: SignalPropertiesAcceleration 
-        - ShaftTorqueSignals: SignalPropertiesMoment 
-        - YawBearingMySignals: SignalPropertiesMoment 
-        - YawBearingMzSignals: SignalPropertiesMoment 
-        - NacelleRollAngleSignals: SignalPropertiesAngle 
-        - NacelleNoddingAngleSignals: SignalPropertiesAngle 
-        - NacelleRollAccelerationSignals: SignalPropertiesAngularAcceleration 
-        - NacelleNoddingAccelerationSignals: SignalPropertiesAngularAcceleration 
-        - NacelleYawAccelerationSignals: SignalPropertiesAngularAcceleration 
-        - RotorAzimuthAngleSignals: SignalPropertiesAngle 
-        - NominalHubFlowSpeedSignals: SignalPropertiesVelocity 
-        - RotatingHubMySignals: SignalPropertiesMoment 
-        - RotatingHubMzSignals: SignalPropertiesMoment 
-        - FixedHubMySignals: SignalPropertiesMoment 
-        - FixedHubMzSignals: SignalPropertiesMoment 
-        - FixedHubFxSignals: SignalPropertiesForce 
-        - FixedHubFySignals: SignalPropertiesForce 
-        - FixedHubFzSignals: SignalPropertiesForce 
-        - PitchAngleSignals: SignalPropertiesAngle 
-        - PitchRateSignals: SignalPropertiesAngularVelocity 
-        - PitchActuatorTorqueSignals: SignalPropertiesMoment 
-        - PitchBearingFrictionSignals: SignalPropertiesMoment 
-        - PitchBearingStictionSignals: SignalPropertiesMoment 
-        - BladeOutOfPlaneBendingMomentSignals: SignalPropertiesMoment 
-        - BladeInPlaneBendingMomentSignals: SignalPropertiesMoment 
-        - PitchBearingMxSignals: SignalPropertiesMoment 
-        - PitchBearingMySignals: SignalPropertiesMoment 
-        - PitchBearingMzSignals: SignalPropertiesMoment 
-        - PitchBearingRadialForceSignals: SignalPropertiesForce 
-        - PitchBearingAxialForceSignals: SignalPropertiesForce 
-        - PitchBearingFxSignals: SignalPropertiesForce 
-        - PitchBearingFySignals: SignalPropertiesForce 
-        - BladeStationWindSpeedSignals: SignalPropertiesVelocity 
-        - BladeStationAngleOfAttackSignals: SignalPropertiesAngle 
-        - AileronAngleSignals: SignalPropertiesAngle 
-        - AileronRateSignals: SignalPropertiesAngularVelocity 
-        - BladeStationPositionXSignals: SignalPropertiesLength 
-        - BladeStationPositionYSignals: SignalPropertiesLength 
-        - BladeStationPositionZSignals: SignalPropertiesLength 
-        - BladeStationPositionXRotationSignals: SignalPropertiesAngle 
-        - BladeStationPositionYRotationSignals: SignalPropertiesAngle 
-        - BladeStationPositionZRotationSignals: SignalPropertiesAngle 
-        - LidarBeamFocalPointVelocitySignals: SignalPropertiesVelocity 
+    ----------
+    RandomNumberSeed : int, default=0
+        A seed for the random number generator to ensure that subsequent runs have identical noise signatures.
+    TurnOffNoise : bool, default=False
+        This allows the noise to be turned off globally.  Note: this turns off noise, but keeps discretisation, sampling time, faults and transducer behaviour.
+    ShaftPowerSignals : SignalPropertiesPower
+    RotorSpeedSignals : SignalPropertiesAngularVelocity
+    ElectricalPowerOutputSignals : SignalPropertiesPower
+    GeneratorSpeedSignals : SignalPropertiesAngularVelocity
+    GeneratorTorqueSignals : SignalPropertiesMoment
+    YawBearingAngularPositionSignals : SignalPropertiesAngle
+    YawBearingAngularVelocitySignals : SignalPropertiesAngularVelocity
+    YawBearingAngularAccelerationSignals : SignalPropertiesAngularAcceleration
+    YawMotorRateSignals : SignalPropertiesAngularVelocity
+    YawErrorSignals : SignalPropertiesAngle
+    NacelleAngleFromNorthSignals : SignalPropertiesAngle
+    TowerTopForeAftAccelerationSignals : SignalPropertiesAcceleration
+    TowerTopSideSideAccelerationSignals : SignalPropertiesAcceleration
+    ShaftTorqueSignals : SignalPropertiesMoment
+    YawBearingMySignals : SignalPropertiesMoment
+    YawBearingMzSignals : SignalPropertiesMoment
+    NacelleRollAngleSignals : SignalPropertiesAngle
+    NacelleNoddingAngleSignals : SignalPropertiesAngle
+    NacelleRollAccelerationSignals : SignalPropertiesAngularAcceleration
+    NacelleNoddingAccelerationSignals : SignalPropertiesAngularAcceleration
+    NacelleYawAccelerationSignals : SignalPropertiesAngularAcceleration
+    RotorAzimuthAngleSignals : SignalPropertiesAngle
+    NominalHubFlowSpeedSignals : SignalPropertiesVelocity
+    RotatingHubMySignals : SignalPropertiesMoment
+    RotatingHubMzSignals : SignalPropertiesMoment
+    FixedHubMySignals : SignalPropertiesMoment
+    FixedHubMzSignals : SignalPropertiesMoment
+    FixedHubFxSignals : SignalPropertiesForce
+    FixedHubFySignals : SignalPropertiesForce
+    FixedHubFzSignals : SignalPropertiesForce
+    PitchAngleSignals : SignalPropertiesAngle
+    PitchRateSignals : SignalPropertiesAngularVelocity
+    PitchActuatorTorqueSignals : SignalPropertiesMoment
+    PitchBearingFrictionSignals : SignalPropertiesMoment
+    PitchBearingStictionSignals : SignalPropertiesMoment
+    BladeOutOfPlaneBendingMomentSignals : SignalPropertiesMoment
+    BladeInPlaneBendingMomentSignals : SignalPropertiesMoment
+    PitchBearingMxSignals : SignalPropertiesMoment
+    PitchBearingMySignals : SignalPropertiesMoment
+    PitchBearingMzSignals : SignalPropertiesMoment
+    PitchBearingRadialForceSignals : SignalPropertiesForce
+    PitchBearingAxialForceSignals : SignalPropertiesForce
+    PitchBearingFxSignals : SignalPropertiesForce
+    PitchBearingFySignals : SignalPropertiesForce
+    BladeStationWindSpeedSignals : SignalPropertiesVelocity
+    BladeStationAngleOfAttackSignals : SignalPropertiesAngle
+    AileronAngleSignals : SignalPropertiesAngle
+    AileronRateSignals : SignalPropertiesAngularVelocity
+    BladeStationPositionXSignals : SignalPropertiesLength
+    BladeStationPositionYSignals : SignalPropertiesLength
+    BladeStationPositionZSignals : SignalPropertiesLength
+    BladeStationPositionXRotationSignals : SignalPropertiesAngle
+    BladeStationPositionYRotationSignals : SignalPropertiesAngle
+    BladeStationPositionZRotationSignals : SignalPropertiesAngle
+    LidarBeamFocalPointVelocitySignals : SignalPropertiesVelocity
+    
     """
 
     RandomNumberSeed: Optional[int] = Field(alias="RandomNumberSeed", default=0)
     TurnOffNoise: Optional[bool] = Field(alias="TurnOffNoise", default=False)
     ShaftPowerSignals: Optional[SignalPropertiesPower] = Field(alias="ShaftPowerSignals", default=None)
     RotorSpeedSignals: Optional[SignalPropertiesAngularVelocity] = Field(alias="RotorSpeedSignals", default=None)
     ElectricalPowerOutputSignals: Optional[SignalPropertiesPower] = Field(alias="ElectricalPowerOutputSignals", default=None)
@@ -158,15 +163,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/mechanical_losses.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/mechanical_losses.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class MechanicalLosses_MechanicalLossesTypeEnum(str, Enum):
     TORQUE_SCALING_LOSSES = "TorqueScalingLosses"
     POWER_SCALING_LOSSES = "PowerScalingLosses"
 
 class MechanicalLosses(Dnv):
     """MechanicalLosses - The common properties for the mechanical losses in the drivetrain.
-
+    
     Attributes:
-        - MechanicalLossesType: MechanicalLosses_MechanicalLossesTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    MechanicalLossesType : MechanicalLosses_MechanicalLossesTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PowerScalingLosses
-        - TorqueScalingLosses
+        PowerScalingLosses
+        TorqueScalingLosses
+    
     """
 
     MechanicalLossesType: Optional[MechanicalLosses_MechanicalLossesTypeEnum] = Field(alias="MechanicalLossesType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'MechanicalLosses'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/midpoint_method_fixed_step.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/midpoint_method_fixed_step.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fixed_step_integrator import FixedStepIntegrator
 
 
 class MidpointMethodFixedStep(FixedStepIntegrator, IntegratorType='MidpointMethodFixedStep'):
     """MidpointMethodFixedStep - Settings for the Midpoint Method Fixed Step integrator.
-
+    
     Attributes:
-        - IntegratorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;MidpointMethodFixedStep&#39;
+    ----------
+    IntegratorType : str, readonly, default='MidpointMethodFixedStep'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'MidpointMethodFixedStep'
+    
     """
 
     IntegratorType: Optional[str] = Field(alias="IntegratorType", default='MidpointMethodFixedStep', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/modal_blade_modelling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/modal_blade_modelling.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,40 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_modelling import BladeModelling
 
 from dnv_bladed_models.structural_mode import StructuralMode
 
 
 class ModalBladeModelling(BladeModelling, BladeModellingType='ModalBladeModelling'):
     """ModalBladeModelling - The properties for the structural modelling of the blade using modal reduction.
-
+    
     Attributes:
-        - WholeBladeModes: List[StructuralMode] => List of StructuralMode
-        - NumberOfModesPerBladePart: int => The number of modes used to represent each blade part, or the whole blade if it is represented by a single multibody object.  This should be sufficiently high to capture all of the relevant blade modes, but the higher it is the slower the simulation will become.
-        - BladeModellingType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ModalBladeModelling&#39;
+    ----------
+    WholeBladeModes : List[StructuralMode], default=list()
+        List of StructuralMode
+    NumberOfModesPerBladePart : int, default=4
+        The number of modes used to represent each blade part, or the whole blade if it is represented by a single multibody object.  This should be sufficiently high to capture all of the relevant blade modes, but the higher it is the slower the simulation will become.
+    BladeModellingType : str, readonly, default='ModalBladeModelling'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ModalBladeModelling'
+    
     """
 
     WholeBladeModes: Optional[List[StructuralMode]] = Field(alias="WholeBladeModes", default=list())
     NumberOfModesPerBladePart: Optional[int] = Field(alias="NumberOfModesPerBladePart", default=4)
     BladeModellingType: Optional[str] = Field(alias="BladeModellingType", default='ModalBladeModelling', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/model_linearisation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/model_linearisation.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,46 +9,50 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.angle_range import AngleRange
 
 from dnv_bladed_models.linearisation_calculation import LinearisationCalculation
 
 from dnv_bladed_models.model_linearisation_perturbations import ModelLinearisationPerturbations
 
 from dnv_bladed_models.velocity_range import VelocityRange
 
 
 class ModelLinearisation(LinearisationCalculation, SteadyCalculationType='ModelLinearisation'):
     """ModelLinearisation - Defines a calculation which produces a linear model.  This can be post-processed into a linearised model of the turbine in state-space form.  This is of particular value in the design of controllers.
-
+    
     Attributes:
-        - Perturbations: ModelLinearisationPerturbations 
-        - WindSpeed: VelocityRange 
-        - Azimuth: AngleRange 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ModelLinearisation&#39;
+    ----------
+    Perturbations : ModelLinearisationPerturbations
+    WindSpeed : VelocityRange
+    Azimuth : AngleRange
+    SteadyCalculationType : str, readonly, default='ModelLinearisation'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ModelLinearisation'
+    
     """
 
     Perturbations: Optional[ModelLinearisationPerturbations] = Field(alias="Perturbations", default=None)
     WindSpeed: Optional[VelocityRange] = Field(alias="WindSpeed", default=None)
     Azimuth: Optional[AngleRange] = Field(alias="Azimuth", default=None)
     SteadyCalculationType: Optional[str] = Field(alias="SteadyCalculationType", default='ModelLinearisation', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/model_linearisation_perturbations.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/model_linearisation_perturbations.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.perturbation_settings import PerturbationSettings
 
 
 class ModelLinearisationPerturbations(PerturbationSettings):
     """ModelLinearisationPerturbations - 
-
+    
     Attributes:
-        - WindSpeedPerturbation: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - PitchPerturbation: float => A number representing an angle.  The SI units for angles are radians.
-        - GeneratorTorquePerturbation: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - WindShearPerturbation: float => A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
-        - ApplyPitchPerturbationToEachBlade: bool => If true, the pitch angle will be perturbed in turn for each blade as well as collectively for all blades
+    ----------
+    WindSpeedPerturbation : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    PitchPerturbation : float
+        A number representing an angle.  The SI units for angles are radians.
+    GeneratorTorquePerturbation : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    WindShearPerturbation : float
+        A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
+    ApplyPitchPerturbationToEachBlade : bool, default=False
+        If true, the pitch angle will be perturbed in turn for each blade as well as collectively for all blades
+    
     """
 
     WindSpeedPerturbation: Optional[float] = Field(alias="WindSpeedPerturbation", default=None)
     PitchPerturbation: Optional[float] = Field(alias="PitchPerturbation", default=None)
     GeneratorTorquePerturbation: Optional[float] = Field(alias="GeneratorTorquePerturbation", default=None)
     WindShearPerturbation: Optional[float] = Field(alias="WindShearPerturbation", default=None)
     ApplyPitchPerturbationToEachBlade: Optional[bool] = Field(alias="ApplyPitchPerturbationToEachBlade", default=False)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/moment_vs_coulomb_friction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/moment_vs_coulomb_friction.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class MomentVsCoulombFriction(Dnv):
     """MomentVsCoulombFriction - The relationship between the Coulomb friction in the yaw bearing and the applied moment.
-
+    
     Attributes:
-        - Moment: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - CoulombFriction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Moment : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    CoulombFriction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Moment: Optional[float] = Field(alias="Moment", default=None)
     CoulombFriction: Optional[float] = Field(alias="CoulombFriction", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/momentum_theory_corrections.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/momentum_theory_corrections.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,53 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.dynamic_wake import DynamicWake
 
 from dnv_bladed_models.glauert_skew_wake_model import GlauertSkewWakeModel
 
 
 class MomentumTheoryCorrections_GlauertCorrectionMethodForHighInductionEnum(str, Enum):
     BLADED = "BLADED"
     NONE = "NONE"
 
 class MomentumTheoryCorrections(Dnv):
     """MomentumTheoryCorrections - The Blade Element Momentum (BEM) theory model [https://www.wikipedia.org/wiki/Blade_element_momentum_theory].
-
+    
     Attributes:
-        - GlauertCorrectionMethodForHighInduction: MomentumTheoryCorrections_GlauertCorrectionMethodForHighInductionEnum => The Glauert correction method for when the rotor has high induction.
-        - GlauertSkewedWakeCorrectionModel: GlauertSkewWakeModel 
-        - InductionFactorsTolerance: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - NoInflowBelowTipSpeedRatio: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - FullInflowAboveTipSpeedRatio: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - DynamicWake: DynamicWake 
-        - IncludeStructuralVelocityInInductionCalculation: bool => If true, the axial structural velocity will be included in the induction calculations. Enabled by default.
+    ----------
+    GlauertCorrectionMethodForHighInduction : MomentumTheoryCorrections_GlauertCorrectionMethodForHighInductionEnum, default='BLADED'
+        The Glauert correction method for when the rotor has high induction.
+    GlauertSkewedWakeCorrectionModel : GlauertSkewWakeModel
+    InductionFactorsTolerance : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    NoInflowBelowTipSpeedRatio : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    FullInflowAboveTipSpeedRatio : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    DynamicWake : DynamicWake, abstract
+    IncludeStructuralVelocityInInductionCalculation : bool, default=True
+        If true, the axial structural velocity will be included in the induction calculations. Enabled by default.
+    
+    Notes:
+    ---------
+    DynamicWake has the following concrete types:
+        - EquilibriumWakeModel
+        - FreeFlowModel
+        - FrozenWakeModel
+        - OyeDynamicWake
+        - PittAndPetersModel
+    
     """
 
     GlauertCorrectionMethodForHighInduction: Optional[MomentumTheoryCorrections_GlauertCorrectionMethodForHighInductionEnum] = Field(alias="GlauertCorrectionMethodForHighInduction", default='BLADED')
     GlauertSkewedWakeCorrectionModel: Optional[GlauertSkewWakeModel] = Field(alias="GlauertSkewedWakeCorrectionModel", default=None)
     InductionFactorsTolerance: Optional[float] = Field(alias="InductionFactorsTolerance", default=None)
     NoInflowBelowTipSpeedRatio: Optional[float] = Field(alias="NoInflowBelowTipSpeedRatio", default=None)
     FullInflowAboveTipSpeedRatio: Optional[float] = Field(alias="FullInflowAboveTipSpeedRatio", default=None)
@@ -48,15 +65,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/mooring_initial_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/mooring_initial_position.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_position import InitialPosition
 
 from dnv_bladed_models.vector3_d import Vector3D
 
 
 class MooringInitialPosition(InitialPosition, InitialConditionType='MooringInitialPosition'):
     """MooringInitialPosition - Initial mooring position.
-
+    
     Attributes:
-        - MooringInitialPosition: Vector3D 
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;MooringInitialPosition&#39;
+    ----------
+    MooringInitialPosition : Vector3D
+    InitialConditionType : str, readonly, default='MooringInitialPosition'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'MooringInitialPosition'
+    
     """
 
     MooringInitialPosition: Optional[Vector3D] = Field(alias="MooringInitialPosition", default=None)
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='MooringInitialPosition', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/multiple_lidar_focal_distances.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/multiple_lidar_focal_distances.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.lidar_focal_distance_control import LidarFocalDistanceControl
 
 
 class MultipleLidarFocalDistances(LidarFocalDistanceControl, FocalDistanceControlType='MultipleFocalDistances'):
     """MultipleLidarFocalDistances - A set number of focal distances at which the Lidar samples.
-
+    
     Attributes:
-        - FocalDistances: List[float] => A list of fixed focal distances.
-        - FocalDistanceControlType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;MultipleFocalDistances&#39;
+    ----------
+    FocalDistances : List[float], default=list()
+        A list of fixed focal distances.
+    FocalDistanceControlType : str, readonly, default='MultipleFocalDistances'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'MultipleFocalDistances'
+    
     """
 
     FocalDistances: Optional[List[float]] = Field(alias="FocalDistances", default=list())
     FocalDistanceControlType: Optional[str] = Field(alias="FocalDistanceControlType", default='MultipleFocalDistances', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 from dnv_bladed_models.main_bearing import MainBearing
 
 from dnv_bladed_models.nacelle_cover import NacelleCover
@@ -24,22 +25,33 @@
 from dnv_bladed_models.nacelle_mass_properties import NacelleMassProperties
 
 from dnv_bladed_models.nacelle_sensor import NacelleSensor
 
 
 class Nacelle(Component, ComponentType='Nacelle'):
     """Nacelle - The definition of a nacelle.  The drivetrain and generator components have separate definitions, but the mass properties of the nacelle includes all of its contents.
-
+    
     Attributes:
-        - MainBearing: MainBearing 
-        - NacelleCover: NacelleCover 
-        - MassProperties: NacelleMassProperties 
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
-        - Sensors: Dict[str, NacelleSensor] => A library of sensor objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Nacelle&#39;
+    ----------
+    MainBearing : MainBearing
+    NacelleCover : NacelleCover, abstract
+    MassProperties : NacelleMassProperties
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    Sensors : Dict[str, NacelleSensor], default=dict()
+        A library of sensor objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
+    ComponentType : str, readonly, default='Nacelle'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Nacelle'
+    
+    Notes:
+    ---------
+    NacelleCover has the following concrete types:
+        - AxisymmetricNacelleCover
+        - CuboidNacelleCover
+    
     """
 
     MainBearing: Optional[MainBearing] = Field(alias="MainBearing", default=None)
     NacelleCover: Optional[NacelleCover] = Field(alias="NacelleCover", default=None)
     MassProperties: Optional[NacelleMassProperties] = Field(alias="MassProperties", default=None)
     ConnectableNodes: Optional[Dict[str, ConnectableNode]] = Field(alias="ConnectableNodes", default=dict())
     Sensors: Optional[Dict[str, NacelleSensor]] = Field(alias="Sensors", default=dict())
@@ -48,15 +60,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_accelerometer.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_accelerometer.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.nacelle_sensor import NacelleSensor
 
 
 class NacelleAccelerometer(NacelleSensor, NacelleSensorType='Accelerometer'):
     """NacelleAccelerometer - An accelerometer mounted on the nacelle.  This can be mounted anywhere on the structure.
-
+    
     Attributes:
-        - NacelleSensorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Accelerometer&#39;
+    ----------
+    NacelleSensorType : str, readonly, default='Accelerometer'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Accelerometer'
+    
     """
 
     NacelleSensorType: Optional[str] = Field(alias="NacelleSensorType", default='Accelerometer', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_centre_of_gravity.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_centre_of_gravity.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class NacelleCentreOfGravity(Dnv):
     """NacelleCentreOfGravity - The offset of the nacelle&#39;s centre of gravity from its origin, provided in the nacelle&#39;s axis system (this is usually the same as the global axis system, assuming that the tower isn&#39;t tilted).
-
+    
     Attributes:
-        - X: float => A number representing a length.  The SI units for length are metres.
-        - Y: float => A number representing a length.  The SI units for length are metres.
-        - Z: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    X : float
+        A number representing a length.  The SI units for length are metres.
+    Y : float
+        A number representing a length.  The SI units for length are metres.
+    Z : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     X: Optional[float] = Field(alias="X", default=None)
     Y: Optional[float] = Field(alias="Y", default=None)
     Z: Optional[float] = Field(alias="Z", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_cover.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_cover.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,31 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class NacelleCover_NacelleCoverTypeEnum(str, Enum):
     CUBOID = "Cuboid"
     AXISYMMETRIC = "Axisymmetric"
 
 class NacelleCover(Dnv):
     """NacelleCover - The common properties for all types of nacelle cover.
-
+    
     Attributes:
-        - Overhang: float => A number representing a length.  The SI units for length are metres.
-        - NacelleCoverType: NacelleCover_NacelleCoverTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    Overhang : float
+        A number representing a length.  The SI units for length are metres.
+    NacelleCoverType : NacelleCover_NacelleCoverTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - AxisymmetricNacelleCover
-        - CuboidNacelleCover
+        AxisymmetricNacelleCover
+        CuboidNacelleCover
+    
     """
 
     Overhang: Optional[float] = Field(alias="Overhang", default=None)
     NacelleCoverType: Optional[NacelleCover_NacelleCoverTypeEnum] = Field(alias="NacelleCoverType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -64,15 +69,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'NacelleCover'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_inclinometer.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_inclinometer.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.nacelle_sensor import NacelleSensor
 
 
 class NacelleInclinometer(NacelleSensor, NacelleSensorType='Inclinometer'):
     """NacelleInclinometer - An inclinometer mounted on the nacelle.  This can be mounted anywhere on the structure.
-
+    
     Attributes:
-        - NacelleSensorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Inclinometer&#39;
+    ----------
+    NacelleSensorType : str, readonly, default='Inclinometer'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Inclinometer'
+    
     """
 
     NacelleSensorType: Optional[str] = Field(alias="NacelleSensorType", default='Inclinometer', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_mass_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_mass_properties.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,42 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.nacelle_centre_of_gravity import NacelleCentreOfGravity
 
 from dnv_bladed_models.nacelle_moment_of_inertia import NacelleMomentOfInertia
 
 
 class NacelleMassProperties(Dnv):
     """NacelleMassProperties - The mass properties of the nacelle and all of its contents, such as the generator or drivetrain.
-
+    
     Attributes:
-        - TotalNacelleMass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - CentreOfGravity: NacelleCentreOfGravity 
-        - MomentOfInertia: NacelleMomentOfInertia 
+    ----------
+    TotalNacelleMass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    CentreOfGravity : NacelleCentreOfGravity
+    MomentOfInertia : NacelleMomentOfInertia
+    
     """
 
     TotalNacelleMass: Optional[float] = Field(alias="TotalNacelleMass", default=None)
     CentreOfGravity: Optional[NacelleCentreOfGravity] = Field(alias="CentreOfGravity", default=None)
     MomentOfInertia: Optional[NacelleMomentOfInertia] = Field(alias="MomentOfInertia", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_moment_of_inertia.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_moment_of_inertia.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class NacelleMomentOfInertia(Dnv):
     """NacelleMomentOfInertia - The total moment of inertias of the nacelle and all of its contents, such as the generator or drivetrain.
-
+    
     Attributes:
-        - Yawing: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - Nodding: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - Rolling: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    ----------
+    Yawing : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    Nodding : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    Rolling : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    
     """
 
     Yawing: Optional[float] = Field(alias="Yawing", default=None)
     Nodding: Optional[float] = Field(alias="Nodding", default=None)
     Rolling: Optional[float] = Field(alias="Rolling", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_sensor.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_sensor.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,33 +9,37 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.vector3_d import Vector3D
 
 
 class NacelleSensor_NacelleSensorTypeEnum(str, Enum):
     ACCELEROMETER = "Accelerometer"
     INCLINOMETER = "Inclinometer"
 
 class NacelleSensor(Dnv):
     """NacelleSensor - The common properties of a sensor mounted on the nacelle.  This can be mounted anywhere on the structure.
-
+    
     Attributes:
-        - OffsetFromOrigin: Vector3D 
-        - NacelleSensorType: NacelleSensor_NacelleSensorTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    OffsetFromOrigin : Vector3D
+    NacelleSensorType : NacelleSensor_NacelleSensorTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - NacelleAccelerometer
-        - NacelleInclinometer
+        NacelleAccelerometer
+        NacelleInclinometer
+    
     """
 
     OffsetFromOrigin: Optional[Vector3D] = Field(alias="OffsetFromOrigin", default=None)
     NacelleSensorType: Optional[NacelleSensor_NacelleSensorTypeEnum] = Field(alias="NacelleSensorType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -66,15 +70,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'NacelleSensor'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/nacelle_vibration.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/nacelle_vibration.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class NacelleVibration(Dnv):
     """NacelleVibration - The definition of the safety system behaviour when the vibration in the nacelle is too high
-
+    
     Attributes:
-        - TripValue: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - Delay: float => A number representing a time.  The SI units for time are seconds.
-        - CircuitToActivate: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    TripValue : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    Delay : float
+        A number representing a time.  The SI units for time are seconds.
+    CircuitToActivate : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     TripValue: Optional[float] = Field(alias="TripValue", default=None)
     Delay: Optional[float] = Field(alias="Delay", default=None)
     CircuitToActivate: Optional[str] = Field(alias="CircuitToActivate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/near_shore_current.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/near_shore_current.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class NearShoreCurrent(Dnv):
     """NearShoreCurrent - The definition of a current which is constant with depth.
-
+    
     Attributes:
-        - Velocity: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - Heading: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    Velocity : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    Heading : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     Velocity: Optional[float] = Field(alias="Velocity", default=None)
     Heading: Optional[float] = Field(alias="Heading", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/near_surface_current.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/near_surface_current.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class NearSurfaceCurrent(Dnv):
     """NearSurfaceCurrent - The definition of a current whose strength decreases linearly with depth.
-
+    
     Attributes:
-        - VelocityAtSurface: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - DepthAtWhichVelocityIsZero: float => A number representing a length.  The SI units for length are metres.
-        - Heading: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    VelocityAtSurface : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    DepthAtWhichVelocityIsZero : float
+        A number representing a length.  The SI units for length are metres.
+    Heading : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     VelocityAtSurface: Optional[float] = Field(alias="VelocityAtSurface", default=None)
     DepthAtWhichVelocityIsZero: Optional[float] = Field(alias="DepthAtWhichVelocityIsZero", default=None)
     Heading: Optional[float] = Field(alias="Heading", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/network_frequency_disturbance.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/network_frequency_disturbance.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fault import Fault
 
 from dnv_bladed_models.time_vs_frequency_change import TimeVsFrequencyChange
 
 
 class NetworkFrequencyDisturbance(Fault, EventType='NetworkFrequencyDisturbance'):
     """NetworkFrequencyDisturbance - A disturbance in the network connection where the grid&#39;s frequency fluctuates at a specified time.
-
+    
     Attributes:
-        - Filepath: str => The filepath or URI of the frequency disturbance data.
-        - TimeVsFrequencyChange: List[TimeVsFrequencyChange] => A series of time vs frequency to apply once the specified time is reached.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;NetworkFrequencyDisturbance&#39;
+    ----------
+    Filepath : str
+        The filepath or URI of the frequency disturbance data.
+    TimeVsFrequencyChange : List[TimeVsFrequencyChange], default=list()
+        A series of time vs frequency to apply once the specified time is reached.
+    EventType : str, readonly, default='NetworkFrequencyDisturbance'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'NetworkFrequencyDisturbance'
+    
     """
 
     Filepath: Optional[str] = Field(alias="Filepath", default=None)
     TimeVsFrequencyChange: Optional[List[TimeVsFrequencyChange]] = Field(alias="TimeVsFrequencyChange", default=list())
     EventType: Optional[str] = Field(alias="EventType", default='NetworkFrequencyDisturbance', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/network_voltage_disturbance.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/network_voltage_disturbance.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,40 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fault import Fault
 
 from dnv_bladed_models.time_vs_voltage_change import TimeVsVoltageChange
 
 
 class NetworkVoltageDisturbance(Fault, EventType='NetworkVoltageDisturbance'):
     """NetworkVoltageDisturbance - A disturbance in the network connection where the grid&#39;s voltage fluctuates at a specified time.
-
+    
     Attributes:
-        - Filepath: str => The filepath or URI of the voltage disturbance data.
-        - TimeVsVoltageChange: List[TimeVsVoltageChange] => A series of time vs voltage to apply once the specified time is reached.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;NetworkVoltageDisturbance&#39;
+    ----------
+    Filepath : str
+        The filepath or URI of the voltage disturbance data.
+    TimeVsVoltageChange : List[TimeVsVoltageChange], default=list()
+        A series of time vs voltage to apply once the specified time is reached.
+    EventType : str, readonly, default='NetworkVoltageDisturbance'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'NetworkVoltageDisturbance'
+    
     """
 
     Filepath: Optional[str] = Field(alias="Filepath", default=None)
     TimeVsVoltageChange: Optional[List[TimeVsVoltageChange]] = Field(alias="TimeVsVoltageChange", default=list())
     EventType: Optional[str] = Field(alias="EventType", default='NetworkVoltageDisturbance', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/non_actuated_yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/non_actuated_yaw_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.yaw_system import YawSystem
 
 
 class NonActuatedYawSystem(YawSystem):
     """NonActuatedYawSystem - Common properties of the simple yaw models that do not model the yaw actuators.
-
+    
     Attributes:
-        - CoulombFriction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    CoulombFriction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     CoulombFriction: Optional[float] = Field(alias="CoulombFriction", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/non_linear_electrical_losses.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/non_linear_electrical_losses.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.electrical_losses import ElectricalLosses
 
 from dnv_bladed_models.input_power_vs_loss import InputPowerVsLoss
 
 
 class NonLinearElectricalLosses(ElectricalLosses, ElectricalLossesType='NonLinearElectricalLosses'):
     """NonLinearElectricalLosses - An electrical loss model where the losses are related to the power being generated, but is not linearly proportional to it. 
-
+    
     Attributes:
-        - InputPowerVsLoss: List[InputPowerVsLoss] => A list of points in a look-up table specifying the relationship between the power and the corresponding losses.
-        - ElectricalLossesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;NonLinearElectricalLosses&#39;
+    ----------
+    InputPowerVsLoss : List[InputPowerVsLoss], default=list()
+        A list of points in a look-up table specifying the relationship between the power and the corresponding losses.
+    ElectricalLossesType : str, readonly, default='NonLinearElectricalLosses'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'NonLinearElectricalLosses'
+    
     """
 
     InputPowerVsLoss: Optional[List[InputPowerVsLoss]] = Field(alias="InputPowerVsLoss", default=list())
     ElectricalLossesType: Optional[str] = Field(alias="ElectricalLossesType", default='NonLinearElectricalLosses', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/non_linear_shaft_brake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/non_linear_shaft_brake.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.brake import Brake
 
 from dnv_bladed_models.time_vs_torque import TimeVsTorque
 
 
 class NonLinearShaftBrake(Brake, BrakeType='NonLinearShaftBrake'):
     """NonLinearShaftBrake - The definition of a shaft brake whose applied torque varies with time.
-
+    
     Attributes:
-        - TimeVsTorque: List[TimeVsTorque] => The relationship between the time following the brake&#39;s activation, and the retarding torque it applies to the shaft.
-        - BrakeType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;NonLinearShaftBrake&#39;
+    ----------
+    TimeVsTorque : List[TimeVsTorque], default=list()
+        The relationship between the time following the brake's activation, and the retarding torque it applies to the shaft.
+    BrakeType : str, readonly, default='NonLinearShaftBrake'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'NonLinearShaftBrake'
+    
     """
 
     TimeVsTorque: Optional[List[TimeVsTorque]] = Field(alias="TimeVsTorque", default=list())
     BrakeType: Optional[str] = Field(alias="BrakeType", default='NonLinearShaftBrake', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/normal_stop_operation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/normal_stop_operation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.controlled_stop import ControlledStop
 
 
 class NormalStopOperation(ControlledStop, EventType='NormalStopOperation'):
     """NormalStopOperation - A request for the controller to stop the turbine or rotor at a specified time in the simulation.  The stop will be performed by the controller.
-
+    
     Attributes:
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;NormalStopOperation&#39;
+    ----------
+    EventType : str, readonly, default='NormalStopOperation'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'NormalStopOperation'
+    
     """
 
     EventType: Optional[str] = Field(alias="EventType", default='NormalStopOperation', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/outputs.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,44 +9,51 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Outputs_FileFormatEnum(str, Enum):
     BINARY = "BINARY"
     ASCII = "ASCII"
 
 class Outputs(Dnv):
     """Outputs - Definition outputs to write for this analysis.
-
+    
     Attributes:
-        - OutputDirectory: str => The output directory for results files.
-        - FileStem: str => A name for the analysis that will be used as the name of all the output files.  If omitted, the run type will be used, such as &#39;powprod&#39;.
-        - FileFormat: Outputs_FileFormatEnum => The output format, whether it is ASCII or binary.
-        - OutputDongleActivity: bool => If true, the dongle activity will be logged.
+    ----------
+    OutputDirectory : str
+        The output directory for results files.
+    FileStem : str, default=''
+        A name for the analysis that will be used as the name of all the output files.  If omitted, the run type will be used, such as 'powprod'.
+    FileFormat : Outputs_FileFormatEnum, default='BINARY'
+        The output format, whether it is ASCII or binary.
+    OutputDongleActivity : bool, default=False
+        If true, the dongle activity will be logged.
+    
     """
 
     OutputDirectory: Optional[str] = Field(alias="OutputDirectory", default=None)
     FileStem: Optional[str] = Field(alias="FileStem", default='')
     FileFormat: Optional[Outputs_FileFormatEnum] = Field(alias="FileFormat", default='BINARY')
     OutputDongleActivity: Optional[bool] = Field(alias="OutputDongleActivity", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/oye_dynamic_wake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/oye_dynamic_wake.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dynamic_wake import DynamicWake
 
 
 class OyeDynamicWake(DynamicWake, DynamicWakeType='OyeDynamicWake'):
-    """OyeDynamicWake - The ye dynamic wake model.
-
+    """OyeDynamicWake - The Oye dynamic wake model captures the lag in induction in response to a change of inflow conditions.  It is the most accurate model for linearisation and time domain, but it does increase linearisation time significantly as it introduces extra states.
+    
     Attributes:
-        - DynamicTangentialInduction: bool => If false, the tangential induction will be solved by iteration on each time step, as per the Pitt and Peters model.
-        - OyeTimeLagMultiplier: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - DynamicWakeType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;OyeDynamicWake&#39;
+    ----------
+    DynamicTangentialInduction : bool, default=True
+        If false, the tangential induction will be solved by iteration on each time step, as per the Pitt and Peters model.
+    OyeTimeLagMultiplier : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    DynamicWakeType : str, readonly, default='OyeDynamicWake'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'OyeDynamicWake'
+    
     """
 
     DynamicTangentialInduction: Optional[bool] = Field(alias="DynamicTangentialInduction", default=True)
     OyeTimeLagMultiplier: Optional[float] = Field(alias="OyeTimeLagMultiplier", default=None)
     DynamicWakeType: Optional[str] = Field(alias="DynamicWakeType", default='OyeDynamicWake', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/oye_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/oye_model.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dynamic_stall import DynamicStall
 
 
 class OyeModel(DynamicStall, DynamicStallType='OyeModel'):
     """OyeModel - The ye dynamic stall model.
-
+    
     Attributes:
-        - DynamicStallType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;OyeModel&#39;
+    ----------
+    DynamicStallType : str, readonly, default='OyeModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'OyeModel'
+    
     """
 
     DynamicStallType: Optional[str] = Field(alias="DynamicStallType", default='OyeModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pallet_mounting_flexibility.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pallet_mounting_flexibility.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,44 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.gearbox_mounting_rolling import GearboxMountingRolling
 
 from dnv_bladed_models.pallet_nodding import PalletNodding
 
 from dnv_bladed_models.pallet_rolling import PalletRolling
 
 
 class PalletMountingFlexibility(Dnv):
     """PalletMountingFlexibility - The parameters defining the flexibility of the drivetrain mounting.
-
+    
     Attributes:
-        - PalletRolling: PalletRolling 
-        - PalletNodding: PalletNodding 
-        - GearboxMountingRolling: GearboxMountingRolling 
+    ----------
+    PalletRolling : PalletRolling
+    PalletNodding : PalletNodding
+    GearboxMountingRolling : GearboxMountingRolling
+    
     """
 
     PalletRolling: Optional[PalletRolling] = Field(alias="PalletRolling", default=None)
     PalletNodding: Optional[PalletNodding] = Field(alias="PalletNodding", default=None)
     GearboxMountingRolling: Optional[GearboxMountingRolling] = Field(alias="GearboxMountingRolling", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pallet_nodding.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pallet_nodding.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PalletNodding(Dnv):
     """PalletNodding - The flexibility of the nodding degree of freedom of the abstract_drivetrain pallet mounting.
-
+    
     Attributes:
-        - Stiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    ----------
+    Stiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    
     """
 
     Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pallet_rolling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pallet_rolling.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PalletRolling(Dnv):
     """PalletRolling - The flexibility of the rolling degree of freedom of the drivetrain pallet mounting.
-
+    
     Attributes:
-        - Stiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    ----------
+    Stiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    
     """
 
     Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/passive.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/passive.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class Passive(ResponseToDemand, ResponseToDemandType='Passive'):
     """Passive - A transfer function representing the response of the pitch controller to the main controller&#39;s demands.
-
+    
     Attributes:
-        - Numerator: List[float] => A list of the numerator terms.  The transfer function must be at least second order.
-        - Denominator: List[float] => A list of the denominator terms.  The transfer function must be at least second order.
-        - ResponseToDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Passive&#39;
+    ----------
+    Numerator : List[float], default=list()
+        A list of the numerator terms.  The transfer function must be at least second order.
+    Denominator : List[float], default=list()
+        A list of the denominator terms.  The transfer function must be at least second order.
+    ResponseToDemandType : str, readonly, default='Passive'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Passive'
+    
     """
 
     Numerator: Optional[List[float]] = Field(alias="Numerator", default=list())
     Denominator: Optional[List[float]] = Field(alias="Denominator", default=list())
     ResponseToDemandType: Optional[str] = Field(alias="ResponseToDemandType", default='Passive', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/passive_yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/passive_yaw_system.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.non_actuated_yaw_system import NonActuatedYawSystem
 
 
 class PassiveYawSystem(NonActuatedYawSystem, ComponentType='PassiveYawSystem'):
     """PassiveYawSystem - The definition of a simple yaw system which has nothing other than a reactive friction opposing any applied moment.  If this friction is overcome, the yaw bearing will move.
-
+    
     Attributes:
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PassiveYawSystem&#39;
+    ----------
+    ComponentType : str, readonly, default='PassiveYawSystem'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PassiveYawSystem'
+    
     """
 
     ComponentType: Optional[str] = Field(alias="ComponentType", default='PassiveYawSystem', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pendulum_damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pendulum_damper.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,38 +9,51 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.angle_vs_stiffness_torque import AngleVsStiffnessTorque
 
 from dnv_bladed_models.damper import Damper
 
 from dnv_bladed_models.pendulum_damper_mounting_position import PendulumDamperMountingPosition
 
 from dnv_bladed_models.velocity_vs_damping_torque import VelocityVsDampingTorque
 
 
 class PendulumDamper(Damper, ComponentType='PendulumDamper'):
     """PendulumDamper - A pendulum or tuned mass damper, which uses a suspended mass to damp oscillations in a tower.
-
+    
     Attributes:
-        - Length: float => A number representing a length.  The SI units for length are metres.
-        - Mass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - Inertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - Stiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
-        - InitialAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - ConstantFriction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - AngleVsStiffnessTorque: List[AngleVsStiffnessTorque] => A look-up table of additional stiffnesses that vary with the pendulum&#39;s position.
-        - VelocityVsDampingTorque: List[VelocityVsDampingTorque] => A look-up table of additional damping that vary with the pendulum&#39;s velocity.
-        - MountingPosition: PendulumDamperMountingPosition 
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PendulumDamper&#39;
+    ----------
+    Length : float
+        A number representing a length.  The SI units for length are metres.
+    Mass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    Inertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    Stiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    InitialAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    ConstantFriction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    AngleVsStiffnessTorque : List[AngleVsStiffnessTorque], default=list()
+        A look-up table of additional stiffnesses that vary with the pendulum's position.
+    VelocityVsDampingTorque : List[VelocityVsDampingTorque], default=list()
+        A look-up table of additional damping that vary with the pendulum's velocity.
+    MountingPosition : PendulumDamperMountingPosition
+    ComponentType : str, readonly, default='PendulumDamper'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PendulumDamper'
+    
     """
 
     Length: Optional[float] = Field(alias="Length", default=None)
     Mass: Optional[float] = Field(alias="Mass", default=None)
     Inertia: Optional[float] = Field(alias="Inertia", default=None)
     Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
@@ -54,15 +67,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pendulum_damper_mounting_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pendulum_damper_mounting_position.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PendulumDamperMountingPosition(Dnv):
     """PendulumDamperMountingPosition - The offset of the pendulum damper from it&#39;s attachment point, in the component&#39;s axis system.
-
+    
     Attributes:
-        - X: float => A number representing a length.  The SI units for length are metres.
-        - Y: float => A number representing a length.  The SI units for length are metres.
-        - Z: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    X : float
+        A number representing a length.  The SI units for length are metres.
+    Y : float
+        A number representing a length.  The SI units for length are metres.
+    Z : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     X: Optional[float] = Field(alias="X", default=None)
     Y: Optional[float] = Field(alias="Y", default=None)
     Z: Optional[float] = Field(alias="Z", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/performance_coefficients_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/performance_coefficients_calculation.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,32 +9,37 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.angle_range import AngleRange
 
 from dnv_bladed_models.dimensionless_range import DimensionlessRange
 
 from dnv_bladed_models.steady_calculation import SteadyCalculation
 
 from dnv_bladed_models.steady_calculation_outputs import SteadyCalculationOutputs
 
 
 class PerformanceCoefficientsCalculation(SteadyCalculation, SteadyCalculationType='PerformanceCoefficients'):
     """PerformanceCoefficientsCalculation - Defines a calculation which produces power, torque and thrust coefficients are calculated in a uniform steady wind field.  The rotor is modelled with flexibility as well as prebend and sweep.  However, the rotor is analysed in isolation without any influence from the rest of the turbine structure.
-
+    
     Attributes:
-        - TipSpeedRatioRange: DimensionlessRange 
-        - PitchRange: AngleRange 
-        - RotorSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Outputs: SteadyCalculationOutputs 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PerformanceCoefficients&#39;
+    ----------
+    TipSpeedRatioRange : DimensionlessRange
+    PitchRange : AngleRange
+    RotorSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Outputs : SteadyCalculationOutputs
+    SteadyCalculationType : str, readonly, default='PerformanceCoefficients'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PerformanceCoefficients'
+    
     """
 
     TipSpeedRatioRange: Optional[DimensionlessRange] = Field(alias="TipSpeedRatioRange", default=None)
     PitchRange: Optional[AngleRange] = Field(alias="PitchRange", default=None)
     RotorSpeed: Optional[float] = Field(alias="RotorSpeed", default=None)
     Outputs: Optional[SteadyCalculationOutputs] = Field(alias="Outputs", default=None)
     SteadyCalculationType: Optional[str] = Field(alias="SteadyCalculationType", default='PerformanceCoefficients', allow_mutation=False)
@@ -42,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/permanently_stuck_pitch_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/permanently_stuck_pitch_system.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.event import Event
 
 
 class PermanentlyStuckPitchSystem(Event, EventType='PermanentlyStuckPitchSystem'):
     """PermanentlyStuckPitchSystem - A pitch fault where the pitch system is stuck for the entire duration of the simulation.
-
+    
     Attributes:
-        - StuckAtAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PermanentlyStuckPitchSystem&#39;
+    ----------
+    StuckAtAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    EventType : str, readonly, default='PermanentlyStuckPitchSystem'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PermanentlyStuckPitchSystem'
+    
     """
 
     StuckAtAngle: Optional[float] = Field(alias="StuckAtAngle", default=None)
     EventType: Optional[str] = Field(alias="EventType", default='PermanentlyStuckPitchSystem', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/perturbation_settings.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/perturbation_settings.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PerturbationSettings(Dnv):
     """PerturbationSettings - 
-
+    
     Attributes:
-        - NumberOfPerturbationPoints: int => The number of values that each state gets perturbed to either side of the equilibrium point.  Larger numbers provide more robustness; smaller numbers provide faster analysis and lower memory usage.
-        - AbsoluteTolerancePerturbationScale: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - StateRelativePerturbation: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    NumberOfPerturbationPoints : int, default=5
+        The number of values that each state gets perturbed to either side of the equilibrium point.  Larger numbers provide more robustness; smaller numbers provide faster analysis and lower memory usage.
+    AbsoluteTolerancePerturbationScale : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    StateRelativePerturbation : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     NumberOfPerturbationPoints: Optional[int] = Field(alias="NumberOfPerturbationPoints", default=5)
     AbsoluteTolerancePerturbationScale: Optional[float] = Field(alias="AbsoluteTolerancePerturbationScale", default=None)
     StateRelativePerturbation: Optional[float] = Field(alias="StateRelativePerturbation", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_acceleration_limits.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_acceleration_limits.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchAccelerationLimits(Dnv):
     """PitchAccelerationLimits - The acceleration limits of the setpoint trajectory planning.
-
+    
     Attributes:
-        - Lower: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - Upper: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    ----------
+    Lower : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    Upper : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    
     """
 
     Lower: Optional[float] = Field(alias="Lower", default=None)
     Upper: Optional[float] = Field(alias="Upper", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_actuation_limited.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_actuation_limited.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,44 +9,51 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.damper_force_or_torque_vs_rate import DamperForceOrTorqueVsRate
 
 from dnv_bladed_models.pitch_controller_torque_safety_system import PitchControllerTorqueSafetySystem
 
 from dnv_bladed_models.spring_force_or_torque_vs_position import SpringForceOrTorqueVsPosition
 
 
 class PitchActuationLimited(PitchControllerTorqueSafetySystem, PitchSafetySystemType='ActuationLimited'):
     """PitchActuationLimited - A safety system where the movement is limited by the available torque.
-
+    
     Attributes:
-        - AppliedForceOrTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - SpringForceOrTorqueVsPosition: List[SpringForceOrTorqueVsPosition] => A look-up of the torque or force applied by a spring in the safety system at a given position.
-        - DamperForceOrTorqueVsRate: List[DamperForceOrTorqueVsRate] => A look-up of the torque or force applied by a damper in the safety system for a given rate.
-        - PitchSafetySystemType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ActuationLimited&#39;
+    ----------
+    AppliedForceOrTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    SpringForceOrTorqueVsPosition : List[SpringForceOrTorqueVsPosition], default=list()
+        A look-up of the torque or force applied by a spring in the safety system at a given position.
+    DamperForceOrTorqueVsRate : List[DamperForceOrTorqueVsRate], default=list()
+        A look-up of the torque or force applied by a damper in the safety system for a given rate.
+    PitchSafetySystemType : str, readonly, default='ActuationLimited'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ActuationLimited'
+    
     """
 
     AppliedForceOrTorque: Optional[float] = Field(alias="AppliedForceOrTorque", default=None)
     SpringForceOrTorqueVsPosition: Optional[List[SpringForceOrTorqueVsPosition]] = Field(alias="SpringForceOrTorqueVsPosition", default=list())
     DamperForceOrTorqueVsRate: Optional[List[DamperForceOrTorqueVsRate]] = Field(alias="DamperForceOrTorqueVsRate", default=list())
     PitchSafetySystemType: Optional[str] = Field(alias="PitchSafetySystemType", default='ActuationLimited', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_actuator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_actuator.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchActuator_ActuatorDriveTypeEnum(str, Enum):
     IDEALISED_ACTUATOR = "IdealisedActuator"
     ROTARY_DRIVE = "RotaryDrive"
     LINEAR_ACTUATOR = "LinearActuator"
 
 class PitchActuator(Dnv):
     """PitchActuator - The common properties of an pitch system&#39;s actuation mechanism.
-
+    
     Attributes:
-        - ActuatorDriveType: PitchActuator_ActuatorDriveTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    ActuatorDriveType : PitchActuator_ActuatorDriveTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - IdealisedPitchActuator
-        - LinearPitchActuator
-        - PitchSystemRotaryDrive
+        IdealisedPitchActuator
+        LinearPitchActuator
+        PitchSystemRotaryDrive
+    
     """
 
     ActuatorDriveType: Optional[PitchActuator_ActuatorDriveTypeEnum] = Field(alias="ActuatorDriveType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -64,15 +68,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'PitchActuator'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_rate_relationship.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_rate_relationship.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.rate_vs_friction_torque import RateVsFrictionTorque
 
 
 class PitchBearingFrictionRateRelationship(Dnv):
     """PitchBearingFrictionRateRelationship - The relationship between the Coulomb friction and the rate at which the bearing is turning.
-
+    
     Attributes:
-        - FrictionTorquePerUnitRate: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - RateVsFrictionTorque: List[RateVsFrictionTorque] => A look-up table describing a non-linear relationship between the angular velocity and the corresponding friction.
+    ----------
+    FrictionTorquePerUnitRate : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    RateVsFrictionTorque : List[RateVsFrictionTorque], default=list()
+        A look-up table describing a non-linear relationship between the angular velocity and the corresponding friction.
+    
     """
 
     FrictionTorquePerUnitRate: Optional[float] = Field(alias="FrictionTorquePerUnitRate", default=None)
     RateVsFrictionTorque: Optional[List[RateVsFrictionTorque]] = Field(alias="RateVsFrictionTorque", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_xy_force_relationship.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_xy_force_relationship.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.applied_xy_force_vs_friction_torque import AppliedXYForceVsFrictionTorque
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchBearingFrictionXYForceRelationship(Dnv):
     """PitchBearingFrictionXYForceRelationship - The relationship between the Coulomb friction and the shear force on the bearing.
-
+    
     Attributes:
-        - FrictionTorquePerUnitAppliedForce: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AppliedForceVsFrictionTorque: List[AppliedXYForceVsFrictionTorque] => A look-up table describing a non-linear relationship between the shear force on the bearing and the corresponding friction.
+    ----------
+    FrictionTorquePerUnitAppliedForce : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AppliedForceVsFrictionTorque : List[AppliedXYForceVsFrictionTorque], default=list()
+        A look-up table describing a non-linear relationship between the shear force on the bearing and the corresponding friction.
+    
     """
 
     FrictionTorquePerUnitAppliedForce: Optional[float] = Field(alias="FrictionTorquePerUnitAppliedForce", default=None)
     AppliedForceVsFrictionTorque: Optional[List[AppliedXYForceVsFrictionTorque]] = Field(alias="AppliedForceVsFrictionTorque", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_xy_moment_relationship.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_xy_moment_relationship.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.applied_moment_vs_friction_torque import AppliedMomentVsFrictionTorque
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchBearingFrictionXYMomentRelationship(Dnv):
     """PitchBearingFrictionXYMomentRelationship - The relationship between the Coulomb friction and the bending moment over the bearing.
-
+    
     Attributes:
-        - FrictionTorquePerUnitAppliedMoment: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AppliedMomentVsFrictionTorque: List[AppliedMomentVsFrictionTorque] => A look-up table describing a non-linear relationship between the bending moment over the bearing and the corresponding friction.
+    ----------
+    FrictionTorquePerUnitAppliedMoment : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AppliedMomentVsFrictionTorque : List[AppliedMomentVsFrictionTorque], default=list()
+        A look-up table describing a non-linear relationship between the bending moment over the bearing and the corresponding friction.
+    
     """
 
     FrictionTorquePerUnitAppliedMoment: Optional[float] = Field(alias="FrictionTorquePerUnitAppliedMoment", default=None)
     AppliedMomentVsFrictionTorque: Optional[List[AppliedMomentVsFrictionTorque]] = Field(alias="AppliedMomentVsFrictionTorque", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_friction_z_force_relationship.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_friction_z_force_relationship.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.applied_z_force_vs_friction_torque import AppliedZForceVsFrictionTorque
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchBearingFrictionZForceRelationship(Dnv):
     """PitchBearingFrictionZForceRelationship - The relationship between the Coulomb friction and the axial force on the bearing.
-
+    
     Attributes:
-        - FrictionTorquePerUnitAppliedForce: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AppliedForceVsFrictionTorque: List[AppliedZForceVsFrictionTorque] => A look-up table describing a non-linear relationship between the axial force on the bearing and the corresponding friction.
+    ----------
+    FrictionTorquePerUnitAppliedForce : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AppliedForceVsFrictionTorque : List[AppliedZForceVsFrictionTorque], default=list()
+        A look-up table describing a non-linear relationship between the axial force on the bearing and the corresponding friction.
+    
     """
 
     FrictionTorquePerUnitAppliedForce: Optional[float] = Field(alias="FrictionTorquePerUnitAppliedForce", default=None)
     AppliedForceVsFrictionTorque: Optional[List[AppliedZForceVsFrictionTorque]] = Field(alias="AppliedForceVsFrictionTorque", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_kinetic_friction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_kinetic_friction.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.pitch_bearing_friction_rate_relationship import PitchBearingFrictionRateRelationship
 
 from dnv_bladed_models.pitch_bearing_friction_xy_force_relationship import PitchBearingFrictionXYForceRelationship
 
 from dnv_bladed_models.pitch_bearing_friction_xy_moment_relationship import PitchBearingFrictionXYMomentRelationship
 
 from dnv_bladed_models.pitch_bearing_friction_z_force_relationship import PitchBearingFrictionZForceRelationship
 
 
 class PitchBearingKineticFriction(Dnv):
     """PitchBearingKineticFriction - The torque resisting movement, once the bearing is in motion.
-
+    
     Attributes:
-        - ConstantFriction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - RelationshipWithXYMoment: PitchBearingFrictionXYMomentRelationship 
-        - RelationshipWithXYForce: PitchBearingFrictionXYForceRelationship 
-        - RelationshipWithZForce: PitchBearingFrictionZForceRelationship 
-        - RelationshipWithRate: PitchBearingFrictionRateRelationship 
+    ----------
+    ConstantFriction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    RelationshipWithXYMoment : PitchBearingFrictionXYMomentRelationship
+    RelationshipWithXYForce : PitchBearingFrictionXYForceRelationship
+    RelationshipWithZForce : PitchBearingFrictionZForceRelationship
+    RelationshipWithRate : PitchBearingFrictionRateRelationship
+    
     """
 
     ConstantFriction: Optional[float] = Field(alias="ConstantFriction", default=None)
     RelationshipWithXYMoment: Optional[PitchBearingFrictionXYMomentRelationship] = Field(alias="RelationshipWithXYMoment", default=None)
     RelationshipWithXYForce: Optional[PitchBearingFrictionXYForceRelationship] = Field(alias="RelationshipWithXYForce", default=None)
     RelationshipWithZForce: Optional[PitchBearingFrictionZForceRelationship] = Field(alias="RelationshipWithZForce", default=None)
     RelationshipWithRate: Optional[PitchBearingFrictionRateRelationship] = Field(alias="RelationshipWithRate", default=None)
@@ -44,15 +48,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_bearing_stiction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_bearing_stiction.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,40 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.friction_torque_vs_stiction_torque import FrictionTorqueVsStictionTorque
 
 
 class PitchBearingStiction(Dnv):
     """PitchBearingStiction - The additional torque resisting the initial movement of the bearing.
-
+    
     Attributes:
-        - ConstantStiction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - StictionTorquePerUnitFrictionTorque: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - FrictionTorqueVsStictionTorque: List[FrictionTorqueVsStictionTorque] => A look-up table describing a non-linear relationship between the friction of the bearing and the corresponding stiction.
+    ----------
+    ConstantStiction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    StictionTorquePerUnitFrictionTorque : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    FrictionTorqueVsStictionTorque : List[FrictionTorqueVsStictionTorque], default=list()
+        A look-up table describing a non-linear relationship between the friction of the bearing and the corresponding stiction.
+    
     """
 
     ConstantStiction: Optional[float] = Field(alias="ConstantStiction", default=None)
     StictionTorquePerUnitFrictionTorque: Optional[float] = Field(alias="StictionTorquePerUnitFrictionTorque", default=None)
     FrictionTorqueVsStictionTorque: Optional[List[FrictionTorqueVsStictionTorque]] = Field(alias="FrictionTorqueVsStictionTorque", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_constant_rate_safety_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_constant_rate_safety_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_controller_rate_safety_system import PitchControllerRateSafetySystem
 
 
 class PitchConstantRateSafetySystem(PitchControllerRateSafetySystem, PitchSafetySystemType='ConstantRate'):
     """PitchConstantRateSafetySystem - A safety system where the pitch system will seek to move at a constant rate once the safety system has been triggered.
-
+    
     Attributes:
-        - Rate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - PitchSafetySystemType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ConstantRate&#39;
+    ----------
+    Rate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    PitchSafetySystemType : str, readonly, default='ConstantRate'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ConstantRate'
+    
     """
 
     Rate: Optional[float] = Field(alias="Rate", default=None)
     PitchSafetySystemType: Optional[str] = Field(alias="PitchSafetySystemType", default='ConstantRate', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_controller.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_controller_rate_safety_system.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,40 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.pitch_safety_system import PitchSafetySystem
 
-from dnv_bladed_models.pitch_system_demand import PitchSystemDemand
 
-
-class PitchController(Dnv):
-    """PitchController - The definition of the pitch controller.  The pitch system generally has its own independent controller (usually physically separate) which received position or rate demands from the main controller.
-
-    Attributes:
-        - Demand: PitchSystemDemand 
-        - SafetySystem: PitchSafetySystem 
+class PitchControllerRateSafetySystem(PitchSafetySystem):
+    """PitchControllerRateSafetySystem - The common properties of any rate-based safety system.
+    
     """
 
-    Demand: Optional[PitchSystemDemand] = Field(alias="Demand", default=None)
-    SafetySystem: Optional[PitchSafetySystem] = Field(alias="SafetySystem", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +100,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchController
+        PitchControllerRateSafetySystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchController.from_file('/path/to/file')
+        >>> model = PitchControllerRateSafetySystem.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +130,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchController
+        PitchControllerRateSafetySystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchController.from_json('{ ... }')
+        >>> model = PitchControllerRateSafetySystem.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +162,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchController
+        PitchControllerRateSafetySystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +194,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchController.update_forward_refs()
+PitchControllerRateSafetySystem.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_controller_rate_safety_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_controller_torque_safety_system.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.pitch_safety_system import PitchSafetySystem
 
+from dnv_bladed_models.pitch_safety_system import PitchSafetySystem
 
-class PitchControllerRateSafetySystem(PitchSafetySystem):
-    """PitchControllerRateSafetySystem - The common properties of any rate-based safety system.
 
-    Attributes:
+class PitchControllerTorqueSafetySystem(PitchSafetySystem):
+    """PitchControllerTorqueSafetySystem - The common properties of a pitch safety system where the motion is dictated by the torque applied.
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -100,25 +100,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchControllerRateSafetySystem
+        PitchControllerTorqueSafetySystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchControllerRateSafetySystem.from_file('/path/to/file')
+        >>> model = PitchControllerTorqueSafetySystem.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -130,25 +130,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchControllerRateSafetySystem
+        PitchControllerTorqueSafetySystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchControllerRateSafetySystem.from_json('{ ... }')
+        >>> model = PitchControllerTorqueSafetySystem.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -162,15 +162,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchControllerRateSafetySystem
+        PitchControllerTorqueSafetySystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -194,8 +194,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchControllerRateSafetySystem.update_forward_refs()
+PitchControllerTorqueSafetySystem.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_controller_torque_safety_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_position_demand.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,32 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.pitch_safety_system import PitchSafetySystem
 
+from dnv_bladed_models.pitch_system_demand import PitchSystemDemand
 
-class PitchControllerTorqueSafetySystem(PitchSafetySystem):
-    """PitchControllerTorqueSafetySystem - The common properties of a pitch safety system where the motion is dictated by the torque applied.
 
+class PitchPositionDemand(PitchSystemDemand, PitchSystemDemandType='Position'):
+    """PitchPositionDemand - A position response, where the pitch controller receives a pitch angle to achieve from the main controller.
+    
     Attributes:
+    ----------
+    PitchSystemDemandType : str, readonly, default='Position'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Position'
+    
     """
 
+    PitchSystemDemandType: Optional[str] = Field(alias="PitchSystemDemandType", default='Position', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -100,25 +106,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchControllerTorqueSafetySystem
+        PitchPositionDemand
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchControllerTorqueSafetySystem.from_file('/path/to/file')
+        >>> model = PitchPositionDemand.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -130,25 +136,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchControllerTorqueSafetySystem
+        PitchPositionDemand
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchControllerTorqueSafetySystem.from_json('{ ... }')
+        >>> model = PitchPositionDemand.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -162,15 +168,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchControllerTorqueSafetySystem
+        PitchPositionDemand
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -194,8 +200,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchControllerTorqueSafetySystem.update_forward_refs()
+PitchPositionDemand.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_end_stops.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_end_stops.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchEndStops(Dnv):
     """PitchEndStops - The pitch angles of end-stops at fine pitch (minimum) and feather pitch (maximum).
-
+    
     Attributes:
-        - MinimumAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - MaximumAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - StiffnessOnceExceeded: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    ----------
+    MinimumAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    MaximumAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    StiffnessOnceExceeded : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    
     """
 
     MinimumAngle: Optional[float] = Field(alias="MinimumAngle", default=None)
     MaximumAngle: Optional[float] = Field(alias="MaximumAngle", default=None)
     StiffnessOnceExceeded: Optional[float] = Field(alias="StiffnessOnceExceeded", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fault import Fault
 
 
 class PitchFault(Fault):
     """PitchFault - A fault in a single pitch system occuring at a specified time.
-
+    
     Attributes:
-        - IsAmenableToSafetySystem: bool => If true, the safety system is able to intervene and take any specified action.  If false, the safety system will not respond to this event.
+    ----------
+    IsAmenableToSafetySystem : bool
+        If true, the safety system is able to intervene and take any specified action.  If false, the safety system will not respond to this event.
+    
     """
 
     IsAmenableToSafetySystem: Optional[bool] = Field(alias="IsAmenableToSafetySystem", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_constant_rate.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_constant_rate.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_fault import PitchFault
 
 
 class PitchFaultConstantRate(PitchFault, EventType='PitchFaultConstantRate'):
     """PitchFaultConstantRate - A fault in a single pitch system where the pitch system moves at a constant rate until it reaches an end stop or the safety system intervenes.
-
+    
     Attributes:
-        - FaultPitchRate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PitchFaultConstantRate&#39;
+    ----------
+    FaultPitchRate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    EventType : str, readonly, default='PitchFaultConstantRate'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PitchFaultConstantRate'
+    
     """
 
     FaultPitchRate: Optional[float] = Field(alias="FaultPitchRate", default=None)
     EventType: Optional[str] = Field(alias="EventType", default='PitchFaultConstantRate', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_constant_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_constant_torque.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_fault import PitchFault
 
 
 class PitchFaultConstantTorque(PitchFault, EventType='PitchFaultConstantTorque'):
     """PitchFaultConstantTorque - A fault in a single pitch system where the actuator applies a constant torque over the bearing.
-
+    
     Attributes:
-        - FaultPitchTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PitchFaultConstantTorque&#39;
+    ----------
+    FaultPitchTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    EventType : str, readonly, default='PitchFaultConstantTorque'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PitchFaultConstantTorque'
+    
     """
 
     FaultPitchTorque: Optional[float] = Field(alias="FaultPitchTorque", default=None)
     EventType: Optional[str] = Field(alias="EventType", default='PitchFaultConstantTorque', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_limp.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_limp.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_fault import PitchFault
 
 
 class PitchFaultLimp(PitchFault, EventType='PitchFaultLimp'):
     """PitchFaultLimp - The failure of a blade&#39;s pitch system at a specified time that leaves it without actuation and free to move.
-
+    
     Attributes:
-        - IncludePitchFriction: bool => If true, the friction defined in the pitch system will be applied to the joint.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PitchFaultLimp&#39;
+    ----------
+    IncludePitchFriction : bool
+        If true, the friction defined in the pitch system will be applied to the joint.
+    EventType : str, readonly, default='PitchFaultLimp'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PitchFaultLimp'
+    
     """
 
     IncludePitchFriction: Optional[bool] = Field(alias="IncludePitchFriction", default=None)
     EventType: Optional[str] = Field(alias="EventType", default='PitchFaultLimp', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_seizure.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_fault_seizure.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_fault import PitchFault
 
 
 class PitchFaultSeizure(PitchFault, EventType='PitchFaultSeizure'):
     """PitchFaultSeizure - The seizure of a blade&#39;s pitch system that occurs at a specified time in the simulation.
-
+    
     Attributes:
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PitchFaultSeizure&#39;
+    ----------
+    EventType : str, readonly, default='PitchFaultSeizure'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PitchFaultSeizure'
+    
     """
 
     EventType: Optional[str] = Field(alias="EventType", default='PitchFaultSeizure', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_fault_seizure_at_angle.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system_output_group.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,57 +9,54 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.conditional_event import ConditionalEvent
 
+from dnv_bladed_models.dnv import Dnv
 
-class PitchFaultSeizureAtAngle(ConditionalEvent, EventType='PitchFaultSeizureAtAngle'):
-    """PitchFaultSeizureAtAngle - The failure of a blade&#39;s pitch system when it passes through a specified angle that leaves it without actuation and free to move.  If the pitch system never passes through the specified angle, no seizure will occur.
 
+class PitchSystemOutputGroup(Dnv):
+    """PitchSystemOutputGroup - An output group definition for the pitch system.
+    
     Attributes:
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
-        - SeizesAsItPassesAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PitchFaultSeizureAtAngle&#39;
+    ----------
+    PitchLoadingOutputs : bool, default=False
+        The outputs selected for the loading on the pitch system.
+    GeneralPitchOutputs : bool, default=False
+        The outputs selected for the control of the pitch system.
+    
     """
 
-    OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
-    SeizesAsItPassesAngle: Optional[float] = Field(alias="SeizesAsItPassesAngle", default=None)
-    EventType: Optional[str] = Field(alias="EventType", default='PitchFaultSeizureAtAngle', allow_mutation=False)
+    PitchLoadingOutputs: Optional[bool] = Field(alias="PitchLoadingOutputs", default=False)
+    GeneralPitchOutputs: Optional[bool] = Field(alias="GeneralPitchOutputs", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
                             allowed_child_vals[child_key] = child_val
                     allowed_vals[key] = allowed_child_vals
                 else:
                     allowed_vals[key] = val
         return allowed_vals
 
-    @validator("OnComponentInAssembly")
-    def OnComponentInAssembly_pattern(cls, value):
-        if value is not None and not re.match(r"^#(\/.+)+$", value):
-            raise ValueError(f"OnComponentInAssembly did not match the expected format (found {value})")
-        return value
-
     def to_json(
         self,
         *, 
         include: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         exclude: Optional[Union['AbstractSetIntStr', 'MappingIntStrAny']] = None, 
         by_alias: bool = True, 
         skip_defaults: Optional[bool] = None, 
@@ -112,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchFaultSeizureAtAngle
+        PitchSystemOutputGroup
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchFaultSeizureAtAngle.from_file('/path/to/file')
+        >>> model = PitchSystemOutputGroup.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -142,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchFaultSeizureAtAngle
+        PitchSystemOutputGroup
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchFaultSeizureAtAngle.from_json('{ ... }')
+        >>> model = PitchSystemOutputGroup.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -174,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchFaultSeizureAtAngle
+        PitchSystemOutputGroup
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -206,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchFaultSeizureAtAngle.update_forward_refs()
+PitchSystemOutputGroup.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_force_limits.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_force_limits.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,40 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.pitch_force_minimum_maximum import PitchForceMinimumMaximum
 
 from dnv_bladed_models.pitch_position_vs_minimum_and_maximum_moment import PitchPositionVsMinimumAndMaximumMoment
 
 
 class PitchForceLimits(Dnv):
     """PitchForceLimits - The limits on the forces that the actuator can apply under normal operation.
-
+    
     Attributes:
-        - Limits: PitchForceMinimumMaximum 
-        - PositionVsLimits: List[PitchPositionVsMinimumAndMaximumMoment] => A list of pitch positions and associated minimum and maximum force limits at those postions.
+    ----------
+    Limits : PitchForceMinimumMaximum
+    PositionVsLimits : List[PitchPositionVsMinimumAndMaximumMoment], default=list()
+        A list of pitch positions and associated minimum and maximum force limits at those postions.
+    
     """
 
     Limits: Optional[PitchForceMinimumMaximum] = Field(alias="Limits", default=None)
     PositionVsLimits: Optional[List[PitchPositionVsMinimumAndMaximumMoment]] = Field(alias="PositionVsLimits", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_force_minimum_maximum.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_force_minimum_maximum.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchForceMinimumMaximum(Dnv):
     """PitchForceMinimumMaximum - The restraining force limits.
-
+    
     Attributes:
-        - Minimum: float => A number representing a force.  The SI units for force are Newtons.
-        - Maximum: float => A number representing a force.  The SI units for force are Newtons.
+    ----------
+    Minimum : float
+        A number representing a force.  The SI units for force are Newtons.
+    Maximum : float
+        A number representing a force.  The SI units for force are Newtons.
+    
     """
 
     Minimum: Optional[float] = Field(alias="Minimum", default=None)
     Maximum: Optional[float] = Field(alias="Maximum", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_limit_switches.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_limit_switches.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.pitch_safety_limit_switches import PitchSafetyLimitSwitches
 
 from dnv_bladed_models.standard_pitch_limit_switches import StandardPitchLimitSwitches
 
 
 class PitchLimitSwitches(Dnv):
     """PitchLimitSwitches - The definition of the limit switches for the standard control system and the safety system.
-
+    
     Attributes:
-        - Safety: PitchSafetyLimitSwitches 
-        - Standard: StandardPitchLimitSwitches 
+    ----------
+    Safety : PitchSafetyLimitSwitches
+    Standard : StandardPitchLimitSwitches
+    
     """
 
     Safety: Optional[PitchSafetyLimitSwitches] = Field(alias="Safety", default=None)
     Standard: Optional[StandardPitchLimitSwitches] = Field(alias="Standard", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_position_demand.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_demand.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.pitch_system_demand import PitchSystemDemand
 
+from dnv_bladed_models.pitch_system_demand import PitchSystemDemand
 
-class PitchPositionDemand(PitchSystemDemand, PitchSystemDemandType='Position'):
-    """PitchPositionDemand - A position response, where the pitch controller receives a pitch angle to achieve from the main controller.
 
+class PitchRateDemand(PitchSystemDemand, PitchSystemDemandType='Rate'):
+    """PitchRateDemand - A rate response, where the pitch controller receives a pitch rate to achieve from the main controller.
+    
     Attributes:
-        - PitchSystemDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Position&#39;
+    ----------
+    PitchSystemDemandType : str, readonly, default='Rate'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Rate'
+    
     """
 
-    PitchSystemDemandType: Optional[str] = Field(alias="PitchSystemDemandType", default='Position', allow_mutation=False)
+    PitchSystemDemandType: Optional[str] = Field(alias="PitchSystemDemandType", default='Rate', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -102,25 +106,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchPositionDemand
+        PitchRateDemand
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchPositionDemand.from_file('/path/to/file')
+        >>> model = PitchRateDemand.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -132,25 +136,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchPositionDemand
+        PitchRateDemand
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchPositionDemand.from_json('{ ... }')
+        >>> model = PitchRateDemand.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -164,15 +168,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchPositionDemand
+        PitchRateDemand
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -196,8 +200,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchPositionDemand.update_forward_refs()
+PitchRateDemand.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_position_vs_minimum_and_maximum_moment.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_position_vs_minimum_and_maximum_moment.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchPositionVsMinimumAndMaximumMoment(Dnv):
     """PitchPositionVsMinimumAndMaximumMoment - The limits on the torque of the rotary drive as they vary with position.
-
+    
     Attributes:
-        - Position: float => A number representing an angle.  The SI units for angles are radians.
-        - Minimum: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - Maximum: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Position : float
+        A number representing an angle.  The SI units for angles are radians.
+    Minimum : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    Maximum : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Position: Optional[float] = Field(alias="Position", default=None)
     Minimum: Optional[float] = Field(alias="Minimum", default=None)
     Maximum: Optional[float] = Field(alias="Maximum", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_demand.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_limits.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,34 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.pitch_system_demand import PitchSystemDemand
 
+from dnv_bladed_models.dnv import Dnv
 
-class PitchRateDemand(PitchSystemDemand, PitchSystemDemandType='Rate'):
-    """PitchRateDemand - A rate response, where the pitch controller receives a pitch rate to achieve from the main controller.
 
+class PitchRateLimits(Dnv):
+    """PitchRateLimits - The rate limits of the setpoint trajectory planning.
+    
     Attributes:
-        - PitchSystemDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Rate&#39;
+    ----------
+    Lower : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Upper : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    
     """
 
-    PitchSystemDemandType: Optional[str] = Field(alias="PitchSystemDemandType", default='Rate', allow_mutation=False)
+    Lower: Optional[float] = Field(alias="Lower", default=None)
+    Upper: Optional[float] = Field(alias="Upper", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -102,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchRateDemand
+        PitchRateLimits
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchRateDemand.from_file('/path/to/file')
+        >>> model = PitchRateLimits.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -132,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchRateDemand
+        PitchRateLimits
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchRateDemand.from_json('{ ... }')
+        >>> model = PitchRateLimits.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -164,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchRateDemand
+        PitchRateLimits
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -196,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchRateDemand.update_forward_refs()
+PitchRateLimits.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_demand_set_by_external_controller.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_demand_set_by_external_controller.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_controller_rate_safety_system import PitchControllerRateSafetySystem
 
 
 class PitchRateDemandSetByExternalController(PitchControllerRateSafetySystem, PitchSafetySystemType='RateDemandSetByExternalController'):
     """PitchRateDemandSetByExternalController - A pitch safety system where the rate is specified by the main controller.
-
+    
     Attributes:
-        - PitchSafetySystemType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RateDemandSetByExternalController&#39;
+    ----------
+    PitchSafetySystemType : str, readonly, default='RateDemandSetByExternalController'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RateDemandSetByExternalController'
+    
     """
 
     PitchSafetySystemType: Optional[str] = Field(alias="PitchSafetySystemType", default='RateDemandSetByExternalController', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_limits.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_frequency_change.py`

 * *Files 9% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class PitchRateLimits(Dnv):
-    """PitchRateLimits - The rate limits of the setpoint trajectory planning.
 
+class TimeVsFrequencyChange(Dnv):
+    """TimeVsFrequencyChange - A single point in the time vs frequency time history.
+    
     Attributes:
-        - Lower: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Upper: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    ----------
+    Time : float
+        The time after the disturbance starts.  The first point in the series should be at Time=0.0.
+    FrequencyChange : float
+        The change in frequency at the corresponding time.    0.0 would represent no disturbance in the frequency.
+    
     """
 
-    Lower: Optional[float] = Field(alias="Lower", default=None)
-    Upper: Optional[float] = Field(alias="Upper", default=None)
+    Time: Optional[float] = Field(alias="Time", default=None)
+    FrequencyChange: Optional[float] = Field(alias="FrequencyChange", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchRateLimits
+        TimeVsFrequencyChange
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchRateLimits.from_file('/path/to/file')
+        >>> model = TimeVsFrequencyChange.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchRateLimits
+        TimeVsFrequencyChange
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchRateLimits.from_json('{ ... }')
+        >>> model = TimeVsFrequencyChange.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchRateLimits
+        TimeVsFrequencyChange
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchRateLimits.update_forward_refs()
+TimeVsFrequencyChange.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_varies_with_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_varies_with_position.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_controller_rate_safety_system import PitchControllerRateSafetySystem
 
 from dnv_bladed_models.rate_vs_position import RateVsPosition
 
 
 class PitchRateVariesWithPosition(PitchControllerRateSafetySystem, PitchSafetySystemType='RateVariesWithPosition'):
     """PitchRateVariesWithPosition - A safety system where the pitch system will adjust its rate dependent on the position of the bearing once the safety system has been triggered.
-
+    
     Attributes:
-        - PositionVsRate: List[RateVsPosition] => A look-up table of the rate versus the position of the bearing.
-        - PitchSafetySystemType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RateVariesWithPosition&#39;
+    ----------
+    PositionVsRate : List[RateVsPosition], default=list()
+        A look-up table of the rate versus the position of the bearing.
+    PitchSafetySystemType : str, readonly, default='RateVariesWithPosition'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RateVariesWithPosition'
+    
     """
 
     PositionVsRate: Optional[List[RateVsPosition]] = Field(alias="PositionVsRate", default=list())
     PitchSafetySystemType: Optional[str] = Field(alias="PitchSafetySystemType", default='RateVariesWithPosition', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_varies_with_time.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_varies_with_time.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_controller_rate_safety_system import PitchControllerRateSafetySystem
 
 from dnv_bladed_models.rate_vs_time import RateVsTime
 
 
 class PitchRateVariesWithTime(PitchControllerRateSafetySystem, PitchSafetySystemType='RateVariesWithTime'):
     """PitchRateVariesWithTime - A safety system where the pitch system will adjust its rate dependent on the time after which the safety system has been triggered.
-
+    
     Attributes:
-        - TimeVsRate: List[RateVsTime] => A look-up table of the rate versus the time after the safety system has been triggered.
-        - PitchSafetySystemType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RateVariesWithTime&#39;
+    ----------
+    TimeVsRate : List[RateVsTime], default=list()
+        A look-up table of the rate versus the time after the safety system has been triggered.
+    PitchSafetySystemType : str, readonly, default='RateVariesWithTime'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RateVariesWithTime'
+    
     """
 
     TimeVsRate: Optional[List[RateVsTime]] = Field(alias="TimeVsRate", default=list())
     PitchSafetySystemType: Optional[str] = Field(alias="PitchSafetySystemType", default='RateVariesWithTime', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_rate_vs_minimum_and_maximum_moment.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_rate_vs_minimum_and_maximum_moment.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchRateVsMinimumAndMaximumMoment(Dnv):
     """PitchRateVsMinimumAndMaximumMoment - The limits on the torque of the rotary drive as they vary with the angular velocity.
-
+    
     Attributes:
-        - Rate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Minimum: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - Maximum: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Rate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Minimum : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    Maximum : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Rate: Optional[float] = Field(alias="Rate", default=None)
     Minimum: Optional[float] = Field(alias="Minimum", default=None)
     Maximum: Optional[float] = Field(alias="Maximum", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_safety_limit_switches.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_safety_limit_switches.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchSafetyLimitSwitches(Dnv):
     """PitchSafetyLimitSwitches - The limit switch positions of the safety system.
-
+    
     Attributes:
-        - MinimumAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - MaximumAngle: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    MinimumAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    MaximumAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     MinimumAngle: Optional[float] = Field(alias="MinimumAngle", default=None)
     MaximumAngle: Optional[float] = Field(alias="MaximumAngle", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_safety_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_safety_system.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,39 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchSafetySystem_PitchSafetySystemTypeEnum(str, Enum):
     CONSTANT_RATE = "ConstantRate"
     RATE_DEMAND_SET_BY_EXTERNAL_CONTROLLER = "RateDemandSetByExternalController"
     RATE_VARIES_WITH_POSITION = "RateVariesWithPosition"
     RATE_VARIES_WITH_TIME = "RateVariesWithTime"
     TORQUE_SET_BY_EXTERNAL_CONTROLLER = "TorqueSetByExternalController"
     ACTUATION_LIMITED = "ActuationLimited"
 
 class PitchSafetySystem(Dnv):
     """PitchSafetySystem - The common properties of a pitch controller&#39;s safety system.
-
+    
     Attributes:
-        - AlwaysUseBackupPower: bool => Backup power is normally only used in a grid loss.  If true, this will trigger a switch to backup power torque limits in all safety system pitch action events.
-        - PitchSafetySystemType: PitchSafetySystem_PitchSafetySystemTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    AlwaysUseBackupPower : bool, default=False
+        Backup power is normally only used in a grid loss.  If true, this will trigger a switch to backup power torque limits in all safety system pitch action events.
+    PitchSafetySystemType : PitchSafetySystem_PitchSafetySystemTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PitchActuationLimited
-        - PitchConstantRateSafetySystem
-        - PitchRateDemandSetByExternalController
-        - PitchRateVariesWithPosition
-        - PitchRateVariesWithTime
-        - PitchTorqueSetByExternalController
+        PitchActuationLimited
+        PitchConstantRateSafetySystem
+        PitchRateDemandSetByExternalController
+        PitchRateVariesWithPosition
+        PitchRateVariesWithTime
+        PitchTorqueSetByExternalController
+    
     """
 
     AlwaysUseBackupPower: Optional[bool] = Field(alias="AlwaysUseBackupPower", default=False)
     PitchSafetySystemType: Optional[PitchSafetySystem_PitchSafetySystemTypeEnum] = Field(alias="PitchSafetySystemType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -72,15 +77,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'PitchSafetySystem'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 from dnv_bladed_models.friction import Friction
 
 from dnv_bladed_models.pitch_actuator import PitchActuator
@@ -28,28 +29,44 @@
 from dnv_bladed_models.pitch_limit_switches import PitchLimitSwitches
 
 from dnv_bladed_models.pitch_system_output_group import PitchSystemOutputGroup
 
 
 class PitchSystem(Component, ComponentType='PitchSystem'):
     """PitchSystem - A pitch system, including bearing, actuation, and independent control system.
-
+    
     Attributes:
-        - PitchController: PitchController 
-        - LimitSwitches: PitchLimitSwitches 
-        - EndStops: PitchEndStops 
-        - Bearing: Friction 
-        - Actuator: PitchActuator 
-        - MinimumPitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - MaximumPitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - MaximumNegativePitchRate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - MaximumPositivePitchRate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - OutputGroups: Dict[str, PitchSystemOutputGroup] => A library which contains any number of named output groups.  These can be referenced from &#39;SelectedOutputGroup&#39;.
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PitchSystem&#39;
+    ----------
+    PitchController : PitchController
+    LimitSwitches : PitchLimitSwitches
+    EndStops : PitchEndStops
+    Bearing : Friction
+    Actuator : PitchActuator, abstract
+    MinimumPitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    MaximumPitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    MaximumNegativePitchRate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    MaximumPositivePitchRate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    OutputGroups : Dict[str, PitchSystemOutputGroup], default=dict()
+        A library which contains any number of named output groups.  These can be referenced from 'SelectedOutputGroup'.
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    ComponentType : str, readonly, default='PitchSystem'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PitchSystem'
+    
+    Notes:
+    ---------
+    Actuator has the following concrete types:
+        - IdealisedPitchActuator
+        - LinearPitchActuator
+        - PitchSystemRotaryDrive
+    
     """
 
     PitchController: Optional[PitchController] = Field(alias="PitchController", default=None)
     LimitSwitches: Optional[PitchLimitSwitches] = Field(alias="LimitSwitches", default=None)
     EndStops: Optional[PitchEndStops] = Field(alias="EndStops", default=None)
     Bearing: Optional[Friction] = Field(alias="Bearing", default=None)
     Actuator: Optional[PitchActuator] = Field(alias="Actuator", default=None)
@@ -64,15 +81,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system_demand.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system_demand.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,33 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class PitchSystemDemand_PitchSystemDemandTypeEnum(str, Enum):
     POSITION = "Position"
     RATE = "Rate"
 
 class PitchSystemDemand(Dnv):
     """PitchSystemDemand - The common propeties of the pitch and rate demand responses.
-
+    
     Attributes:
-        - ResponseToDemand: ResponseToDemand 
-        - PitchSystemDemandType: PitchSystemDemand_PitchSystemDemandTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    ResponseToDemand : ResponseToDemand, abstract
+    PitchSystemDemandType : PitchSystemDemand_PitchSystemDemandTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PitchPositionDemand
-        - PitchRateDemand
+        PitchPositionDemand
+        PitchRateDemand
+    
+    Notes:
+    ---------
+    ResponseToDemand has the following concrete types:
+        - FirstOrderResponse
+        - Instantaneous
+        - Passive
+        - ProportionalIntegralDeriviative
+        - SecondOrderResponse
+        - SetpointTrajectoryPlanning
+    
     """
 
     ResponseToDemand: Optional[ResponseToDemand] = Field(alias="ResponseToDemand", default=None)
     PitchSystemDemandType: Optional[PitchSystemDemand_PitchSystemDemandTypeEnum] = Field(alias="PitchSystemDemandType", default=None)
 
     class Config:
         extra = Extra.forbid
@@ -66,15 +80,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'PitchSystemDemand'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/point_on_span.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,36 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class PitchSystemOutputGroup(Dnv):
-    """PitchSystemOutputGroup - An output group definition for the pitch system.
 
+class PointOnSpan(Dnv):
+    """PointOnSpan - A position along the span of the blade, defined either in full 3D, or as if the blade had been flattened into 2D (i.e. disregarding pre-bend and pre-sweep).
+    
     Attributes:
-        - PitchLoadingOutputs: bool => The outputs selected for the loading on the pitch system.
-        - GeneralPitchOutputs: bool => The outputs selected for the control of the pitch system.
+    ----------
+    X : float
+        A number representing a length.  The SI units for length are metres.
+    Y : float
+        A number representing a length.  The SI units for length are metres.
+    Z : float
+        A number representing a length.  The SI units for length are metres.
+    DistanceAlongSpan : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
-    PitchLoadingOutputs: Optional[bool] = Field(alias="PitchLoadingOutputs", default=False)
-    GeneralPitchOutputs: Optional[bool] = Field(alias="GeneralPitchOutputs", default=False)
+    X: Optional[float] = Field(alias="X", default=None)
+    Y: Optional[float] = Field(alias="Y", default=None)
+    Z: Optional[float] = Field(alias="Z", default=None)
+    DistanceAlongSpan: Optional[float] = Field(alias="DistanceAlongSpan", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +115,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PitchSystemOutputGroup
+        PointOnSpan
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchSystemOutputGroup.from_file('/path/to/file')
+        >>> model = PointOnSpan.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +145,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PitchSystemOutputGroup
+        PointOnSpan
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PitchSystemOutputGroup.from_json('{ ... }')
+        >>> model = PointOnSpan.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +177,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PitchSystemOutputGroup
+        PointOnSpan
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +209,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PitchSystemOutputGroup.update_forward_refs()
+PointOnSpan.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_system_rotary_drive.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_system_rotary_drive.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,37 +9,55 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.linear_spring_damper import LinearSpringDamper
 
 from dnv_bladed_models.pitch_actuator import PitchActuator
 
 from dnv_bladed_models.pitch_torque_limits import PitchTorqueLimits
 
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class PitchSystemRotaryDrive(PitchActuator, ActuatorDriveType='RotaryDrive'):
     """PitchSystemRotaryDrive - A rotary pitch actuation system.
-
+    
     Attributes:
-        - TorqueResponse: ResponseToDemand 
-        - GearRatio: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - GearEfficiency: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - MotorInertia: float => A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
-        - BrakeTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - TorqueLimits: PitchTorqueLimits 
-        - BackupPowerTorqueLimits: PitchTorqueLimits 
-        - TorqueLimitsOnceSafetySystemTripped: PitchTorqueLimits 
-        - LinearSpringDamper: LinearSpringDamper 
-        - ActuatorDriveType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RotaryDrive&#39;
+    ----------
+    TorqueResponse : ResponseToDemand, abstract
+    GearRatio : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    GearEfficiency : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    MotorInertia : float
+        A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
+    BrakeTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    TorqueLimits : PitchTorqueLimits
+    BackupPowerTorqueLimits : PitchTorqueLimits
+    TorqueLimitsOnceSafetySystemTripped : PitchTorqueLimits
+    LinearSpringDamper : LinearSpringDamper
+    ActuatorDriveType : str, readonly, default='RotaryDrive'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RotaryDrive'
+    
+    Notes:
+    ---------
+    TorqueResponse has the following concrete types:
+        - FirstOrderResponse
+        - Instantaneous
+        - Passive
+        - ProportionalIntegralDeriviative
+        - SecondOrderResponse
+        - SetpointTrajectoryPlanning
+    
     """
 
     TorqueResponse: Optional[ResponseToDemand] = Field(alias="TorqueResponse", default=None)
     GearRatio: Optional[float] = Field(alias="GearRatio", default=None)
     GearEfficiency: Optional[float] = Field(alias="GearEfficiency", default=None)
     MotorInertia: Optional[float] = Field(alias="MotorInertia", default=None)
     BrakeTorque: Optional[float] = Field(alias="BrakeTorque", default=None)
@@ -52,15 +70,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_torque_limits.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_torque_limits.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,44 +9,49 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.pitch_position_vs_minimum_and_maximum_moment import PitchPositionVsMinimumAndMaximumMoment
 
 from dnv_bladed_models.pitch_rate_vs_minimum_and_maximum_moment import PitchRateVsMinimumAndMaximumMoment
 
 from dnv_bladed_models.pitch_torque_minimum_maximum import PitchTorqueMinimumMaximum
 
 
 class PitchTorqueLimits(Dnv):
     """PitchTorqueLimits - The limits on the torque produced by the rotary drive.
-
+    
     Attributes:
-        - Limits: PitchTorqueMinimumMaximum 
-        - PositionVsLimits: List[PitchPositionVsMinimumAndMaximumMoment] => The limits on the torque of the rotary drive as they vary with position.
-        - RateVsLimits: List[PitchRateVsMinimumAndMaximumMoment] => The limits on the torque of the rotary drive as they vary with the angular velocity.
+    ----------
+    Limits : PitchTorqueMinimumMaximum
+    PositionVsLimits : List[PitchPositionVsMinimumAndMaximumMoment], default=list()
+        The limits on the torque of the rotary drive as they vary with position.
+    RateVsLimits : List[PitchRateVsMinimumAndMaximumMoment], default=list()
+        The limits on the torque of the rotary drive as they vary with the angular velocity.
+    
     """
 
     Limits: Optional[PitchTorqueMinimumMaximum] = Field(alias="Limits", default=None)
     PositionVsLimits: Optional[List[PitchPositionVsMinimumAndMaximumMoment]] = Field(alias="PositionVsLimits", default=list())
     RateVsLimits: Optional[List[PitchRateVsMinimumAndMaximumMoment]] = Field(alias="RateVsLimits", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_torque_minimum_maximum.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_torque_minimum_maximum.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PitchTorqueMinimumMaximum(Dnv):
     """PitchTorqueMinimumMaximum - The constant limits on the torque produced by the rotary drive.
-
+    
     Attributes:
-        - Minimum: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - Maximum: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Minimum : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    Maximum : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Minimum: Optional[float] = Field(alias="Minimum", default=None)
     Maximum: Optional[float] = Field(alias="Maximum", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitch_torque_set_by_external_controller.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_torque_set_by_external_controller.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_controller_torque_safety_system import PitchControllerTorqueSafetySystem
 
 
 class PitchTorqueSetByExternalController(PitchControllerTorqueSafetySystem, PitchSafetySystemType='TorqueSetByExternalController'):
     """PitchTorqueSetByExternalController - A pitch safety system where the torque is specified by the main controller.
-
+    
     Attributes:
-        - PitchSafetySystemType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TorqueSetByExternalController&#39;
+    ----------
+    PitchSafetySystemType : str, readonly, default='TorqueSetByExternalController'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TorqueSetByExternalController'
+    
     """
 
     PitchSafetySystemType: Optional[str] = Field(alias="PitchSafetySystemType", default='TorqueSetByExternalController', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/pitt_and_peters_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitt_and_peters_model.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dynamic_wake import DynamicWake
 
 
 class PittAndPetersModel(DynamicWake, DynamicWakeType='PittAndPetersModel'):
     """PittAndPetersModel - The Pitt and Peters dynamic wake model.
-
+    
     Attributes:
-        - DynamicWakeType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PittAndPetersModel&#39;
+    ----------
+    DynamicWakeType : str, readonly, default='PittAndPetersModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PittAndPetersModel'
+    
     """
 
     DynamicWakeType: Optional[str] = Field(alias="DynamicWakeType", default='PittAndPetersModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/point_inertia.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault_limp.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,44 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.added_inertia import AddedInertia
 
-from dnv_bladed_models.vector3_d import Vector3D
+from dnv_bladed_models.yaw_fault import YawFault
 
 
-class PointInertia(AddedInertia, AddedInertiaType='PointInertia'):
-    """PointInertia - An inertia added to a structural component.
-
+class YawFaultLimp(YawFault, EventType='YawFaultLimp'):
+    """YawFaultLimp - A fault in the yaw system where the yaw system is left without actuation.  The reaction torques are specified, and if these forces are overcome, the yaw bearing will move.
+    
     Attributes:
-        - Mass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - Offset: Vector3D 
-        - MomentOfInertiaTensor: List[float] => Moment of inertia tensor acting about the centre of mass. This tensor is defined using the local coordinate system of the attachment node.
-        - CompleteInertiaTensor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AddedInertiaType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PointInertia&#39;
+    ----------
+    Friction : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Stiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    Damping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    EventType : str, readonly, default='YawFaultLimp'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'YawFaultLimp'
+    
     """
 
-    Mass: Optional[float] = Field(alias="Mass", default=None)
-    Offset: Optional[Vector3D] = Field(alias="Offset", default=None)
-    MomentOfInertiaTensor: Optional[List[float]] = Field(alias="MomentOfInertiaTensor", default=list())
-    CompleteInertiaTensor: Optional[float] = Field(alias="CompleteInertiaTensor", default=None)
-    AddedInertiaType: Optional[str] = Field(alias="AddedInertiaType", default='PointInertia', allow_mutation=False)
+    Friction: Optional[float] = Field(alias="Friction", default=None)
+    Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
+    Damping: Optional[float] = Field(alias="Damping", default=None)
+    EventType: Optional[str] = Field(alias="EventType", default='YawFaultLimp', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -112,25 +115,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PointInertia
+        YawFaultLimp
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PointInertia.from_file('/path/to/file')
+        >>> model = YawFaultLimp.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -142,25 +145,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PointInertia
+        YawFaultLimp
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PointInertia.from_json('{ ... }')
+        >>> model = YawFaultLimp.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -174,15 +177,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PointInertia
+        YawFaultLimp
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -206,8 +209,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PointInertia.update_forward_refs()
+YawFaultLimp.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/point_on_span.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/shaft_speed_vs_power_loss.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,40 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class PointOnSpan(Dnv):
-    """PointOnSpan - A position along the span of the blade, defined either in full 3D, or as if the blade had been flattened into 2D (i.e. disregarding pre-bend and pre-sweep).
 
+class ShaftSpeedVsPowerLoss(Dnv):
+    """ShaftSpeedVsPowerLoss - A line or row in a look-up table, specifying a value of PowerLoss for a specified ShaftSpeed.
+    
     Attributes:
-        - X: float => A number representing a length.  The SI units for length are metres.
-        - Y: float => A number representing a length.  The SI units for length are metres.
-        - Z: float => A number representing a length.  The SI units for length are metres.
-        - DistanceAlongSpan: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    ShaftSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    PowerLoss : float
+        A number representing a power.  The SI units for enery are Watts.
+    
     """
 
-    X: Optional[float] = Field(alias="X", default=None)
-    Y: Optional[float] = Field(alias="Y", default=None)
-    Z: Optional[float] = Field(alias="Z", default=None)
-    DistanceAlongSpan: Optional[float] = Field(alias="DistanceAlongSpan", default=None)
+    ShaftSpeed: Optional[float] = Field(alias="ShaftSpeed", default=None)
+    PowerLoss: Optional[float] = Field(alias="PowerLoss", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        PointOnSpan
+        ShaftSpeedVsPowerLoss
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PointOnSpan.from_file('/path/to/file')
+        >>> model = ShaftSpeedVsPowerLoss.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        PointOnSpan
+        ShaftSpeedVsPowerLoss
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = PointOnSpan.from_json('{ ... }')
+        >>> model = ShaftSpeedVsPowerLoss.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        PointOnSpan
+        ShaftSpeedVsPowerLoss
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-PointOnSpan.update_forward_refs()
+ShaftSpeedVsPowerLoss.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/position_of_upwind_turbine.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/position_of_upwind_turbine.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class PositionOfUpwindTurbine(Dnv):
     """PositionOfUpwindTurbine - 
-
+    
     Attributes:
-        - DistanceUpwind: float => A number representing a length.  The SI units for length are metres.
-        - OffsetY: float => A number representing a length.  The SI units for length are metres.
-        - RelativeHubHeight: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    DistanceUpwind : float
+        A number representing a length.  The SI units for length are metres.
+    OffsetY : float
+        A number representing a length.  The SI units for length are metres.
+    RelativeHubHeight : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     DistanceUpwind: Optional[float] = Field(alias="DistanceUpwind", default=None)
     OffsetY: Optional[float] = Field(alias="OffsetY", default=None)
     RelativeHubHeight: Optional[float] = Field(alias="RelativeHubHeight", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/power_scaling_losses.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/power_scaling_losses.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.mechanical_losses import MechanicalLosses
 
 from dnv_bladed_models.shaft_speed_vs_power_loss import ShaftSpeedVsPowerLoss
 
 
 class PowerScalingLosses(MechanicalLosses, MechanicalLossesType='PowerScalingLosses'):
     """PowerScalingLosses - A loss model where the losses are expressed as a power loss which varies with the shaft speed and the input torque.
-
+    
     Attributes:
-        - InputTorqueVsShaftSpeedVsPowerLoss: Dict[str, ShaftSpeedVsPowerLoss] => A series of look-up tables for the losses, each valid for the specified input torque.
-        - MechanicalLossesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PowerScalingLosses&#39;
+    ----------
+    InputTorqueVsShaftSpeedVsPowerLoss : Dict[str, ShaftSpeedVsPowerLoss], default=dict()
+        A series of look-up tables for the losses, each valid for the specified input torque.
+    MechanicalLossesType : str, readonly, default='PowerScalingLosses'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PowerScalingLosses'
+    
     """
 
     InputTorqueVsShaftSpeedVsPowerLoss: Optional[Dict[str, ShaftSpeedVsPowerLoss]] = Field(alias="InputTorqueVsShaftSpeedVsPowerLoss", default=dict())
     MechanicalLossesType: Optional[str] = Field(alias="MechanicalLossesType", default='PowerScalingLosses', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_direction_shear_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_direction_shear_transient.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_direction_shear_variation import CurrentDirectionShearVariation
 
 
 class PresetCurrentDirectionShearTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetCurrentDirectionShearTransient(CurrentDirectionShearVariation, DirectionShearVariationType='PresetTransient'):
     """PresetCurrentDirectionShearTransient - A preset transient in the vertical direction shear.  This is a linear relationship between the height and the local direction, with the direction being its nominal value at the reference height.
-
+    
     Attributes:
-        - DirectionShearAtStart: float => A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
-        - AmplitudeOfVariation: float => A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetCurrentDirectionShearTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the shear remains at the initial value plus the amplitude after the transient has been completed) or a full-cycle (where the shear returns to its original value).
-        - DirectionShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    DirectionShearAtStart : float
+        A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    AmplitudeOfVariation : float
+        A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetCurrentDirectionShearTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the shear remains at the initial value plus the amplitude after the transient has been completed) or a full-cycle (where the shear returns to its original value).
+    DirectionShearVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     DirectionShearAtStart: Optional[float] = Field(alias="DirectionShearAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetCurrentDirectionShearTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_direction_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_direction_transient.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_direction_variation import CurrentDirectionVariation
 
 
 class PresetCurrentDirectionTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetCurrentDirectionTransient(CurrentDirectionVariation, DirectionVariationType='PresetTransient'):
     """PresetCurrentDirectionTransient - A preset transient in the current&#39;s direction.
-
+    
     Attributes:
-        - DirectionAtStart: float => A number representing an angle.  The SI units for angles are radians.
-        - AmplitudeOfVariation: float => A number representing an angle.  The SI units for angles are radians.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetCurrentDirectionTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the direction remains at the initial direction plus the amplitude after the transient has been completed) or a full-cycle (where the direction returns to its original value).
-        - DirectionVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    DirectionAtStart : float
+        A number representing an angle.  The SI units for angles are radians.
+    AmplitudeOfVariation : float
+        A number representing an angle.  The SI units for angles are radians.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetCurrentDirectionTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the direction remains at the initial direction plus the amplitude after the transient has been completed) or a full-cycle (where the direction returns to its original value).
+    DirectionVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     DirectionAtStart: Optional[float] = Field(alias="DirectionAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetCurrentDirectionTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_horizontal_shear_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_horizontal_shear_transient.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_horizontal_shear_variation import CurrentHorizontalShearVariation
 
 
 class PresetCurrentHorizontalShearTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetCurrentHorizontalShearTransient(CurrentHorizontalShearVariation, HorizontalShearVariationType='PresetTransient'):
     """PresetCurrentHorizontalShearTransient - A preset transient in the current&#39;s horizontal shear.
-
+    
     Attributes:
-        - HorizontalShearAtStart: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AmplitudeOfVariation: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetCurrentHorizontalShearTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the current&#39;s shear remains at the initial current&#39;s shear plus the amplitude after the transient has been completed) or a full-cycle (where the current&#39;s shear returns to its original value).
-        - HorizontalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    HorizontalShearAtStart : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AmplitudeOfVariation : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetCurrentHorizontalShearTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the current's shear remains at the initial current's shear plus the amplitude after the transient has been completed) or a full-cycle (where the current's shear returns to its original value).
+    HorizontalShearVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     HorizontalShearAtStart: Optional[float] = Field(alias="HorizontalShearAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetCurrentHorizontalShearTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_mean_speed_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_mean_speed_transient.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_mean_speed_variation import CurrentMeanSpeedVariation
 
 
 class PresetCurrentMeanSpeedTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetCurrentMeanSpeedTransient(CurrentMeanSpeedVariation, MeanSpeedVariationType='PresetTransient'):
     """PresetCurrentMeanSpeedTransient - A preset transient in the current&#39;s mean speed.
-
+    
     Attributes:
-        - MeanSpeedAtStart: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - AmplitudeOfVariation: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetCurrentMeanSpeedTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the current&#39;s speed remains at the initial current&#39;s speed plus the amplitude after the transient has been completed) or a full-cycle (where the wind speed returns to its original value).
-        - MeanSpeedVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    MeanSpeedAtStart : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    AmplitudeOfVariation : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetCurrentMeanSpeedTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the current's speed remains at the initial current's speed plus the amplitude after the transient has been completed) or a full-cycle (where the wind speed returns to its original value).
+    MeanSpeedVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     MeanSpeedAtStart: Optional[float] = Field(alias="MeanSpeedAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetCurrentMeanSpeedTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_current_vertical_shear_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_current_vertical_shear_transient.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current_vertical_shear_variation import CurrentVerticalShearVariation
 
 
 class PresetCurrentVerticalShearTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetCurrentVerticalShearTransient(CurrentVerticalShearVariation, VerticalShearVariationType='PresetTransient'):
     """PresetCurrentVerticalShearTransient - A preset transient in the current &#39;s vertical shear.
-
+    
     Attributes:
-        - VerticalShearAtStart: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AmplitudeOfVariation: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetCurrentVerticalShearTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the current&#39;s shear remains at the initial current&#39;s shear plus the amplitude after the transient has been completed) or a full-cycle (where the current&#39;s shear returns to its original value).
-        - VerticalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    VerticalShearAtStart : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AmplitudeOfVariation : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetCurrentVerticalShearTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the current's shear remains at the initial current's shear plus the amplitude after the transient has been completed) or a full-cycle (where the current's shear returns to its original value).
+    VerticalShearVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     VerticalShearAtStart: Optional[float] = Field(alias="VerticalShearAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetCurrentVerticalShearTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_lidar_scan.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_lidar_scan.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,42 +9,48 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.lidar_scanning_pattern import LidarScanningPattern
 
 
 class PresetLidarScan_BeamSamplingEnum(str, Enum):
     SIMULTANEOUS = "Simultaneous"
     SEQUENTIAL = "Sequential"
 
 class PresetLidarScan(LidarScanningPattern):
     """PresetLidarScan - the common properties of scan patterns which are set by the Lidar, rather than controlled in realtime by the turbine&#39;s controller.
-
+    
     Attributes:
-        - SamplesPerScan: int => The number of samples taken during a complete scan.
-        - LidarInterval: float => A number representing a time.  The SI units for time are seconds.
-        - BeamSampling: PresetLidarScan_BeamSamplingEnum => The method of how each beam is sampled - whether is is simultaneously, or in sequence.
+    ----------
+    SamplesPerScan : int, default=1
+        The number of samples taken during a complete scan.
+    LidarInterval : float
+        A number representing a time.  The SI units for time are seconds.
+    BeamSampling : PresetLidarScan_BeamSamplingEnum
+        The method of how each beam is sampled - whether is is simultaneously, or in sequence.
+    
     """
 
     SamplesPerScan: Optional[int] = Field(alias="SamplesPerScan", default=1)
     LidarInterval: Optional[float] = Field(alias="LidarInterval", default=None)
     BeamSampling: Optional[PresetLidarScan_BeamSamplingEnum] = Field(alias="BeamSampling", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_direction_shear_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_direction_shear_transient.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wind_direction_shear_variation import WindDirectionShearVariation
 
 
 class PresetWindDirectionShearTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetWindDirectionShearTransient(WindDirectionShearVariation, DirectionShearVariationType='PresetTransient'):
     """PresetWindDirectionShearTransient - A preset transient in the vertical direction shear (wind veer).  This is a linear relationship between the height and the local direction, with the direction being its nominal value at the reference height.
-
+    
     Attributes:
-        - DirectionShearAtStart: float => A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
-        - AmplitudeOfVariation: float => A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetWindDirectionShearTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the shear remains at the initial value plus the amplitude after the transient has been completed) or a full-cycle (where the shear returns to its original value).
-        - DirectionShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    DirectionShearAtStart : float
+        A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    AmplitudeOfVariation : float
+        A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetWindDirectionShearTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the shear remains at the initial value plus the amplitude after the transient has been completed) or a full-cycle (where the shear returns to its original value).
+    DirectionShearVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     DirectionShearAtStart: Optional[float] = Field(alias="DirectionShearAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetWindDirectionShearTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_direction_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_direction_transient.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wind_direction_variation import WindDirectionVariation
 
 
 class PresetWindDirectionTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetWindDirectionTransient(WindDirectionVariation, DirectionVariationType='PresetTransient'):
     """PresetWindDirectionTransient - A preset transient in the wind direction.
-
+    
     Attributes:
-        - DirectionAtStart: float => A number representing an angle.  The SI units for angles are radians.
-        - AmplitudeOfVariation: float => A number representing an angle.  The SI units for angles are radians.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetWindDirectionTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the direction remains at the initial direction plus the amplitude after the transient has been completed) or a full-cycle (where the direction returns to its original value).
-        - DirectionVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    DirectionAtStart : float
+        A number representing an angle.  The SI units for angles are radians.
+    AmplitudeOfVariation : float
+        A number representing an angle.  The SI units for angles are radians.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetWindDirectionTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the direction remains at the initial direction plus the amplitude after the transient has been completed) or a full-cycle (where the direction returns to its original value).
+    DirectionVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     DirectionAtStart: Optional[float] = Field(alias="DirectionAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetWindDirectionTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_horizontal_shear_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_horizontal_shear_transient.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wind_horizontal_shear_variation import WindHorizontalShearVariation
 
 
 class PresetWindHorizontalShearTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetWindHorizontalShearTransient(WindHorizontalShearVariation, HorizontalShearVariationType='PresetTransient'):
     """PresetWindHorizontalShearTransient - A preset transient in the horizontal wind shear.
-
+    
     Attributes:
-        - HorizontalShearAtStart: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AmplitudeOfVariation: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetWindHorizontalShearTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the wind shear remains at the initial wind shear plus the amplitude after the transient has been completed) or a full-cycle (where the wind shear returns to its original value).
-        - HorizontalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    HorizontalShearAtStart : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AmplitudeOfVariation : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetWindHorizontalShearTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the wind shear remains at the initial wind shear plus the amplitude after the transient has been completed) or a full-cycle (where the wind shear returns to its original value).
+    HorizontalShearVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     HorizontalShearAtStart: Optional[float] = Field(alias="HorizontalShearAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetWindHorizontalShearTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_mean_speed_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_mean_speed_transient.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wind_mean_speed_variation import WindMeanSpeedVariation
 
 
 class PresetWindMeanSpeedTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetWindMeanSpeedTransient(WindMeanSpeedVariation, MeanSpeedVariationType='PresetTransient'):
     """PresetWindMeanSpeedTransient - A preset transient in the mean wind speed.
-
+    
     Attributes:
-        - MeanSpeedAtStart: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - AmplitudeOfVariation: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetWindMeanSpeedTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the wind speed remains at the initial wind speed plus the amplitude after the transient has been completed) or a full-cycle (where the wind speed returns to its original value).
-        - MeanSpeedVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    MeanSpeedAtStart : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    AmplitudeOfVariation : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetWindMeanSpeedTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the wind speed remains at the initial wind speed plus the amplitude after the transient has been completed) or a full-cycle (where the wind speed returns to its original value).
+    MeanSpeedVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     MeanSpeedAtStart: Optional[float] = Field(alias="MeanSpeedAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetWindMeanSpeedTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/preset_wind_vertical_shear_transient.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/preset_wind_vertical_shear_transient.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wind_vertical_shear_variation import WindVerticalShearVariation
 
 
 class PresetWindVerticalShearTransient_VariationShapeEnum(str, Enum):
     FULL = "FULL"
     HALF = "HALF"
     IEC_2 = "IEC-2"
 
 class PresetWindVerticalShearTransient(WindVerticalShearVariation, VerticalShearVariationType='PresetTransient'):
     """PresetWindVerticalShearTransient - A preset transient in the vertical wind shear.
-
+    
     Attributes:
-        - VerticalShearAtStart: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AmplitudeOfVariation: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - StartTime: float => A number representing a time.  The SI units for time are seconds.
-        - DurationOfVariation: float => A number representing a time.  The SI units for time are seconds.
-        - VariationShape: PresetWindVerticalShearTransient_VariationShapeEnum => The shape of the transient.  This can either be a half-cycle (where the wind shear remains at the initial wind shear plus the amplitude after the transient has been completed) or a full-cycle (where the wind shear returns to its original value).
-        - VerticalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;PresetTransient&#39;
+    ----------
+    VerticalShearAtStart : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AmplitudeOfVariation : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    StartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    DurationOfVariation : float
+        A number representing a time.  The SI units for time are seconds.
+    VariationShape : PresetWindVerticalShearTransient_VariationShapeEnum
+        The shape of the transient.  This can either be a half-cycle (where the wind shear remains at the initial wind shear plus the amplitude after the transient has been completed) or a full-cycle (where the wind shear returns to its original value).
+    VerticalShearVariationType : str, readonly, default='PresetTransient'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PresetTransient'
+    
     """
 
     VerticalShearAtStart: Optional[float] = Field(alias="VerticalShearAtStart", default=None)
     AmplitudeOfVariation: Optional[float] = Field(alias="AmplitudeOfVariation", default=None)
     StartTime: Optional[float] = Field(alias="StartTime", default=None)
     DurationOfVariation: Optional[float] = Field(alias="DurationOfVariation", default=None)
     VariationShape: Optional[PresetWindVerticalShearTransient_VariationShapeEnum] = Field(alias="VariationShape", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/proportional_integral_deriviative.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/proportional_integral_deriviative.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,33 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class ProportionalIntegralDeriviative_DifferentialGainActionEnum(str, Enum):
     ERROR = "Error"
     FEEDBACK = "Feedback"
     SETPOINT = "Setpoint"
 
 class ProportionalIntegralDeriviative(ResponseToDemand, ResponseToDemandType='ProportionalIntegralDeriviative'):
     """ProportionalIntegralDeriviative - Defines an proportional, integral, deriviative (PID) response to the controller&#39;s demands.
-
+    
     Attributes:
-        - ProportionalGain: float => A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
-        - IntegralGain: float => A number representing the square of frequency.  The SI units for frequency are one over seconds squared.
-        - DifferentialGain: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - DifferentialGainAction: ProportionalIntegralDeriviative_DifferentialGainActionEnum => The proportional and integral terms are apllied on error.  Derivative term may also be applied on the feedback or setpoint signals.
-        - DifferentialGainTimeConstant: float => A number representing a time.  The SI units for time are seconds.
-        - DesaturationTimeConstant: float => A number representing a time.  The SI units for time are seconds.
-        - ResponseToDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ProportionalIntegralDeriviative&#39;
+    ----------
+    ProportionalGain : float
+        A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
+    IntegralGain : float
+        A number representing the square of frequency.  The SI units for frequency are one over seconds squared.
+    DifferentialGain : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    DifferentialGainAction : ProportionalIntegralDeriviative_DifferentialGainActionEnum, default='Feedback'
+        The proportional and integral terms are apllied on error.  Derivative term may also be applied on the feedback or setpoint signals.
+    DifferentialGainTimeConstant : float
+        A number representing a time.  The SI units for time are seconds.
+    DesaturationTimeConstant : float
+        A number representing a time.  The SI units for time are seconds.
+    ResponseToDemandType : str, readonly, default='ProportionalIntegralDeriviative'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ProportionalIntegralDeriviative'
+    
     """
 
     ProportionalGain: Optional[float] = Field(alias="ProportionalGain", default=None)
     IntegralGain: Optional[float] = Field(alias="IntegralGain", default=None)
     DifferentialGain: Optional[float] = Field(alias="DifferentialGain", default=None)
     DifferentialGainAction: Optional[ProportionalIntegralDeriviative_DifferentialGainActionEnum] = Field(alias="DifferentialGainAction", default='Feedback')
     DifferentialGainTimeConstant: Optional[float] = Field(alias="DifferentialGainTimeConstant", default=None)
@@ -45,15 +55,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/quadratic_yaw_damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/quadratic_yaw_damper.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class QuadraticYawDamper(Dnv):
     """QuadraticYawDamper - The definition of a quadratic damper for the yaw system.
-
+    
     Attributes:
-        - QuadraticCoefficient: float => A number representing the moment per angular velocity squared.  This is always provided in SI units, using radians.
-        - DeadBandWidth: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - QuadraticYawTorqueLimit: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - TotalYawTorqueLimit: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    QuadraticCoefficient : float
+        A number representing the moment per angular velocity squared.  This is always provided in SI units, using radians.
+    DeadBandWidth : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    QuadraticYawTorqueLimit : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    TotalYawTorqueLimit : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     QuadraticCoefficient: Optional[float] = Field(alias="QuadraticCoefficient", default=None)
     DeadBandWidth: Optional[float] = Field(alias="DeadBandWidth", default=None)
     QuadraticYawTorqueLimit: Optional[float] = Field(alias="QuadraticYawTorqueLimit", default=None)
     TotalYawTorqueLimit: Optional[float] = Field(alias="TotalYawTorqueLimit", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/radius_vs_deficit.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/radius_vs_deficit.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class RadiusVsDeficit(Dnv):
     """RadiusVsDeficit - A line or row in a look-up table, specifying a value of deficit for a specified radius.
-
+    
     Attributes:
-        - Radius: float => A number representing a length.  The SI units for length are metres.
-        - Deficit: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Radius : float
+        A number representing a length.  The SI units for length are metres.
+    Deficit : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Radius: Optional[float] = Field(alias="Radius", default=None)
     Deficit: Optional[float] = Field(alias="Deficit", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rate_vs_friction_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rate_vs_friction_torque.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class RateVsFrictionTorque(Dnv):
     """RateVsFrictionTorque - A line or row in a look-up table, specifying a value of FrictionTorque for a specified Rate.
-
+    
     Attributes:
-        - Rate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - FrictionTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Rate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    FrictionTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Rate: Optional[float] = Field(alias="Rate", default=None)
     FrictionTorque: Optional[float] = Field(alias="FrictionTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rate_vs_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rate_vs_position.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class RateVsPosition(Dnv):
     """RateVsPosition - A line or row in a look-up table, specifying a value of Position for a specified Rate.
-
+    
     Attributes:
-        - Rate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Position: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    Rate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Position : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     Rate: Optional[float] = Field(alias="Rate", default=None)
     Position: Optional[float] = Field(alias="Position", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rate_vs_time.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rate_vs_time.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class RateVsTime(Dnv):
     """RateVsTime - A line or row in a look-up table, specifying a value of Time for a specified Rate.
-
+    
     Attributes:
-        - Rate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Time: float => A number representing a time.  The SI units for time are seconds.
+    ----------
+    Rate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    
     """
 
     Rate: Optional[float] = Field(alias="Rate", default=None)
     Time: Optional[float] = Field(alias="Time", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/regular_waves.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/regular_waves.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.waves import Waves
 
 
 class RegularWaves(Waves):
     """RegularWaves - The definition of regular waves.
-
+    
     Attributes:
-        - DirectionOfApproachClockwiseFromNorth: float => A number representing an angle.  The SI units for angles are radians.
-        - WaveHeight: float => A number representing a length.  The SI units for length are metres.
-        - Period: float => A number representing a time.  The SI units for time are seconds.
+    ----------
+    DirectionOfApproachClockwiseFromNorth : float
+        A number representing an angle.  The SI units for angles are radians.
+    WaveHeight : float
+        A number representing a length.  The SI units for length are metres.
+    Period : float
+        A number representing a time.  The SI units for time are seconds.
+    
     """
 
     DirectionOfApproachClockwiseFromNorth: Optional[float] = Field(alias="DirectionOfApproachClockwiseFromNorth", default=None)
     WaveHeight: Optional[float] = Field(alias="WaveHeight", default=None)
     Period: Optional[float] = Field(alias="Period", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/response_to_demand.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/response_to_demand.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ResponseToDemand_ResponseToDemandTypeEnum(str, Enum):
     INSTANTANEOUS = "Instantaneous"
     SETPOINT_TRAJECTORY_PLANNING = "SetpointTrajectoryPlanning"
     FIRST_ORDER_RESPONSE = "FirstOrderResponse"
     SECOND_ORDER_RESPONSE = "SecondOrderResponse"
     PASSIVE = "Passive"
     PROPORTIONAL_INTEGRAL_DERIVIATIVE = "ProportionalIntegralDeriviative"
 
 class ResponseToDemand(Dnv):
     """ResponseToDemand - The common properties of all control responses.
-
+    
     Attributes:
-        - ResponseToDemandType: ResponseToDemand_ResponseToDemandTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    ResponseToDemandType : ResponseToDemand_ResponseToDemandTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - FirstOrderResponse
-        - Instantaneous
-        - Passive
-        - ProportionalIntegralDeriviative
-        - SecondOrderResponse
-        - SetpointTrajectoryPlanning
+        FirstOrderResponse
+        Instantaneous
+        Passive
+        ProportionalIntegralDeriviative
+        SecondOrderResponse
+        SetpointTrajectoryPlanning
+    
     """
 
     ResponseToDemandType: Optional[ResponseToDemand_ResponseToDemandTypeEnum] = Field(alias="ResponseToDemandType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -70,15 +74,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'ResponseToDemand'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rigid_blade_modelling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rigid_blade_modelling.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.blade_modelling import BladeModelling
 
 
 class RigidBladeModelling(BladeModelling, BladeModellingType='RigidBladeModelling'):
     """RigidBladeModelling - The properties for modelling the blade as being completely rigid.
-
+    
     Attributes:
-        - BladeModellingType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RigidBladeModelling&#39;
+    ----------
+    BladeModellingType : str, readonly, default='RigidBladeModelling'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RigidBladeModelling'
+    
     """
 
     BladeModellingType: Optional[str] = Field(alias="BladeModellingType", default='RigidBladeModelling', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rosette_lidar_scan.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rosette_lidar_scan.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.preset_lidar_scan import PresetLidarScan
 
 
 class RosetteLidarScan(PresetLidarScan, ScanningPatternType='RosetteScan'):
     """RosetteLidarScan - A petal-shaped scan pattern, passing through several &#39;lobes&#39; as it circles the centreline.
-
+    
     Attributes:
-        - MaximumHalfAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - MinimumHalfAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - NumberOfLobes: int => The number of lobes cycled through as the beam rotates around the centreline.
-        - ScanningPatternType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RosetteScan&#39;
+    ----------
+    MaximumHalfAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    MinimumHalfAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    NumberOfLobes : int, default=3
+        The number of lobes cycled through as the beam rotates around the centreline.
+    ScanningPatternType : str, readonly, default='RosetteScan'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RosetteScan'
+    
     """
 
     MaximumHalfAngle: Optional[float] = Field(alias="MaximumHalfAngle", default=None)
     MinimumHalfAngle: Optional[float] = Field(alias="MinimumHalfAngle", default=None)
     NumberOfLobes: Optional[int] = Field(alias="NumberOfLobes", default=3)
     ScanningPatternType: Optional[str] = Field(alias="ScanningPatternType", default='RosetteScan', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_idling_control_state.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_idling_control_state.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_inactive_control_state import InitialInactiveControlState
 
 
 class RotorIdlingControlState(InitialInactiveControlState, InitialConditionType='RotorIdling'):
     """RotorIdlingControlState - The initial condition of the rotor or turbine idling.  The external controller is still able to change the state of the turbine during the simulation.
-
+    
     Attributes:
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RotorIdling&#39;
+    ----------
+    InitialConditionType : str, readonly, default='RotorIdling'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RotorIdling'
+    
     """
 
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='RotorIdling', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_imbalances.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_imbalances.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class RotorImbalances(Dnv):
     """RotorImbalances - The definition of any imbalances on the hub.
-
+    
     Attributes:
-        - OutOfBalanceMass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - RadiusOfMass: float => A number representing a length.  The SI units for length are metres.
-        - AzimuthalPositionOfMass: float => A number representing an angle.  The SI units for angles are radians.
-        - ErrorsInSetAngle: List[float] => The manufacturing or assembly errors in the angle the pitch bearings or blades are attached to the hub.
-        - ErrorsInPitchAngle: List[float] => The manufacturing or assembly errors in the angle the blades are attached to the pitch bearings.
-        - ErrorsInAzimuthAngle: List[float] => The manufacturing or assembly errors in the azimuth angle for each blade mounting point.
+    ----------
+    OutOfBalanceMass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    RadiusOfMass : float
+        A number representing a length.  The SI units for length are metres.
+    AzimuthalPositionOfMass : float
+        A number representing an angle.  The SI units for angles are radians.
+    ErrorsInSetAngle : List[float], default=list()
+        The manufacturing or assembly errors in the angle the pitch bearings or blades are attached to the hub.
+    ErrorsInPitchAngle : List[float], default=list()
+        The manufacturing or assembly errors in the angle the blades are attached to the pitch bearings.
+    ErrorsInAzimuthAngle : List[float], default=list()
+        The manufacturing or assembly errors in the azimuth angle for each blade mounting point.
+    
     """
 
     OutOfBalanceMass: Optional[float] = Field(alias="OutOfBalanceMass", default=None)
     RadiusOfMass: Optional[float] = Field(alias="RadiusOfMass", default=None)
     AzimuthalPositionOfMass: Optional[float] = Field(alias="AzimuthalPositionOfMass", default=None)
     ErrorsInSetAngle: Optional[List[float]] = Field(alias="ErrorsInSetAngle", default=list())
     ErrorsInPitchAngle: Optional[List[float]] = Field(alias="ErrorsInPitchAngle", default=list())
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_in_power_production.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_in_power_production.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_control_state import InitialControlState
 
 
 class RotorInPowerProduction(InitialControlState, InitialConditionType='RotorInPowerProduction'):
     """RotorInPowerProduction - The rotor begins the simulation in the power production state.  This is the default behaviour, and this initial condition is not required to be present, although it can be added to make it explicit.
-
+    
     Attributes:
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RotorInPowerProduction&#39;
+    ----------
+    InitialConditionType : str, readonly, default='RotorInPowerProduction'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RotorInPowerProduction'
+    
     """
 
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='RotorInPowerProduction', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_overspeed.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_overspeed.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class RotorOverspeed(Dnv):
     """RotorOverspeed - The definition of the safety system behaviour when the rotor is rotating too fast.
-
+    
     Attributes:
-        - TripValue: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Delay: float => A number representing a time.  The SI units for time are seconds.
-        - CircuitToActivate: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    TripValue : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Delay : float
+        A number representing a time.  The SI units for time are seconds.
+    CircuitToActivate : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     TripValue: Optional[float] = Field(alias="TripValue", default=None)
     Delay: Optional[float] = Field(alias="Delay", default=None)
     CircuitToActivate: Optional[str] = Field(alias="CircuitToActivate", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/rotor_parked_control_state.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/rotor_parked_control_state.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.initial_inactive_control_state import InitialInactiveControlState
 
 
 class RotorParkedControlState(InitialInactiveControlState, InitialConditionType='RotorParked'):
     """RotorParkedControlState - The initial condition of the rotor or turbine being parked.  The external controller is still able to change the state of the turbine during the simulation.
-
+    
     Attributes:
-        - InitialConditionType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RotorParked&#39;
+    ----------
+    InitialConditionType : str, readonly, default='RotorParked'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RotorParked'
+    
     """
 
     InitialConditionType: Optional[str] = Field(alias="InitialConditionType", default='RotorParked', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/runge_kutta4th_order_fixed_step.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/runge_kutta4th_order_fixed_step.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fixed_step_integrator import FixedStepIntegrator
 
 
 class RungeKutta4thOrderFixedStep(FixedStepIntegrator, IntegratorType='RungeKutta4thOrderFixedStep'):
     """RungeKutta4thOrderFixedStep - Settings for the Runge Kutta 4th Order Fixed Step integrator.
-
+    
     Attributes:
-        - IntegratorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RungeKutta4thOrderFixedStep&#39;
+    ----------
+    IntegratorType : str, readonly, default='RungeKutta4thOrderFixedStep'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RungeKutta4thOrderFixedStep'
+    
     """
 
     IntegratorType: Optional[str] = Field(alias="IntegratorType", default='RungeKutta4thOrderFixedStep', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/runge_kutta_variable_step.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/runge_kutta_variable_step.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.integrator import Integrator
 
 
 class RungeKuttaVariableStep(Integrator, IntegratorType='RungeKuttaVariableStep'):
     """RungeKuttaVariableStep - Settings for the Runge Kutta Variable Step integrator.
-
+    
     Attributes:
-        - InitialStep: float => A number representing a time.  The SI units for time are seconds.
-        - Tolerance: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - MinimumTimeStep: float => A number representing a time.  The SI units for time are seconds.
-        - MaximumTimeStep: float => A number representing a time.  The SI units for time are seconds.
-        - IntegratorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;RungeKuttaVariableStep&#39;
+    ----------
+    InitialStep : float
+        A number representing a time.  The SI units for time are seconds.
+    Tolerance : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    MinimumTimeStep : float
+        A number representing a time.  The SI units for time are seconds.
+    MaximumTimeStep : float
+        A number representing a time.  The SI units for time are seconds.
+    IntegratorType : str, readonly, default='RungeKuttaVariableStep'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'RungeKuttaVariableStep'
+    
     """
 
     InitialStep: Optional[float] = Field(alias="InitialStep", default=None)
     Tolerance: Optional[float] = Field(alias="Tolerance", default=None)
     MinimumTimeStep: Optional[float] = Field(alias="MinimumTimeStep", default=None)
     MaximumTimeStep: Optional[float] = Field(alias="MaximumTimeStep", default=None)
     IntegratorType: Optional[str] = Field(alias="IntegratorType", default='RungeKuttaVariableStep', allow_mutation=False)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/safety_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/safety_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.emergency_stop_button import EmergencyStopButton
 
 from dnv_bladed_models.generator_overpower import GeneratorOverpower
 
 from dnv_bladed_models.generator_overspeed import GeneratorOverspeed
@@ -28,24 +29,28 @@
 from dnv_bladed_models.rotor_overspeed import RotorOverspeed
 
 from dnv_bladed_models.safety_system_circuit import SafetySystemCircuit
 
 
 class SafetySystem(Dnv):
     """SafetySystem - The definition of the turbine&#39;s safety system.  This supersedes the turbine&#39;s controller requests.
-
+    
     Attributes:
-        - Circuits: Dict[str, SafetySystemCircuit] => A library of the available safety system circuits.  These can be referenced by the individual trips.
-        - GeneratorOverspeed: GeneratorOverspeed 
-        - RotorOverspeed: RotorOverspeed 
-        - GeneratorOverpower: GeneratorOverpower 
-        - NacelleVibration: NacelleVibration 
-        - GeneratorShortCircuit: GeneratorShortCircuit 
-        - EmergencyStopButton: EmergencyStopButton 
-        - OnlyActivateOnceLoggingHasCommenced: bool => If true, the safety system will be inactivated during the lead in time when there is no logging.
+    ----------
+    Circuits : Dict[str, SafetySystemCircuit], default=dict()
+        A library of the available safety system circuits.  These can be referenced by the individual trips.
+    GeneratorOverspeed : GeneratorOverspeed
+    RotorOverspeed : RotorOverspeed
+    GeneratorOverpower : GeneratorOverpower
+    NacelleVibration : NacelleVibration
+    GeneratorShortCircuit : GeneratorShortCircuit
+    EmergencyStopButton : EmergencyStopButton
+    OnlyActivateOnceLoggingHasCommenced : bool
+        If true, the safety system will be inactivated during the lead in time when there is no logging.
+    
     """
 
     Circuits: Optional[Dict[str, SafetySystemCircuit]] = Field(alias="Circuits", default=dict())
     GeneratorOverspeed: Optional[GeneratorOverspeed] = Field(alias="GeneratorOverspeed", default=None)
     RotorOverspeed: Optional[RotorOverspeed] = Field(alias="RotorOverspeed", default=None)
     GeneratorOverpower: Optional[GeneratorOverpower] = Field(alias="GeneratorOverpower", default=None)
     NacelleVibration: Optional[NacelleVibration] = Field(alias="NacelleVibration", default=None)
@@ -56,15 +61,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/safety_system_circuit.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/safety_system_circuit.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,28 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SafetySystemCircuit(Dnv):
     """SafetySystemCircuit - A safety system circuit, which defines the actions that will be taken if it is triggered.
-
+    
     Attributes:
-        - PitchAction: bool => If true, the pitch system will be asked to activate its own safety system.
-        - ApplyShaftBrake1: bool => If true, the third shaft brake will be activated.
-        - ApplyShaftBrake2: bool => If true, the third shaft brake will be activated.
-        - ApplyShaftBrake3: bool => If true, the third shaft brake will be activated.
-        - DisconnectGenerator: bool => If true, the generator will be disconneted from the grid.
-        - ApplyGeneratorBrake: bool => If true, the generator brake will be activated.
-        - DisconnectYawDrive: bool => If true, the yaw drive will be disconnected.
+    ----------
+    PitchAction : bool
+        If true, the pitch system will be asked to activate its own safety system.
+    ApplyShaftBrake1 : bool
+        If true, the third shaft brake will be activated.
+    ApplyShaftBrake2 : bool
+        If true, the third shaft brake will be activated.
+    ApplyShaftBrake3 : bool
+        If true, the third shaft brake will be activated.
+    DisconnectGenerator : bool
+        If true, the generator will be disconneted from the grid.
+    ApplyGeneratorBrake : bool
+        If true, the generator brake will be activated.
+    DisconnectYawDrive : bool
+        If true, the yaw drive will be disconnected.
+    
     """
 
     PitchAction: Optional[bool] = Field(alias="PitchAction", default=None)
     ApplyShaftBrake1: Optional[bool] = Field(alias="ApplyShaftBrake1", default=None)
     ApplyShaftBrake2: Optional[bool] = Field(alias="ApplyShaftBrake2", default=None)
     ApplyShaftBrake3: Optional[bool] = Field(alias="ApplyShaftBrake3", default=None)
     DisconnectGenerator: Optional[bool] = Field(alias="DisconnectGenerator", default=None)
@@ -40,15 +50,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sea_state.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/sea_state.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,46 +9,63 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current import Current
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.tide import Tide
 
 from dnv_bladed_models.waves import Waves
 
 
 class SeaState(Dnv):
     """SeaState - The sea state at the turbine location.
-
+    
     Attributes:
-        - MeanSeaLevel: float => A number representing a length.  The SI units for length are metres.
-        - Current: Current 
-        - Waves: Waves 
-        - Tide: Tide 
+    ----------
+    MeanSeaLevel : float
+        A number representing a length.  The SI units for length are metres.
+    Current : Current, abstract
+    Waves : Waves, abstract
+    Tide : Tide
+    
+    Notes:
+    ---------
+    Current has the following concrete types:
+        - Currents
+        - LaminarFlowCurrent
+        - TurbulentCurrent
+    Waves has the following concrete types:
+        - JonswapPiersonMoskowitz
+        - LinearAiry
+        - StreamFunction
+        - UserDefinedWaves
+        - WaveSpectrum
+    
     """
 
     MeanSeaLevel: Optional[float] = Field(alias="MeanSeaLevel", default=None)
     Current: Optional[Current] = Field(alias="Current", default=None)
     Waves: Optional[Waves] = Field(alias="Waves", default=None)
     Tide: Optional[Tide] = Field(alias="Tide", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/second_order_response.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/second_order_response.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class SecondOrderResponse(ResponseToDemand, ResponseToDemandType='SecondOrderResponse'):
     """SecondOrderResponse - Defines a second-order response to the controller&#39;s demands.
-
+    
     Attributes:
-        - Frequency: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Damping: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - ResponseToDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SecondOrderResponse&#39;
+    ----------
+    Frequency : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Damping : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    ResponseToDemandType : str, readonly, default='SecondOrderResponse'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SecondOrderResponse'
+    
     """
 
     Frequency: Optional[float] = Field(alias="Frequency", default=None)
     Damping: Optional[float] = Field(alias="Damping", default=None)
     ResponseToDemandType: Optional[str] = Field(alias="ResponseToDemandType", default='SecondOrderResponse', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/selected_component_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/selected_component_output_group.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SelectedComponentOutputGroup(Dnv):
     """SelectedComponentOutputGroup - A reference to the OutputGroup on a specific component within the assembly.
-
+    
     Attributes:
-        - ComponentWithinAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
-        - SelectedOutputGroup: str => This is a JSON $ref style reference to another element in the JSON model.
+    ----------
+    ComponentWithinAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    SelectedOutputGroup : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    
     """
 
     ComponentWithinAssembly: Optional[str] = Field(alias="ComponentWithinAssembly", default=None)
     SelectedOutputGroup: Optional[str] = Field(alias="SelectedOutputGroup", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sensor_mapping.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/sensor_mapping.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.sensor_reference import SensorReference
 
 from dnv_bladed_models.sensors_used import SensorsUsed
 
 
 class SensorMapping(SensorsUsed, SensorsUsedType='SensorMapping'):
     """SensorMapping - The mapping of the sensors in the Bladed Next Generation controller interface.
-
+    
     Attributes:
-        - SensorsAvailableToController: Dict[str, SensorReference] => A library of sensors that the controller has access to.  The details of exactly where the sensors are will be unavailable if the component is encrypted, but other users will be able to see and reference the sensors by their name.
-        - SensorsUsedType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SensorMapping&#39;
+    ----------
+    SensorsAvailableToController : Dict[str, SensorReference], default=dict()
+        A library of sensors that the controller has access to.  The details of exactly where the sensors are will be unavailable if the component is encrypted, but other users will be able to see and reference the sensors by their name.
+    SensorsUsedType : str, readonly, default='SensorMapping'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SensorMapping'
+    
     """
 
     SensorsAvailableToController: Optional[Dict[str, SensorReference]] = Field(alias="SensorsAvailableToController", default=dict())
     SensorsUsedType: Optional[str] = Field(alias="SensorsUsedType", default='SensorMapping', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sensor_reference.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/sensor_reference.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SensorReference(Dnv):
     """SensorReference - The controller will reference all sensors by name.  This object maps a sensor on the assemble to a unique name.
-
+    
     Attributes:
-        - OnComponentInAssembly: str => This is a JSON $ref style reference to a component in the assembly tree.
-        - Sensor: str => The name of the sensor within the \&quot;Sensors\&quot; area of the component definition.
+    ----------
+    OnComponentInAssembly : str
+        This is a JSON $ref style reference to a component in the assembly tree.
+    Sensor : str
+        The name of the sensor within the \"Sensors\" area of the component definition.
+    
     """
 
     OnComponentInAssembly: Optional[str] = Field(alias="OnComponentInAssembly", default=None)
     Sensor: Optional[str] = Field(alias="Sensor", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sensors_used.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/sensors_used.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SensorsUsed_SensorsUsedTypeEnum(str, Enum):
     SENSOR_MAPPING = "SensorMapping"
     LEGACY_SENSOR_MAPPINGS = "LegacySensorMappings"
 
 class SensorsUsed(Dnv):
     """SensorsUsed - This section references all of the sensors that can be used by the external controller.
-
+    
     Attributes:
-        - SensorsUsedType: SensorsUsed_SensorsUsedTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    SensorsUsedType : SensorsUsed_SensorsUsedTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - LegacySensorMappings
-        - SensorMapping
+        LegacySensorMappings
+        SensorMapping
+    
     """
 
     SensorsUsedType: Optional[SensorsUsed_SensorsUsedTypeEnum] = Field(alias="SensorsUsedType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'SensorsUsed'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sequential_timestep_adaptation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/sequential_timestep_adaptation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SequentialTimestepAdaptation(Dnv):
     """SequentialTimestepAdaptation - Parameters for the Sequential Timestep Adaptation (STA) technique.
-
+    
     Attributes:
-        - FineTimeStepStart: float => A number representing a time.  The SI units for time are seconds.
-        - MaximumTimestepCoarseningLevel: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    FineTimeStepStart : float
+        A number representing a time.  The SI units for time are seconds.
+    MaximumTimestepCoarseningLevel : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     FineTimeStepStart: Optional[float] = Field(alias="FineTimeStepStart", default=None)
     MaximumTimestepCoarseningLevel: Optional[float] = Field(alias="MaximumTimestepCoarseningLevel", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/setpoint_trajectory_planning.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/setpoint_trajectory_planning.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,42 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.pitch_acceleration_limits import PitchAccelerationLimits
 
 from dnv_bladed_models.pitch_rate_limits import PitchRateLimits
 
 from dnv_bladed_models.response_to_demand import ResponseToDemand
 
 
 class SetpointTrajectoryPlanning(ResponseToDemand, ResponseToDemandType='SetpointTrajectoryPlanning'):
     """SetpointTrajectoryPlanning - Defines a setpoint trajectory planning response to the controller&#39;s demands.
-
+    
     Attributes:
-        - RateLimits: PitchRateLimits 
-        - AccelerationLimits: PitchAccelerationLimits 
-        - ResponseToDemandType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SetpointTrajectoryPlanning&#39;
+    ----------
+    RateLimits : PitchRateLimits
+    AccelerationLimits : PitchAccelerationLimits
+    ResponseToDemandType : str, readonly, default='SetpointTrajectoryPlanning'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SetpointTrajectoryPlanning'
+    
     """
 
     RateLimits: Optional[PitchRateLimits] = Field(alias="RateLimits", default=None)
     AccelerationLimits: Optional[PitchAccelerationLimits] = Field(alias="AccelerationLimits", default=None)
     ResponseToDemandType: Optional[str] = Field(alias="ResponseToDemandType", default='SetpointTrajectoryPlanning', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/settings.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/settings.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,48 +9,51 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.aerodynamic_settings import AerodynamicSettings
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.hydrodynamic_calculation import HydrodynamicCalculation
 
 from dnv_bladed_models.solver_settings import SolverSettings
 
 from dnv_bladed_models.structural_modelling_settings import StructuralModellingSettings
 
 
 class Settings(Dnv):
     """Settings - The settings for both time domain simulations and steady calculations.  These are typically calculation options which have reliable defaults, but offer the users other options to control the analysis.
-
+    
     Attributes:
-        - Solver: SolverSettings 
-        - StructuralModelling: StructuralModellingSettings 
-        - Aerodynamics: AerodynamicSettings 
-        - Hydrodynamics: HydrodynamicCalculation 
+    ----------
+    Solver : SolverSettings
+    StructuralModelling : StructuralModellingSettings
+    Aerodynamics : AerodynamicSettings
+    Hydrodynamics : HydrodynamicCalculation
+    
     """
 
     Solver: Optional[SolverSettings] = Field(alias="Solver", default=None)
     StructuralModelling: Optional[StructuralModellingSettings] = Field(alias="StructuralModelling", default=None)
     Aerodynamics: Optional[AerodynamicSettings] = Field(alias="Aerodynamics", default=None)
     Hydrodynamics: Optional[HydrodynamicCalculation] = Field(alias="Hydrodynamics", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/shaft_speed_vs_power_loss.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/standard_pitch_limit_switches.py`

 * *Files 9% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class ShaftSpeedVsPowerLoss(Dnv):
-    """ShaftSpeedVsPowerLoss - A line or row in a look-up table, specifying a value of PowerLoss for a specified ShaftSpeed.
 
+class StandardPitchLimitSwitches(Dnv):
+    """StandardPitchLimitSwitches - The limit switch positions of the control system.
+    
     Attributes:
-        - ShaftSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - PowerLoss: float => A number representing a power.  The SI units for enery are Watts.
+    ----------
+    MinimumAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    MaximumAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
-    ShaftSpeed: Optional[float] = Field(alias="ShaftSpeed", default=None)
-    PowerLoss: Optional[float] = Field(alias="PowerLoss", default=None)
+    MinimumAngle: Optional[float] = Field(alias="MinimumAngle", default=None)
+    MaximumAngle: Optional[float] = Field(alias="MaximumAngle", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        ShaftSpeedVsPowerLoss
+        StandardPitchLimitSwitches
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = ShaftSpeedVsPowerLoss.from_file('/path/to/file')
+        >>> model = StandardPitchLimitSwitches.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        ShaftSpeedVsPowerLoss
+        StandardPitchLimitSwitches
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = ShaftSpeedVsPowerLoss.from_json('{ ... }')
+        >>> model = StandardPitchLimitSwitches.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        ShaftSpeedVsPowerLoss
+        StandardPitchLimitSwitches
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-ShaftSpeedVsPowerLoss.update_forward_refs()
+StandardPitchLimitSwitches.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/shaft_speed_vs_resisting_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/shaft_speed_vs_resisting_torque.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ShaftSpeedVsResistingTorque(Dnv):
     """ShaftSpeedVsResistingTorque - A line or row in a look-up table, specifying a value of ResistingTorque for a specified ShaftSpeed.
-
+    
     Attributes:
-        - ShaftSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - ResistingTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    ShaftSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    ResistingTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     ShaftSpeed: Optional[float] = Field(alias="ShaftSpeed", default=None)
     ResistingTorque: Optional[float] = Field(alias="ResistingTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/shear_stiffnesses.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/shear_stiffnesses.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ShearStiffnesses(Dnv):
     """ShearStiffnesses - The shear moduli or shear stiffnesses of the blade section [https://www.wikipedia.org/wiki/Shear_modulus].  If omitted, it will be assumed that the shear stiffness is infinite.
-
+    
     Attributes:
-        - ShearStiffnessInX: float => A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
-        - ShearStiffnessInY: float => A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    ----------
+    ShearStiffnessInX : float
+        A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    ShearStiffnessInY : float
+        A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    
     """
 
     ShearStiffnessInX: Optional[float] = Field(alias="ShearStiffnessInX", default=None)
     ShearStiffnessInY: Optional[float] = Field(alias="ShearStiffnessInY", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/short_circuit.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/short_circuit.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fault import Fault
 
 
 class ShortCircuit(Fault, EventType='ShortCircuit'):
     """ShortCircuit - A short circuit that occurs at a specified time in the simulation.
-
+    
     Attributes:
-        - Filepath: str => The filepath or URI of the short circuit data.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;ShortCircuit&#39;
+    ----------
+    Filepath : str
+        The filepath or URI of the short circuit data.
+    EventType : str, readonly, default='ShortCircuit'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'ShortCircuit'
+    
     """
 
     Filepath: Optional[str] = Field(alias="Filepath", default=None)
     EventType: Optional[str] = Field(alias="EventType", default='ShortCircuit', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.transducer_behaviour import TransducerBehaviour
 
 
 class SignalProperties_SignalQualityEnum(str, Enum):
     RAW = "Raw"
@@ -26,22 +27,29 @@
 class SignalProperties_SignalNoiseEnum(str, Enum):
     NONE = "None"
     UNIFORM = "Uniform"
     GAUSSIAN = "Gaussian"
 
 class SignalProperties(Dnv):
     """SignalProperties - 
-
+    
     Attributes:
-        - SignalQuality: SignalProperties_SignalQualityEnum => The representation of the signal quality - whether it has transducer lag and signal noise.
-        - SignalNoise: SignalProperties_SignalNoiseEnum => The type of noise on the measured signal.
-        - NoiseMagnitude: float => The magnitude of the signal noise.
-        - SamplingPeriod: float => A number representing a time.  The SI units for time are seconds.
-        - DiscretisationStep: float => The intervals at which values can be represented.
-        - Transducer: TransducerBehaviour 
+    ----------
+    SignalQuality : SignalProperties_SignalQualityEnum, default='Raw'
+        The representation of the signal quality - whether it has transducer lag and signal noise.
+    SignalNoise : SignalProperties_SignalNoiseEnum, default='None'
+        The type of noise on the measured signal.
+    NoiseMagnitude : float, default=0.0
+        The magnitude of the signal noise.
+    SamplingPeriod : float
+        A number representing a time.  The SI units for time are seconds.
+    DiscretisationStep : float, default=0.0
+        The intervals at which values can be represented.
+    Transducer : TransducerBehaviour
+    
     """
 
     SignalQuality: Optional[SignalProperties_SignalQualityEnum] = Field(alias="SignalQuality", default='Raw')
     SignalNoise: Optional[SignalProperties_SignalNoiseEnum] = Field(alias="SignalNoise", default='None')
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=0.0)
     SamplingPeriod: Optional[float] = Field(alias="SamplingPeriod", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=0.0)
@@ -50,15 +58,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_acceleration.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_acceleration.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_acceleration import TransducerFaultsAcceleration
 
 
 class SignalPropertiesAcceleration(SignalProperties):
     """SignalPropertiesAcceleration - 
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - DiscretisationStep: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - TransducerFaults: TransducerFaultsAcceleration 
+    ----------
+    NoiseMagnitude : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    DiscretisationStep : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    TransducerFaults : TransducerFaultsAcceleration
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsAcceleration] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_angle.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_angle.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_angle import TransducerFaultsAngle
 
 
 class SignalPropertiesAngle(SignalProperties):
     """SignalPropertiesAngle - 
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing an angle.  The SI units for angles are radians.
-        - DiscretisationStep: float => A number representing an angle.  The SI units for angles are radians.
-        - TransducerFaults: TransducerFaultsAngle 
+    ----------
+    NoiseMagnitude : float
+        A number representing an angle.  The SI units for angles are radians.
+    DiscretisationStep : float
+        A number representing an angle.  The SI units for angles are radians.
+    TransducerFaults : TransducerFaultsAngle
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsAngle] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_angular_acceleration.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_angular_acceleration.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_angular_acceleration import TransducerFaultsAngularAcceleration
 
 
 class SignalPropertiesAngularAcceleration(SignalProperties):
     """SignalPropertiesAngularAcceleration - 
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - DiscretisationStep: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - TransducerFaults: TransducerFaultsAngularAcceleration 
+    ----------
+    NoiseMagnitude : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    DiscretisationStep : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    TransducerFaults : TransducerFaultsAngularAcceleration
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsAngularAcceleration] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_angular_velocity.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_angular_velocity.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_angular_velocity import TransducerFaultsAngularVelocity
 
 
 class SignalPropertiesAngularVelocity(SignalProperties):
     """SignalPropertiesAngularVelocity - 
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - DiscretisationStep: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - TransducerFaults: TransducerFaultsAngularVelocity 
+    ----------
+    NoiseMagnitude : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    DiscretisationStep : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    TransducerFaults : TransducerFaultsAngularVelocity
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsAngularVelocity] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_force.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_force.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_force import TransducerFaultsForce
 
 
 class SignalPropertiesForce(SignalProperties):
     """SignalPropertiesForce - 
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing a force.  The SI units for force are Newtons.
-        - DiscretisationStep: float => A number representing a force.  The SI units for force are Newtons.
-        - TransducerFaults: TransducerFaultsForce 
+    ----------
+    NoiseMagnitude : float
+        A number representing a force.  The SI units for force are Newtons.
+    DiscretisationStep : float
+        A number representing a force.  The SI units for force are Newtons.
+    TransducerFaults : TransducerFaultsForce
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsForce] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_length.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_length.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_length import TransducerFaultsLength
 
 
 class SignalPropertiesLength(SignalProperties):
     """SignalPropertiesLength - 
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing a length.  The SI units for length are metres.
-        - DiscretisationStep: float => A number representing a length.  The SI units for length are metres.
-        - TransducerFaults: TransducerFaultsLength 
+    ----------
+    NoiseMagnitude : float
+        A number representing a length.  The SI units for length are metres.
+    DiscretisationStep : float
+        A number representing a length.  The SI units for length are metres.
+    TransducerFaults : TransducerFaultsLength
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsLength] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_moment.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_moment.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_moment import TransducerFaultsMoment
 
 
 class SignalPropertiesMoment(SignalProperties):
     """SignalPropertiesMoment - 
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - DiscretisationStep: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - TransducerFaults: TransducerFaultsMoment 
+    ----------
+    NoiseMagnitude : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    DiscretisationStep : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    TransducerFaults : TransducerFaultsMoment
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsMoment] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_power.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_power.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.signal_properties import SignalProperties
 
 from dnv_bladed_models.transducer_faults_power import TransducerFaultsPower
 
 
 class SignalPropertiesPower(SignalProperties):
     """SignalPropertiesPower - Allows the representation of faults on a given measured signal, including signal interference.
-
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing a power.  The SI units for enery are Watts.
-        - DiscretisationStep: float => A number representing a power.  The SI units for enery are Watts.
-        - TransducerFaults: TransducerFaultsPower 
+    ----------
+    NoiseMagnitude : float
+        A number representing a power.  The SI units for enery are Watts.
+    DiscretisationStep : float
+        A number representing a power.  The SI units for enery are Watts.
+    TransducerFaults : TransducerFaultsPower
+    
     """
 
     NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
     DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
     TransducerFaults: Optional[TransducerFaultsPower] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/signal_properties_velocity.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_aerodynamic_properties.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,40 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.signal_properties import SignalProperties
 
-from dnv_bladed_models.transducer_faults_velocity import TransducerFaultsVelocity
+from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.flow_obstruction import FlowObstruction
 
-class SignalPropertiesVelocity(SignalProperties):
-    """SignalPropertiesVelocity - 
 
+class TowerAerodynamicProperties(Dnv):
+    """TowerAerodynamicProperties - The aerodynamic properties of the tower, including tower shadow.
+    
     Attributes:
-        - NoiseMagnitude: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - DiscretisationStep: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - TransducerFaults: TransducerFaultsVelocity 
+    ----------
+    CoefficientOfDrag : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TowerShadow : FlowObstruction
+    
     """
 
-    NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
-    DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
-    TransducerFaults: Optional[TransducerFaultsVelocity] = Field(alias="TransducerFaults", default=None)
+    CoefficientOfDrag: Optional[float] = Field(alias="CoefficientOfDrag", default=None)
+    TowerShadow: Optional[FlowObstruction] = Field(alias="TowerShadow", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +110,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        SignalPropertiesVelocity
+        TowerAerodynamicProperties
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = SignalPropertiesVelocity.from_file('/path/to/file')
+        >>> model = TowerAerodynamicProperties.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +140,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        SignalPropertiesVelocity
+        TowerAerodynamicProperties
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = SignalPropertiesVelocity.from_json('{ ... }')
+        >>> model = TowerAerodynamicProperties.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +172,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        SignalPropertiesVelocity
+        TowerAerodynamicProperties
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +204,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-SignalPropertiesVelocity.update_forward_refs()
+TowerAerodynamicProperties.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simple_cutoff_frequency.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/simple_cutoff_frequency.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.wave_diffraction_approximation import WaveDiffractionApproximation
 
 
 class SimpleCutoffFrequency(WaveDiffractionApproximation, WaveDiffractionApproximationType='SimpleCutoffFrequency'):
     """SimpleCutoffFrequency - The MacCamy-Fuchs methodology will be used, with the member diameter automatically determined from the model.
-
+    
     Attributes:
-        - CutoffFrequency: float => A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
-        - WaveDiffractionApproximationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SimpleCutoffFrequency&#39;
+    ----------
+    CutoffFrequency : float
+        A number representing a frequency.  The SI units for frequency are Hertz (per seconds).
+    WaveDiffractionApproximationType : str, readonly, default='SimpleCutoffFrequency'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SimpleCutoffFrequency'
+    
     """
 
     CutoffFrequency: Optional[float] = Field(alias="CutoffFrequency", default=None)
     WaveDiffractionApproximationType: Optional[str] = Field(alias="WaveDiffractionApproximationType", default='SimpleCutoffFrequency', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simple_shaft_brake.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/simple_shaft_brake.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.brake import Brake
 
 
 class SimpleShaftBrake(Brake, BrakeType='SimpleShaftBrake'):
     """SimpleShaftBrake - The definition of a simple shaft brake whose retarding torque will increase linearly over a set time.
-
+    
     Attributes:
-        - MaximumTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - RampTime: float => A number representing a time.  The SI units for time are seconds.
-        - BrakeType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SimpleShaftBrake&#39;
+    ----------
+    MaximumTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    RampTime : float
+        A number representing a time.  The SI units for time are seconds.
+    BrakeType : str, readonly, default='SimpleShaftBrake'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SimpleShaftBrake'
+    
     """
 
     MaximumTorque: Optional[float] = Field(alias="MaximumTorque", default=None)
     RampTime: Optional[float] = Field(alias="RampTime", default=None)
     BrakeType: Optional[str] = Field(alias="BrakeType", default='SimpleShaftBrake', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simple_tower_can.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/simple_tower_can.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,44 +9,49 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_can import TowerCan
 
 from dnv_bladed_models.tower_can_properties_by_material import TowerCanPropertiesByMaterial
 
 from dnv_bladed_models.tower_can_properties_by_material_where_different import TowerCanPropertiesByMaterialWhereDifferent
 
 
 class SimpleTowerCan(TowerCan, TowerCanType='SimpleTowerCan'):
     """SimpleTowerCan - A tower can where the physical properties will be calculated from the material properties and the can dimensions.
-
+    
     Attributes:
-        - Material: str => This is a JSON $ref style reference to another element in the JSON model.
-        - BaseSection: TowerCanPropertiesByMaterial 
-        - TopSection: TowerCanPropertiesByMaterialWhereDifferent 
-        - TowerCanType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SimpleTowerCan&#39;
+    ----------
+    Material : str
+        This is a JSON $ref style reference to another element in the JSON model.
+    BaseSection : TowerCanPropertiesByMaterial
+    TopSection : TowerCanPropertiesByMaterialWhereDifferent
+    TowerCanType : str, readonly, default='SimpleTowerCan'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SimpleTowerCan'
+    
     """
 
     Material: Optional[str] = Field(alias="Material", default=None)
     BaseSection: Optional[TowerCanPropertiesByMaterial] = Field(alias="BaseSection", default=None)
     TopSection: Optional[TowerCanPropertiesByMaterialWhereDifferent] = Field(alias="TopSection", default=None)
     TowerCanType: Optional[str] = Field(alias="TowerCanType", default='SimpleTowerCan', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/simplified_linear_foundation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/simplified_linear_foundation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.foundation import Foundation
 
 
 class SimplifiedLinearFoundation(Foundation, ComponentType='SimplifiedLinearFoundation'):
     """SimplifiedLinearFoundation - Foundation type ?.  This *may* be a super-simple foundation.
-
+    
     Attributes:
-        - TranslationalStiffness: float => A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
-        - Mass: float => A number representing a mass.  The SI units for mass are kilograms.
-        - RotationalStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - RotationalInertia: float => A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SimplifiedLinearFoundation&#39;
+    ----------
+    TranslationalStiffness : float
+        A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
+    Mass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    RotationalStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    RotationalInertia : float
+        A number representing an angular inertia.  The SI units for angular inertia are kilogram radian squareds.
+    ComponentType : str, readonly, default='SimplifiedLinearFoundation'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SimplifiedLinearFoundation'
+    
     """
 
     TranslationalStiffness: Optional[float] = Field(alias="TranslationalStiffness", default=None)
     Mass: Optional[float] = Field(alias="Mass", default=None)
     RotationalStiffness: Optional[float] = Field(alias="RotationalStiffness", default=None)
     RotationalInertia: Optional[float] = Field(alias="RotationalInertia", default=None)
     ComponentType: Optional[str] = Field(alias="ComponentType", default='SimplifiedLinearFoundation', allow_mutation=False)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/single_actuator_collective_hub.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/single_actuator_collective_hub.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.standard_hub import StandardHub
 
 
 class SingleActuatorCollectiveHub(StandardHub, ComponentType='SingleActuatorCollectiveHub'):
     """SingleActuatorCollectiveHub - A collective hub with two or more blades.  A single pitch system controls all blade angles through a mechanism within the hub.
-
+    
     Attributes:
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SingleActuatorCollectiveHub&#39;
+    ----------
+    ComponentType : str, readonly, default='SingleActuatorCollectiveHub'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SingleActuatorCollectiveHub'
+    
     """
 
     ComponentType: Optional[str] = Field(alias="ComponentType", default='SingleActuatorCollectiveHub', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/single_lidar_focal_distance.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/single_lidar_focal_distance.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.lidar_focal_distance_control import LidarFocalDistanceControl
 
 
 class SingleLidarFocalDistance(LidarFocalDistanceControl, FocalDistanceControlType='SingleFocalDistance'):
     """SingleLidarFocalDistance - A single focal distance at which the Lidar samples.
-
+    
     Attributes:
-        - FocalDistance: float => A number representing a length.  The SI units for length are metres.
-        - FocalDistanceControlType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SingleFocalDistance&#39;
+    ----------
+    FocalDistance : float
+        A number representing a length.  The SI units for length are metres.
+    FocalDistanceControlType : str, readonly, default='SingleFocalDistance'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SingleFocalDistance'
+    
     """
 
     FocalDistance: Optional[float] = Field(alias="FocalDistance", default=None)
     FocalDistanceControlType: Optional[str] = Field(alias="FocalDistanceControlType", default='SingleFocalDistance', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/six_by_six_inertia.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/six_by_six_inertia.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.added_inertia import AddedInertia
 
 
 class SixBySixInertia(AddedInertia, AddedInertiaType='SixBySixInertia'):
     """SixBySixInertia - An inertia added to a structural component specified with a 6x6 inertia matrix.
-
+    
     Attributes:
-        - Inertia: List[List[float]] => A 6x6 matrix of the linear and rotational inertias of an object.
-        - AddedInertiaType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SixBySixInertia&#39;
+    ----------
+    Inertia : List[List[float]], default=list()
+        A 6x6 matrix of the linear and rotational inertias of an object.
+    AddedInertiaType : str, readonly, default='SixBySixInertia'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SixBySixInertia'
+    
     """
 
     Inertia: Optional[List[List[float]]] = Field(alias="Inertia", default=list())
     AddedInertiaType: Optional[str] = Field(alias="AddedInertiaType", default='SixBySixInertia', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/skew_wake_model.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/skew_wake_model.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SkewWakeModel(Dnv):
     """SkewWakeModel - Common properties for all skew wake models.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/slipping_clutch.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/slipping_clutch.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SlippingClutch(Dnv):
     """SlippingClutch - Slipping clutch model
-
+    
     Attributes:
-        - Friction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - Stiction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Friction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    Stiction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Friction: Optional[float] = Field(alias="Friction", default=None)
     Stiction: Optional[float] = Field(alias="Stiction", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/solver_settings.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/solver_settings.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,40 +9,55 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.integrator import Integrator
 
 
 class SolverSettings(Dnv):
     """SolverSettings - Settings for the numerical solving of the time domain simulations.
-
+    
     Attributes:
-        - InitialConditionsRelaxationFactor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - NumberSolveSystemIterations: int => The maximum number of iterations permited to solve the system.
-        - Integrator: Integrator 
+    ----------
+    InitialConditionsRelaxationFactor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    NumberSolveSystemIterations : int
+        The maximum number of iterations permited to solve the system.
+    Integrator : Integrator, abstract
+    
+    Notes:
+    ---------
+    Integrator has the following concrete types:
+        - ExplicitNewmarkBetaFixedStep
+        - GeneralisedAlphaFixedStep
+        - ImplicitNewmarkBetaFixedStep
+        - MidpointMethodFixedStep
+        - RungeKutta4thOrderFixedStep
+        - RungeKuttaVariableStep
+    
     """
 
     InitialConditionsRelaxationFactor: Optional[float] = Field(alias="InitialConditionsRelaxationFactor", default=None)
     NumberSolveSystemIterations: Optional[int] = Field(alias="NumberSolveSystemIterations", default=None)
     Integrator: Optional[Integrator] = Field(alias="Integrator", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/spinner.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/spinner.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,40 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.angle_vs_coefficient_of_drag_multiplied_by_area import AngleVsCoefficientOfDragMultipliedByArea
 
 from dnv_bladed_models.dnv import Dnv
 
 
 class Spinner(Dnv):
     """Spinner - The aerodynamic fairing covering the hub structure.
-
+    
     Attributes:
-        - Diameter: float => A number representing a length.  The SI units for length are metres.
-        - CentreOfDragX: float => A number representing a length.  The SI units for length are metres.
-        - AngleVsCoefficientOfDragMultipliedByArea: List[AngleVsCoefficientOfDragMultipliedByArea] => The relationship between the subtending angle and the coefficient of drag multiplied by the area.
+    ----------
+    Diameter : float
+        A number representing a length.  The SI units for length are metres.
+    CentreOfDragX : float
+        A number representing a length.  The SI units for length are metres.
+    AngleVsCoefficientOfDragMultipliedByArea : List[AngleVsCoefficientOfDragMultipliedByArea], default=list()
+        The relationship between the subtending angle and the coefficient of drag multiplied by the area.
+    
     """
 
     Diameter: Optional[float] = Field(alias="Diameter", default=None)
     CentreOfDragX: Optional[float] = Field(alias="CentreOfDragX", default=None)
     AngleVsCoefficientOfDragMultipliedByArea: Optional[List[AngleVsCoefficientOfDragMultipliedByArea]] = Field(alias="AngleVsCoefficientOfDragMultipliedByArea", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/spring_force_or_torque_vs_position.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_torque.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class SpringForceOrTorqueVsPosition(Dnv):
-    """SpringForceOrTorqueVsPosition - A line or row in a look-up table, specifying a value of Position for a specified SpringForceOrTorque.
 
+class TimeVsTorque(Dnv):
+    """TimeVsTorque - The relationship between the time following the brake&#39;s activation, and the retarding torque it applies to the shaft.
+    
     Attributes:
-        - SpringForceOrTorque: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - Position: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    Torque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
-    SpringForceOrTorque: Optional[float] = Field(alias="SpringForceOrTorque", default=None)
-    Position: Optional[float] = Field(alias="Position", default=None)
+    Time: Optional[float] = Field(alias="Time", default=None)
+    Torque: Optional[float] = Field(alias="Torque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        SpringForceOrTorqueVsPosition
+        TimeVsTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = SpringForceOrTorqueVsPosition.from_file('/path/to/file')
+        >>> model = TimeVsTorque.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        SpringForceOrTorqueVsPosition
+        TimeVsTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = SpringForceOrTorqueVsPosition.from_json('{ ... }')
+        >>> model = TimeVsTorque.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        SpringForceOrTorqueVsPosition
+        TimeVsTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-SpringForceOrTorqueVsPosition.update_forward_refs()
+TimeVsTorque.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/standard_hub.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/standard_hub.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,38 +9,42 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.hub import Hub
 
 from dnv_bladed_models.rotor_imbalances import RotorImbalances
 
 
 class StandardHub(Hub):
     """StandardHub - The common properties of hubs with rotational symmetry and more than two blades.
-
+    
     Attributes:
-        - NumberOfBlades: int => The number of blades on the hub.
-        - Imbalances: RotorImbalances 
+    ----------
+    NumberOfBlades : int
+        The number of blades on the hub.
+    Imbalances : RotorImbalances
+    
     """
 
     NumberOfBlades: Optional[int] = Field(alias="NumberOfBlades", default=None)
     Imbalances: Optional[RotorImbalances] = Field(alias="Imbalances", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/standard_pitch_limit_switches.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_hydrodynamic_properties.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,36 +9,46 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.height_up_tower_vs_cd_cm import HeightUpTowerVsCdCm
 
-class StandardPitchLimitSwitches(Dnv):
-    """StandardPitchLimitSwitches - The limit switch positions of the control system.
 
+class TowerHydrodynamicProperties(Dnv):
+    """TowerHydrodynamicProperties - The hydrodynamic properties of the tower, if fully or partly submerged in water.
+    
     Attributes:
-        - MinimumAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - MaximumAngle: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    CoefficientOfDrag : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    CoefficientOfMass : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    HeightUpTowerVsCdCm : List[HeightUpTowerVsCdCm], default=list()
+        A look-up table for the hydrodynamic properties of the tower at different heights.
+    
     """
 
-    MinimumAngle: Optional[float] = Field(alias="MinimumAngle", default=None)
-    MaximumAngle: Optional[float] = Field(alias="MaximumAngle", default=None)
+    CoefficientOfDrag: Optional[float] = Field(alias="CoefficientOfDrag", default=None)
+    CoefficientOfMass: Optional[float] = Field(alias="CoefficientOfMass", default=None)
+    HeightUpTowerVsCdCm: Optional[List[HeightUpTowerVsCdCm]] = Field(alias="HeightUpTowerVsCdCm", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +114,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        StandardPitchLimitSwitches
+        TowerHydrodynamicProperties
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = StandardPitchLimitSwitches.from_file('/path/to/file')
+        >>> model = TowerHydrodynamicProperties.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +144,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        StandardPitchLimitSwitches
+        TowerHydrodynamicProperties
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = StandardPitchLimitSwitches.from_json('{ ... }')
+        >>> model = TowerHydrodynamicProperties.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +176,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        StandardPitchLimitSwitches
+        TowerHydrodynamicProperties
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +208,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-StandardPitchLimitSwitches.update_forward_refs()
+TowerHydrodynamicProperties.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/start_up_operation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/start_up_operation.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.controlled_operation import ControlledOperation
 
 
 class StartUpOperation(ControlledOperation, EventType='StartUpOperation'):
     """StartUpOperation - A request for the controller to start the turbine at a specified time in the simulation.  The start up will be performed by the controller.
-
+    
     Attributes:
-        - InitialPitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;StartUpOperation&#39;
+    ----------
+    InitialPitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    EventType : str, readonly, default='StartUpOperation'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'StartUpOperation'
+    
     """
 
     InitialPitchAngle: Optional[float] = Field(alias="InitialPitchAngle", default=None)
     EventType: Optional[str] = Field(alias="EventType", default='StartUpOperation', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_calculation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SteadyCalculation_SteadyCalculationTypeEnum(str, Enum):
     AERODYNAMIC_INFORMATION = "AerodynamicInformation"
     PERFORMANCE_COEFFICIENTS = "PerformanceCoefficients"
     STEADY_POWER_CURVE = "SteadyPowerCurve"
@@ -24,27 +25,30 @@
     STEADY_PARKED_LOADS = "SteadyParkedLoads"
     CAMPBELL_DIAGRAM = "CampbellDiagram"
     BLADE_STABILITY_ANALYSIS = "BladeStabilityAnalysis"
     MODEL_LINEARISATION = "ModelLinearisation"
 
 class SteadyCalculation(Dnv):
     """SteadyCalculation - The common properties of the steady calculations in Bladed.
-
+    
     Attributes:
-        - SteadyCalculationType: SteadyCalculation_SteadyCalculationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    SteadyCalculationType : SteadyCalculation_SteadyCalculationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - AerodynamicInformationCalculation
-        - BladeStabilityAnalysis
-        - CampbellDiagram
-        - ModelLinearisation
-        - PerformanceCoefficientsCalculation
-        - SteadyOperationalLoadsCalculation
-        - SteadyParkedLoadsCalculation
-        - SteadyPowerCurveCalculation
+        AerodynamicInformationCalculation
+        BladeStabilityAnalysis
+        CampbellDiagram
+        ModelLinearisation
+        PerformanceCoefficientsCalculation
+        SteadyOperationalLoadsCalculation
+        SteadyParkedLoadsCalculation
+        SteadyPowerCurveCalculation
+    
     """
 
     SteadyCalculationType: Optional[SteadyCalculation_SteadyCalculationTypeEnum] = Field(alias="SteadyCalculationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -74,15 +78,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'SteadyCalculation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_calculation_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_calculation_outputs.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.outputs import Outputs
 
 
 class SteadyCalculationOutputs(Outputs):
     """SteadyCalculationOutputs - 
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_calculation_with_component_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_calculation_with_component_outputs.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,40 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.outputs import Outputs
 
 from dnv_bladed_models.selected_component_output_group import SelectedComponentOutputGroup
 
 
 class SteadyCalculationWithComponentOutputs(Outputs):
     """SteadyCalculationWithComponentOutputs - 
-
+    
     Attributes:
-        - SelectedComponentOutputGroups: List[SelectedComponentOutputGroup] => List of SelectedComponentOutputGroup
+    ----------
+    SelectedComponentOutputGroups : List[SelectedComponentOutputGroup], default=list()
+        List of SelectedComponentOutputGroup
+    
     """
 
     SelectedComponentOutputGroups: Optional[List[SelectedComponentOutputGroup]] = Field(alias="SelectedComponentOutputGroups", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_operational_loads_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_operational_loads_calculation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,31 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.steady_calculation import SteadyCalculation
 
 from dnv_bladed_models.steady_calculation_with_component_outputs import SteadyCalculationWithComponentOutputs
 
 from dnv_bladed_models.velocity_range import VelocityRange
 
 
 class SteadyOperationalLoadsCalculation(SteadyCalculation, SteadyCalculationType='SteadyOperationalLoads'):
     """SteadyOperationalLoadsCalculation - Defines a calculation which produces all the loads in a steady, uniform wind field.  The entire turbine is modelled for this analysis and prebend and sweep included, as well as flexibility.  Most realities such as tower shadow and wind shear are ignored.
-
+    
     Attributes:
-        - WindSpeedRange: VelocityRange 
-        - CalculateSpeedAndPitchChange: bool => If true, the pitch and speed change schedule will be calculated by Bladed.
-        - PitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - RotorSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Outputs: SteadyCalculationWithComponentOutputs 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SteadyOperationalLoads&#39;
+    ----------
+    WindSpeedRange : VelocityRange
+    CalculateSpeedAndPitchChange : bool
+        If true, the pitch and speed change schedule will be calculated by Bladed.
+    PitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    RotorSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Outputs : SteadyCalculationWithComponentOutputs
+    SteadyCalculationType : str, readonly, default='SteadyOperationalLoads'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SteadyOperationalLoads'
+    
     """
 
     WindSpeedRange: Optional[VelocityRange] = Field(alias="WindSpeedRange", default=None)
     CalculateSpeedAndPitchChange: Optional[bool] = Field(alias="CalculateSpeedAndPitchChange", default=None)
     PitchAngle: Optional[float] = Field(alias="PitchAngle", default=None)
     RotorSpeed: Optional[float] = Field(alias="RotorSpeed", default=None)
     Outputs: Optional[SteadyCalculationWithComponentOutputs] = Field(alias="Outputs", default=None)
@@ -42,15 +49,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_parked_loads_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_parked_loads_calculation.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,39 +9,51 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.steady_calculation import SteadyCalculation
 
 from dnv_bladed_models.steady_calculation_with_component_outputs import SteadyCalculationWithComponentOutputs
 
 
 class SteadyParkedLoadsCalculation_SweepParameterEnum(str, Enum):
     AZIMUTH_ANGLE = "AZIMUTH_ANGLE"
     YAW_ANGLE = "YAW_ANGLE"
     FLOW_INCLINATION = "FLOW_INCLINATION"
     PITCH_ANGLE = "PITCH_ANGLE"
 
 class SteadyParkedLoadsCalculation(SteadyCalculation, SteadyCalculationType='SteadyParkedLoads'):
     """SteadyParkedLoadsCalculation - Defines a calculation which produces loads on the parked turbine in a steady wind.  Most realities such as tower shadow and wind shear are included, making the calculation almost equivalent to a time domain simulation in a steady wind.
-
+    
     Attributes:
-        - WindSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - AzimuthAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - YawAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - FlowInclination: float => A number representing an angle.  The SI units for angles are radians.
-        - PitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - SweepParameter: SteadyParkedLoadsCalculation_SweepParameterEnum => The parameter to perform the sweep over.
-        - SweepEnd: float => The value for the end of the sweep.  The start value will be whatever it is in the parameters for the calculation.
-        - SweepInterval: float => The step size to take from the lowest to the highest value.
-        - Outputs: SteadyCalculationWithComponentOutputs 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SteadyParkedLoads&#39;
+    ----------
+    WindSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    AzimuthAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    YawAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    FlowInclination : float
+        A number representing an angle.  The SI units for angles are radians.
+    PitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    SweepParameter : SteadyParkedLoadsCalculation_SweepParameterEnum
+        The parameter to perform the sweep over.
+    SweepEnd : float
+        The value for the end of the sweep.  The start value will be whatever it is in the parameters for the calculation.
+    SweepInterval : float
+        The step size to take from the lowest to the highest value.
+    Outputs : SteadyCalculationWithComponentOutputs
+    SteadyCalculationType : str, readonly, default='SteadyParkedLoads'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SteadyParkedLoads'
+    
     """
 
     WindSpeed: Optional[float] = Field(alias="WindSpeed", default=None)
     AzimuthAngle: Optional[float] = Field(alias="AzimuthAngle", default=None)
     YawAngle: Optional[float] = Field(alias="YawAngle", default=None)
     FlowInclination: Optional[float] = Field(alias="FlowInclination", default=None)
     PitchAngle: Optional[float] = Field(alias="PitchAngle", default=None)
@@ -54,15 +66,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_power_curve_calculation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_power_curve_calculation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,31 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.steady_calculation import SteadyCalculation
 
 from dnv_bladed_models.steady_calculation_outputs import SteadyCalculationOutputs
 
 from dnv_bladed_models.velocity_range import VelocityRange
 
 
 class SteadyPowerCurveCalculation(SteadyCalculation, SteadyCalculationType='SteadyPowerCurve'):
     """SteadyPowerCurveCalculation - Defines a calculation which produces power, torque and thrust as a function of wind speed, assuming a uniform steady wind field.  The entire turbine is modelled for this analysis and prebend and sweep included, but without any flexibility.  Most realities such as tower shadow and wind shear are ignored.
-
+    
     Attributes:
-        - WindSpeedRange: VelocityRange 
-        - CalculateSpeedAndPitchChange: bool => If true, the pitch and speed change schedule will be calculated by Bladed.
-        - PitchAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - RotorSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Outputs: SteadyCalculationOutputs 
-        - SteadyCalculationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;SteadyPowerCurve&#39;
+    ----------
+    WindSpeedRange : VelocityRange
+    CalculateSpeedAndPitchChange : bool
+        If true, the pitch and speed change schedule will be calculated by Bladed.
+    PitchAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    RotorSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Outputs : SteadyCalculationOutputs
+    SteadyCalculationType : str, readonly, default='SteadyPowerCurve'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'SteadyPowerCurve'
+    
     """
 
     WindSpeedRange: Optional[VelocityRange] = Field(alias="WindSpeedRange", default=None)
     CalculateSpeedAndPitchChange: Optional[bool] = Field(alias="CalculateSpeedAndPitchChange", default=None)
     PitchAngle: Optional[float] = Field(alias="PitchAngle", default=None)
     RotorSpeed: Optional[float] = Field(alias="RotorSpeed", default=None)
     Outputs: Optional[SteadyCalculationOutputs] = Field(alias="Outputs", default=None)
@@ -42,15 +49,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/steady_wake_deficit.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/steady_wake_deficit.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,32 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SteadyWakeDeficit_SteadyWakeDeficitTypeEnum(str, Enum):
     GAUSSIAN = "Gaussian"
     USER_DEFINED = "UserDefined"
 
 class SteadyWakeDeficit(Dnv):
     """SteadyWakeDeficit - A simple model for the reduced velocity of the wind behind another turbine.  This deficit will be applied across a certain region, and this region will not move around during the simulation.
-
+    
     Attributes:
-        - HorizontalOffsetFromHub: float => A number representing a length.  The SI units for length are metres.
-        - VerticalOffsetFromHub: float => A number representing a length.  The SI units for length are metres.
-        - SteadyWakeDeficitType: SteadyWakeDeficit_SteadyWakeDeficitTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    HorizontalOffsetFromHub : float
+        A number representing a length.  The SI units for length are metres.
+    VerticalOffsetFromHub : float
+        A number representing a length.  The SI units for length are metres.
+    SteadyWakeDeficitType : SteadyWakeDeficit_SteadyWakeDeficitTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - Gaussian
-        - UserDefinedWakeDeficit
+        Gaussian
+        UserDefinedWakeDeficit
+    
     """
 
     HorizontalOffsetFromHub: Optional[float] = Field(alias="HorizontalOffsetFromHub", default=None)
     VerticalOffsetFromHub: Optional[float] = Field(alias="VerticalOffsetFromHub", default=None)
     SteadyWakeDeficitType: Optional[SteadyWakeDeficit_SteadyWakeDeficitTypeEnum] = Field(alias="SteadyWakeDeficitType", default=None)
 
     class Config:
@@ -66,15 +72,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'SteadyWakeDeficit'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/stream_function.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/stream_function.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.regular_waves import RegularWaves
 
 
 class StreamFunction(RegularWaves, WavesType='StreamFunction'):
     """StreamFunction - The definition of Stream Function waves.
-
+    
     Attributes:
-        - UseTotalAcceleration: bool => If true, total acceleration (convective and time derivative) will be used for Morison loading when using the stream function.  The water particle accelerations in Bladed only include the time derivative terms. The convective terms of the water particles can be added when using the stream function for the water particle acceleration calculations.  If irregular waves and additional constrained waves are applied, where the constrained wave is defined by a stream function and total acceleration is turned on, the water particle acceleration will be calculated using local derivatives for the irregular waves and substantive derivatives for the constrained waves and will therefore be inconsistent.
-        - WavesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;StreamFunction&#39;
+    ----------
+    UseTotalAcceleration : bool
+        If true, total acceleration (convective and time derivative) will be used for Morison loading when using the stream function.  The water particle accelerations in Bladed only include the time derivative terms. The convective terms of the water particles can be added when using the stream function for the water particle acceleration calculations.  If irregular waves and additional constrained waves are applied, where the constrained wave is defined by a stream function and total acceleration is turned on, the water particle acceleration will be calculated using local derivatives for the irregular waves and substantive derivatives for the constrained waves and will therefore be inconsistent.
+    WavesType : str, readonly, default='StreamFunction'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'StreamFunction'
+    
     """
 
     UseTotalAcceleration: Optional[bool] = Field(alias="UseTotalAcceleration", default=None)
     WavesType: Optional[str] = Field(alias="WavesType", default='StreamFunction', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structural_mode.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/structural_mode.py`

 * *Files 0% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class StructuralMode(Dnv):
     """StructuralMode - The existance of one of these objects in the list of whole blade modes denotes that a mode should be calculated - even if it is empty.  If no modes are provided, the structure will be considered rigid.
-
+    
     Attributes:
-        - DampingRatio: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    DampingRatio : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     DampingRatio: Optional[float] = Field(alias="DampingRatio", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structural_modelling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/structural_modelling.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.structural_mode import StructuralMode
 
 
 class StructuralModelling(Dnv):
     """StructuralModelling - The modelling options for a component with flexibility.  This is primarily the blade and the support structure.
-
+    
     Attributes:
-        - MaximumNodeSpacing: float => A number representing a length.  The SI units for length are metres.
-        - StructuralModes: List[StructuralMode] => List of StructuralMode
+    ----------
+    MaximumNodeSpacing : float
+        A number representing a length.  The SI units for length are metres.
+    StructuralModes : List[StructuralMode], default=list()
+        List of StructuralMode
+    
     """
 
     MaximumNodeSpacing: Optional[float] = Field(alias="MaximumNodeSpacing", default=None)
     StructuralModes: Optional[List[StructuralMode]] = Field(alias="StructuralModes", default=list())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structural_modelling_settings.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/structural_modelling_settings.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class StructuralModellingSettings_BladeGeometricStiffnessModelEnum(str, Enum):
     AXIAL_LOADS_ONLY = "AxialLoadsOnly"
     FULL_MODEL_WITH_ORIENTATION_CORRECTION = "FullModelWithOrientationCorrection"
     INTERNAL_LOADS_ONLY = "InternalLoadsOnly"
@@ -25,31 +26,35 @@
 class StructuralModellingSettings_SupportStructureGeometricStiffnessModelEnum(str, Enum):
     AXIAL_LOADS_ONLY = "AxialLoadsOnly"
     INTERNAL_LOADS_ONLY = "InternalLoadsOnly"
     DISABLED = "Disabled"
 
 class StructuralModellingSettings(Dnv):
     """StructuralModellingSettings - Settings affecting the structural modelling.
-
+    
     Attributes:
-        - BladeGeometricStiffnessModel: StructuralModellingSettings_BladeGeometricStiffnessModelEnum => The geometric stiffness model to use for the blades. For blades with 1 part, the \&quot;axial loads only\&quot; model is recommended. This configuration is only appropriate for relatively stiff blades, undergoing small deflection.  For more flexible blade models, a multi-part blade model is more appropriate. In this case, the \&quot;full with orientation correction\&quot; is the recommended option, as long as deflection remains small within each blade part.
-        - SupportStructureGeometricStiffnessModel: StructuralModellingSettings_SupportStructureGeometricStiffnessModelEnum => The geometric stiffness model to use for the support structure
+    ----------
+    BladeGeometricStiffnessModel : StructuralModellingSettings_BladeGeometricStiffnessModelEnum, default='AxialLoadsOnly'
+        The geometric stiffness model to use for the blades. For blades with 1 part, the \"axial loads only\" model is recommended. This configuration is only appropriate for relatively stiff blades, undergoing small deflection.  For more flexible blade models, a multi-part blade model is more appropriate. In this case, the \"full with orientation correction\" is the recommended option, as long as deflection remains small within each blade part.
+    SupportStructureGeometricStiffnessModel : StructuralModellingSettings_SupportStructureGeometricStiffnessModelEnum, default='AxialLoadsOnly'
+        The geometric stiffness model to use for the support structure
+    
     """
 
     BladeGeometricStiffnessModel: Optional[StructuralModellingSettings_BladeGeometricStiffnessModelEnum] = Field(alias="BladeGeometricStiffnessModel", default='AxialLoadsOnly')
     SupportStructureGeometricStiffnessModel: Optional[StructuralModellingSettings_SupportStructureGeometricStiffnessModelEnum] = Field(alias="SupportStructureGeometricStiffnessModel", default='AxialLoadsOnly')
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/structure.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/structure.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.flex_body import FlexBody
 
 
 class Structure(FlexBody):
     """Structure - The common properties of components with structural flexibility.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/sub_surface_current.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/sub_surface_current.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class SubSurfaceCurrent(Dnv):
     """SubSurfaceCurrent - The definition of a current whose strength depends on height above the sea bed according to a power law or a user defined relationship.
-
+    
     Attributes:
-        - VelocityAtSurface: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - Heading: float => A number representing an angle.  The SI units for angles are radians.
-        - ShearExponent: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    VelocityAtSurface : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    Heading : float
+        A number representing an angle.  The SI units for angles are radians.
+    ShearExponent : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     VelocityAtSurface: Optional[float] = Field(alias="VelocityAtSurface", default=None)
     Heading: Optional[float] = Field(alias="Heading", default=None)
     ShearExponent: Optional[float] = Field(alias="ShearExponent", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/superelement.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/superelement.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,36 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.component import Component
 
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 from dnv_bladed_models.interface_load_file import InterfaceLoadFile
 
 
 class Superelement_WaveLoadingUnitsInFileEnum(str, Enum):
     N = "N"
     K_N = "kN"
 
 class Superelement(Component, ComponentType='Superelement'):
     """Superelement - A structural superelement, mathematically representing a structural component.
-
+    
     Attributes:
-        - DefinitionFilepath: str => The filepath of URI to the superelement data.
-        - WaveLoadingFilepath: str => The filepath of URI to the accompanying wave data.
-        - WaveLoadingUnitsInFile: Superelement_WaveLoadingUnitsInFileEnum => The units that were used when generating the wave load files.  The industry standard is to provide wave loads in kN and kNm.
-        - CoordinateTransformationMatrix: List[List[float]] => A 6x6 matrix to transform one set of coordinates into another.
-        - InterfaceLoadFile: InterfaceLoadFile 
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Superelement&#39;
+    ----------
+    DefinitionFilepath : str
+        The filepath of URI to the superelement data.
+    WaveLoadingFilepath : str
+        The filepath of URI to the accompanying wave data.
+    WaveLoadingUnitsInFile : Superelement_WaveLoadingUnitsInFileEnum, default='kN'
+        The units that were used when generating the wave load files.  The industry standard is to provide wave loads in kN and kNm.
+    CoordinateTransformationMatrix : List[List[float]], default=list()
+        A 6x6 matrix to transform one set of coordinates into another.
+    InterfaceLoadFile : InterfaceLoadFile
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    ComponentType : str, readonly, default='Superelement'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Superelement'
+    
     """
 
     DefinitionFilepath: Optional[str] = Field(alias="DefinitionFilepath", default=None)
     WaveLoadingFilepath: Optional[str] = Field(alias="WaveLoadingFilepath", default=None)
     WaveLoadingUnitsInFile: Optional[Superelement_WaveLoadingUnitsInFileEnum] = Field(alias="WaveLoadingUnitsInFile", default='kN')
     CoordinateTransformationMatrix: Optional[List[List[float]]] = Field(alias="CoordinateTransformationMatrix", default=list())
     InterfaceLoadFile: Optional[InterfaceLoadFile] = Field(alias="InterfaceLoadFile", default=None)
@@ -48,15 +57,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/support_structure_point_loading.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/support_structure_point_loading.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.applied_load import AppliedLoad
 
 
 class SupportStructurePointLoading(AppliedLoad):
     """SupportStructurePointLoading - 
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/thrust_vs_coulomb_friction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/thrust_vs_coulomb_friction.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class ThrustVsCoulombFriction(Dnv):
     """ThrustVsCoulombFriction - The relationship between the Coulomb friction in the yaw bearing and the applied thrust.
-
+    
     Attributes:
-        - Thrust: float => A number representing a force.  The SI units for force are Newtons.
-        - CoulombFriction: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Thrust : float
+        A number representing a force.  The SI units for force are Newtons.
+    CoulombFriction : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     Thrust: Optional[float] = Field(alias="Thrust", default=None)
     CoulombFriction: Optional[float] = Field(alias="CoulombFriction", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tide.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tide.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Tide(Dnv):
     """Tide - The definition of the tide to be considered for the analysis.
-
+    
     Attributes:
-        - TideHeight: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    TideHeight : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     TideHeight: Optional[float] = Field(alias="TideHeight", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_current.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_current.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,42 +9,54 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.current import Current
 
 from dnv_bladed_models.current_direction_variation import CurrentDirectionVariation
 
 
 class TimeDomainCurrent(Current):
     """TimeDomainCurrent - The definition of a current field that varies throughout a time domain simulation.
-
+    
     Attributes:
-        - ReferenceHeight: float => A number representing a length.  The SI units for length are metres.
-        - Inclination: float => A number representing an angle.  The SI units for angles are radians.
-        - Direction: float => A number representing an angle.  The SI units for angles are radians.
-        - DirectionVariation: CurrentDirectionVariation 
+    ----------
+    ReferenceHeight : float
+        A number representing a length.  The SI units for length are metres.
+    Inclination : float
+        A number representing an angle.  The SI units for angles are radians.
+    Direction : float
+        A number representing an angle.  The SI units for angles are radians.
+    DirectionVariation : CurrentDirectionVariation, abstract
+    
+    Notes:
+    ---------
+    DirectionVariation has the following concrete types:
+        - PresetCurrentDirectionTransient
+        - CurrentDirectionTimeHistory
+    
     """
 
     ReferenceHeight: Optional[float] = Field(alias="ReferenceHeight", default=None)
     Inclination: Optional[float] = Field(alias="Inclination", default=None)
     Direction: Optional[float] = Field(alias="Direction", default=None)
     DirectionVariation: Optional[CurrentDirectionVariation] = Field(alias="DirectionVariation", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_outputs.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,36 +9,52 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.outputs import Outputs
 
 from dnv_bladed_models.selected_component_output_group import SelectedComponentOutputGroup
 
 
 class TimeDomainOutputs(Outputs):
     """TimeDomainOutputs - The definition outputs to write for this simulation.
-
+    
     Attributes:
-        - TimeStepForOutputs: float => A number representing a time.  The SI units for time are seconds.
-        - LengthOfOutputBuffer: float => A number representing a time.  The SI units for time are seconds.
-        - OutputSummaryInformation: bool => If true, the summary information output group will be created.
-        - OutputExternalControllers: bool => If true, the controller output group will be created.
-        - OutputBuoyancyInformation: bool => If true, the buoyancy output group will be created.
-        - OutputFiniteElementMatrices: bool => If true, the finite element output group will be created, providing far more detail about the finite element matrices.
-        - OutputSignalProperties: bool => If true, the signal properties output group will be created.  This records the properties provided to the controller, with and without noise and other distortions.
-        - OutputWakePropagation: bool => If true, the eddy viscosity propagation of the wake is output as a 2D table of relative velocity against radial position and distance travelled to a \&quot;.wake\&quot; file in the output folder.
-        - OutputSoftwarePerformance: bool => If true, the software performance output group will be created.
-        - OutputStateInformation: bool => If true, the integrator state output group will be created.  This can be used to help understand how efficiently the integrator is coping with the simulation.
-        - OutputExternalControllerExchangeObject: bool => If true, this will output all of the values contained in the external controller interface before and after each external controller call.  This is intended to assist debugging external controllers.
-        - OutputExternalControllerLegacySwapArray: bool => If true, the contents of the swap array passed to a legacy controller will be logged.  This is used only when trying to debug legacy controllers, and will not produce useful results if there is more than one legacy controller being run.
-        - SelectedComponentOutputGroups: List[SelectedComponentOutputGroup] => A list of references to the OutputGroup of specific components to output.  This allows the outputs of individual components to be switched off, or chosen from an available list of output regimes.  If a component is not mentioned, it will produce outputs according to its default output group, if there is one available.
+    ----------
+    TimeStepForOutputs : float
+        A number representing a time.  The SI units for time are seconds.
+    LengthOfOutputBuffer : float
+        A number representing a time.  The SI units for time are seconds.
+    OutputSummaryInformation : bool, default=True
+        If true, the summary information output group will be created.
+    OutputExternalControllers : bool, default=True
+        If true, the controller output group will be created.
+    OutputBuoyancyInformation : bool, default=False
+        If true, the buoyancy output group will be created.
+    OutputFiniteElementMatrices : bool, default=False
+        If true, the finite element output group will be created, providing far more detail about the finite element matrices.
+    OutputSignalProperties : bool, default=False
+        If true, the signal properties output group will be created.  This records the properties provided to the controller, with and without noise and other distortions.
+    OutputWakePropagation : bool, default=False
+        If true, the eddy viscosity propagation of the wake is output as a 2D table of relative velocity against radial position and distance travelled to a \".wake\" file in the output folder.
+    OutputSoftwarePerformance : bool, default=False
+        If true, the software performance output group will be created.
+    OutputStateInformation : bool, default=False
+        If true, the integrator state output group will be created.  This can be used to help understand how efficiently the integrator is coping with the simulation.
+    OutputExternalControllerExchangeObject : bool, default=False
+        If true, this will output all of the values contained in the external controller interface before and after each external controller call.  This is intended to assist debugging external controllers.
+    OutputExternalControllerLegacySwapArray : bool, default=False
+        If true, the contents of the swap array passed to a legacy controller will be logged.  This is used only when trying to debug legacy controllers, and will not produce useful results if there is more than one legacy controller being run.
+    SelectedComponentOutputGroups : List[SelectedComponentOutputGroup], default=list()
+        A list of references to the OutputGroup of specific components to output.  This allows the outputs of individual components to be switched off, or chosen from an available list of output regimes.  If a component is not mentioned, it will produce outputs according to its default output group, if there is one available.
+    
     """
 
     TimeStepForOutputs: Optional[float] = Field(alias="TimeStepForOutputs", default=None)
     LengthOfOutputBuffer: Optional[float] = Field(alias="LengthOfOutputBuffer", default=None)
     OutputSummaryInformation: Optional[bool] = Field(alias="OutputSummaryInformation", default=True)
     OutputExternalControllers: Optional[bool] = Field(alias="OutputExternalControllers", default=True)
     OutputBuoyancyInformation: Optional[bool] = Field(alias="OutputBuoyancyInformation", default=False)
@@ -54,15 +70,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_simulation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_simulation.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.applied_load import AppliedLoad
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.environment import Environment
 
 from dnv_bladed_models.event import Event
@@ -28,25 +29,33 @@
 from dnv_bladed_models.initial_condition import InitialCondition
 
 from dnv_bladed_models.time_domain_outputs import TimeDomainOutputs
 
 
 class TimeDomainSimulation(Dnv):
     """TimeDomainSimulation - The definition of a time domain analysis - mutually exclusive with &#39;SteadyCalculation&#39;
-
+    
     Attributes:
-        - Duration: float => A number representing a time.  The SI units for time are seconds.
-        - LeadInTime: float => A number representing a time.  The SI units for time are seconds.
-        - Outputs: TimeDomainOutputs 
-        - AdditionalExternalModules: List[ExternalModule] => A list of external module that will be used for this simulation only.  This could be used to apply loading to the structure, or some other load case specific purpose.  If the external module is seeking to simulate an intrinsic property of the turbine, consider moving it into the GlobalExternalModules, or adding it to the Assembly tree.
-        - ExternallySteppedSimulation: ExternallySteppedSimulation 
-        - Environment: Environment 
-        - AppliedLoads: List[AppliedLoad] => A list of point loading definitions which apply a time history of forces to the structure.
-        - InitialConditions: List[InitialCondition] => A list of initial conditions to apply at the beginning of the simulation.
-        - Events: List[Event] => A list of events that occur during the simulation.
+    ----------
+    Duration : float
+        A number representing a time.  The SI units for time are seconds.
+    LeadInTime : float
+        A number representing a time.  The SI units for time are seconds.
+    Outputs : TimeDomainOutputs
+    AdditionalExternalModules : List[ExternalModule], default=list()
+        A list of external module that will be used for this simulation only.  This could be used to apply loading to the structure, or some other load case specific purpose.  If the external module is seeking to simulate an intrinsic property of the turbine, consider moving it into the GlobalExternalModules, or adding it to the Assembly tree.
+    ExternallySteppedSimulation : ExternallySteppedSimulation
+    Environment : Environment
+    AppliedLoads : List[AppliedLoad], default=list()
+        A list of point loading definitions which apply a time history of forces to the structure.
+    InitialConditions : List[InitialCondition], default=list()
+        A list of initial conditions to apply at the beginning of the simulation.
+    Events : List[Event], default=list()
+        A list of events that occur during the simulation.
+    
     """
 
     Duration: Optional[float] = Field(alias="Duration", default=None)
     LeadInTime: Optional[float] = Field(alias="LeadInTime", default=None)
     Outputs: Optional[TimeDomainOutputs] = Field(alias="Outputs", default=None)
     AdditionalExternalModules: Optional[List[ExternalModule]] = Field(alias="AdditionalExternalModules", default=list())
     ExternallySteppedSimulation: Optional[ExternallySteppedSimulation] = Field(alias="ExternallySteppedSimulation", default=None)
@@ -58,15 +67,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_domain_wind.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_domain_wind.py`

 * *Files 9% similar despite different names*

```diff
@@ -9,38 +9,59 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dynamic_upstream_wake import DynamicUpstreamWake
 
 from dnv_bladed_models.steady_wake_deficit import SteadyWakeDeficit
 
 from dnv_bladed_models.vertical_shear import VerticalShear
 
 from dnv_bladed_models.wind import Wind
 
 from dnv_bladed_models.wind_direction_variation import WindDirectionVariation
 
 
 class TimeDomainWind(Wind):
     """TimeDomainWind - The definition of a wind field that varies throughout a time domain simulation.
-
+    
     Attributes:
-        - ReferenceHeight: float => A number representing a length.  The SI units for length are metres.
-        - Inclination: float => A number representing an angle.  The SI units for angles are radians.
-        - UseGustPropagation: bool => If true, gust propagation will be applied (where the transient properties only \&quot;arrive\&quot; at the turbine as the flow does).  This is only relevant to transient flows.
-        - Direction: float => A number representing an angle.  The SI units for angles are radians.
-        - DirectionVariation: WindDirectionVariation 
-        - SteadyWakeDeficit: SteadyWakeDeficit 
-        - VerticalShear: VerticalShear 
-        - VerticalDirectionShear: float => A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
-        - DynamicUpstreamWake: DynamicUpstreamWake 
+    ----------
+    ReferenceHeight : float
+        A number representing a length.  The SI units for length are metres.
+    Inclination : float
+        A number representing an angle.  The SI units for angles are radians.
+    UseGustPropagation : bool
+        If true, gust propagation will be applied (where the transient properties only \"arrive\" at the turbine as the flow does).  This is only relevant to transient flows.
+    Direction : float
+        A number representing an angle.  The SI units for angles are radians.
+    DirectionVariation : WindDirectionVariation, abstract
+    SteadyWakeDeficit : SteadyWakeDeficit, abstract
+    VerticalShear : VerticalShear, abstract
+    VerticalDirectionShear : float
+        A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    DynamicUpstreamWake : DynamicUpstreamWake
+    
+    Notes:
+    ---------
+    DirectionVariation has the following concrete types:
+        - PresetWindDirectionTransient
+        - WindDirectionTimeHistory
+    SteadyWakeDeficit has the following concrete types:
+        - Gaussian
+        - UserDefinedWakeDeficit
+    VerticalShear has the following concrete types:
+        - ExponentialShearModel
+        - LogarithmicShearModel
+        - LookUpShearModel
+    
     """
 
     ReferenceHeight: Optional[float] = Field(alias="ReferenceHeight", default=None)
     Inclination: Optional[float] = Field(alias="Inclination", default=None)
     UseGustPropagation: Optional[bool] = Field(alias="UseGustPropagation", default=None)
     Direction: Optional[float] = Field(alias="Direction", default=None)
     DirectionVariation: Optional[WindDirectionVariation] = Field(alias="DirectionVariation", default=None)
@@ -52,15 +73,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_direction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_direction.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsCurrentDirection(Dnv):
     """TimeVsCurrentDirection - A line or row in a look-up table, specifying a value of Direction for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - Direction: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    Direction : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     Direction: Optional[float] = Field(alias="Direction", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_direction_shear.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_direction_shear.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsCurrentDirectionShear(Dnv):
     """TimeVsCurrentDirectionShear - A line or row in a look-up table, specifying a value of DirectionShear for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - DirectionShear: float => A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    DirectionShear : float
+        A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     DirectionShear: Optional[float] = Field(alias="DirectionShear", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_horizontal_shear.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_horizontal_shear.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsCurrentHorizontalShear(Dnv):
     """TimeVsCurrentHorizontalShear - A line or row in a look-up table, specifying a value of HorizontalShear for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - HorizontalShear: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    HorizontalShear : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     HorizontalShear: Optional[float] = Field(alias="HorizontalShear", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_mean_speed.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_mean_speed.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsCurrentMeanSpeed(Dnv):
     """TimeVsCurrentMeanSpeed - A line or row in a look-up table, specifying a value of MeanSpeed for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - MeanSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    MeanSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     MeanSpeed: Optional[float] = Field(alias="MeanSpeed", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_current_vertical_shear.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_current_vertical_shear.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsCurrentVerticalShear(Dnv):
     """TimeVsCurrentVerticalShear - A line or row in a look-up table, specifying a value of VerticalShear for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - VerticalShear: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    VerticalShear : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     VerticalShear: Optional[float] = Field(alias="VerticalShear", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_frequency_change.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/waves.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,36 +9,78 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
-class TimeVsFrequencyChange(Dnv):
-    """TimeVsFrequencyChange - A single point in the time vs frequency time history.
+class Waves_WavesTypeEnum(str, Enum):
+    WAVE_SPECTRUM = "WaveSpectrum"
+    LINEAR_AIRY = "LinearAiry"
+    STREAM_FUNCTION = "StreamFunction"
+    JONSWAP_PIERSON_MOSKOWITZ = "JonswapPiersonMoskowitz"
+    USER_DEFINED = "UserDefined"
 
+class Waves(Dnv):
+    """Waves - The definition of a wave field to apply to this analysis
+    
     Attributes:
-        - Time: float => The time after the disturbance starts.  The first point in the series should be at Time&#x3D;0.0.
-        - FrequencyChange: float => The change in frequency at the corresponding time.    0.0 would represent no disturbance in the frequency.
+    ----------
+    WavesType : Waves_WavesTypeEnum
+        Allows the schema to identify the type of the object.
+
+    This class is an abstraction, with the following concrete implementations:
+        JonswapPiersonMoskowitz
+        LinearAiry
+        StreamFunction
+        UserDefinedWaves
+        WaveSpectrum
+    
     """
 
-    Time: Optional[float] = Field(alias="Time", default=None)
-    FrequencyChange: Optional[float] = Field(alias="FrequencyChange", default=None)
+    WavesType: Optional[Waves_WavesTypeEnum] = Field(alias="WavesType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
+    _subtypes_ = dict()
+
+    def __init_subclass__(cls, WavesType=None):
+        cls._subtypes_[WavesType or cls.__name__.lower()] = cls
+
+    @classmethod
+    def __get_validators__(cls):
+        yield cls._convert_to_real_type_
+
+    @classmethod
+    def _convert_to_real_type_(cls, data: dict):
+        if isinstance(data, dict):
+            data_type = data.get("WavesType")
+
+            if data_type is None:
+                raise ValueError("Missing 'WavesType' in Waves")
+
+            sub = cls._subtypes_.get(data_type)
+
+            if sub is None:
+                raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Waves'")
+
+            return sub(**data)
+
+        return data
+
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +146,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TimeVsFrequencyChange
+        Waves
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TimeVsFrequencyChange.from_file('/path/to/file')
+        >>> model = Waves.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +176,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TimeVsFrequencyChange
+        Waves
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TimeVsFrequencyChange.from_json('{ ... }')
+        >>> model = Waves.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +208,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TimeVsFrequencyChange
+        Waves
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +240,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TimeVsFrequencyChange.update_forward_refs()
+Waves.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_voltage_change.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class TimeVsTorque(Dnv):
-    """TimeVsTorque - The relationship between the time following the brake&#39;s activation, and the retarding torque it applies to the shaft.
 
+class TimeVsVoltageChange(Dnv):
+    """TimeVsVoltageChange - A single point in the time vs voltage time history.
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - Torque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    VoltageChange : float
+        A number representing an electrical voltage [https://ww.wikipedia.org/wiki/Voltage].  The SI units for volatage are Volts.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
-    Torque: Optional[float] = Field(alias="Torque", default=None)
+    VoltageChange: Optional[float] = Field(alias="VoltageChange", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TimeVsTorque
+        TimeVsVoltageChange
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TimeVsTorque.from_file('/path/to/file')
+        >>> model = TimeVsVoltageChange.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TimeVsTorque
+        TimeVsVoltageChange
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TimeVsTorque.from_json('{ ... }')
+        >>> model = TimeVsVoltageChange.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TimeVsTorque
+        TimeVsVoltageChange
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TimeVsTorque.update_forward_refs()
+TimeVsVoltageChange.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_voltage_change.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/water_characteristics.py`

 * *Files 9% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class TimeVsVoltageChange(Dnv):
-    """TimeVsVoltageChange - A single point in the time vs voltage time history.
 
+class WaterCharacteristics(Dnv):
+    """WaterCharacteristics - The properties of water for use in offshore calculations.
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - VoltageChange: float => A number representing an electrical voltage [https://ww.wikipedia.org/wiki/Voltage].  The SI units for volatage are Volts.
+    ----------
+    Density : float
+        A number representing a material's density.  The SI units for volume are kilograms per cubic metre.
+    VapourPressure : float
+        A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    
     """
 
-    Time: Optional[float] = Field(alias="Time", default=None)
-    VoltageChange: Optional[float] = Field(alias="VoltageChange", default=None)
+    Density: Optional[float] = Field(alias="Density", default=None)
+    VapourPressure: Optional[float] = Field(alias="VapourPressure", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TimeVsVoltageChange
+        WaterCharacteristics
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TimeVsVoltageChange.from_file('/path/to/file')
+        >>> model = WaterCharacteristics.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TimeVsVoltageChange
+        WaterCharacteristics
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TimeVsVoltageChange.from_json('{ ... }')
+        >>> model = WaterCharacteristics.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TimeVsVoltageChange
+        WaterCharacteristics
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TimeVsVoltageChange.update_forward_refs()
+WaterCharacteristics.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_direction.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_direction.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsWindDirection(Dnv):
     """TimeVsWindDirection - A line or row in a look-up table, specifying a value of Direction for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - Direction: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    Direction : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     Direction: Optional[float] = Field(alias="Direction", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_direction_shear.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_direction_shear.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsWindDirectionShear(Dnv):
     """TimeVsWindDirectionShear - A line or row in a look-up table, specifying a value of DirectionShear for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - DirectionShear: float => A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    DirectionShear : float
+        A number representing an angle varying by a length.  The SI units for angle per length radians per metre.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     DirectionShear: Optional[float] = Field(alias="DirectionShear", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_horizontal_shear.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_horizontal_shear.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsWindHorizontalShear(Dnv):
     """TimeVsWindHorizontalShear - A line or row in a look-up table, specifying a value of HorizontalShear for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - HorizontalShear: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    HorizontalShear : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     HorizontalShear: Optional[float] = Field(alias="HorizontalShear", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_mean_speed.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_mean_speed.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsWindMeanSpeed(Dnv):
     """TimeVsWindMeanSpeed - A line or row in a look-up table, specifying a value of MeanSpeed for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - MeanSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    MeanSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     MeanSpeed: Optional[float] = Field(alias="MeanSpeed", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/time_vs_wind_vertical_shear.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/time_vs_wind_vertical_shear.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TimeVsWindVerticalShear(Dnv):
     """TimeVsWindVerticalShear - A line or row in a look-up table, specifying a value of VerticalShear for a specified Time.
-
+    
     Attributes:
-        - Time: float => A number representing a time.  The SI units for time are seconds.
-        - VerticalShear: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    Time : float
+        A number representing a time.  The SI units for time are seconds.
+    VerticalShear : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     Time: Optional[float] = Field(alias="Time", default=None)
     VerticalShear: Optional[float] = Field(alias="VerticalShear", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/timed_event.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/timed_event.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.event import Event
 
 
 class TimedEvent(Event):
     """TimedEvent - An event that occurs at a specified time in the simulation.
-
+    
     Attributes:
-        - TimeOccurs: float => A number representing a time.  The SI units for time are seconds.
+    ----------
+    TimeOccurs : float
+        A number representing a time.  The SI units for time are seconds.
+    
     """
 
     TimeOccurs: Optional[float] = Field(alias="TimeOccurs", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/torque_controlled_yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/torque_controlled_yaw_system.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.non_actuated_yaw_system import NonActuatedYawSystem
 
 
 class TorqueControlledYawSystem(NonActuatedYawSystem, ComponentType='TorqueControlledYawSystem'):
     """TorqueControlledYawSystem - A simple yaw system definition, applying a torque to move the yaw bearing which is specified by the controller.
-
+    
     Attributes:
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TorqueControlledYawSystem&#39;
+    ----------
+    ComponentType : str, readonly, default='TorqueControlledYawSystem'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TorqueControlledYawSystem'
+    
     """
 
     ComponentType: Optional[str] = Field(alias="ComponentType", default='TorqueControlledYawSystem', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/torque_scaling_losses.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/torque_scaling_losses.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.mechanical_losses import MechanicalLosses
 
 from dnv_bladed_models.shaft_speed_vs_resisting_torque import ShaftSpeedVsResistingTorque
 
 
 class TorqueScalingLosses(MechanicalLosses, MechanicalLossesType='TorqueScalingLosses'):
     """TorqueScalingLosses - A loss model where the losses are expressed as an opposing torque which varies with the shaft speed.
-
+    
     Attributes:
-        - InputTorqueVsShaftSpeedVsResistingTorque: Dict[str, ShaftSpeedVsResistingTorque] => A series of look-up tables for the losses, each valid for the specified input torque.
-        - MechanicalLossesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TorqueScalingLosses&#39;
+    ----------
+    InputTorqueVsShaftSpeedVsResistingTorque : Dict[str, ShaftSpeedVsResistingTorque], default=dict()
+        A series of look-up tables for the losses, each valid for the specified input torque.
+    MechanicalLossesType : str, readonly, default='TorqueScalingLosses'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TorqueScalingLosses'
+    
     """
 
     InputTorqueVsShaftSpeedVsResistingTorque: Optional[Dict[str, ShaftSpeedVsResistingTorque]] = Field(alias="InputTorqueVsShaftSpeedVsResistingTorque", default=dict())
     MechanicalLossesType: Optional[str] = Field(alias="MechanicalLossesType", default='TorqueScalingLosses', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.added_inertia import AddedInertia
 
 from dnv_bladed_models.structure import Structure
 
 from dnv_bladed_models.tower_aerodynamic_properties import TowerAerodynamicProperties
 
 from dnv_bladed_models.tower_can import TowerCan
@@ -30,25 +31,34 @@
 from dnv_bladed_models.tower_output_group import TowerOutputGroup
 
 from dnv_bladed_models.tower_sensor import TowerSensor
 
 
 class Tower(Structure, ComponentType='Tower'):
     """Tower - An axisymmetric tower, made from a series of tower cans.
-
+    
     Attributes:
-        - MaterialsLibrary: Dict[str, TowerMaterial] => A library of material definitions.  These can be referenced by the tower cans.
-        - Cans: List[TowerCan] => A list of cans, each one sitting on the top of the previous one.  These cans can be constant-section or tapered.
-        - AerodynamicProperties: TowerAerodynamicProperties 
-        - HydrodynamicProperties: TowerHydrodynamicProperties 
-        - PointInertias: List[AddedInertia] => A list of additional inertias to add to the tower.
-        - OutputGroups: Dict[str, TowerOutputGroup] => A library which contains any number of named output groups.  These can be referenced from &#39;SelectedOutputGroup&#39;.
-        - ConnectableNodes: Dict[str, TowerConnectableNode] => A declaration of what nodes can be connected to.
-        - Sensors: Dict[str, TowerSensor] => A library of sensor objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Tower&#39;
+    ----------
+    MaterialsLibrary : Dict[str, TowerMaterial], default=dict()
+        A library of material definitions.  These can be referenced by the tower cans.
+    Cans : List[TowerCan], default=list()
+        A list of cans, each one sitting on the top of the previous one.  These cans can be constant-section or tapered.
+    AerodynamicProperties : TowerAerodynamicProperties
+    HydrodynamicProperties : TowerHydrodynamicProperties
+    PointInertias : List[AddedInertia], default=list()
+        A list of additional inertias to add to the tower.
+    OutputGroups : Dict[str, TowerOutputGroup], default=dict()
+        A library which contains any number of named output groups.  These can be referenced from 'SelectedOutputGroup'.
+    ConnectableNodes : Dict[str, TowerConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    Sensors : Dict[str, TowerSensor], default=dict()
+        A library of sensor objects, each specified with a unique keyword.  This keyword is used for referencing the item elsewhere in the model.
+    ComponentType : str, readonly, default='Tower'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Tower'
+    
     """
 
     MaterialsLibrary: Optional[Dict[str, TowerMaterial]] = Field(alias="MaterialsLibrary", default=dict())
     Cans: Optional[List[TowerCan]] = Field(alias="Cans", default=list())
     AerodynamicProperties: Optional[TowerAerodynamicProperties] = Field(alias="AerodynamicProperties", default=None)
     HydrodynamicProperties: Optional[TowerHydrodynamicProperties] = Field(alias="HydrodynamicProperties", default=None)
     PointInertias: Optional[List[AddedInertia]] = Field(alias="PointInertias", default=list())
@@ -60,15 +70,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_accelerometer.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_accelerometer.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_sensor import TowerSensor
 
 
 class TowerAccelerometer(TowerSensor, TowerSensorType='Accelerometer'):
     """TowerAccelerometer - An accelerometer attached to the tower.
-
+    
     Attributes:
-        - TowerSensorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Accelerometer&#39;
+    ----------
+    TowerSensorType : str, readonly, default='Accelerometer'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Accelerometer'
+    
     """
 
     TowerSensorType: Optional[str] = Field(alias="TowerSensorType", default='Accelerometer', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_aerodynamic_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_shadow_potential_flow.py`

 * *Files 9% similar despite different names*

```diff
@@ -9,38 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
-
-from dnv_bladed_models.flow_obstruction import FlowObstruction
 
+from dnv_bladed_models.dnv import Dnv
 
-class TowerAerodynamicProperties(Dnv):
-    """TowerAerodynamicProperties - The aerodynamic properties of the tower, including tower shadow.
 
+class TowerShadowPotentialFlow(Dnv):
+    """TowerShadowPotentialFlow - Parameters for the potential flow [https://www.wikipedia.org/wiki/Potential_flow] tower shadow model.
+    
     Attributes:
-        - CoefficientOfDrag: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TowerShadow: FlowObstruction 
+    ----------
+    CorrectionFactor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
-    CoefficientOfDrag: Optional[float] = Field(alias="CoefficientOfDrag", default=None)
-    TowerShadow: Optional[FlowObstruction] = Field(alias="TowerShadow", default=None)
+    CorrectionFactor: Optional[float] = Field(alias="CorrectionFactor", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -106,25 +106,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TowerAerodynamicProperties
+        TowerShadowPotentialFlow
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerAerodynamicProperties.from_file('/path/to/file')
+        >>> model = TowerShadowPotentialFlow.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -136,25 +136,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TowerAerodynamicProperties
+        TowerShadowPotentialFlow
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerAerodynamicProperties.from_json('{ ... }')
+        >>> model = TowerShadowPotentialFlow.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -168,15 +168,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TowerAerodynamicProperties
+        TowerShadowPotentialFlow
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -200,8 +200,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TowerAerodynamicProperties.update_forward_refs()
+TowerShadowPotentialFlow.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,39 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.tower_can_modelling import TowerCanModelling
 
 
 class TowerCan_TowerCanTypeEnum(str, Enum):
     SIMPLE_TOWER_CAN = "SimpleTowerCan"
     EXPLICIT_TOWER_CAN = "ExplicitTowerCan"
 
 class TowerCan(Dnv):
     """TowerCan - The common properties for a single can in the tower
-
+    
     Attributes:
-        - Modelling: TowerCanModelling 
-        - CanHeight: float => A number representing a length.  The SI units for length are metres.
-        - TowerCanType: TowerCan_TowerCanTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    Modelling : TowerCanModelling
+    CanHeight : float
+        A number representing a length.  The SI units for length are metres.
+    TowerCanType : TowerCan_TowerCanTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - ExplicitTowerCan
-        - SimpleTowerCan
+        ExplicitTowerCan
+        SimpleTowerCan
+    
     """
 
     Modelling: Optional[TowerCanModelling] = Field(alias="Modelling", default=None)
     CanHeight: Optional[float] = Field(alias="CanHeight", default=None)
     TowerCanType: Optional[TowerCan_TowerCanTypeEnum] = Field(alias="TowerCanType", default=None)
 
     class Config:
@@ -68,15 +73,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'TowerCan'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_modelling.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_modelling.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TowerCanModelling(Dnv):
     """TowerCanModelling - Parameters controlling the way the can will be modelled by Bladed.
-
+    
     Attributes:
-        - MaximumNodeSpacing: float => A number representing a length.  The SI units for length are metres.
-        - NumberOfIntermediateNodes: int => This many structural nodes will be added equally spaced between the top and bottom of the can.  The default is 0, meaning that (unless the MaximumNodeSpacing requires additional nodes to be added) the can will be represented by a single beam.
+    ----------
+    MaximumNodeSpacing : float
+        A number representing a length.  The SI units for length are metres.
+    NumberOfIntermediateNodes : int, default=0
+        This many structural nodes will be added equally spaced between the top and bottom of the can.  The default is 0, meaning that (unless the MaximumNodeSpacing requires additional nodes to be added) the can will be represented by a single beam.
+    
     """
 
     MaximumNodeSpacing: Optional[float] = Field(alias="MaximumNodeSpacing", default=None)
     NumberOfIntermediateNodes: Optional[int] = Field(alias="NumberOfIntermediateNodes", default=0)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_by_material.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_by_material.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_can_section_properties import TowerCanSectionProperties
 
 
 class TowerCanPropertiesByMaterial(TowerCanSectionProperties):
     """TowerCanPropertiesByMaterial - The definition of a can section where the material properties, the diameter, and the wall thickness will be used to calculate the physical properties.  Any properties which are omitted will be taken from the BaseSection definition.
-
+    
     Attributes:
-        - WallThickness: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    WallThickness : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     WallThickness: Optional[float] = Field(alias="WallThickness", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_by_material_where_different.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_by_material_where_different.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_can_section_properties_where_different import TowerCanSectionPropertiesWhereDifferent
 
 
 class TowerCanPropertiesByMaterialWhereDifferent(TowerCanSectionPropertiesWhereDifferent):
     """TowerCanPropertiesByMaterialWhereDifferent - The definition of a can section where the material properties, the diameter, and the wall thickness will be used to calculate the physical properties.  Any properties which are omitted will be taken from the BaseSection definition.
-
+    
     Attributes:
-        - WallThickness: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    WallThickness : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     WallThickness: Optional[float] = Field(alias="WallThickness", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_explicit.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_explicit.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_can_section_properties import TowerCanSectionProperties
 
 
 class TowerCanPropertiesExplicit(TowerCanSectionProperties):
     """TowerCanPropertiesExplicit - The definition of a single tower can cross-section where any physical properties can be explicitly specified if they are different to those specified in the BaseSection.
-
+    
     Attributes:
-        - MassPerUnitLength: float => A number representing a mass per unit length.  The SI units for mass per unit length are kilograms per metre.
-        - BendingStiffness: float => A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
-        - ShearStiffness: float => A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
-        - TorsionalStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - MomentOfInertia: float => A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
-        - PolarMomentOfInertia: float => A number representing a polar moment of inertia.  The SI units for inertia are metres to the fourth power.
+    ----------
+    MassPerUnitLength : float
+        A number representing a mass per unit length.  The SI units for mass per unit length are kilograms per metre.
+    BendingStiffness : float
+        A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
+    ShearStiffness : float
+        A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
+    TorsionalStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    MomentOfInertia : float
+        A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
+    PolarMomentOfInertia : float
+        A number representing a polar moment of inertia.  The SI units for inertia are metres to the fourth power.
+    
     """
 
     MassPerUnitLength: Optional[float] = Field(alias="MassPerUnitLength", default=None)
     BendingStiffness: Optional[float] = Field(alias="BendingStiffness", default=None)
     ShearStiffness: Optional[float] = Field(alias="ShearStiffness", default=None)
     TorsionalStiffness: Optional[float] = Field(alias="TorsionalStiffness", default=None)
     MomentOfInertia: Optional[float] = Field(alias="MomentOfInertia", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_properties_explicit_where_different.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_properties_explicit_where_different.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_can_section_properties_where_different import TowerCanSectionPropertiesWhereDifferent
 
 
 class TowerCanPropertiesExplicitWhereDifferent(TowerCanSectionPropertiesWhereDifferent):
     """TowerCanPropertiesExplicitWhereDifferent - The definition of a single tower can cross-section where any physical properties can be explicitly specified if they are different to those specified in the BaseSection.
-
+    
     Attributes:
-        - MassPerUnitLength: float => A number representing a mass per unit length.  The SI units for mass per unit length are kilograms per metre.
-        - BendingStiffness: float => A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
-        - ShearStiffness: float => A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
-        - TorsionalStiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - MomentOfInertia: float => A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
-        - PolarMomentOfInertia: float => A number representing a polar moment of inertia.  The SI units for inertia are metres to the fourth power.
+    ----------
+    MassPerUnitLength : float
+        A number representing a mass per unit length.  The SI units for mass per unit length are kilograms per metre.
+    BendingStiffness : float
+        A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
+    ShearStiffness : float
+        A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
+    TorsionalStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    MomentOfInertia : float
+        A number representing an inertia.  The SI units for inertia are kilogram metre squareds.
+    PolarMomentOfInertia : float
+        A number representing a polar moment of inertia.  The SI units for inertia are metres to the fourth power.
+    
     """
 
     MassPerUnitLength: Optional[float] = Field(alias="MassPerUnitLength", default=None)
     BendingStiffness: Optional[float] = Field(alias="BendingStiffness", default=None)
     ShearStiffness: Optional[float] = Field(alias="ShearStiffness", default=None)
     TorsionalStiffness: Optional[float] = Field(alias="TorsionalStiffness", default=None)
     MomentOfInertia: Optional[float] = Field(alias="MomentOfInertia", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_section_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_section_properties.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TowerCanSectionProperties(Dnv):
     """TowerCanSectionProperties - The definition of a can section where the material properties, the diameter, and the wall thickness will be used to calculate the physical properties.  Any properties which are omitted will be taken from the BaseSection definition.
-
+    
     Attributes:
-        - OutsideDiameter: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    OutsideDiameter : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     OutsideDiameter: Optional[float] = Field(alias="OutsideDiameter", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_can_section_properties_where_different.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_can_section_properties_where_different.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TowerCanSectionPropertiesWhereDifferent(Dnv):
     """TowerCanSectionPropertiesWhereDifferent - The definition of a can section where the material properties, the diameter, and the wall thickness will be used to calculate the physical properties.  Any properties which are omitted will be taken from the BaseSection definition.
-
+    
     Attributes:
-        - OutsideDiameter: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    OutsideDiameter : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     OutsideDiameter: Optional[float] = Field(alias="OutsideDiameter", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_connectable_node.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_connectable_node.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.connectable_node import ConnectableNode
 
 
 class TowerConnectableNode(ConnectableNode):
     """TowerConnectableNode - The definition of a connectable node on a tower component.
-
+    
     Attributes:
-        - HeightUpTower: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    HeightUpTower : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     HeightUpTower: Optional[float] = Field(alias="HeightUpTower", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_hydrodynamic_properties.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_strain_gauge.py`

 * *Files 9% similar despite different names*

```diff
@@ -9,40 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
-from dnv_bladed_models.height_up_tower_vs_cd_cm import HeightUpTowerVsCdCm
+from dnv_bladed_models.tower_sensor import TowerSensor
 
 
-class TowerHydrodynamicProperties(Dnv):
-    """TowerHydrodynamicProperties - The hydrodynamic properties of the tower, if fully or partly submerged in water.
-
+class TowerStrainGauge(TowerSensor, TowerSensorType='StrainGauge'):
+    """TowerStrainGauge - A strain gauge (load sensor) affixed to the tower.
+    
     Attributes:
-        - CoefficientOfDrag: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - CoefficientOfMass: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - HeightUpTowerVsCdCm: List[HeightUpTowerVsCdCm] => A look-up table for the hydrodynamic properties of the tower at different heights.
+    ----------
+    TowerSensorType : str, readonly, default='StrainGauge'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'StrainGauge'
+    
     """
 
-    CoefficientOfDrag: Optional[float] = Field(alias="CoefficientOfDrag", default=None)
-    CoefficientOfMass: Optional[float] = Field(alias="CoefficientOfMass", default=None)
-    HeightUpTowerVsCdCm: Optional[List[HeightUpTowerVsCdCm]] = Field(alias="HeightUpTowerVsCdCm", default=list())
+    TowerSensorType: Optional[str] = Field(alias="TowerSensorType", default='StrainGauge', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +106,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TowerHydrodynamicProperties
+        TowerStrainGauge
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerHydrodynamicProperties.from_file('/path/to/file')
+        >>> model = TowerStrainGauge.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +136,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TowerHydrodynamicProperties
+        TowerStrainGauge
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerHydrodynamicProperties.from_json('{ ... }')
+        >>> model = TowerStrainGauge.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +168,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TowerHydrodynamicProperties
+        TowerStrainGauge
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +200,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TowerHydrodynamicProperties.update_forward_refs()
+TowerStrainGauge.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_inclinometer.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_inclinometer.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.tower_sensor import TowerSensor
 
 
 class TowerInclinometer(TowerSensor, TowerSensorType='Inclinometer'):
     """TowerInclinometer - An inclinometer attached to the tower.
-
+    
     Attributes:
-        - TowerSensorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;Inclinometer&#39;
+    ----------
+    TowerSensorType : str, readonly, default='Inclinometer'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'Inclinometer'
+    
     """
 
     TowerSensorType: Optional[str] = Field(alias="TowerSensorType", default='Inclinometer', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_material.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_material.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TowerMaterial(Dnv):
     """TowerMaterial - A material definition for use by the tower cans.
-
+    
     Attributes:
-        - Density: float => A number representing a material&#39;s density.  The SI units for volume are kilograms per cubic metre.
-        - YoungsModulus: float => A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
-        - ShearModulus: float => A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    ----------
+    Density : float
+        A number representing a material's density.  The SI units for volume are kilograms per cubic metre.
+    YoungsModulus : float
+        A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    ShearModulus : float
+        A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    
     """
 
     Density: Optional[float] = Field(alias="Density", default=None)
     YoungsModulus: Optional[float] = Field(alias="YoungsModulus", default=None)
     ShearModulus: Optional[float] = Field(alias="ShearModulus", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_node_at_height_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_node_outputs.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,40 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class TowerNodeAtHeightOutputs(Dnv):
-    """TowerNodeAtHeightOutputs - The outputs to produce for a specified node or node type.
 
+class TowerNodeOutputs(Dnv):
+    """TowerNodeOutputs - The outputs to produce for a specified node or node type.
+    
     Attributes:
-        - Displacements: bool => If true, the displacements of the nodes from their undeformed position will be output.
-        - Loads: bool => If true, the loads on the tower at the node&#39;s location will be output.
-        - WaterKinematics: bool => If true, the water velocities and accelerations at the node&#39;s location will be output.
-        - SnapToNearestNode: bool => If true, the outputs will be at the nearest structural node, which may be at a significantly different height to that specified.
+    ----------
+    Displacements : bool, default=False
+        If true, the displacements of the nodes from their undeformed position will be output.
+    Loads : bool, default=False
+        If true, the loads on the tower at the node's location will be output.
+    WaterKinematics : bool, default=False
+        If true, the water velocities and accelerations at the node's location will be output.
+    
     """
 
     Displacements: Optional[bool] = Field(alias="Displacements", default=False)
     Loads: Optional[bool] = Field(alias="Loads", default=False)
     WaterKinematics: Optional[bool] = Field(alias="WaterKinematics", default=False)
-    SnapToNearestNode: Optional[bool] = Field(alias="SnapToNearestNode", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +112,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TowerNodeAtHeightOutputs
+        TowerNodeOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerNodeAtHeightOutputs.from_file('/path/to/file')
+        >>> model = TowerNodeOutputs.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +142,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TowerNodeAtHeightOutputs
+        TowerNodeOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerNodeAtHeightOutputs.from_json('{ ... }')
+        >>> model = TowerNodeOutputs.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +174,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TowerNodeAtHeightOutputs
+        TowerNodeOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +206,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TowerNodeAtHeightOutputs.update_forward_refs()
+TowerNodeOutputs.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_node_loads.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_node_loads.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,29 +9,40 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TowerNodeLoads(Dnv):
     """TowerNodeLoads - The moments and forces to output for any node location where the loads are requested.
-
+    
     Attributes:
-        - Mx: bool => If true, the moment about the X axis will be output.
-        - My: bool => If true, the moment about the Y axis will be output.
-        - Mz: bool => If true, the moment about the Z axis will be output.
-        - Mxy: bool => If true, the maximum moment about any axis that lies in the XY plane will be output.
-        - Fx: bool => If true, the force in the X direction will be output.
-        - Fy: bool => If true, the force in the Y direction will be output.
-        - Fz: bool => If true, the force in the Z direction will be output.
-        - Fxy: bool => If true, the maximum force along any axis that lies in the XY plane will be output.
+    ----------
+    Mx : bool, default=False
+        If true, the moment about the X axis will be output.
+    My : bool, default=False
+        If true, the moment about the Y axis will be output.
+    Mz : bool, default=False
+        If true, the moment about the Z axis will be output.
+    Mxy : bool
+        If true, the maximum moment about any axis that lies in the XY plane will be output.
+    Fx : bool, default=False
+        If true, the force in the X direction will be output.
+    Fy : bool, default=False
+        If true, the force in the Y direction will be output.
+    Fz : bool, default=False
+        If true, the force in the Z direction will be output.
+    Fxy : bool
+        If true, the maximum force along any axis that lies in the XY plane will be output.
+    
     """
 
     Mx: Optional[bool] = Field(alias="Mx", default=False)
     My: Optional[bool] = Field(alias="My", default=False)
     Mz: Optional[bool] = Field(alias="Mz", default=False)
     Mxy: Optional[bool] = Field(alias="Mxy", default=None)
     Fx: Optional[bool] = Field(alias="Fx", default=False)
@@ -42,15 +53,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_node_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_point_loading.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,38 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.support_structure_point_loading import SupportStructurePointLoading
 
-class TowerNodeOutputs(Dnv):
-    """TowerNodeOutputs - The outputs to produce for a specified node or node type.
 
+class TowerPointLoading(SupportStructurePointLoading, AppliedLoadType='TowerPointLoading'):
+    """TowerPointLoading - A time history of point loading applied to an axisymmetric tower.
+    
     Attributes:
-        - Displacements: bool => If true, the displacements of the nodes from their undeformed position will be output.
-        - Loads: bool => If true, the loads on the tower at the node&#39;s location will be output.
-        - WaterKinematics: bool => If true, the water velocities and accelerations at the node&#39;s location will be output.
+    ----------
+    HeightOfImpact : float
+        A number representing a length.  The SI units for length are metres.
+    AppliedLoadType : str, readonly, default='TowerPointLoading'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TowerPointLoading'
+    
     """
 
-    Displacements: Optional[bool] = Field(alias="Displacements", default=False)
-    Loads: Optional[bool] = Field(alias="Loads", default=False)
-    WaterKinematics: Optional[bool] = Field(alias="WaterKinematics", default=False)
+    HeightOfImpact: Optional[float] = Field(alias="HeightOfImpact", default=None)
+    AppliedLoadType: Optional[str] = Field(alias="AppliedLoadType", default='TowerPointLoading', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -106,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TowerNodeOutputs
+        TowerPointLoading
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerNodeOutputs.from_file('/path/to/file')
+        >>> model = TowerPointLoading.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -136,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TowerNodeOutputs
+        TowerPointLoading
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerNodeOutputs.from_json('{ ... }')
+        >>> model = TowerPointLoading.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -168,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TowerNodeOutputs
+        TowerPointLoading
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -200,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TowerNodeOutputs.update_forward_refs()
+TowerPointLoading.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_output_group.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,35 +9,40 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.tower_node_at_height_outputs import TowerNodeAtHeightOutputs
 
 from dnv_bladed_models.tower_node_loads import TowerNodeLoads
 
 from dnv_bladed_models.tower_node_outputs import TowerNodeOutputs
 
 
 class TowerOutputGroup_AxisSystemEnum(str, Enum):
     GLOBAL = "GLOBAL"
 
 class TowerOutputGroup(Dnv):
     """TowerOutputGroup - A named tower output group. 
-
+    
     Attributes:
-        - SelectedNodeLoads: TowerNodeLoads 
-        - AxisSystem: TowerOutputGroup_AxisSystemEnum => The axis sytstem to be used for the outputs.
-        - AllConnectableNodes: TowerNodeOutputs 
-        - AllNodes: TowerNodeOutputs 
-        - NodesAtHeights: Dict[str, TowerNodeAtHeightOutputs] => A set of tower heights at which to output loads.
+    ----------
+    SelectedNodeLoads : TowerNodeLoads
+    AxisSystem : TowerOutputGroup_AxisSystemEnum, default='GLOBAL'
+        The axis sytstem to be used for the outputs.
+    AllConnectableNodes : TowerNodeOutputs
+    AllNodes : TowerNodeOutputs
+    NodesAtHeights : Dict[str, TowerNodeAtHeightOutputs], default=dict()
+        A set of tower heights at which to output loads.
+    
     """
 
     SelectedNodeLoads: Optional[TowerNodeLoads] = Field(alias="SelectedNodeLoads", default=None)
     AxisSystem: Optional[TowerOutputGroup_AxisSystemEnum] = Field(alias="AxisSystem", default='GLOBAL')
     AllConnectableNodes: Optional[TowerNodeOutputs] = Field(alias="AllConnectableNodes", default=None)
     AllNodes: Optional[TowerNodeOutputs] = Field(alias="AllNodes", default=None)
     NodesAtHeights: Optional[Dict[str, TowerNodeAtHeightOutputs]] = Field(alias="NodesAtHeights", default=dict())
@@ -45,15 +50,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_point_loading.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/velocity_vs_damping_torque.py`

 * *Files 11% similar despite different names*

```diff
@@ -9,36 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.support_structure_point_loading import SupportStructurePointLoading
 
+from dnv_bladed_models.dnv import Dnv
 
-class TowerPointLoading(SupportStructurePointLoading, AppliedLoadType='TowerPointLoading'):
-    """TowerPointLoading - A time history of point loading applied to an axisymmetric tower.
 
+class VelocityVsDampingTorque(Dnv):
+    """VelocityVsDampingTorque - A line or row in a look-up table, specifying a value of DampingTorque for a specified Velocity.
+    
     Attributes:
-        - HeightOfImpact: float => A number representing a length.  The SI units for length are metres.
-        - AppliedLoadType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TowerPointLoading&#39;
+    ----------
+    Velocity : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    DampingTorque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
-    HeightOfImpact: Optional[float] = Field(alias="HeightOfImpact", default=None)
-    AppliedLoadType: Optional[str] = Field(alias="AppliedLoadType", default='TowerPointLoading', allow_mutation=False)
+    Velocity: Optional[float] = Field(alias="Velocity", default=None)
+    DampingTorque: Optional[float] = Field(alias="DampingTorque", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TowerPointLoading
+        VelocityVsDampingTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerPointLoading.from_file('/path/to/file')
+        >>> model = VelocityVsDampingTorque.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TowerPointLoading
+        VelocityVsDampingTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerPointLoading.from_json('{ ... }')
+        >>> model = VelocityVsDampingTorque.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TowerPointLoading
+        VelocityVsDampingTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TowerPointLoading.update_forward_refs()
+VelocityVsDampingTorque.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_sensor.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_sensor.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,34 +9,40 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TowerSensor_TowerSensorTypeEnum(str, Enum):
     ACCELEROMETER = "Accelerometer"
     STRAIN_GAUGE = "StrainGauge"
     INCLINOMETER = "Inclinometer"
 
 class TowerSensor(Dnv):
     """TowerSensor - The common properties for all tower sensors.
-
+    
     Attributes:
-        - HeightUpTower: float => A number representing a length.  The SI units for length are metres.
-        - SnapToNearestNode: bool => If true, the sensorwill be placed at the nearest structural node, which may be at a significantly different height to that specified.
-        - TowerSensorType: TowerSensor_TowerSensorTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    HeightUpTower : float
+        A number representing a length.  The SI units for length are metres.
+    SnapToNearestNode : bool
+        If true, the sensorwill be placed at the nearest structural node, which may be at a significantly different height to that specified.
+    TowerSensorType : TowerSensor_TowerSensorTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - TowerAccelerometer
-        - TowerInclinometer
-        - TowerStrainGauge
+        TowerAccelerometer
+        TowerInclinometer
+        TowerStrainGauge
+    
     """
 
     HeightUpTower: Optional[float] = Field(alias="HeightUpTower", default=None)
     SnapToNearestNode: Optional[bool] = Field(alias="SnapToNearestNode", default=None)
     TowerSensorType: Optional[TowerSensor_TowerSensorTypeEnum] = Field(alias="TowerSensorType", default=None)
 
     class Config:
@@ -68,15 +74,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'TowerSensor'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_shadow_potential_flow.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wake_properties_of_upstream_turbine.py`

 * *Files 12% similar despite different names*

```diff
@@ -9,34 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class TowerShadowPotentialFlow(Dnv):
-    """TowerShadowPotentialFlow - Parameters for the potential flow [https://www.wikipedia.org/wiki/Potential_flow] tower shadow model.
 
+class WakePropertiesOfUpstreamTurbine(Dnv):
+    """WakePropertiesOfUpstreamTurbine - 
+    
     Attributes:
-        - CorrectionFactor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    AerodynamicInformationResultsPath : str
+        A file or URI containing aerodynamic information results, used to generate the initial wake profile of the upwind turbine
+    SteadyOperationalLoadsResultsPath : str
+        A file or URI containing aerodynamic information outputs from this 'Steady Operational Loads' run generates the initial wake profile of the upwind turbine. The steady operational loads run should be performed over all wind speeds and have aerodynamic information outputs switched on for blade 1 at all blade stations.
+    
     """
 
-    CorrectionFactor: Optional[float] = Field(alias="CorrectionFactor", default=None)
+    AerodynamicInformationResultsPath: Optional[str] = Field(alias="AerodynamicInformationResultsPath", default=None)
+    SteadyOperationalLoadsResultsPath: Optional[str] = Field(alias="SteadyOperationalLoadsResultsPath", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -102,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TowerShadowPotentialFlow
+        WakePropertiesOfUpstreamTurbine
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerShadowPotentialFlow.from_file('/path/to/file')
+        >>> model = WakePropertiesOfUpstreamTurbine.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -132,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TowerShadowPotentialFlow
+        WakePropertiesOfUpstreamTurbine
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerShadowPotentialFlow.from_json('{ ... }')
+        >>> model = WakePropertiesOfUpstreamTurbine.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -164,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TowerShadowPotentialFlow
+        WakePropertiesOfUpstreamTurbine
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -196,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TowerShadowPotentialFlow.update_forward_refs()
+WakePropertiesOfUpstreamTurbine.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_shadow_powles.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_shadow_powles.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,32 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TowerShadowPowles_DownstreamVariationEnum(str, Enum):
     NONE = "None"
     INVERSE_SQUARE = "InverseSquare"
     LINEAR = "Linear"
 
 class TowerShadowPowles(Dnv):
     """TowerShadowPowles - Parameters for the empirical Powles tower shadow model.
-
+    
     Attributes:
-        - DownstreamVariation: TowerShadowPowles_DownstreamVariationEnum => The type of downstream variation there is in the Powles&#39; wake model: none, linear between two values, or an inverse square root drop-off (with the wake width scaling in proportion to the square root of the distance downstream).
-        - MaximumDeficit: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - ShadowWidthProportion: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - LengthReference1: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - MaximumDeficit2: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - LengthReference2: float => A number representing a fraction, ratio, or other non-dimensionalised property.
+    ----------
+    DownstreamVariation : TowerShadowPowles_DownstreamVariationEnum, default='None'
+        The type of downstream variation there is in the Powles' wake model: none, linear between two values, or an inverse square root drop-off (with the wake width scaling in proportion to the square root of the distance downstream).
+    MaximumDeficit : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    ShadowWidthProportion : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    LengthReference1 : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    MaximumDeficit2 : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    LengthReference2 : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    
     """
 
     DownstreamVariation: Optional[TowerShadowPowles_DownstreamVariationEnum] = Field(alias="DownstreamVariation", default='None')
     MaximumDeficit: Optional[float] = Field(alias="MaximumDeficit", default=None)
     ShadowWidthProportion: Optional[float] = Field(alias="ShadowWidthProportion", default=None)
     LengthReference1: Optional[float] = Field(alias="LengthReference1", default=None)
     MaximumDeficit2: Optional[float] = Field(alias="MaximumDeficit2", default=None)
@@ -43,15 +52,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/tower_strain_gauge.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/frozen_wake_model.py`

 * *Files 14% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.tower_sensor import TowerSensor
 
+from dnv_bladed_models.dynamic_wake import DynamicWake
 
-class TowerStrainGauge(TowerSensor, TowerSensorType='StrainGauge'):
-    """TowerStrainGauge - A strain gauge (load sensor) affixed to the tower.
 
+class FrozenWakeModel(DynamicWake, DynamicWakeType='FrozenWakeModel'):
+    """FrozenWakeModel - The frozen wake model keeps the induction velocity from the initial conditions, and doesn’t change the induction either during linearisation perturbations or in the time domain.  In linearisation this lack of response can be a good approximation to the lagged dynamic wake response.  The frozen wake approach is not appropriate in the time domain.
+    
     Attributes:
-        - TowerSensorType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;StrainGauge&#39;
+    ----------
+    DynamicWakeType : str, readonly, default='FrozenWakeModel'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'FrozenWakeModel'
+    
     """
 
-    TowerSensorType: Optional[str] = Field(alias="TowerSensorType", default='StrainGauge', allow_mutation=False)
+    DynamicWakeType: Optional[str] = Field(alias="DynamicWakeType", default='FrozenWakeModel', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -102,25 +106,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        TowerStrainGauge
+        FrozenWakeModel
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerStrainGauge.from_file('/path/to/file')
+        >>> model = FrozenWakeModel.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -132,25 +136,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        TowerStrainGauge
+        FrozenWakeModel
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = TowerStrainGauge.from_json('{ ... }')
+        >>> model = FrozenWakeModel.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -164,15 +168,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        TowerStrainGauge
+        FrozenWakeModel
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -196,8 +200,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-TowerStrainGauge.update_forward_refs()
+FrozenWakeModel.update_forward_refs()
```

#### encoding

```diff
@@ -1 +1 @@
-us-ascii
+utf-8
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_behaviour.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_behaviour.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TransducerBehaviour(Dnv):
     """TransducerBehaviour - 
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,41 +9,56 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class TransducerFaults_FaultTypeEnum(str, Enum):
     NONE = "None"
     NOISE = "Noise"
     TO_CONSTANT = "ToConstant"
     CONSTANT_OFFSET = "ConstantOffset"
     GROWING_OFFSET = "GrowingOffset"
     HARMONIC_OFFSET = "HarmonicOffset"
 
 class TransducerFaults(Dnv):
     """TransducerFaults - 
-
+    
     Attributes:
-        - ApplyOnlyToFirst: bool => If true, the fault will be aplied only to the first instance of this transducer.  If false, all sensors of this type will fail identically.
-        - MiscalibrationOffset: float => A constant value added to the sensor&#39;s output - for example, if the sensor had been incorrectly installed or poorly calibrated.
-        - InverseInstallation: bool => Whether the sensor was installed in reverse, resulting in its outputs being multiplied by -1.0.
-        - FailTime: float => A number representing a time.  The SI units for time are seconds.
-        - FaultType: TransducerFaults_FaultTypeEnum => What fault occurs at a specified time in the simulation.
-        - NoiseMultiplier: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - FailureValue: float => The value which the sensor reverts to in the case of failure.
-        - FailureValueRampRate: float => The rate at which the signal reverts to its fail value.  A rate of 0.0 represents an instantaneous step change to the fail value.
-        - ConstantOffset: float => The value by which the signal is offset by.
-        - GrowingOffset: float => The rate at which the signal is deviating from its calibrated value.
-        - Magnitude: float => The greatest magnitude of the harmonic interference from the &#39;true&#39; value.
-        - Period: float => A number representing a time.  The SI units for time are seconds.
+    ----------
+    ApplyOnlyToFirst : bool, default=True
+        If true, the fault will be aplied only to the first instance of this transducer.  If false, all sensors of this type will fail identically.
+    MiscalibrationOffset : float, default=0.0
+        A constant value added to the sensor's output - for example, if the sensor had been incorrectly installed or poorly calibrated.
+    InverseInstallation : bool, default=False
+        Whether the sensor was installed in reverse, resulting in its outputs being multiplied by -1.0.
+    FailTime : float
+        A number representing a time.  The SI units for time are seconds.
+    FaultType : TransducerFaults_FaultTypeEnum, default='None'
+        What fault occurs at a specified time in the simulation.
+    NoiseMultiplier : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    FailureValue : float, default=0.0
+        The value which the sensor reverts to in the case of failure.
+    FailureValueRampRate : float, default=0.0
+        The rate at which the signal reverts to its fail value.  A rate of 0.0 represents an instantaneous step change to the fail value.
+    ConstantOffset : float, default=0.0
+        The value by which the signal is offset by.
+    GrowingOffset : float, default=0.0
+        The rate at which the signal is deviating from its calibrated value.
+    Magnitude : float, default=0.0
+        The greatest magnitude of the harmonic interference from the 'true' value.
+    Period : float
+        A number representing a time.  The SI units for time are seconds.
+    
     """
 
     ApplyOnlyToFirst: Optional[bool] = Field(alias="ApplyOnlyToFirst", default=True)
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=0.0)
     InverseInstallation: Optional[bool] = Field(alias="InverseInstallation", default=False)
     FailTime: Optional[float] = Field(alias="FailTime", default=None)
     FaultType: Optional[TransducerFaults_FaultTypeEnum] = Field(alias="FaultType", default='None')
@@ -58,15 +73,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_acceleration.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_acceleration.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsAcceleration(TransducerFaults):
     """TransducerFaultsAcceleration - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - FailureValue: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - FailureValueRampRate: float => A number representing a linear jerk.  The SI units for jerk are metres per second cubed.
-        - ConstantOffset: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - GrowingOffset: float => A number representing a linear jerk.  The SI units for jerk are metres per second cubed.
-        - Magnitude: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    ----------
+    MiscalibrationOffset : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    FailureValue : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    FailureValueRampRate : float
+        A number representing a linear jerk.  The SI units for jerk are metres per second cubed.
+    ConstantOffset : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    GrowingOffset : float
+        A number representing a linear jerk.  The SI units for jerk are metres per second cubed.
+    Magnitude : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_angle.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_angle.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsAngle(TransducerFaults):
     """TransducerFaultsAngle - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing an angle.  The SI units for angles are radians.
-        - FailureValue: float => A number representing an angle.  The SI units for angles are radians.
-        - FailureValueRampRate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - ConstantOffset: float => A number representing an angle.  The SI units for angles are radians.
-        - GrowingOffset: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - Magnitude: float => A number representing an angle.  The SI units for angles are radians.
+    ----------
+    MiscalibrationOffset : float
+        A number representing an angle.  The SI units for angles are radians.
+    FailureValue : float
+        A number representing an angle.  The SI units for angles are radians.
+    FailureValueRampRate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    ConstantOffset : float
+        A number representing an angle.  The SI units for angles are radians.
+    GrowingOffset : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    Magnitude : float
+        A number representing an angle.  The SI units for angles are radians.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_angular_acceleration.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_angular_acceleration.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsAngularAcceleration(TransducerFaults):
     """TransducerFaultsAngularAcceleration - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - FailureValue: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - FailureValueRampRate: float => A number representing the rate of change of an angular acceleration.  The SI units for angular jerk are radians per second cubed.
-        - ConstantOffset: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - GrowingOffset: float => A number representing the rate of change of an angular acceleration.  The SI units for angular jerk are radians per second cubed.
-        - Magnitude: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    ----------
+    MiscalibrationOffset : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    FailureValue : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    FailureValueRampRate : float
+        A number representing the rate of change of an angular acceleration.  The SI units for angular jerk are radians per second cubed.
+    ConstantOffset : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    GrowingOffset : float
+        A number representing the rate of change of an angular acceleration.  The SI units for angular jerk are radians per second cubed.
+    Magnitude : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_angular_velocity.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_angular_velocity.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsAngularVelocity(TransducerFaults):
     """TransducerFaultsAngularVelocity - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - FailureValue: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - FailureValueRampRate: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - ConstantOffset: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - GrowingOffset: float => A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
-        - Magnitude: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    ----------
+    MiscalibrationOffset : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    FailureValue : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    FailureValueRampRate : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    ConstantOffset : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    GrowingOffset : float
+        A number representing an angular acceleration.  The SI units for angular acceleration are radians per second squared.
+    Magnitude : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_force.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_force.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsForce(TransducerFaults):
     """TransducerFaultsForce - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing a force.  The SI units for force are Newtons.
-        - FailureValue: float => A number representing a force.  The SI units for force are Newtons.
-        - FailureValueRampRate: float => A number representing a rate of change of a force.  The SI units for force are Newtons per second.
-        - ConstantOffset: float => A number representing a force.  The SI units for force are Newtons.
-        - GrowingOffset: float => A number representing a rate of change of a force.  The SI units for force are Newtons per second.
-        - Magnitude: float => A number representing a force.  The SI units for force are Newtons.
+    ----------
+    MiscalibrationOffset : float
+        A number representing a force.  The SI units for force are Newtons.
+    FailureValue : float
+        A number representing a force.  The SI units for force are Newtons.
+    FailureValueRampRate : float
+        A number representing a rate of change of a force.  The SI units for force are Newtons per second.
+    ConstantOffset : float
+        A number representing a force.  The SI units for force are Newtons.
+    GrowingOffset : float
+        A number representing a rate of change of a force.  The SI units for force are Newtons per second.
+    Magnitude : float
+        A number representing a force.  The SI units for force are Newtons.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_length.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_length.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsLength(TransducerFaults):
     """TransducerFaultsLength - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing a length.  The SI units for length are metres.
-        - FailureValue: float => A number representing a length.  The SI units for length are metres.
-        - FailureValueRampRate: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - ConstantOffset: float => A number representing a length.  The SI units for length are metres.
-        - GrowingOffset: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - Magnitude: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    MiscalibrationOffset : float
+        A number representing a length.  The SI units for length are metres.
+    FailureValue : float
+        A number representing a length.  The SI units for length are metres.
+    FailureValueRampRate : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    ConstantOffset : float
+        A number representing a length.  The SI units for length are metres.
+    GrowingOffset : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    Magnitude : float
+        A number representing a length.  The SI units for length are metres.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_moment.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_moment.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsMoment(TransducerFaults):
     """TransducerFaultsMoment - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - FailureValue: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - FailureValueRampRate: float => A number representing the rate of change of a torque or moment.  The SI units for the rate of change of moments are Newton-metres per second.
-        - ConstantOffset: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - GrowingOffset: float => A number representing the rate of change of a torque or moment.  The SI units for the rate of change of moments are Newton-metres per second.
-        - Magnitude: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    MiscalibrationOffset : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    FailureValue : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    FailureValueRampRate : float
+        A number representing the rate of change of a torque or moment.  The SI units for the rate of change of moments are Newton-metres per second.
+    ConstantOffset : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    GrowingOffset : float
+        A number representing the rate of change of a torque or moment.  The SI units for the rate of change of moments are Newton-metres per second.
+    Magnitude : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_power.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_power.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsPower(TransducerFaults):
     """TransducerFaultsPower - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing a power.  The SI units for enery are Watts.
-        - FailureValue: float => A number representing a power.  The SI units for enery are Watts.
-        - FailureValueRampRate: float => A number representing the rate of change of a power.  The SI units for enery are Watts per second.
-        - ConstantOffset: float => A number representing a power.  The SI units for enery are Watts.
-        - GrowingOffset: float => A number representing the rate of change of a power.  The SI units for enery are Watts per second.
-        - Magnitude: float => A number representing a power.  The SI units for enery are Watts.
+    ----------
+    MiscalibrationOffset : float
+        A number representing a power.  The SI units for enery are Watts.
+    FailureValue : float
+        A number representing a power.  The SI units for enery are Watts.
+    FailureValueRampRate : float
+        A number representing the rate of change of a power.  The SI units for enery are Watts per second.
+    ConstantOffset : float
+        A number representing a power.  The SI units for enery are Watts.
+    GrowingOffset : float
+        A number representing the rate of change of a power.  The SI units for enery are Watts per second.
+    Magnitude : float
+        A number representing a power.  The SI units for enery are Watts.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/transducer_faults_velocity.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/transducer_faults_velocity.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,27 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.transducer_faults import TransducerFaults
 
 
 class TransducerFaultsVelocity(TransducerFaults):
     """TransducerFaultsVelocity - 
-
+    
     Attributes:
-        - MiscalibrationOffset: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - FailureValue: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - FailureValueRampRate: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - ConstantOffset: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - GrowingOffset: float => A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
-        - Magnitude: float => A number representing a velocity.  The SI units for velocity are metres per second.
+    ----------
+    MiscalibrationOffset : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    FailureValue : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    FailureValueRampRate : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    ConstantOffset : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    GrowingOffset : float
+        A number representing a linear acceleration.  The SI units for acceleration are metres per second squared.
+    Magnitude : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    
     """
 
     MiscalibrationOffset: Optional[float] = Field(alias="MiscalibrationOffset", default=None)
     FailureValue: Optional[float] = Field(alias="FailureValue", default=None)
     FailureValueRampRate: Optional[float] = Field(alias="FailureValueRampRate", default=None)
     ConstantOffset: Optional[float] = Field(alias="ConstantOffset", default=None)
     GrowingOffset: Optional[float] = Field(alias="GrowingOffset", default=None)
@@ -38,15 +47,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/turbine.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/turbine.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,48 +9,53 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.assembly_node import AssemblyNode
 
 from dnv_bladed_models.bladed_control import BladedControl
 
 from dnv_bladed_models.dnv import Dnv
 
 from dnv_bladed_models.electrical_grid import ElectricalGrid
 
 from dnv_bladed_models.external_module import ExternalModule
 
 
 class Turbine(Dnv):
     """Turbine - The definition of the turbine and its installation that is to be modelled.
-
+    
     Attributes:
-        - ElectricalGrid: ElectricalGrid 
-        - Control: BladedControl 
-        - GlobalExternalModules: List[ExternalModule] => A list of any external modules that will be run with the time domain simulations.  It is expected that external modules defined here will interact with more than one area of the turbine, such as to apply additional aerodynamics loads to the entire structure.  Any external modules that represent a single component should be added to the Assembly tree.
-        - Assembly: Dict[str, AssemblyNode] => A tree definition of the turbine&#39;s physical structure
+    ----------
+    ElectricalGrid : ElectricalGrid
+    Control : BladedControl
+    GlobalExternalModules : List[ExternalModule], default=list()
+        A list of any external modules that will be run with the time domain simulations.  It is expected that external modules defined here will interact with more than one area of the turbine, such as to apply additional aerodynamics loads to the entire structure.  Any external modules that represent a single component should be added to the Assembly tree.
+    Assembly : Dict[str, AssemblyNode], default=dict()
+        A tree definition of the turbine's physical structure
+    
     """
 
     ElectricalGrid: Optional[ElectricalGrid] = Field(alias="ElectricalGrid", default=None)
     Control: Optional[BladedControl] = Field(alias="Control", default=None)
     GlobalExternalModules: Optional[List[ExternalModule]] = Field(alias="GlobalExternalModules", default=list())
     Assembly: Optional[Dict[str, AssemblyNode]] = Field(alias="Assembly", default=dict())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/turbulent_current.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/turbulent_current.py`

 * *Files 12% similar despite different names*

```diff
@@ -9,38 +9,50 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.evolving_current_turbulence import EvolvingCurrentTurbulence
 
 from dnv_bladed_models.time_domain_current import TimeDomainCurrent
 
 
 class TurbulentCurrent_CentreTurbulenceFileOnEnum(str, Enum):
     CENTRED_ON_HUB = "CENTRED_ON_HUB"
     BEST_FIT = "BEST_FIT"
     USER_SPECIFIED = "USER_SPECIFIED"
 
 class TurbulentCurrent(TimeDomainCurrent, CurrentType='TurbulentCurrent'):
     """TurbulentCurrent - The definition of a turbulent flow field, with the values for the turbulence defined in an external file.
-
+    
     Attributes:
-        - MeanSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - TurbulenceFilepath: str => The filepath or URI of the turbulence file. 
-        - TurbulenceIntensity: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TurbulenceIntensityLateral: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TurbulenceIntensityVertical: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - CentreTurbulenceFileOn: TurbulentCurrent_CentreTurbulenceFileOnEnum => The method used to position the data in the turbulence file relative to the turbine.  If any part of the rotor exceeds this box, the simulation will terminate with an exception.
-        - RepeatTurbulenceFile: bool => If true, the turbulence file will be \&quot;looped\&quot;.  If false, the turbulence will be 0 in all three components once the end of the file has been reached.  Using part of a turbulence file may invalidate its turbulence statistics, and no effort is made by Bladed to ensure coherence at the point when it transitions from the end of the wind file back to the beginning.
-        - EvolvingTurbulence: EvolvingCurrentTurbulence 
-        - TurbulenceFileStartTime: float => A number representing a time.  The SI units for time are seconds.
-        - CurrentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TurbulentCurrent&#39;
+    ----------
+    MeanSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    TurbulenceFilepath : str
+        The filepath or URI of the turbulence file. 
+    TurbulenceIntensity : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TurbulenceIntensityLateral : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TurbulenceIntensityVertical : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    CentreTurbulenceFileOn : TurbulentCurrent_CentreTurbulenceFileOnEnum
+        The method used to position the data in the turbulence file relative to the turbine.  If any part of the rotor exceeds this box, the simulation will terminate with an exception.
+    RepeatTurbulenceFile : bool
+        If true, the turbulence file will be \"looped\".  If false, the turbulence will be 0 in all three components once the end of the file has been reached.  Using part of a turbulence file may invalidate its turbulence statistics, and no effort is made by Bladed to ensure coherence at the point when it transitions from the end of the wind file back to the beginning.
+    EvolvingTurbulence : EvolvingCurrentTurbulence
+    TurbulenceFileStartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    CurrentType : str, readonly, default='TurbulentCurrent'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TurbulentCurrent'
+    
     """
 
     MeanSpeed: Optional[float] = Field(alias="MeanSpeed", default=None)
     TurbulenceFilepath: Optional[str] = Field(alias="TurbulenceFilepath", default=None)
     TurbulenceIntensity: Optional[float] = Field(alias="TurbulenceIntensity", default=None)
     TurbulenceIntensityLateral: Optional[float] = Field(alias="TurbulenceIntensityLateral", default=None)
     TurbulenceIntensityVertical: Optional[float] = Field(alias="TurbulenceIntensityVertical", default=None)
@@ -53,15 +65,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/turbulent_wind.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/turbulent_wind.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.evolving_wind_turbulence import EvolvingWindTurbulence
 
 from dnv_bladed_models.time_domain_wind import TimeDomainWind
 
 
 class TurbulentWind_CentreTurbulenceFileOnEnum(str, Enum):
     CENTRED_ON_HUB = "CENTRED_ON_HUB"
@@ -26,27 +27,39 @@
 class TurbulentWind_InterpolationMethodEnum(str, Enum):
     LINEAR = "LINEAR"
     CUBIC_IN_ROTOR_PLANE = "CUBIC_IN_ROTOR_PLANE"
     ALL_CUBIC = "ALL_CUBIC"
 
 class TurbulentWind(TimeDomainWind, WindType='TurbulentWind'):
     """TurbulentWind - The definition of a turbulent flow field, with the values for the turbulence defined in an external file.
-
+    
     Attributes:
-        - MeanSpeed: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - TurbulenceFilepath: str => The filepath or URI of the turbulence file. 
-        - TurbulenceIntensity: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TurbulenceIntensityLateral: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TurbulenceIntensityVertical: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - CentreTurbulenceFileOn: TurbulentWind_CentreTurbulenceFileOnEnum => The method used to position the data in the turbulence file relative to the turbine.  If any part of the rotor exceeds this box, the simulation will terminate with an exception.
-        - InterpolationMethod: TurbulentWind_InterpolationMethodEnum => The method used to interpolate the wind speed inbetween the provided turbulence locations.
-        - RepeatTurbulenceFile: bool => If true, the turbulence file will be \&quot;looped\&quot;.  If false, the turbulence will be 0 in all three components once the end of the file has been reached.  Using part of a turbulence file may invalidate its turbulence statistics, and no effort is made by Bladed to ensure coherence at the point when it transitions from the end of the wind file back to the beginning.
-        - EvolvingTurbulence: EvolvingWindTurbulence 
-        - TurbulenceFileStartTime: float => A number representing a time.  The SI units for time are seconds.
-        - WindType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TurbulentWind&#39;
+    ----------
+    MeanSpeed : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    TurbulenceFilepath : str
+        The filepath or URI of the turbulence file. 
+    TurbulenceIntensity : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TurbulenceIntensityLateral : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TurbulenceIntensityVertical : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    CentreTurbulenceFileOn : TurbulentWind_CentreTurbulenceFileOnEnum
+        The method used to position the data in the turbulence file relative to the turbine.  If any part of the rotor exceeds this box, the simulation will terminate with an exception.
+    InterpolationMethod : TurbulentWind_InterpolationMethodEnum
+        The method used to interpolate the wind speed inbetween the provided turbulence locations.
+    RepeatTurbulenceFile : bool
+        If true, the turbulence file will be \"looped\".  If false, the turbulence will be 0 in all three components once the end of the file has been reached.  Using part of a turbulence file may invalidate its turbulence statistics, and no effort is made by Bladed to ensure coherence at the point when it transitions from the end of the wind file back to the beginning.
+    EvolvingTurbulence : EvolvingWindTurbulence
+    TurbulenceFileStartTime : float
+        A number representing a time.  The SI units for time are seconds.
+    WindType : str, readonly, default='TurbulentWind'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TurbulentWind'
+    
     """
 
     MeanSpeed: Optional[float] = Field(alias="MeanSpeed", default=None)
     TurbulenceFilepath: Optional[str] = Field(alias="TurbulenceFilepath", default=None)
     TurbulenceIntensity: Optional[float] = Field(alias="TurbulenceIntensity", default=None)
     TurbulenceIntensityLateral: Optional[float] = Field(alias="TurbulenceIntensityLateral", default=None)
     TurbulenceIntensityVertical: Optional[float] = Field(alias="TurbulenceIntensityVertical", default=None)
@@ -60,15 +73,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/user_defined_wake_deficit.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/user_defined_wake_deficit.py`

 * *Files 5% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.radius_vs_deficit import RadiusVsDeficit
 
 from dnv_bladed_models.steady_wake_deficit import SteadyWakeDeficit
 
 
 class UserDefinedWakeDeficit(SteadyWakeDeficit, SteadyWakeDeficitType='UserDefined'):
     """UserDefinedWakeDeficit - A simple model for the reduced velocity of the wind behind another turbine.  This deficit will be applied across a certain region, and this region will not move around during the simulation.  The velocity deficit profile is a look-up table of the radius against the deficit at that radius.
-
+    
     Attributes:
-        - RadiusVsDeficit: List[RadiusVsDeficit] => A list of radii vs the deficit at that radius.  The first value should be at a radius of 0.0, and the largest radius will be the extent of the affected region.  At this point, the deficit is usually 0.0.
-        - SteadyWakeDeficitType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;UserDefined&#39;
+    ----------
+    RadiusVsDeficit : List[RadiusVsDeficit], default=list()
+        A list of radii vs the deficit at that radius.  The first value should be at a radius of 0.0, and the largest radius will be the extent of the affected region.  At this point, the deficit is usually 0.0.
+    SteadyWakeDeficitType : str, readonly, default='UserDefined'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'UserDefined'
+    
     """
 
     RadiusVsDeficit: Optional[List[RadiusVsDeficit]] = Field(alias="RadiusVsDeficit", default=list())
     SteadyWakeDeficitType: Optional[str] = Field(alias="SteadyWakeDeficitType", default='UserDefined', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/user_defined_waves.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/user_defined_waves.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.frequency_vs_spectral_density import FrequencyVsSpectralDensity
 
 from dnv_bladed_models.irregular_waves import IrregularWaves
 
 
 class UserDefinedWaves(IrregularWaves, WavesType='UserDefined'):
     """UserDefinedWaves - A look-up table for a user-defined wave spectrum.
-
+    
     Attributes:
-        - FrequencyVsSpectralDensity: List[FrequencyVsSpectralDensity] => List of FrequencyVsSpectralDensity
-        - WavesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;UserDefined&#39;
+    ----------
+    FrequencyVsSpectralDensity : List[FrequencyVsSpectralDensity], default=list()
+        List of FrequencyVsSpectralDensity
+    WavesType : str, readonly, default='UserDefined'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'UserDefined'
+    
     """
 
     FrequencyVsSpectralDensity: Optional[List[FrequencyVsSpectralDensity]] = Field(alias="FrequencyVsSpectralDensity", default=list())
     WavesType: Optional[str] = Field(alias="WavesType", default='UserDefined', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_slip_generator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_slip_generator.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,30 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fixed_speed_induction_generator import FixedSpeedInductionGenerator
 
 from dnv_bladed_models.generator import Generator
 
 from dnv_bladed_models.variable_speed_induction_generator import VariableSpeedInductionGenerator
 
 
 class VariableSlipGenerator(Generator, ComponentType='VariableSlipGenerator'):
     """VariableSlipGenerator - An induction generator with a controlled resistance in series with the generator rotor windings, giving a fixed speed induction generator characteristic below rated and limited range variable speed operation above rated.
-
+    
     Attributes:
-        - MaximumTorqueDemand: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - MinimumTorqueDemand: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - MainGenerator: VariableSpeedInductionGenerator 
-        - LowSpeedGenerator: FixedSpeedInductionGenerator 
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;VariableSlipGenerator&#39;
+    ----------
+    MaximumTorqueDemand : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    MinimumTorqueDemand : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    MainGenerator : VariableSpeedInductionGenerator
+    LowSpeedGenerator : FixedSpeedInductionGenerator
+    ComponentType : str, readonly, default='VariableSlipGenerator'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'VariableSlipGenerator'
+    
     """
 
     MaximumTorqueDemand: Optional[float] = Field(alias="MaximumTorqueDemand", default=None)
     MinimumTorqueDemand: Optional[float] = Field(alias="MinimumTorqueDemand", default=None)
     MainGenerator: Optional[VariableSpeedInductionGenerator] = Field(alias="MainGenerator", default=None)
     LowSpeedGenerator: Optional[FixedSpeedInductionGenerator] = Field(alias="LowSpeedGenerator", default=None)
     ComponentType: Optional[str] = Field(alias="ComponentType", default='VariableSlipGenerator', allow_mutation=False)
@@ -40,15 +46,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_speed_active_damper.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_speed_active_damper.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.active_damper import ActiveDamper
 
 
 class VariableSpeedActiveDamper(ActiveDamper, ComponentType='VariableSpeedActiveDamper'):
     """VariableSpeedActiveDamper - 
-
+    
     Attributes:
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;VariableSpeedActiveDamper&#39;
+    ----------
+    ComponentType : str, readonly, default='VariableSpeedActiveDamper'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'VariableSpeedActiveDamper'
+    
     """
 
     ComponentType: Optional[str] = Field(alias="ComponentType", default='VariableSpeedActiveDamper', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_speed_generator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_speed_generator.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,26 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.generator import Generator
 
 
 class VariableSpeedGenerator(Generator, ComponentType='VariableSpeedGenerator'):
     """VariableSpeedGenerator - A variable speed generator where the torque is actively controlled by the controller.
-
+    
     Attributes:
-        - GeneratorTimeConstant: float => A number representing a time.  The SI units for time are seconds.
-        - PowerFactor: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - MaximumTorqueDemand: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - MinimumTorqueDemand: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - ComponentType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;VariableSpeedGenerator&#39;
+    ----------
+    GeneratorTimeConstant : float
+        A number representing a time.  The SI units for time are seconds.
+    PowerFactor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    MaximumTorqueDemand : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    MinimumTorqueDemand : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ComponentType : str, readonly, default='VariableSpeedGenerator'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'VariableSpeedGenerator'
+    
     """
 
     GeneratorTimeConstant: Optional[float] = Field(alias="GeneratorTimeConstant", default=None)
     PowerFactor: Optional[float] = Field(alias="PowerFactor", default=None)
     MaximumTorqueDemand: Optional[float] = Field(alias="MaximumTorqueDemand", default=None)
     MinimumTorqueDemand: Optional[float] = Field(alias="MinimumTorqueDemand", default=None)
     ComponentType: Optional[str] = Field(alias="ComponentType", default='VariableSpeedGenerator', allow_mutation=False)
@@ -36,15 +44,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/variable_speed_induction_generator.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/low_speed_shaft.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,44 +9,53 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class VariableSpeedInductionGenerator(Dnv):
-    """VariableSpeedInductionGenerator - The properties of a variable speed induction generator.
 
+class LowSpeedShaft(Dnv):
+    """LowSpeedShaft - The low speed shaft bending definition.
+    
     Attributes:
-        - SlipAtMaximumResistance: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - TorqueControlTimeConstant: float => A number representing a time.  The SI units for time are seconds.
-        - RatedPower: float => A number representing a power.  The SI units for enery are Watts.
-        - SlipAtNominalResistance: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - GeneratorTimeConstant: float => A number representing a time.  The SI units for time are seconds.
-        - SynchronousSpeed: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    ----------
+    EffectiveShaftLength : float
+        A number representing a length.  The SI units for length are metres.
+    HingePosition : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TorsionalStiffness : float
+        A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
+    TorsionalDamping : float
+        A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
+    BendingStiffness : float
+        A number representing an axial stiffness.  The SI units for axial stiffness are Newtons per metre.
+    BendingDamping : float
+        A number representing a linear damping term.  The SI units for linear damping are Newton seconds per metre.
+    
     """
 
-    SlipAtMaximumResistance: Optional[float] = Field(alias="SlipAtMaximumResistance", default=None)
-    TorqueControlTimeConstant: Optional[float] = Field(alias="TorqueControlTimeConstant", default=None)
-    RatedPower: Optional[float] = Field(alias="RatedPower", default=None)
-    SlipAtNominalResistance: Optional[float] = Field(alias="SlipAtNominalResistance", default=None)
-    GeneratorTimeConstant: Optional[float] = Field(alias="GeneratorTimeConstant", default=None)
-    SynchronousSpeed: Optional[float] = Field(alias="SynchronousSpeed", default=None)
+    EffectiveShaftLength: Optional[float] = Field(alias="EffectiveShaftLength", default=None)
+    HingePosition: Optional[float] = Field(alias="HingePosition", default=None)
+    TorsionalStiffness: Optional[float] = Field(alias="TorsionalStiffness", default=None)
+    TorsionalDamping: Optional[float] = Field(alias="TorsionalDamping", default=None)
+    BendingStiffness: Optional[float] = Field(alias="BendingStiffness", default=None)
+    BendingDamping: Optional[float] = Field(alias="BendingDamping", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -112,25 +121,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        VariableSpeedInductionGenerator
+        LowSpeedShaft
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = VariableSpeedInductionGenerator.from_file('/path/to/file')
+        >>> model = LowSpeedShaft.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -142,25 +151,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        VariableSpeedInductionGenerator
+        LowSpeedShaft
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = VariableSpeedInductionGenerator.from_json('{ ... }')
+        >>> model = LowSpeedShaft.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -174,15 +183,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        VariableSpeedInductionGenerator
+        LowSpeedShaft
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -206,8 +215,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-VariableSpeedInductionGenerator.update_forward_refs()
+LowSpeedShaft.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/vector3_d.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault_constant_rate.py`

 * *Files 10% similar despite different names*

```diff
@@ -10,35 +10,43 @@
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
 
-class Vector3D(BaseModel):
-    """Vector3D - A 3x1 vector representing a location or direction.
+from dnv_bladed_models.yaw_fault import YawFault
 
+
+class YawFaultConstantRate(YawFault, EventType='YawFaultConstantRate'):
+    """YawFaultConstantRate - A fault in the yaw system where the yaw system moves at a constant rate until it reaches an end stop.
+    
     Attributes:
-        - X: float => A number representing a length.  The SI units for length are metres.
-        - Y: float => A number representing a length.  The SI units for length are metres.
-        - Z: float => A number representing a length.  The SI units for length are metres.
+    ----------
+    FaultYawRate : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    YawEndStop : float
+        A number representing an angle.  The SI units for angles are radians.
+    EventType : str, readonly, default='YawFaultConstantRate'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'YawFaultConstantRate'
+    
     """
 
-    X: Optional[float] = Field(alias="X", default=None)
-    Y: Optional[float] = Field(alias="Y", default=None)
-    Z: Optional[float] = Field(alias="Z", default=None)
+    FaultYawRate: Optional[float] = Field(alias="FaultYawRate", default=None)
+    YawEndStop: Optional[float] = Field(alias="YawEndStop", default=None)
+    EventType: Optional[str] = Field(alias="EventType", default='YawFaultConstantRate', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +112,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        Vector3D
+        YawFaultConstantRate
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = Vector3D.from_file('/path/to/file')
+        >>> model = YawFaultConstantRate.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +142,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        Vector3D
+        YawFaultConstantRate
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = Vector3D.from_json('{ ... }')
+        >>> model = YawFaultConstantRate.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +174,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        Vector3D
+        YawFaultConstantRate
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +206,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-Vector3D.update_forward_refs()
+YawFaultConstantRate.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/velocity_range.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/velocity_range.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class VelocityRange(Dnv):
     """VelocityRange - The range of wind speeds to consider.
-
+    
     Attributes:
-        - Minimum: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - Maximum: float => A number representing a velocity.  The SI units for velocity are metres per second.
-        - Interval: float => A number representing a velocity.  The SI units for velocity are metres per second.
+    ----------
+    Minimum : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    Maximum : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    Interval : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    
     """
 
     Minimum: Optional[float] = Field(alias="Minimum", default=None)
     Maximum: Optional[float] = Field(alias="Maximum", default=None)
     Interval: Optional[float] = Field(alias="Interval", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/velocity_vs_damping_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/point_inertia.py`

 * *Files 12% similar despite different names*

```diff
@@ -9,36 +9,51 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.added_inertia import AddedInertia
 
-class VelocityVsDampingTorque(Dnv):
-    """VelocityVsDampingTorque - A line or row in a look-up table, specifying a value of DampingTorque for a specified Velocity.
+from dnv_bladed_models.vector3_d import Vector3D
 
+
+class PointInertia(AddedInertia, AddedInertiaType='PointInertia'):
+    """PointInertia - An inertia added to a structural component.
+    
     Attributes:
-        - Velocity: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - DampingTorque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    ----------
+    Mass : float
+        A number representing a mass.  The SI units for mass are kilograms.
+    Offset : Vector3D
+    MomentOfInertiaTensor : List[float], default=list()
+        Moment of inertia tensor acting about the centre of mass. This tensor is defined using the local coordinate system of the attachment node.
+    CompleteInertiaTensor : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AddedInertiaType : str, readonly, default='PointInertia'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'PointInertia'
+    
     """
 
-    Velocity: Optional[float] = Field(alias="Velocity", default=None)
-    DampingTorque: Optional[float] = Field(alias="DampingTorque", default=None)
+    Mass: Optional[float] = Field(alias="Mass", default=None)
+    Offset: Optional[Vector3D] = Field(alias="Offset", default=None)
+    MomentOfInertiaTensor: Optional[List[float]] = Field(alias="MomentOfInertiaTensor", default=list())
+    CompleteInertiaTensor: Optional[float] = Field(alias="CompleteInertiaTensor", default=None)
+    AddedInertiaType: Optional[str] = Field(alias="AddedInertiaType", default='PointInertia', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +119,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        VelocityVsDampingTorque
+        PointInertia
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = VelocityVsDampingTorque.from_file('/path/to/file')
+        >>> model = PointInertia.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +149,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        VelocityVsDampingTorque
+        PointInertia
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = VelocityVsDampingTorque.from_json('{ ... }')
+        >>> model = PointInertia.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +181,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        VelocityVsDampingTorque
+        PointInertia
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +213,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-VelocityVsDampingTorque.update_forward_refs()
+PointInertia.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/vertical_shear.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/vertical_shear.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,32 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class VerticalShear_VerticalShearTypeEnum(str, Enum):
     EXPONENTIAL_SHEAR_MODEL = "ExponentialShearModel"
     LOGARITHMIC_SHEAR_MODEL = "LogarithmicShearModel"
     LOOK_UP_SHEAR_MODEL = "LookUpShearModel"
 
 class VerticalShear(Dnv):
     """VerticalShear - The vertical wind shear to apply to the wind field.  This will vary the wind speed from 100% at the reference height (usually the hub height) down to 0% at ground level.
-
+    
     Attributes:
-        - VerticalShearType: VerticalShear_VerticalShearTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    VerticalShearType : VerticalShear_VerticalShearTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - ExponentialShearModel
-        - LogarithmicShearModel
-        - LookUpShearModel
+        ExponentialShearModel
+        LogarithmicShearModel
+        LookUpShearModel
+    
     """
 
     VerticalShearType: Optional[VerticalShear_VerticalShearTypeEnum] = Field(alias="VerticalShearType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -64,15 +68,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'VerticalShear'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/vortex_line.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/vortex_line.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,14 +9,15 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.aerodynamic_model import AerodynamicModel
 
 
 class VortexLine_WakeTypeEnum(str, Enum):
     FREE_WAKE = "FreeWake"
     FIXED_WAKE = "FixedWake"
 
@@ -28,28 +29,42 @@
 class VortexLine_InitialVortexCoreSizeModelEnum(str, Enum):
     RL_MODEL = "RL_Model"
     LO_MODEL = "LO_Model"
     FIXED = "Fixed"
 
 class VortexLine(AerodynamicModel, AerodynamicModelType='VortexLine'):
     """VortexLine - The Vortex Line aerodynamic model.
-
+    
     Attributes:
-        - MaximumNumberofFreeWakeSteps: int => Each free wake node that is emitted from the trailing edge will be allowed a maximum number of free wake steps after it will be no longer considered in the free wake solution and convected with local wind speed and last computed induction. 
-        - MaximumNumberofWakeSteps: int => Each wake node will be allowed a maximum number of steps before it is removed. This option puts an upper bound on the number of wake nodes.
-        - NumberOfThreads: int => The number of parallel CPU threads used in evaluation of the Biot-Savart law.  This option is only relevant when the wake type is set to \&quot;Free Wake\&quot;.
-        - VortexWakeTimeStep: float => A number representing a time.  The SI units for time are seconds.
-        - WakeType: VortexLine_WakeTypeEnum => The \&quot;Free Wake\&quot; option will calculate the mutual influence of all wake elements on all wake nodes during each time step.  The \&quot;Fixed Wake\&quot; option will assume that the induced velocity in all wake nodes is equal to the average wake induced velocity at 70% blade radius.  The \&quot;Free Wake\&quot; option requires substantially more calculations to be performed, and is likely to significantly slow the analysis.
-        - CoreGrowthModel: VortexLine_CoreGrowthModelEnum => The Core Growth Model.
-        - InitialVortexCoreSizeModel: VortexLine_InitialVortexCoreSizeModelEnum => The intial vortex core size Model.
-        - FilamentStrain: bool => The filament strain.
-        - LambOseenCoreGrowthConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - CoreGrowthConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - RamasamyLeishmanConstant: float => A number representing a fraction, ratio, or other non-dimensionalised property.
-        - AerodynamicModelType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;VortexLine&#39;
+    ----------
+    MaximumNumberofFreeWakeSteps : int, default=200
+        Each free wake node that is emitted from the trailing edge will be allowed a maximum number of free wake steps after it will be no longer considered in the free wake solution and convected with local wind speed and last computed induction. 
+    MaximumNumberofWakeSteps : int, default=10000
+        Each wake node will be allowed a maximum number of steps before it is removed. This option puts an upper bound on the number of wake nodes.
+    NumberOfThreads : int, default=1
+        The number of parallel CPU threads used in evaluation of the Biot-Savart law.  This option is only relevant when the wake type is set to \"Free Wake\".
+    VortexWakeTimeStep : float
+        A number representing a time.  The SI units for time are seconds.
+    WakeType : VortexLine_WakeTypeEnum, default='FreeWake'
+        The \"Free Wake\" option will calculate the mutual influence of all wake elements on all wake nodes during each time step.  The \"Fixed Wake\" option will assume that the induced velocity in all wake nodes is equal to the average wake induced velocity at 70% blade radius.  The \"Free Wake\" option requires substantially more calculations to be performed, and is likely to significantly slow the analysis.
+    CoreGrowthModel : VortexLine_CoreGrowthModelEnum, default='RL_Model'
+        The Core Growth Model.
+    InitialVortexCoreSizeModel : VortexLine_InitialVortexCoreSizeModelEnum, default='RL_Model'
+        The intial vortex core size Model.
+    FilamentStrain : bool, default=True
+        The filament strain.
+    LambOseenCoreGrowthConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    CoreGrowthConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    RamasamyLeishmanConstant : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    AerodynamicModelType : str, readonly, default='VortexLine'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'VortexLine'
+    
     """
 
     MaximumNumberofFreeWakeSteps: Optional[int] = Field(alias="MaximumNumberofFreeWakeSteps", default=200)
     MaximumNumberofWakeSteps: Optional[int] = Field(alias="MaximumNumberofWakeSteps", default=10000)
     NumberOfThreads: Optional[int] = Field(alias="NumberOfThreads", default=1)
     VortexWakeTimeStep: Optional[float] = Field(alias="VortexWakeTimeStep", default=None)
     WakeType: Optional[VortexLine_WakeTypeEnum] = Field(alias="WakeType", default='FreeWake')
@@ -64,15 +79,15 @@
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wake_properties_of_upstream_turbine.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/pitch_controller.py`

 * *Files 12% similar despite different names*

```diff
@@ -9,36 +9,56 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.pitch_safety_system import PitchSafetySystem
+
+from dnv_bladed_models.pitch_system_demand import PitchSystemDemand
 
-class WakePropertiesOfUpstreamTurbine(Dnv):
-    """WakePropertiesOfUpstreamTurbine - 
 
+class PitchController(Dnv):
+    """PitchController - The definition of the pitch controller.  The pitch system generally has its own independent controller (usually physically separate) which received position or rate demands from the main controller.
+    
     Attributes:
-        - AerodynamicInformationResultsPath: str => A file or URI containing aerodynamic information results, used to generate the initial wake profile of the upwind turbine
-        - SteadyOperationalLoadsResultsPath: str => A file or URI containing aerodynamic information outputs from this &#39;Steady Operational Loads&#39; run generates the initial wake profile of the upwind turbine. The steady operational loads run should be performed over all wind speeds and have aerodynamic information outputs switched on for blade 1 at all blade stations.
+    ----------
+    Demand : PitchSystemDemand, abstract
+    SafetySystem : PitchSafetySystem, abstract
+    
+    Notes:
+    ---------
+    Demand has the following concrete types:
+        - PitchPositionDemand
+        - PitchRateDemand
+    SafetySystem has the following concrete types:
+        - PitchActuationLimited
+        - PitchConstantRateSafetySystem
+        - PitchRateDemandSetByExternalController
+        - PitchRateVariesWithPosition
+        - PitchRateVariesWithTime
+        - PitchTorqueSetByExternalController
+    
     """
 
-    AerodynamicInformationResultsPath: Optional[str] = Field(alias="AerodynamicInformationResultsPath", default=None)
-    SteadyOperationalLoadsResultsPath: Optional[str] = Field(alias="SteadyOperationalLoadsResultsPath", default=None)
+    Demand: Optional[PitchSystemDemand] = Field(alias="Demand", default=None)
+    SafetySystem: Optional[PitchSafetySystem] = Field(alias="SafetySystem", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +124,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        WakePropertiesOfUpstreamTurbine
+        PitchController
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = WakePropertiesOfUpstreamTurbine.from_file('/path/to/file')
+        >>> model = PitchController.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +154,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        WakePropertiesOfUpstreamTurbine
+        PitchController
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = WakePropertiesOfUpstreamTurbine.from_json('{ ... }')
+        >>> model = PitchController.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +186,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        WakePropertiesOfUpstreamTurbine
+        PitchController
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +218,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-WakePropertiesOfUpstreamTurbine.update_forward_refs()
+PitchController.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/water_characteristics.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/tower_node_at_height_outputs.py`

 * *Files 12% similar despite different names*

```diff
@@ -9,36 +9,47 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class WaterCharacteristics(Dnv):
-    """WaterCharacteristics - The properties of water for use in offshore calculations.
 
+class TowerNodeAtHeightOutputs(Dnv):
+    """TowerNodeAtHeightOutputs - The outputs to produce for a specified node or node type.
+    
     Attributes:
-        - Density: float => A number representing a material&#39;s density.  The SI units for volume are kilograms per cubic metre.
-        - VapourPressure: float => A number representing a pressure.  The SI units for pressure are Newtons per metre squareds.
+    ----------
+    Displacements : bool, default=False
+        If true, the displacements of the nodes from their undeformed position will be output.
+    Loads : bool, default=False
+        If true, the loads on the tower at the node's location will be output.
+    WaterKinematics : bool, default=False
+        If true, the water velocities and accelerations at the node's location will be output.
+    SnapToNearestNode : bool
+        If true, the outputs will be at the nearest structural node, which may be at a significantly different height to that specified.
+    
     """
 
-    Density: Optional[float] = Field(alias="Density", default=None)
-    VapourPressure: Optional[float] = Field(alias="VapourPressure", default=None)
+    Displacements: Optional[bool] = Field(alias="Displacements", default=False)
+    Loads: Optional[bool] = Field(alias="Loads", default=False)
+    WaterKinematics: Optional[bool] = Field(alias="WaterKinematics", default=False)
+    SnapToNearestNode: Optional[bool] = Field(alias="SnapToNearestNode", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +115,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        WaterCharacteristics
+        TowerNodeAtHeightOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = WaterCharacteristics.from_file('/path/to/file')
+        >>> model = TowerNodeAtHeightOutputs.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +145,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        WaterCharacteristics
+        TowerNodeAtHeightOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = WaterCharacteristics.from_json('{ ... }')
+        >>> model = TowerNodeAtHeightOutputs.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +177,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        WaterCharacteristics
+        TowerNodeAtHeightOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +209,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-WaterCharacteristics.update_forward_refs()
+TowerNodeAtHeightOutputs.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wave_diffraction_approximation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wave_diffraction_approximation.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,34 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class WaveDiffractionApproximation_WaveDiffractionApproximationTypeEnum(str, Enum):
     AUTOMATIC_MAC_CAMY_FUCHS = "AutomaticMacCamyFuchs"
     MAC_CAMY_FUCHS = "MacCamyFuchs"
     AUTOMATIC_SIMPLE_CUTOFF_FREQUENCY = "AutomaticSimpleCutoffFrequency"
     SIMPLE_CUTOFF_FREQUENCY = "SimpleCutoffFrequency"
 
 class WaveDiffractionApproximation(Dnv):
     """WaveDiffractionApproximation - The method used for wave diffraction approximation and the option for parameters to be auto-defined.
-
+    
     Attributes:
-        - WaveDiffractionApproximationType: WaveDiffractionApproximation_WaveDiffractionApproximationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    WaveDiffractionApproximationType : WaveDiffractionApproximation_WaveDiffractionApproximationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - AutomaticMacCamyFuchs
-        - AutomaticSimpleCutoffFrequency
-        - MacCamyFuchs
-        - SimpleCutoffFrequency
+        AutomaticMacCamyFuchs
+        AutomaticSimpleCutoffFrequency
+        MacCamyFuchs
+        SimpleCutoffFrequency
+    
     """
 
     WaveDiffractionApproximationType: Optional[WaveDiffractionApproximation_WaveDiffractionApproximationTypeEnum] = Field(alias="WaveDiffractionApproximationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -66,15 +70,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'WaveDiffractionApproximation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wave_spectrum.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wave_spectrum.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,49 +9,55 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.vector3_d import Vector3D
 
 from dnv_bladed_models.waves import Waves
 
 
 class WaveSpectrum_WaveCurrentInteractionMethodEnum(str, Enum):
     TYPE1 = "Type1"
     TYPE2A = "Type2a"
     TYPE2B = "Type2b"
     TYPE2C_I = "Type2c_i"
     TYPE2C_II = "Type2c_ii"
 
 class WaveSpectrum(Waves, WavesType='WaveSpectrum'):
     """WaveSpectrum - 
-
+    
     Attributes:
-        - PhaseOrigin: Vector3D 
-        - SeaStateFilepath: str => The location of .SEA file describing the simulation seastate.
-        - WaveCurrentInteractionMethod: WaveSpectrum_WaveCurrentInteractionMethodEnum => The method to be used in combining waves and currents to obtain overall water particle kinematics.
-        - WavesType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;WaveSpectrum&#39;
+    ----------
+    PhaseOrigin : Vector3D
+    SeaStateFilepath : str
+        The location of .SEA file describing the simulation seastate.
+    WaveCurrentInteractionMethod : WaveSpectrum_WaveCurrentInteractionMethodEnum, default='Type1'
+        The method to be used in combining waves and currents to obtain overall water particle kinematics.
+    WavesType : str, readonly, default='WaveSpectrum'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'WaveSpectrum'
+    
     """
 
     PhaseOrigin: Optional[Vector3D] = Field(alias="PhaseOrigin", default=None)
     SeaStateFilepath: Optional[str] = Field(alias="SeaStateFilepath", default=None)
     WaveCurrentInteractionMethod: Optional[WaveSpectrum_WaveCurrentInteractionMethodEnum] = Field(alias="WaveCurrentInteractionMethod", default='Type1')
     WavesType: Optional[str] = Field(alias="WavesType", default='WaveSpectrum', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/waves.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/variable_speed_induction_generator.py`

 * *Files 16% similar despite different names*

```diff
@@ -9,74 +9,53 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
-
 
-class Waves_WavesTypeEnum(str, Enum):
-    WAVE_SPECTRUM = "WaveSpectrum"
-    LINEAR_AIRY = "LinearAiry"
-    STREAM_FUNCTION = "StreamFunction"
-    JONSWAP_PIERSON_MOSKOWITZ = "JonswapPiersonMoskowitz"
-    USER_DEFINED = "UserDefined"
+from dnv_bladed_models.dnv import Dnv
 
-class Waves(Dnv):
-    """Waves - The definition of a wave field to apply to this analysis
 
+class VariableSpeedInductionGenerator(Dnv):
+    """VariableSpeedInductionGenerator - The properties of a variable speed induction generator.
+    
     Attributes:
-        - WavesType: Waves_WavesTypeEnum => Allows the schema to identify the type of the object.
-        
-    This class is an abstraction, with the following concrete implementations:
-        - JonswapPiersonMoskowitz
-        - LinearAiry
-        - StreamFunction
-        - UserDefinedWaves
-        - WaveSpectrum
+    ----------
+    SlipAtMaximumResistance : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    TorqueControlTimeConstant : float
+        A number representing a time.  The SI units for time are seconds.
+    RatedPower : float
+        A number representing a power.  The SI units for enery are Watts.
+    SlipAtNominalResistance : float
+        A number representing a fraction, ratio, or other non-dimensionalised property.
+    GeneratorTimeConstant : float
+        A number representing a time.  The SI units for time are seconds.
+    SynchronousSpeed : float
+        A number representing angular velocity.  The SI units for angular velocity are radians per second.
+    
     """
 
-    WavesType: Optional[Waves_WavesTypeEnum] = Field(alias="WavesType", default=None)
+    SlipAtMaximumResistance: Optional[float] = Field(alias="SlipAtMaximumResistance", default=None)
+    TorqueControlTimeConstant: Optional[float] = Field(alias="TorqueControlTimeConstant", default=None)
+    RatedPower: Optional[float] = Field(alias="RatedPower", default=None)
+    SlipAtNominalResistance: Optional[float] = Field(alias="SlipAtNominalResistance", default=None)
+    GeneratorTimeConstant: Optional[float] = Field(alias="GeneratorTimeConstant", default=None)
+    SynchronousSpeed: Optional[float] = Field(alias="SynchronousSpeed", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
-    _subtypes_ = dict()
-
-    def __init_subclass__(cls, WavesType=None):
-        cls._subtypes_[WavesType or cls.__name__.lower()] = cls
-
-    @classmethod
-    def __get_validators__(cls):
-        yield cls._convert_to_real_type_
-
-    @classmethod
-    def _convert_to_real_type_(cls, data: dict):
-        if isinstance(data, dict):
-            data_type = data.get("WavesType")
-
-            if data_type is None:
-                raise ValueError("Missing 'WavesType' in Waves")
-
-            sub = cls._subtypes_.get(data_type)
-
-            if sub is None:
-                raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Waves'")
-
-            return sub(**data)
-
-        return data
-
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -142,25 +121,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        Waves
+        VariableSpeedInductionGenerator
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = Waves.from_file('/path/to/file')
+        >>> model = VariableSpeedInductionGenerator.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -172,25 +151,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        Waves
+        VariableSpeedInductionGenerator
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = Waves.from_json('{ ... }')
+        >>> model = VariableSpeedInductionGenerator.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -204,15 +183,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        Waves
+        VariableSpeedInductionGenerator
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -236,8 +215,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-Waves.update_forward_refs()
+VariableSpeedInductionGenerator.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,32 +9,36 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class Wind_WindTypeEnum(str, Enum):
     TURBULENT_WIND = "TurbulentWind"
     LAMINAR_FLOW = "LaminarFlow"
     EXTERNAL_FLOW_SOURCE_FOR_WIND = "ExternalFlowSourceForWind"
 
 class Wind(Dnv):
     """Wind - The definition of the wind conditions during the simulation.
-
+    
     Attributes:
-        - WindType: Wind_WindTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    WindType : Wind_WindTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - ExternalFlowSourceForWind
-        - LaminarFlowWind
-        - TurbulentWind
+        ExternalFlowSourceForWind
+        LaminarFlowWind
+        TurbulentWind
+    
     """
 
     WindType: Optional[Wind_WindTypeEnum] = Field(alias="WindType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -64,15 +68,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'Wind'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_shear_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_shear_time_history.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.time_vs_wind_direction_shear import TimeVsWindDirectionShear
 
 from dnv_bladed_models.wind_direction_shear_variation import WindDirectionShearVariation
 
 
 class WindDirectionShearTimeHistory(WindDirectionShearVariation, DirectionShearVariationType='TimeHistory'):
     """WindDirectionShearTimeHistory - A time history of the vertical direction shear (veer).  This is a linear relationship between the height and the local direction, with the direction being its nominal value at the reference height.
-
+    
     Attributes:
-        - TimeVsDirectionShear: List[TimeVsWindDirectionShear] => A list of points specifying the wind veer at the corresponding time.  The wind veer will be interpolated between these points; the wind veer before the first point will be constant at the first point&#39;s value; and the wind veer after the last point will remain constant at the last point&#39;s value
-        - DirectionShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsDirectionShear : List[TimeVsWindDirectionShear], default=list()
+        A list of points specifying the wind veer at the corresponding time.  The wind veer will be interpolated between these points; the wind veer before the first point will be constant at the first point's value; and the wind veer after the last point will remain constant at the last point's value
+    DirectionShearVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsDirectionShear: Optional[List[TimeVsWindDirectionShear]] = Field(alias="TimeVsDirectionShear", default=list())
     DirectionShearVariationType: Optional[str] = Field(alias="DirectionShearVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_shear_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_shear_variation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class WindDirectionShearVariation_DirectionShearVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class WindDirectionShearVariation(Dnv):
     """WindDirectionShearVariation - A defined variation in the direction wind shear.  This is a linear relationship between the height and the local direction, with the direction being its nominal value at the reference height.
-
+    
     Attributes:
-        - DirectionShearVariationType: WindDirectionShearVariation_DirectionShearVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    DirectionShearVariationType : WindDirectionShearVariation_DirectionShearVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetWindDirectionShearTransient
-        - WindDirectionShearTimeHistory
+        PresetWindDirectionShearTransient
+        WindDirectionShearTimeHistory
+    
     """
 
     DirectionShearVariationType: Optional[WindDirectionShearVariation_DirectionShearVariationTypeEnum] = Field(alias="DirectionShearVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'WindDirectionShearVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_time_history.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.time_vs_wind_direction import TimeVsWindDirection
 
 from dnv_bladed_models.wind_direction_variation import WindDirectionVariation
 
 
 class WindDirectionTimeHistory(WindDirectionVariation, DirectionVariationType='TimeHistory'):
     """WindDirectionTimeHistory - A time history of the wind direction.
-
+    
     Attributes:
-        - TimeVsDirection: List[TimeVsWindDirection] => A list of points specifying the direction at the corresponding time.  The direction will be interpolated between these points; the direction before the first point will be constant at the first point&#39;s value; and the direction after the last point will remain constant at the last point&#39;s value
-        - DirectionVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsDirection : List[TimeVsWindDirection], default=list()
+        A list of points specifying the direction at the corresponding time.  The direction will be interpolated between these points; the direction before the first point will be constant at the first point's value; and the direction after the last point will remain constant at the last point's value
+    DirectionVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsDirection: Optional[List[TimeVsWindDirection]] = Field(alias="TimeVsDirection", default=list())
     DirectionVariationType: Optional[str] = Field(alias="DirectionVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_direction_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_direction_variation.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class WindDirectionVariation_DirectionVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class WindDirectionVariation(Dnv):
     """WindDirectionVariation - A defined variation in the wind direction.
-
+    
     Attributes:
-        - DirectionVariationType: WindDirectionVariation_DirectionVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    DirectionVariationType : WindDirectionVariation_DirectionVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetWindDirectionTransient
-        - WindDirectionTimeHistory
+        PresetWindDirectionTransient
+        WindDirectionTimeHistory
+    
     """
 
     DirectionVariationType: Optional[WindDirectionVariation_DirectionVariationTypeEnum] = Field(alias="DirectionVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'WindDirectionVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_horizontal_shear_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_horizontal_shear_time_history.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.time_vs_wind_horizontal_shear import TimeVsWindHorizontalShear
 
 from dnv_bladed_models.wind_horizontal_shear_variation import WindHorizontalShearVariation
 
 
 class WindHorizontalShearTimeHistory(WindHorizontalShearVariation, HorizontalShearVariationType='TimeHistory'):
     """WindHorizontalShearTimeHistory - A time history of the horizontal wind shear.
-
+    
     Attributes:
-        - TimeVsHorizontalShear: List[TimeVsWindHorizontalShear] => A list of points specifying the horizontal wind shear at the corresponding time.  The horizontal wind shear will be interpolated between these points; the horizontal wind shear before the first point will be constant at the first point&#39;s value; and the horizontal wind shear after the last point will remain constant at the last point&#39;s value
-        - HorizontalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsHorizontalShear : List[TimeVsWindHorizontalShear], default=list()
+        A list of points specifying the horizontal wind shear at the corresponding time.  The horizontal wind shear will be interpolated between these points; the horizontal wind shear before the first point will be constant at the first point's value; and the horizontal wind shear after the last point will remain constant at the last point's value
+    HorizontalShearVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsHorizontalShear: Optional[List[TimeVsWindHorizontalShear]] = Field(alias="TimeVsHorizontalShear", default=list())
     HorizontalShearVariationType: Optional[str] = Field(alias="HorizontalShearVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_horizontal_shear_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_horizontal_shear_variation.py`

 * *Files 7% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class WindHorizontalShearVariation_HorizontalShearVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class WindHorizontalShearVariation(Dnv):
     """WindHorizontalShearVariation - A defined variation in the horizontal wind shear.
-
+    
     Attributes:
-        - HorizontalShearVariationType: WindHorizontalShearVariation_HorizontalShearVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    HorizontalShearVariationType : WindHorizontalShearVariation_HorizontalShearVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetWindHorizontalShearTransient
-        - WindHorizontalShearTimeHistory
+        PresetWindHorizontalShearTransient
+        WindHorizontalShearTimeHistory
+    
     """
 
     HorizontalShearVariationType: Optional[WindHorizontalShearVariation_HorizontalShearVariationTypeEnum] = Field(alias="HorizontalShearVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'WindHorizontalShearVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_mean_speed_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_mean_speed_time_history.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.time_vs_wind_mean_speed import TimeVsWindMeanSpeed
 
 from dnv_bladed_models.wind_mean_speed_variation import WindMeanSpeedVariation
 
 
 class WindMeanSpeedTimeHistory(WindMeanSpeedVariation, MeanSpeedVariationType='TimeHistory'):
     """WindMeanSpeedTimeHistory - A time history of the wind speed.
-
+    
     Attributes:
-        - TimeVsMeanSpeed: List[TimeVsWindMeanSpeed] => A list of points specifying the wind speed at the corresponding time.  The wind speed will be interpolated between these points; the wind speed before the first point will be constant at the first point&#39;s value; and the wind speed after the last point will remain constant at the last point&#39;s value
-        - MeanSpeedVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsMeanSpeed : List[TimeVsWindMeanSpeed], default=list()
+        A list of points specifying the wind speed at the corresponding time.  The wind speed will be interpolated between these points; the wind speed before the first point will be constant at the first point's value; and the wind speed after the last point will remain constant at the last point's value
+    MeanSpeedVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsMeanSpeed: Optional[List[TimeVsWindMeanSpeed]] = Field(alias="TimeVsMeanSpeed", default=list())
     MeanSpeedVariationType: Optional[str] = Field(alias="MeanSpeedVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_mean_speed_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_mean_speed_variation.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class WindMeanSpeedVariation_MeanSpeedVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class WindMeanSpeedVariation(Dnv):
     """WindMeanSpeedVariation - A defined variation in the mean wind speed.
-
+    
     Attributes:
-        - MeanSpeedVariationType: WindMeanSpeedVariation_MeanSpeedVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    MeanSpeedVariationType : WindMeanSpeedVariation_MeanSpeedVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetWindMeanSpeedTransient
-        - WindMeanSpeedTimeHistory
+        PresetWindMeanSpeedTransient
+        WindMeanSpeedTimeHistory
+    
     """
 
     MeanSpeedVariationType: Optional[WindMeanSpeedVariation_MeanSpeedVariationTypeEnum] = Field(alias="MeanSpeedVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'WindMeanSpeedVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_vertical_shear_time_history.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_vertical_shear_time_history.py`

 * *Files 2% similar despite different names*

```diff
@@ -9,38 +9,43 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.time_vs_wind_vertical_shear import TimeVsWindVerticalShear
 
 from dnv_bladed_models.wind_vertical_shear_variation import WindVerticalShearVariation
 
 
 class WindVerticalShearTimeHistory(WindVerticalShearVariation, VerticalShearVariationType='TimeHistory'):
     """WindVerticalShearTimeHistory - A time history of the wind shear.
-
+    
     Attributes:
-        - TimeVsVerticalShear: List[TimeVsWindVerticalShear] => A list of points specifying the wind shear at the corresponding time.  The wind shear will be interpolated between these points; the wind shear before the first point will be constant at the first point&#39;s value; and the wind shear after the last point will remain constant at the last point&#39;s value
-        - VerticalShearVariationType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;TimeHistory&#39;
+    ----------
+    TimeVsVerticalShear : List[TimeVsWindVerticalShear], default=list()
+        A list of points specifying the wind shear at the corresponding time.  The wind shear will be interpolated between these points; the wind shear before the first point will be constant at the first point's value; and the wind shear after the last point will remain constant at the last point's value
+    VerticalShearVariationType : str, readonly, default='TimeHistory'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'TimeHistory'
+    
     """
 
     TimeVsVerticalShear: Optional[List[TimeVsWindVerticalShear]] = Field(alias="TimeVsVerticalShear", default=list())
     VerticalShearVariationType: Optional[str] = Field(alias="VerticalShearVariationType", default='TimeHistory', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/wind_vertical_shear_variation.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/wind_vertical_shear_variation.py`

 * *Files 1% similar despite different names*

```diff
@@ -9,30 +9,34 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class WindVerticalShearVariation_VerticalShearVariationTypeEnum(str, Enum):
     PRESET_TRANSIENT = "PresetTransient"
     TIME_HISTORY = "TimeHistory"
 
 class WindVerticalShearVariation(Dnv):
     """WindVerticalShearVariation - A defined variation in the veritcal wind shear.
-
+    
     Attributes:
-        - VerticalShearVariationType: WindVerticalShearVariation_VerticalShearVariationTypeEnum => Allows the schema to identify the type of the object.
-        
+    ----------
+    VerticalShearVariationType : WindVerticalShearVariation_VerticalShearVariationTypeEnum
+        Allows the schema to identify the type of the object.
+
     This class is an abstraction, with the following concrete implementations:
-        - PresetWindVerticalShearTransient
-        - WindVerticalShearTimeHistory
+        PresetWindVerticalShearTransient
+        WindVerticalShearTimeHistory
+    
     """
 
     VerticalShearVariationType: Optional[WindVerticalShearVariation_VerticalShearVariationTypeEnum] = Field(alias="VerticalShearVariationType", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
@@ -62,15 +66,15 @@
                 raise TypeError(f"Unsupported sub-type: '{data_type}' for base-type 'WindVerticalShearVariation'")
 
             return sub(**data)
 
         return data
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/xml_blade_outputs.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/xml_blade_outputs.py`

 * *Files 3% similar despite different names*

```diff
@@ -9,38 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.dnv import Dnv
 
 
 class XMLBladeOutputs(Dnv):
     """XMLBladeOutputs - LM output group
-
+    
     Attributes:
-        - OutputExtremeLoads: bool => If true, output fatigue loads according to an XML format load configuration file
-        - OutputFatigueLoads: bool => If true, output extreme loads according to an XML format load configuration file
-        - FormatFilepath: bool => The path to an XML format load configuration file.
+    ----------
+    OutputExtremeLoads : bool, default=False
+        If true, output fatigue loads according to an XML format load configuration file
+    OutputFatigueLoads : bool, default=False
+        If true, output extreme loads according to an XML format load configuration file
+    FormatFilepath : bool, default=False
+        The path to an XML format load configuration file.
+    
     """
 
     OutputExtremeLoads: Optional[bool] = Field(alias="OutputExtremeLoads", default=False)
     OutputFatigueLoads: Optional[bool] = Field(alias="OutputFatigueLoads", default=False)
     FormatFilepath: Optional[bool] = Field(alias="FormatFilepath", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault.py`

 * *Files 4% similar despite different names*

```diff
@@ -9,32 +9,32 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
+
 from dnv_bladed_models.fault import Fault
 
 
 class YawFault(Fault):
     """YawFault - A fault in the yaw system, occuring at a specified time.
-
-    Attributes:
+    
     """
 
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault_constant_rate.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_fault_constant_torque.py`

 * *Files 6% similar despite different names*

```diff
@@ -9,38 +9,41 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.yaw_fault import YawFault
 
+from dnv_bladed_models.yaw_fault import YawFault
 
-class YawFaultConstantRate(YawFault, EventType='YawFaultConstantRate'):
-    """YawFaultConstantRate - A fault in the yaw system where the yaw system moves at a constant rate until it reaches an end stop.
 
+class YawFaultConstantTorque(YawFault, EventType='YawFaultConstantTorque'):
+    """YawFaultConstantTorque - A fault in the yaw system where the actuator applies a constant torque.
+    
     Attributes:
-        - FaultYawRate: float => A number representing angular velocity.  The SI units for angular velocity are radians per second.
-        - YawEndStop: float => A number representing an angle.  The SI units for angles are radians.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;YawFaultConstantRate&#39;
+    ----------
+    Torque : float
+        A number representing a torque or moment.  The SI units for moments are Newton-metres.
+    EventType : str, readonly, default='YawFaultConstantTorque'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'YawFaultConstantTorque'
+    
     """
 
-    FaultYawRate: Optional[float] = Field(alias="FaultYawRate", default=None)
-    YawEndStop: Optional[float] = Field(alias="YawEndStop", default=None)
-    EventType: Optional[str] = Field(alias="EventType", default='YawFaultConstantRate', allow_mutation=False)
+    Torque: Optional[float] = Field(alias="Torque", default=None)
+    EventType: Optional[str] = Field(alias="EventType", default='YawFaultConstantTorque', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -106,25 +109,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        YawFaultConstantRate
+        YawFaultConstantTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawFaultConstantRate.from_file('/path/to/file')
+        >>> model = YawFaultConstantTorque.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -136,25 +139,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        YawFaultConstantRate
+        YawFaultConstantTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawFaultConstantRate.from_json('{ ... }')
+        >>> model = YawFaultConstantTorque.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -168,15 +171,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        YawFaultConstantRate
+        YawFaultConstantTorque
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -200,8 +203,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-YawFaultConstantRate.update_forward_refs()
+YawFaultConstantTorque.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault_constant_torque.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/signal_properties_velocity.py`

 * *Files 14% similar despite different names*

```diff
@@ -9,36 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.yaw_fault import YawFault
 
+from dnv_bladed_models.signal_properties import SignalProperties
 
-class YawFaultConstantTorque(YawFault, EventType='YawFaultConstantTorque'):
-    """YawFaultConstantTorque - A fault in the yaw system where the actuator applies a constant torque.
+from dnv_bladed_models.transducer_faults_velocity import TransducerFaultsVelocity
 
+
+class SignalPropertiesVelocity(SignalProperties):
+    """SignalPropertiesVelocity - 
+    
     Attributes:
-        - Torque: float => A number representing a torque or moment.  The SI units for moments are Newton-metres.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;YawFaultConstantTorque&#39;
+    ----------
+    NoiseMagnitude : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    DiscretisationStep : float
+        A number representing a velocity.  The SI units for velocity are metres per second.
+    TransducerFaults : TransducerFaultsVelocity
+    
     """
 
-    Torque: Optional[float] = Field(alias="Torque", default=None)
-    EventType: Optional[str] = Field(alias="EventType", default='YawFaultConstantTorque', allow_mutation=False)
+    NoiseMagnitude: Optional[float] = Field(alias="NoiseMagnitude", default=None)
+    DiscretisationStep: Optional[float] = Field(alias="DiscretisationStep", default=None)
+    TransducerFaults: Optional[TransducerFaultsVelocity] = Field(alias="TransducerFaults", default=None)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -104,25 +113,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        YawFaultConstantTorque
+        SignalPropertiesVelocity
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawFaultConstantTorque.from_file('/path/to/file')
+        >>> model = SignalPropertiesVelocity.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -134,25 +143,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        YawFaultConstantTorque
+        SignalPropertiesVelocity
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawFaultConstantTorque.from_json('{ ... }')
+        >>> model = SignalPropertiesVelocity.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -166,15 +175,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        YawFaultConstantTorque
+        SignalPropertiesVelocity
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -198,8 +207,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-YawFaultConstantTorque.update_forward_refs()
+SignalPropertiesVelocity.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_fault_limp.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_manoever.py`

 * *Files 10% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.yaw_fault import YawFault
 
+from dnv_bladed_models.controlled_operation import ControlledOperation
 
-class YawFaultLimp(YawFault, EventType='YawFaultLimp'):
-    """YawFaultLimp - A fault in the yaw system where the yaw system is left without actuation.  The reaction torques are specified, and if these forces are overcome, the yaw bearing will move.
+from dnv_bladed_models.idealised_yaw_manoever import IdealisedYawManoever
 
+
+class YawManoever(ControlledOperation, EventType='YawManoever'):
+    """YawManoever - A prescribed yaw manoever, occuring at a specified time in the simulation.
+    
     Attributes:
-        - Friction: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Stiffness: float => A number representing a rotational stiffness.  The SI units for rotational stiffness are Newton metres per radian.
-        - Damping: float => A number representing a rotational damping term.  The SI units for angular damping are Newton metre per radian.
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;YawFaultLimp&#39;
+    ----------
+    FinalYawAngle : float
+        A number representing an angle.  The SI units for angles are radians.
+    Idealised : IdealisedYawManoever
+    EventType : str, readonly, default='YawManoever'
+        Allows the schema to identify the type of the object.  For this type of object, this must always be set to 'YawManoever'
+    
     """
 
-    Friction: Optional[float] = Field(alias="Friction", default=None)
-    Stiffness: Optional[float] = Field(alias="Stiffness", default=None)
-    Damping: Optional[float] = Field(alias="Damping", default=None)
-    EventType: Optional[str] = Field(alias="EventType", default='YawFaultLimp', allow_mutation=False)
+    FinalYawAngle: Optional[float] = Field(alias="FinalYawAngle", default=None)
+    Idealised: Optional[IdealisedYawManoever] = Field(alias="Idealised", default=None)
+    EventType: Optional[str] = Field(alias="EventType", default='YawManoever', allow_mutation=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +113,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        YawFaultLimp
+        YawManoever
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawFaultLimp.from_file('/path/to/file')
+        >>> model = YawManoever.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +143,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        YawFaultLimp
+        YawManoever
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawFaultLimp.from_json('{ ... }')
+        >>> model = YawManoever.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +175,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        YawFaultLimp
+        YawManoever
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +207,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-YawFaultLimp.update_forward_refs()
+YawManoever.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_manoever.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_system.py`

 * *Files 16% similar despite different names*

```diff
@@ -9,40 +9,45 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.controlled_operation import ControlledOperation
 
-from dnv_bladed_models.idealised_yaw_manoever import IdealisedYawManoever
+from dnv_bladed_models.component import Component
 
+from dnv_bladed_models.connectable_node import ConnectableNode
 
-class YawManoever(ControlledOperation, EventType='YawManoever'):
-    """YawManoever - A prescribed yaw manoever, occuring at a specified time in the simulation.
+from dnv_bladed_models.yaw_system_output_group import YawSystemOutputGroup
 
+
+class YawSystem(Component):
+    """YawSystem - A definition of a yaw bearing and any activation.
+    
     Attributes:
-        - FinalYawAngle: float => A number representing an angle.  The SI units for angles are radians.
-        - Idealised: IdealisedYawManoever 
-        - EventType: str [Read-only] => Allows the schema to identify the type of the object.  For this type of object, this must always be set to &#39;YawManoever&#39;
+    ----------
+    OutputGroups : Dict[str, YawSystemOutputGroup], default=dict()
+        A library which contains any number of named output groups.  These can be referenced from 'SelectedOutputGroup'
+    ConnectableNodes : Dict[str, ConnectableNode], default=dict()
+        A declaration of what nodes can be connected to.
+    
     """
 
-    FinalYawAngle: Optional[float] = Field(alias="FinalYawAngle", default=None)
-    Idealised: Optional[IdealisedYawManoever] = Field(alias="Idealised", default=None)
-    EventType: Optional[str] = Field(alias="EventType", default='YawManoever', allow_mutation=False)
+    OutputGroups: Optional[Dict[str, YawSystemOutputGroup]] = Field(alias="OutputGroups", default=dict())
+    ConnectableNodes: Optional[Dict[str, ConnectableNode]] = Field(alias="ConnectableNodes", default=dict())
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +113,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        YawManoever
+        YawSystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawManoever.from_file('/path/to/file')
+        >>> model = YawSystem.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +143,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        YawManoever
+        YawSystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawManoever.from_json('{ ... }')
+        >>> model = YawSystem.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +175,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        YawManoever
+        YawSystem
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +207,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-YawManoever.update_forward_refs()
+YawSystem.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_system.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/yaw_system_output_group.py`

 * *Files 8% similar despite different names*

```diff
@@ -9,40 +9,38 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.component import Component
 
-from dnv_bladed_models.connectable_node import ConnectableNode
+from dnv_bladed_models.dnv import Dnv
 
-from dnv_bladed_models.yaw_system_output_group import YawSystemOutputGroup
-
-
-class YawSystem(Component):
-    """YawSystem - A definition of a yaw bearing and any activation.
 
+class YawSystemOutputGroup(Dnv):
+    """YawSystemOutputGroup - An output group definition for the yaw system.
+    
     Attributes:
-        - OutputGroups: Dict[str, YawSystemOutputGroup] => A library which contains any number of named output groups.  These can be referenced from &#39;SelectedOutputGroup&#39;
-        - ConnectableNodes: Dict[str, ConnectableNode] => A declaration of what nodes can be connected to.
+    ----------
+    OutputYawSystemData : bool, default=False
+        If true, the output group for the yaw system will be produced.
+    
     """
 
-    OutputGroups: Optional[Dict[str, YawSystemOutputGroup]] = Field(alias="OutputGroups", default=dict())
-    ConnectableNodes: Optional[Dict[str, ConnectableNode]] = Field(alias="ConnectableNodes", default=dict())
+    OutputYawSystemData: Optional[bool] = Field(alias="OutputYawSystemData", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -108,25 +106,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        YawSystem
+        YawSystemOutputGroup
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawSystem.from_file('/path/to/file')
+        >>> model = YawSystemOutputGroup.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -138,25 +136,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        YawSystem
+        YawSystemOutputGroup
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawSystem.from_json('{ ... }')
+        >>> model = YawSystemOutputGroup.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -170,15 +168,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        YawSystem
+        YawSystemOutputGroup
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -202,8 +200,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-YawSystem.update_forward_refs()
+YawSystemOutputGroup.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models/yaw_system_output_group.py` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models/blade_motion_outputs.py`

 * *Files 12% similar despite different names*

```diff
@@ -9,34 +9,44 @@
 from pathlib import Path
 from typing import TypeVar
 Model = TypeVar('Model', bound='BaseModel')
 StrBytes = Union[str, bytes]
 
 from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator, root_validator, Extra  # noqa: F401
 
-from dnv_bladed_models.dnv import Dnv
 
+from dnv_bladed_models.dnv import Dnv
 
-class YawSystemOutputGroup(Dnv):
-    """YawSystemOutputGroup - An output group definition for the yaw system.
 
+class BladeMotionOutputs(Dnv):
+    """BladeMotionOutputs - Loads selected
+    
     Attributes:
-        - OutputYawSystemData: bool => If true, the output group for the yaw system will be produced.
+    ----------
+    OutOfPlaneDeflection : bool, default=False
+         Which blade deflections to output in the out of plane direction (x, y, z).
+    InPlaneDeflection : bool, default=False
+         Which blade deflections to output in the in plane direction (x, y, z).
+    BladeAccelerations : bool, default=False
+         Which blade accelerations to output (x, y, z).
+    
     """
 
-    OutputYawSystemData: Optional[bool] = Field(alias="OutputYawSystemData", default=False)
+    OutOfPlaneDeflection: Optional[bool] = Field(alias="OutOfPlaneDeflection", default=False)
+    InPlaneDeflection: Optional[bool] = Field(alias="InPlaneDeflection", default=False)
+    BladeAccelerations: Optional[bool] = Field(alias="BladeAccelerations", default=False)
 
     class Config:
         extra = Extra.forbid
         validate_assignment = True
         allow_population_by_field_name = True
         pass
 
     @root_validator(pre=True)
-    def parsing_ignores_underscore_properties(cls, values: dict[str, any]):
+    def _parsing_ignores_underscore_properties(cls, values: dict[str, any]):
         allowed_vals = {}
         for key, val in values.items():
             if not key.startswith('_'):
                 if isinstance(val, dict):
                     allowed_child_vals = {}
                     for child_key, child_val in val.items():
                         if not child_key.startswith('_'):
@@ -102,25 +112,25 @@
         Parameters
         ----------
         path : string
             The file path to the model.
 
         Returns
         -------
-        YawSystemOutputGroup
+        BladeMotionOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawSystemOutputGroup.from_file('/path/to/file')
+        >>> model = BladeMotionOutputs.from_file('/path/to/file')
         """
         
         return super().parse_file(path=path)
     
     @classmethod
     def from_json(
         cls: Type['Model'],
@@ -132,25 +142,25 @@
         Parameters
         ----------
         b: StrBytes
             The JSON string describing the model.
 
         Returns
         -------
-        YawSystemOutputGroup
+        BladeMotionOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
 
         Examples
         --------
-        >>> model = YawSystemOutputGroup.from_json('{ ... }')
+        >>> model = BladeMotionOutputs.from_json('{ ... }')
         """
 
         return super().parse_raw(
             b=b,
             content_type='application/json')
     
     @classmethod
@@ -164,15 +174,15 @@
         Parameters
         ----------
         obj : Any
             The dictionary object describing the model.
 
         Returns
         -------
-        YawSystemOutputGroup
+        BladeMotionOutputs
             The model object.
 
         Raises
         ------
         ValueError, ValidationError
             If the JSON document does not correctly describe the model according to the model schema.
         """
@@ -196,8 +206,8 @@
         >>> model.to_file('/path/to/file')
 
         """
 
         with open(file=path, mode='w', encoding="utf8") as output_file:
             output_file.write(self.to_json())
 
-YawSystemOutputGroup.update_forward_refs()
+BladeMotionOutputs.update_forward_refs()
```

### Comparing `dnv_bladed_models-0.3.32a686/src/dnv_bladed_models.egg-info/SOURCES.txt` & `dnv_bladed_models-0.3.44/src/dnv_bladed_models.egg-info/SOURCES.txt`

 * *Files 0% similar despite different names*

```diff
@@ -139,14 +139,15 @@
 src/dnv_bladed_models/flexible_joint.py
 src/dnv_bladed_models/flow_obstruction.py
 src/dnv_bladed_models/foundation.py
 src/dnv_bladed_models/free_flow_model.py
 src/dnv_bladed_models/frequency_vs_spectral_density.py
 src/dnv_bladed_models/friction.py
 src/dnv_bladed_models/friction_torque_vs_stiction_torque.py
+src/dnv_bladed_models/frozen_wake_model.py
 src/dnv_bladed_models/gaussian.py
 src/dnv_bladed_models/gearbox.py
 src/dnv_bladed_models/gearbox_mounting_rolling.py
 src/dnv_bladed_models/generalised_alpha_fixed_step.py
 src/dnv_bladed_models/generator.py
 src/dnv_bladed_models/generator_output_group.py
 src/dnv_bladed_models/generator_overpower.py
```

